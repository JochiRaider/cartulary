{
  "schema_id": "cartulary.schema_object_ownership_manifest.v1",
  "migration_root": "db/migrations",
  "synthetic_objects": [
    {
      "kind": "bookkeeping",
      "name": "goose_db_version",
      "source": "goose runtime",
      "owner": "harness_support",
      "profiles": ["harness"],
      "downstream_surfaces": ["migration runner", "restore preflight", "test fixture identity"],
      "notes": "Created and maintained by goose, not authored migration SQL."
    }
  ],
  "allowed_owners": [
    "audit",
    "assessments",
    "artifacts",
    "auth",
    "collaboration",
    "database_migrations",
    "deployment_admin",
    "entities",
    "evidence",
    "extensions",
    "graphprojection",
    "harness_support",
    "imports",
    "incidentbundles",
    "incidents",
    "indicators",
    "jobapi",
    "links",
    "networkflow",
    "parties",
    "platform_jobs",
    "projections",
    "records",
    "recovery",
    "reference_data",
    "reportcomposition",
    "reporting",
    "revisions",
    "savedviews",
    "tasksdecisions",
    "timeline",
    "viewschemas",
    "workbook"
  ],
  "allowed_profiles": [
    "base",
    "enterprise_authentication",
    "harness",
    "import",
    "incident_portability",
    "network_flow_activity",
    "reference_pack",
    "snapshot_reporting"
  ],
  "entries": [
    {
      "id": "goose-bookkeeping",
      "owner": "harness_support",
      "profiles": ["harness"],
      "object_patterns": [
        { "kind": "bookkeeping", "name_pattern": "^goose_db_version$" }
      ],
      "downstream_surfaces": ["migration runner", "restore preflight", "test fixture identity"],
      "notes": "Runtime migration bookkeeping is external to authored migration SQL."
    },
    {
      "id": "schema-migration-lineage",
      "owner": "database_migrations",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(schema_migration_lineage|schema_migration_lineage_.*)$" }
      ],
      "downstream_surfaces": ["migration runner", "migration drift", "operator reset/reimport diagnostics"],
      "notes": "Current production DDL line marker used to reject incompatible historical goose ledgers."
    },
    {
      "id": "extension-coordination",
      "owner": "extensions",
      "profiles": ["base", "network_flow_activity"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^extension_.*$" }
      ],
      "downstream_surfaces": ["extension state admission", "shared transactions", "staged-object cleanup", "extension job reconciliation", "backup and restore"],
      "notes": "Generic coordination metadata and proof state only; authoritative profile tables remain with their named profile owners."
    },
    {
      "id": "administrative-audit",
      "owner": "audit",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(administrative_audit_projections|administrative_audit_projections_.*|reject_administrative_audit_mutation)$" }
      ],
      "downstream_surfaces": ["deployment administrative audit", "incident membership audit", "backup", "operator recovery"],
      "notes": "Immutable safe administrative-audit projection and append-only enforcement shared by existing semantic owners."
    },
    {
      "id": "deployment-admin-and-recovery",
      "owner": "deployment_admin",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(pgcrypto|citext|deployment_bootstrap_state|deployment_admin_audit_events|backup_sets|restore_verification_runs)$" },
        { "kind": "any", "name_pattern": "^(idx_deployment_|idx_backup_|idx_restore_|backup_|restore_|deployment_bootstrap_state_|deployment_admin_audit_events_).*$" }
      ],
      "downstream_surfaces": ["bootstrap", "deployment audit", "backup", "restore verification"],
      "notes": "Deployment-local administrative and recovery schema."
    },
    {
      "id": "operator-recovery-journal",
      "owner": "recovery",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^operator_recovery_journal$" },
        { "kind": "any", "name_pattern": "^operator_recovery_journal_.*$" }
      ],
      "downstream_surfaces": ["operator recovery journal", "backup", "restore verification", "safe administrative audit"],
      "notes": "Encrypted append-only journal for deployment-local recovery operations; not deployment-admin authorization state."
    },
    {
      "id": "auth-and-current-account",
      "owner": "auth",
      "profiles": ["base", "enterprise_authentication"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(users|user_sessions|bootstrap_tokens|pending_totp_enrollments|route_idempotency|account_preferences)$" },
        { "kind": "any", "name_pattern": "^(enterprise_auth_providers|enterprise_auth_transactions|enterprise_auth_bindings)$" },
        { "kind": "any", "name_pattern": "^(idx_users|idx_user_sessions|idx_bootstrap_tokens|idx_pending_totp|idx_route_idempotency|idx_account_preferences|idx_enterprise_auth).*$" },
        { "kind": "any", "name_pattern": "^(users_|user_sessions_|bootstrap_tokens_|pending_totp_enrollments_|route_idempotency_|account_preferences_|enterprise_auth_).*$" }
      ],
      "downstream_surfaces": ["auth routes", "sessions", "current account", "enterprise authentication"],
      "notes": "Includes current-account preferences because they are deployment-local user account state."
    },
    {
      "id": "incident-core",
      "owner": "incidents",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(incidents|incident_memberships|incident_workbook_preferences|user_workbook_preferences)$" },
        { "kind": "any", "name_pattern": "^(idx_incidents|idx_incident_memberships|idx_incident_workbook|idx_user_workbook|incidents_|incident_memberships_|incident_workbook_preferences_|user_workbook_preferences_).*$" }
      ],
      "downstream_surfaces": ["incident routes", "membership authorization", "workbook startup preferences"],
      "notes": "Incident metadata, memberships, and incident-scoped workbook preference anchors."
    },
    {
      "id": "collaboration-durable-stream",
      "owner": "collaboration",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(collaboration_event_intents|collaboration_incident_stream_cursors|collaboration_replay_events|collaboration_resume_tokens)$" },
        { "kind": "any", "name_pattern": "^(idx_collaboration_|collaboration_event_intents_|collaboration_incident_stream_cursors_|collaboration_replay_events_|collaboration_resume_tokens_).*$" },
        { "kind": "function", "name_pattern": "^collaboration_(rfc3339|capture_record_revision|capture_job_progress|capture_network_flow_resource)_v1$" }
      ],
      "downstream_surfaces": ["incident collaboration stream", "replay", "resume", "dispatcher recovery"],
      "notes": "Durable sequencing, replay, and hashed resume-token state. Presence and live connection state remain ephemeral."
    },
    {
      "id": "timeline-source",
      "owner": "timeline",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(timeline_events|timeline_source_provenance|timeline_time_conversion_profiles)$" },
        { "kind": "any", "name_pattern": "^(idx_timeline_events|idx_timeline_source_provenance|idx_timeline_time_conversion_profiles|timeline_events_|timeline_source_provenance_|timeline_time_conversion_profiles_).*$" }
      ],
      "downstream_surfaces": ["timeline routes", "timeline workbook surface", "timeline projections", "source provenance", "incident portability"],
      "notes": "Source-state Timeline rows, normalized source provenance, and time-conversion profile schema."
    },
    {
      "id": "record-envelope",
      "owner": "records",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^records$" },
        { "kind": "any", "name_pattern": "^(idx_records|records_).*$" }
      ],
      "downstream_surfaces": ["record envelope queries and mutations", "destructive locking", "incident portability", "reporting adaptation", "recovery"],
      "notes": "Current record-envelope state and local relation objects. Retained history remains under record-revisions."
    },
    {
      "id": "record-revisions",
      "owner": "revisions",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(change_sets|change_set_mutations|record_revisions|record_history_entry_refs)$" },
        { "kind": "any", "name_pattern": "^(idx_change_sets|idx_change_set_mutations|idx_record_revisions|idx_record_history|change_sets_|change_set_mutations_|record_revisions_|record_history_entry_refs_).*$" },
        { "kind": "function", "name_pattern": "^revisions_incident_bundle_sequence_(begin|finish)_v1$" }
      ],
      "downstream_surfaces": ["record history", "rollback", "delete/restore coordination", "workbook mutation audit"],
      "notes": "Retained change-set, revision, and history-reference substrate; current envelopes are owned by record-envelope."
    },
    {
      "id": "links-and-tags",
      "owner": "links",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(record_links|record_tags|active_record_links_v1|active_record_tags_v1)$" },
        { "kind": "any", "name_pattern": "^(idx_record_links|idx_record_tags).*$" },
        { "kind": "any", "name_pattern": "^(record_links_|record_tags_).*$" }
      ],
      "downstream_surfaces": ["relationship inspector", "record tags", "workbook linked-record surfaces"],
      "notes": "Record relationship vocabulary and tagging source state."
    },
    {
      "id": "parties",
      "owner": "parties",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^parties$" },
        { "kind": "any", "name_pattern": "^idx_parties.*$" },
        { "kind": "any", "name_pattern": "^parties_.*$" }
      ],
      "downstream_surfaces": ["party workbook surface", "coordination identity", "reporting disclosure partitions"],
      "notes": "Incident-scoped party source state."
    },
    {
      "id": "tasks-and-decisions",
      "owner": "tasksdecisions",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(task_requests|decisions)$" },
        { "kind": "any", "name_pattern": "^(idx_task_requests|idx_decisions).*$" },
        { "kind": "any", "name_pattern": "^(task_requests_|decisions_).*$" }
      ],
      "downstream_surfaces": ["task request workbook surface", "decision workbook surface", "coordination workflows"],
      "notes": "Task-request and decision source state."
    },
    {
      "id": "artifacts-and-optional-surfaces",
      "owner": "artifacts",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(artifacts|handoff_risk_refs|artifact_findings|artifact_investigative_queries|artifact_forensic_keywords|cartulary_confidence_band)$" },
        { "kind": "any", "name_pattern": "^(idx_artifacts|idx_handoff|idx_artifact_findings|idx_artifact_investigative_queries|idx_artifact_forensic_keywords).*$" },
        { "kind": "any", "name_pattern": "^(artifacts_|handoff_risk_refs_|artifact_findings_|artifact_investigative_queries_|artifact_forensic_keywords_).*$" }
      ],
      "downstream_surfaces": ["optional artifact surfaces", "handoff risk references", "artifact analysis surfaces"],
      "notes": "Artifact, handoff-risk, finding, investigative-query, forensic-keyword, and confidence-band source state."
    },
    {
      "id": "entity-source",
      "owner": "entities",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(entity_mentions|hosts|identities|entity_preserved_identifiers|entity_aliases)$" },
        { "kind": "any", "name_pattern": "^(idx_entity_mentions|idx_hosts|idx_identities|idx_entity_preserved_identifiers|idx_entity_aliases|entity_mentions_|hosts_|identities_|entity_preserved_identifiers_|entity_aliases_).*$" }
      ],
      "downstream_surfaces": ["entity stores", "host and identity workbook surfaces", "timeline resolution"],
      "notes": "Host, identity, mention, preserved identifier, and alias source state."
    },
    {
      "id": "indicator-source",
      "owner": "indicators",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(indicators|indicator_state_intervals|indicator_observations)$" },
        { "kind": "any", "name_pattern": "^(idx_indicators|idx_indicator_state_intervals|idx_indicator_observations|indicators_|indicator_state_intervals_|indicator_observations_).*$" }
      ],
      "downstream_surfaces": ["indicator stores", "indicator workbook surface", "observation resolution"],
      "notes": "Indicator and observation source state."
    },
    {
      "id": "assessment-source",
      "owner": "assessments",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(compromise_assessments|assessments)$" },
        { "kind": "any", "name_pattern": "^(idx_compromise_assessments|idx_assessments|compromise_assessments_|assessments_).*$" }
      ],
      "downstream_surfaces": ["assessment routes", "assessment workbook surface", "assessment projection"],
      "notes": "Assessment source-state ownership; projection read models remain under projections."
    },
    {
      "id": "evidence-source-and-handles",
      "owner": "evidence",
      "profiles": ["base", "incident_portability"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(evidence|object_blobs|evidence_access_handles|evidence_custody_events)$" },
        { "kind": "any", "name_pattern": "^(idx_evidence|idx_object_blobs|idx_evidence_access_handles|idx_evidence_custody_events|evidence_pkey|evidence_(record_|object_blob_|incident_|collector_|source_).*|object_blobs_|evidence_access_handles_|evidence_custody_events_).*$" }
      ],
      "downstream_surfaces": ["evidence routes", "object storage", "evidence handles", "incident bundle custody"],
      "notes": "Evidence metadata, blob references, access handles, and custody events."
    },
    {
      "id": "workbook-projections",
      "owner": "projections",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(timeline_grid_projection|host_grid_projection|identity_grid_projection|indicator_grid_projection|assessment_grid_projection|task_request_grid_projection|decision_grid_projection|artifact_grid_projection|evidence_grid_projection|party_grid_projection)$" },
        { "kind": "any", "name_pattern": "^(idx_.*_grid_projection|.*_grid_projection_).*$" }
      ],
      "downstream_surfaces": ["workbook query stores", "view schemas", "projection rebuilds"],
      "notes": "Read-model tables and views used by workbook surfaces."
    },
    {
      "id": "graphprojection",
      "owner": "graphprojection",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(graph_projection_views|graph_projection_runs|graph_projection_vertices|graph_projection_edges|graph_projection_idempotency)$" },
        { "kind": "any", "name_pattern": "^(graph_projection_views_|graph_projection_runs_|graph_projection_vertices_|graph_projection_edges_|graph_projection_idempotency_|graph_projection_.*_idx).*$" }
      ],
      "downstream_surfaces": ["graph projection internal runtime", "graph projection conformance tests"],
      "notes": "Derived graph projection state for the internal graphprojection subsystem; source ownership stays outside graphprojection."
    },
    {
      "id": "savedviews",
      "owner": "savedviews",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(saved_views|idx_saved_views|saved_views_).*$" }
      ],
      "downstream_surfaces": ["saved-view routes", "workbook startup", "query layout persistence"],
      "notes": "Saved view persistence and reusable identifier layout schema."
    },
    {
      "id": "imports",
      "owner": "imports",
      "profiles": ["import"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(import_sessions|import_units|import_apply_journal|import_source_streams|import_apply_unit_plans|import_unit_apply_outcomes|idx_import_sessions|idx_import_units|import_sessions_|import_units_|import_apply_journal_|import_source_streams_|import_apply_unit_plans_|import_unit_apply_outcomes_).*$" }
      ],
      "downstream_surfaces": ["file import routes", "import jobs", "mapping provenance", "apply provenance", "unit recovery and finalization"],
      "notes": "Structured import session, unit, mapping, durable apply-plan/outcome, and apply-journal persistence."
    },
    {
      "id": "network-flow-activity-storage",
      "owner": "networkflow",
      "profiles": ["network_flow_activity"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(network_flow_tables|network_flow_rows|network_flow_rejected_row_diagnostics)$" },
        { "kind": "any", "name_pattern": "^network_flow_.*$" }
      ],
      "downstream_surfaces": ["Network Flow Activity storage", "Network Analysis workspace", "Network Flow import facade", "Network Flow graph adapter"],
      "notes": "Incident-scoped analytical Network Flow table, immutable accepted-row, and retained rejected-row diagnostic storage."
    },
    {
      "id": "platform-jobs",
      "owner": "platform_jobs",
      "profiles": ["base"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(jobs|idx_jobs|jobs_).*$" }
      ],
      "downstream_surfaces": ["jobapi routes", "background worker shell", "extension job payloads"],
      "notes": "Shared background-job storage substrate; public route ownership is jobapi."
    },
    {
      "id": "reporting",
      "owner": "reporting",
      "profiles": ["snapshot_reporting"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(reporting_snapshots|reporting_releases|reporting_release_approvals|reporting_job_payloads|reporting_render_bundles|reporting_render_bundle_files|reporting_composition_preview_outputs|reporting_composition_preview_output_files)$" },
        { "kind": "any", "name_pattern": "^(idx_reporting_|reporting_snapshots_|reporting_releases_|reporting_release_approvals_|reporting_job_payloads_|reporting_render_|reporting_composition_preview_output).*$" }
      ],
      "downstream_surfaces": ["snapshot reporting routes", "reporting jobs", "release approvals", "render bundle storage", "internal-draft composition preview output"],
      "notes": "Snapshot-reporting extension schema, including release tuple, render bundle, and non-release preview output metadata."
    },
    {
      "id": "report-composition",
      "owner": "reportcomposition",
      "profiles": ["snapshot_reporting"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(report_compositions|report_composition_versions|report_composition_release_bindings|report_composition_preview_attempts)$" },
        { "kind": "any", "name_pattern": "^(idx_report_composition|report_compositions_|report_composition_).*$" }
      ],
      "downstream_surfaces": ["report composition routes", "report composition authoring", "reporting release tuple bindings"],
      "notes": "Report composition authoring and immutable composition-version binding state."
    },
    {
      "id": "reference-data",
      "owner": "reference_data",
      "profiles": ["reference_pack"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(reference_packs|reference_pack_activation_state|reference_pack_attestations|reference_pack_job_payloads)$" },
        { "kind": "any", "name_pattern": "^(idx_reference_|reference_packs_|reference_pack_activation_state_|reference_pack_activation_sta_|reference_pack_attestations_|reference_pack_job_payloads_).*$" }
      ],
      "downstream_surfaces": ["reference pack routes", "reference pack jobs", "workbook optional packs"],
      "notes": "Reference-pack extension schema."
    },
    {
      "id": "incident-bundles",
      "owner": "incidentbundles",
      "profiles": ["incident_portability"],
      "object_patterns": [
        { "kind": "any", "name_pattern": "^(incident_bundle_exports|incident_bundle_manifest_files|incident_bundle_job_payloads|incident_bundle_imported_actors|incident_bundle_imported_attributions)$" },
        { "kind": "any", "name_pattern": "^(idx_incident_bundle_|incident_bundle_).*$" }
      ],
      "downstream_surfaces": ["incident bundle export", "incident bundle import", "portability jobs"],
      "notes": "Incident Portability Extension Profile schema."
    }
  ]
}
