---
doc_id: THR-S0-SOURCE-LIMITS
title: Testing Harness Recovery Source-Limit Log
status: active
role: source-limit-log
---

# Testing Harness Recovery Source-Limit Log

## Document role

This log records unavailable, incomplete, or intentionally uninspected sources
during testing harness reverse-specification recovery. Do not delete unresolved
rows. Mark resolved limits with a follow-up note instead.

## Source limits

| Source-limit ID | Surface | Limit type | What was inspected | What was not inspected | Impact | Follow-up needed | Evidence status | Notes |
|---|---|---|---|---|---|---|---|---|
| SL-0001 | `.github/**` | `inaccessible_file` | S0 checked repository root with `test -d .github && find .github -maxdepth 3 -type f \| sort \|\| printf '.github absent\n'`. S1 rechecked with `test -d .github && find .github -maxdepth 3 -type f \| sort \|\| true`, which printed no files. S1 also inspected `scripts/ci/**`. | CI workflow files under `.github/**`; directory was absent. | S1 cannot record repository CI workflow behavior from `.github` sources. S2 must classify CI as absent, external, or represented by provider-neutral scripts if no other evidence appears. | During S2, map `make ci` and `scripts/ci/**`, then ask for owner decision if provider CI mapping is required. | `source_limit` | Required S0/S1 input named CI configuration, but no `.github/` directory exists in the working tree. See `AMB-0001`. |
| SL-0002 | Broad verification commands | `unexecuted_command` | S1 ran non-mutating discovery commands: `make help`, `make help-all`, `make task-guide`, `make task-surface-report TASK_SURFACE_REPORT_ARGS=--all`, `make target-plan`, `make target-plan-json`, `make explain-phase PHASE=phase0` through `phase6`, and selected `make explain-target ... DETAIL=summary` commands. | Runtime execution of `make test-fast`, `make test`, `make check`, `make ci`, `make release-check`, browser E2E targets, backend test targets, frontend unit/typecheck targets, service-backed targets, lint targets, and build targets. | S1 inventory is static/discovery-based. It does not prove runtime success/failure behavior or failure bundle contents. | S2 should trace command declarations without executing broad gates. S3/S5 may inspect retained artifacts or run targeted commands if later authorized. | `source_limit` | S1 intentionally avoided broad verification and implementation-changing commands. |
| SL-0003 | Live service-backed runtime behavior | `unexecuted_command` | S1 inspected `docker-compose.dev.yml`, `configs/dev/**`, `Makefile`, `tools/testservices/**`, `internal/testutil/pgtest/**`, `internal/testutil/s3test/**`, `internal/testutil/testcontainersx/**`, `apps/web/playwright*.config.ts`, and `scripts/start-web-e2e.sh`. | Live Docker, Postgres, MinIO, browser stack, port allocation, readiness, reset, retry, and teardown behavior during an actual service-backed run. | Service lifecycle conclusions remain static until S4/S6 recover runtime evidence. | S4 should build `service_lifecycle_map`; S6 should record resource/timing hazards. | `source_limit` | Static inspection found service surfaces, but S1 did not start backing services. |
| SL-0004 | Retained `.cartulary/test-results/**` artifacts | `partial_search` | S1 listed existing retained run summaries under `.cartulary/test-results/**`, inspected ignore rules with `git check-ignore -v`, and observed Make default `CARTULARY_TEST_RESULTS_DIR`. | Full provenance, freshness, completeness, and schema coverage of retained artifacts; failure-only traces/screenshots/log bundles not exhaustively opened. | Existing retained artifacts cannot be treated as authoritative current run evidence. | S3/S5 should recover artifact ownership, schema, freshness, and consumer rules. | `source_limit` | `.cartulary/test-results/**` is ignored by the unanchored `test-results/` rule. See `AMB-0002` and `AMB-0010`. |
| SL-0005 | Planned phase7/phase8 files | `inaccessible_file` | S1 inspected `tools/phase_registry.json` and checked `tools/phase7_test_map.json`, `tools/phase8_test_map.json`, `docs/testing/phase7_coverage_ledger.md`, and `docs/testing/phase8_coverage_ledger.md` with `test -e`. | Planned phase7/phase8 manifest and ledger files; all four named files were absent. | Phase7/phase8 cannot be inventoried as active harness files during S1. | Later phase owner decision or future phase creation should resolve whether planned rows belong in the recovered harness spec. | `source_limit` | Active phase evidence is phase0 through phase6. See `AMB-0004`. |
| SL-0006 | S2 broad runtime behavior | `unexecuted_command` | S2 ran non-mutating command-surface discovery: `make task-surface-report TASK_SURFACE_REPORT_ARGS=--all`, `make help-all`, `make target-plan`, `make target-plan-json`, `make explain-phase PHASE=phase0` through `phase6`, selected `make explain-target ... DETAIL=summary`, static manifest inspection, package script inspection, and CLI source inspection. | Runtime execution of `make test-fast`, `make test`, `make check`, `make ci`, `make release-check`, service-backed targets, browser E2E targets, cleanup targets, formatters, generators, baseline refresh/update targets, and failure scenarios. | S2 maps declared entrypoints and flows, but does not prove live success/failure behavior, service readiness, cleanup on failure, or failure bundle schemas. | S3 should link artifacts to entrypoints; S4/S5/S6 should recover live lifecycle, failure, and resource behavior from targeted evidence. | `source_limit` | This is the S2-specific continuation of `SL-0002`. |
| SL-0007 | Script CLI usage recovered statically | `unexecuted_command` | S2 inspected usage strings and parse logic in `scripts/cartulary-runner.mjs`, `scripts/run-make-sequence.sh`, `scripts/run-check-schedule.mjs`, `scripts/run-service-backed-schedule.mjs`, `scripts/run-phase-slice.mjs`, `scripts/run-harness-smoke.mjs`, `scripts/run-make-node-tool.mjs`, `scripts/start-web-e2e.sh`, `tools/testservices/main.go`, Playwright configs, and Vite config. | Exhaustive `--help` or invalid-usage execution for every script CLI; runtime confirmation that every usage branch and exit code behaves as statically read. | Command map can cite declared usage and parse behavior, but detailed usage-error contract remains static-only. | S5 should run targeted usage/failure checks only if needed and authorized. | `source_limit` | Avoided executing scripts whose usage paths might create runtime roots or service artifacts. |
| SL-0008 | S3 mutating commands intentionally not executed | `unexecuted_command` | S3 statically inspected `Makefile`, generated recipes, cleanup scripts, generation scripts, browser reset scripts, fixture helpers, and retained artifact path shapes. | `make generate`, `make format`, `make phase-ledgers`, `make phase-schedules`, duration baseline refresh targets, `make clean`, `make distclean`, `db-reset`, browser E2E targets, service-backed targets, release evidence generation, fixture refresh, golden refresh, and snapshot update commands. | S3 artifact and cleanup matrices are static evidence, not runtime proof of writer or cleanup behavior. | S4/S5/S6 may run targeted non-mutating or controlled runtime checks only if authorized by the recovery process. | `source_limit` | Required by S3 non-scope. |
| SL-0009 | Retained artifact provenance during S3 | `partial_search` | S3 listed retained `.cartulary/test-results/**` file names and inspected artifact discovery/reporting code. | Freshness, run success, completeness, schema validity, and failure-bundle contents of retained local runs. | Artifact rows can classify result paths and consumers, but cannot treat existing retained files as authoritative current-run evidence. | S5 should decide retained artifact selection and schema/freshness contracts before observable-interface recovery; proposed rule is explicit `RESULTS_DIR` or active run context for drift/baseline tools, with newest-run fallback only for human investigation that reports selected `run_id`. | `source_limit` | Extends `SL-0004`; see `AMB-0017`. |
| SL-0010 | Failure-only browser and runner artifacts | `unexecuted_command` | S3 inspected Playwright config, result artifact discovery, harness artifact assertion, and retained result path names. | Actual failing Playwright traces/screenshots/videos, watchdog JSON behavior under timeout, runner stdout/stderr contents under representative failures, and CI upload behavior. | Failure bundle schema remains incomplete for observable-interface and failure-mode recovery. | S5 should recover schemas for runner logs, watchdog JSON, Playwright traces/screenshots/videos, and reports from targeted retained failures or controlled failing commands if later authorized. | `source_limit` | See `AMB-0010`, `AMB-0018`; no runtime failures were generated during S3 follow-up. |
| SL-0011 | Live external-state cleanup behavior | `unexecuted_command` | S3 inspected `pgtest`, `s3test`, `tools/testservices`, browser lifecycle scripts, reset script, and Playwright setup/teardown code. | Actual Docker/testcontainers startup, Postgres/MinIO cleanup, stale fixture janitor, active-connection leak handling, port release, interrupt, timeout, and service reaper behavior. | Cleanup and hazard rows for external state are static and must be refined by S4/S6. | S4 should build service lifecycle/resource maps for live cleanup, active connections, stale janitors, ports, process groups, and reset behavior; S6 should recover timing, interrupt, timeout, and race hazards if authorized. | `source_limit` | Extends `SL-0003` for S3 cleanup evidence; see `AMB-0018`, `AMB-0019`. |
| SL-0012 | S4 live service readiness and runtime behavior | `unexecuted_command` | S4 inspected S2/S3 controlling docs plus `Makefile`, generated task/schedule manifests, scheduler scripts, `tools/testservices`, `pgtest`, `s3test`, `testcontainersx`, dev service scripts, browser lifecycle scripts, Playwright setup/teardown, and reset route source. | Actual execution of service-backed targets, browser E2E targets, Docker/testcontainers startup, Docker Compose startup, Postgres/MinIO readiness, browser backend/frontend readiness, and reset-route calls. | S4 lifecycle rows record source-observed readiness and retry loops, but not live success/failure timing or host-specific readiness behavior. | S6 may run controlled runtime observation only if authorized; S5 should not treat S4 readiness rows as output/failure interface proof. | `source_limit` | Continues `SL-0003`, `SL-0006`, and `SL-0011`; see `AMB-0023`. |
| SL-0013 | Unsupported platform and missing-tool behavior during S4 | `partial_search` | S4 inspected shell and Go source for assumptions around Docker, Docker Compose, testcontainers, `ss`, `curl`, `setsid`, `bash`, `realpath`, local loopback, Node, pnpm, Playwright, and WSL/Linux runtime metadata. | Execution on non-Linux hosts, hosts without `ss`, hosts without Docker, restricted-network CI, alternate shells, missing Node/pnpm/browser runtimes, and non-localhost networking. | Platform support remains incomplete; S4 docs use `TODO: platform_unknown` except where source explicitly validates or fails. | S8 or maintainer decision should define supported platform and required tool profile; S6 may add hazards for missing-tool behavior. | `source_limit` | See `AMB-0025`, `AMB-0028`, `ENV-0024`, `HAZ-S4-0005`. |
| SL-0014 | Timeout, interrupt, and detached cleanup behavior during S4 | `unexecuted_command` | S4 inspected shell traps, process-group helpers, suite cleanup/reaper code, cleanup status recording, stale fixture janitor source, Playwright teardown, and reset cleanup scripts. | Actual signal delivery, process timeout, parent death, detached reaper completion, failed child cleanup, active DB connection cleanup, port release wait, and stale fixture janitor execution. | Cleanup fields can distinguish source paths, but not guaranteed cleanup under timeout, interrupt, or abrupt process death. | S6 should classify cleanup guarantees as guaranteed, best-effort, or source-limited after controlled evidence or owner decision. | `source_limit` | Continues `AMB-0018`, `AMB-0019`; see `AMB-0024`, `AMB-0027`, `HAZ-S4-0003`, `HAZ-S4-0004`. |
| SL-0015 | Environment override precedence matrix during S4 | `unexecuted_command` | S4 inspected env assignments and reads in Make, generated recipes, scheduler runners, runner context, service wrappers, `pgtest`, `s3test`, dev/browser shell scripts, Playwright configs, Vite/Vitest-related paths, and reset route startup. | Systematic override tests across Make, package scripts, nested schedulers, browser wrapper env, Playwright worker env, config file env, and direct script invocation. | `environment-contract-observations.md` records defaults and observed validation but leaves cross-layer precedence unresolved where not source-proved. | S8 should decide public env contracts; S5 should avoid using S4 env rows as complete caller-visible interface behavior. | `source_limit` | Continues `AMB-0012`; see `AMB-0026`. |

## S4 Follow-Up Routing Notes

These rows record S5/S6/S8 disposition for S4 source limits. They do not close
the original source limits. No service-backed, browser, Docker, Compose, reset,
cleanup, format, generate, or broad verification commands were executed while
adding these notes.

| Follow-up note ID | Preserved source limits | Routed to | Evidence status | Notes |
|---|---|---|---|---|
| SL-FU-0001 | `SL-0012` | S5 `observable-interface-map.md`, `harness-lifecycle-map.md`, `failure-mode-register.md`; S6 `race-timing-resource-register.md`, `timeout-retry-register.md`; S8 `harness-authority-map.md` platform/tool rows | `source_limit` | Live service readiness for Docker/testcontainers, service-backed targets, browser E2E, Docker Compose, reset route calls, and host-specific wait behavior remains unobserved. Static lifecycle rows must not be upgraded to `runtime_observed` without a separately authorized controlled run. |
| SL-FU-0002 | `SL-0014` | S5 cleanup output/failure rows; S6 cleanup, timeout, interrupt, parent-death, detached-reaper, active-connection, stale-janitor, and port-release hazard rows; S8 stale-janitor authority rows | `source_limit` | Cleanup guarantee strength remains unresolved. S6 may classify as `guaranteed`, `best_effort`, or `source_limited` only after controlled evidence or owner decision. |
| SL-FU-0003 | `SL-0013` | S5 missing-tool observable failure rows; S6 platform and port/readiness hazard rows; S8 supported tool/platform profile rows | `source_limit` | Docker, Compose, `ss`, `curl`, `setsid`, `realpath`, shell, localhost networking, Node/pnpm, and browser runtime support remain platform-authority decisions unless source explicitly validates or rejects a profile. |
| SL-FU-0004 | `SL-0015` | S5 environment observable-interface rows; S6 override timing/resource hazard rows; S8 public env-var contract and precedence rows | `source_limit` | Make, generated recipes, schedulers, shell wrappers, package scripts, Go helpers, config files, and Playwright precedence remain unresolved without an override matrix or owner decision. |
| SL-FU-0005 | `SL-0004`, `SL-0009`, `SL-0010` | S5 structured schema, output consumer, partial completion, and failure bundle rows | `observed/source_limit` | Retained artifact and failure-only schema recovery remains limited by provenance and by absence of controlled failure runs. Existing retained files are not current-run evidence unless explicitly selected and validated. |
| SL-FU-0006 | `SL-0008`, `SL-0011`, `SL-0012`, `SL-0013`, `SL-0014`, `SL-0015` | S6 `race-timing-resource-register.md`, `timeout-retry-register.md`, `concurrency-model-notes.md`, `preservation-matrix.md`, `harness-authority-map.md`, and `main-spec-conflict-list.md` | `observed/source_limit` | S6 was static documentation work only. It searched and classified sleeps, waits, polls, retries, timeouts, debounce/watch triggers, sharding, resource allocation, cleanup, signal, interrupt, reset, and authority-sensitive flows, but did not execute service-backed, browser, Docker, Compose, reset, cleanup, formatter, generator, baseline refresh, snapshot update, or broad verification commands. Runtime-sensitive claims remain source-limited. |
| SL-FU-0007 | `SL-0004`, `SL-0008` through `SL-0015`, `SL-FU-0001` through `SL-FU-0006` | S7 `s0-s6-gap-closure-plan.md` and `s7-s6-audit-gap-follow-up.md` | `source_limit` | S7 has dedicated carry-forward inputs for remaining S0-S6 and S6 audit gaps. They record later authorized evidence phases and commands, but do not execute service-backed, browser, Docker, Compose, reset, cleanup, formatter, generator, baseline refresh, snapshot update, package-test, or broad verification commands and do not close the preserved source limits. |
| SL-FU-0008 | `SL-0002`, `SL-0004`, `SL-0006`, `SL-0007`, `SL-0009`, `SL-0010`, `SL-0011`, `SL-0012`, `SL-0013`, `SL-0014` | S7 selected runtime evidence registers | `observed/source_limit` | Authorized S7 evidence was collected in `runtime-evidence-register.md` and `cleanup-signal-evidence-register.md` with explicit run IDs and result dirs. Passing selected runs close only current-host, selected-command claims. CI/release failures, parent-death cleanup, live active-DB cleanup, stale janitor authority, browser startup cleanup after process start, local-dev contract status, platform matrix, CI provider behavior, and visual snapshot update authority remain source-limited or maintainer-decision-required. |
