---
doc_id: THR-S2-ENTRYPOINTS
title: Testing Harness Recovery Entrypoint Command Map
status: active
role: entrypoint-command-map
---

# Testing Harness Recovery Entrypoint Command Map

## Document role

This S2 artifact maps the current harness invocation surface. It records how
commands are declared and routed today; it is not a harness specification and
does not authorize implementation changes.

Evidence was gathered by static inspection and non-mutating discovery commands
on 2026-05-08. No broad verification gates, service-backed targets, browser
E2E targets, cleanup targets, formatters, generators, baseline refreshes, or
fixture update commands were executed during S2.

## Session metadata

| Field | Value |
|---|---|
| Repository root | `/home/askahn/code/cartulary` |
| Branch state at S2 start | `## main...origin/main` |
| HEAD revision | `9e523d9b110a7433ed08d4c35474f63f8c6c8080` |
| Dirty state at S2 start | Clean |
| Runtime platform | `Linux DeskRip 6.6.87.2-microsoft-standard-WSL2 #1 SMP PREEMPT_DYNAMIC Thu Jun 5 18:30:46 UTC 2025 x86_64 x86_64 x86_64 GNU/Linux` |
| S2 timestamp | `2026-05-08T20:23:28-04:00` |
| Recovery write boundary | `docs/testing-harness-spec-recovery-docs/**` only |

## Discovery summary

| Surface | S2 result | Evidence | Evidence status |
|---|---|---|---|
| Make task surface | `122` total targets: `77` public, `17` check-internal, `28` helper-only. | `make task-surface-report TASK_SURFACE_REPORT_ARGS=--all`; `tools/task_surface_manifest.json` | `runtime_observed` |
| Public help surface | `77` public targets across local dev, fast verification, full gates, investigation, phase maintenance, and release tiers. | `make help-all` | `runtime_observed` |
| Logical harness smoke checks | `47` logical checks under `tools/task_surface_manifest.json`. | `make task-surface-report TASK_SURFACE_REPORT_ARGS=--all`; manifest `harness_checks` | `runtime_observed/observed` |
| Aggregate Make sequences | `test-fast`, `test`, `ci`, and `release-check`. | `tools/task_surface_manifest.json`; `scripts/run-make-sequence.sh` | `observed` |
| Check scheduler | `check` has `96` work units and resource limits for host, service stack, Postgres, MinIO, process, and browser-stage lanes. | `tools/check_schedule_manifest.json`; `scripts/run-check-schedule.mjs` | `observed` |
| Service-backed scheduler | `test-service-backed`, `test-fast-service-backed`, and `check-service-backed` expand from `work_unit_sources`. | `tools/service_backed_schedule_manifest.json`; `scripts/run-service-backed-schedule.mjs` | `observed` |
| Browser batch stages | `webserver-backed`, `functional`, `support`, `stateful`, `measurement`, `visual`, `resettable`, and `isolated`. | `tools/browser_e2e_batch_manifest.json`; browser scripts | `observed` |
| Package scripts | Root has `build`, `test`, `typecheck`; `apps/web` has Vite/Vitest/Playwright aliases; `packages/*` manifests have no scripts. | `package.json`; `apps/web/package.json`; `packages/*/package.json` | `observed` |
| Repository CI workflow | `.github/**` is absent; recoverable provider-neutral CI surface is `make ci` plus `scripts/ci/**`. | `.github` absence check from S0/S1; S2 script inspection | `source_limit/observed` |

## Command map

| Entrypoint ID | Declared command | Caller | Mode | Inputs | Defaults/omitted cases | Outputs | Side effects | Ordering dependencies | Parallel safety | Failure behavior | Evidence | Evidence status | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| EP-0001 | `make help`, `make help-all`; generated recipes print `TASK_SURFACE_HELP_LINES` or `TASK_SURFACE_HELP_ALL_LINES`. | developer/agent | debug/investigation | None. | Default Make goal is `help`. | Human stdout; no harness artifact policy. | None observed. | None. | Parallel-safe. | Shell/printf failure only. | `Makefile`; `tools/task_surface.generated.mk`; `make help-all` | `observed/runtime_observed` | Covers `help`, `help-all`. |
| EP-0002 | `make <phase_command_target>` routes through `scripts/lib/run-phase.sh "<label>" -- <command...>` for generated `phase_command` recipes. | developer/ci/release/check/agent | test/validation/service/dev/fixture_update/cleanup depending on target | Target-specific Make env, exported `CARTULARY_TEST_TARGET`, and command args from `tools/task_surface.generated.mk`. | `CARTULARY_OUTPUT_MODE=summary`; `CARTULARY_TEST_RESULTS_DIR=.cartulary/test-results`; `CARTULARY_TEST_RUN_ID=<utc>-p<pid>` unless overridden. | Phase summaries, target summaries for many targets, stdout/stderr according to output policy. | Varies by target; includes local service start/reset, generated output, formatting, linting, build, release evidence, or validation. | Make prerequisites and wrapper setup run before child command. | Depends on target; service, cleanup, format, generate, and build targets are not assumed parallel-safe. | Child non-zero becomes phase failure; usage failures from child scripts usually exit `2`; runtime behavior not executed in S2. | `tools/task_surface.generated.mk`; `scripts/lib/run-phase.sh`; `scripts/lib/run-phase-common.sh`; `tools/task_surface_manifest.json` | `observed` | Row covers all `phase_command` targets listed in target reconciliation. |
| EP-0003 | `make <alias_target>` runs Make prerequisites and emits target summary for selected generated aliases. | developer/ci/release/check/agent | alias/setup/aggregate | Prerequisite target or file list from `tools/task_surface_manifest.json` `make_recipes`. | If target has no explicit command, Make freshness and prerequisite status control behavior. | Target summaries for some aliases; otherwise prerequisite output. | Varies by prerequisite; can install tools, build binaries, run checks, or mutate generated/build artifacts. | Strict Make prerequisite order unless Make parallelism is requested by caller. | Depends on prerequisites. | Prerequisite failure fails alias. | `tools/task_surface.generated.mk`; `tools/task_surface_manifest.json` | `observed` | Covers generated `alias` targets. |
| EP-0004 | `$(NODE_BIN) scripts/cartulary-runner.mjs go-target <target-or-command> [...]`. | Make/check/ci/developer | test | Go target name or runner subcommand; `GO`, `GO_CACHE_DIR`, `GO_MOD_CACHE_DIR`, `CARTULARY_TEST_TARGET`, `CARTULARY_GO_TARGET_PHASE`, `TEST_OUTPUT_SCRIPT`. | Runner context defaults Go to `go`, caches to `/tmp/cartulary-go-*`, output to summary mode. | Go target summaries, phase summaries, timing and package artifacts under result root. | Runs Go tests; service-backed variants may require external Postgres/MinIO through scheduler/wrapper. | Direct `backend-unit` uses go-target runner; service-backed Go targets usually flow through service-backed scheduler. | `backend-unit` is non-service-backed; service-backed Go shards are scheduler-controlled. | Usage errors exit `2`; child failures propagate; detailed runtime failures unobserved in S2. | `scripts/cartulary-runner.mjs`; `scripts/run-go-target.mjs`; `scripts/lib/go-target-runner.mjs`; `make target-plan` | `observed/runtime_observed` | Covers `backend-unit` as direct Make `go_target`. |
| EP-0005 | `$(NODE_BIN) scripts/cartulary-runner.mjs service-backed-target --target <target> --phase-label <label> --service-wrapper <test-services|none>`. | Make/check/ci | test/service-backed | `TEST_SERVICES_BIN`, `RUN_SERVICE_BACKED_SCHEDULE_SCRIPT`, `SERVICE_BACKED_SCHEDULE_MANIFEST`, `CARTULARY_TEST_GO_TARGET_RUNNER`. | `--service-wrapper test-services` requires `TEST_SERVICES_BIN`; wrapper injects `CARTULARY_SUPPRESS_CHILD_SUCCESS=1`. | Target summary with derived service-backed children. | Starts or reuses service wrapper when configured; runs service-backed scheduler. | Builds/prereqs are Make-owned before command; service-backed scheduler controls child work. | Scheduler resource claims govern child concurrency. | Missing `TEST_SERVICES_BIN` exits `2`; child/scheduler failure propagates after summary emission. | `scripts/cartulary-runner.mjs`; `tools/task_surface.generated.mk`; `tools/service_backed_schedule_manifest.json` | `observed` | Covers `backend-store`, `backend-integration`, `backend-integration-support`, `backend-process`. |
| EP-0006 | `$(NODE_BIN) scripts/run-service-backed-schedule.mjs --target <target> --manifest <path> [--defer-summary] [--resource-limit <name=value>...]`. | `cartulary-runner`, service-backed Make targets, phase slices | scheduler/service-backed | Schedule target, manifest, optional resource overrides; `MAKE`, `TEST_OUTPUT_SCRIPT`, `TEST_SERVICES_BIN`, `CARTULARY_TEST_GO_TARGET_RUNNER`, browser/session env. | Manifest defaults to `tools/service_backed_schedule_manifest.json`; browser batch manifest defaults to `tools/browser_e2e_batch_manifest.json`. | Scheduler summary, scheduler events, progress log, child target summaries. | Runs Go shards, Make targets, and browser stages under Postgres/MinIO/browser/process resource limits. | Expands `work_unit_sources` after validation; finalizers run after shards where needed. | Resource-limited by `postgres`, `minio`, `go_cpu`, `go_io`, `postgres_reset`, `process`, and browser lanes. | Usage errors exit `2`; scheduler/child failures fail schedule; live failure modes deferred to S5. | `scripts/run-service-backed-schedule.mjs`; `tools/service_backed_schedule_manifest.json`; `make explain-target TARGET=check-service-backed` | `observed/runtime_observed` | Covers `test-service-backed`, `test-fast-service-backed`, `check-service-backed` as scheduler targets. |
| EP-0007 | `$(NODE_BIN) scripts/run-check-schedule.mjs --target check --manifest "$(CHECK_SCHEDULE_MANIFEST)" [--resource-limit <name=value>...]`. | `make check`, `make ci`, `make release-check` | scheduler/full-gate | `CHECK_SCHEDULE_MANIFEST`, `CHECK_HOST_CPU_JOBS`, `CHECK_HOST_IO_JOBS`, `CARTULARY_SERVICE_BACKED_BROWSER_STACK_LIMIT`, `TEST_SERVICES_BIN`, `MAKE`, `TEST_OUTPUT_SCRIPT`. | Manifest defaults to `tools/check_schedule_manifest.json`; resource limits may be auto-estimated. | `check/target-summary.json`, `check/scheduler-summary.json`, `progress-summary.log`, run summary. | Runs readiness, static checks, service-backed scheduler sessions, browser stages, build and release-related checks. | Work units honor `needs`; nested service-backed scheduler units use service sessions. | Resource-limited by host CPU/IO and service/browser lanes. | Usage exits `2`; unknown resource overrides or child failures fail scheduler. | `scripts/run-check-schedule.mjs`; `tools/check_schedule_manifest.json`; `make explain-target TARGET=check` | `observed/runtime_observed` | `check` itself was not executed in S2. |
| EP-0008 | `scripts/run-make-sequence.sh --sequence <test-fast|test|ci|release-check> [--manifest <path>]`. | `make test-fast`, `make test`, `make ci`, `make release-check` | aggregate/test/release | Sequence name and `TASK_SURFACE_MANIFEST`; `MAKE`, `NODE_BIN`, `TEST_OUTPUT_SCRIPT`, optional `SERVICE_BACKED_SCHEDULE_MANIFEST`, `BROWSER_E2E_BATCH_MANIFEST`. | Manifest defaults to `tools/task_surface_manifest.json`; CI target exports `CI=1`. | Run summary and target summary for sequence plus child target summaries. | Runs child Make targets; may transitively start services, browser stack, release evidence, and builds. | Sequence manifest declares serial and parallel steps. | Only manifest-declared parallel steps use Make `-j`; `test-local` uses `jobs=3`. | Usage errors exit `2`; first failing child aborts later sequence steps after summary. | `scripts/run-make-sequence.sh`; `tools/task_surface_manifest.json`; `make explain-target TARGET=ci` | `observed/runtime_observed` | Covers `test-fast`, `test`, `ci`, `release-check`. |
| EP-0009 | `scripts/run-browser-e2e-target.sh <stage>` wraps `scripts/start-web-e2e.sh -- scripts/run-browser-e2e-batch.sh <stage> --defer-summary`. | Make/browser/package scripts/service-backed scheduler | browser/test | Browser stage, `BROWSER_E2E_BATCH_MANIFEST`, `NODE_BIN`, `TEST_OUTPUT_SCRIPT`, `PLAYWRIGHT_WORKERS`, `BROWSER_E2E_FUNCTIONAL_SHARDS`, repo-root server/migrate/testservices binaries. | Manifest defaults to `tools/browser_e2e_batch_manifest.json`; `PLAYWRIGHT_WORKERS` defaults from Make or Playwright shared config. | Browser target summaries, phase summaries, Playwright traces/reports on failures, owned-stack logs. | Starts web E2E stack unless external-server mode is used; may reset web E2E stack between groups. | Stage metadata controls child groups and summaries. | Stateful, measurement, visual, resettable, and isolated stages have explicit browser-stack resource assumptions. | Usage errors exit `2`; group failure marks child and aggregate summaries fail. | `scripts/run-browser-e2e-target.sh`; `scripts/run-browser-e2e-batch.sh`; `tools/browser_e2e_batch_manifest.json`; Playwright configs | `observed` | Covers eight browser batch targets. |
| EP-0010 | `bash ./scripts/start-web-e2e.sh [-- <command...>]`, `--session-start --env-file <path> --lease-file <path>`, or `--session-stop --lease-file <path>`. | Playwright webServer, browser target scripts, schedulers | service_start/service_stop/browser | `CARTULARY_SERVER_BIN`, `CARTULARY_MIGRATE_BIN`, `CARTULARY_TEST_SERVICES_BIN`, `NODE_RUNTIME_DIR`, `CARTULARY_WEB_E2E_*`, `CARTULARY_TEST_TARGET`. | Without `CARTULARY_WEB_E2E_ARTIFACT_DIR`, runtime root and logs are under `/tmp`; with target artifact dir, stack files are retained under target artifacts. | Stack env/json files, server/web logs, session lease/env files, Playwright state dir. | Starts/stops server and Vite processes, allocates ports/runtime roots, may prepare/cleanup web E2E state. | Child command runs after stack readiness; teardown follows command/session stop. | Not parallel-safe without isolated ports/runtime roots; scheduler lanes own parallel browser stack safety. | Usage errors exit `2`; child or teardown failures propagate according to script. | `scripts/start-web-e2e.sh`; `apps/web/playwright.shared.config.ts` | `observed` | Lifecycle details belong to S4/S5. |
| EP-0011 | `tools/testservices` commands: `run -- <command>`, `start-suite`, `prepare-web-e2e`, `cleanup-web-e2e`, `terminate-suite`, `warm-images`. | Make, service-backed scheduler, browser stack | service_start/service_stop/service_wrapper | Command args plus suite env from `internal/testutil/suiteservices`. | `run` pass-throughs when suite is already active; otherwise owns suite startup. | Suite service summaries, events, fixture summaries, cleanup summaries. | Starts Postgres and MinIO, warms images, prepares/cleans web E2E fixtures, terminates suite lease. | Startup preflight precedes service starts; cleanup follows child exit or startup failure. | Provides suite-level resource sharing; exact concurrency details deferred to S4/S6. | Usage errors exit `2`; child startup/service failures produce failure summaries and non-zero exit. | `tools/testservices/main.go`; `make explain-target TARGET=test-service-backed` | `observed/runtime_observed` | Built by `$(TEST_SERVICES_BIN)` Make prerequisite. |
| EP-0012 | `scripts/run-phase-slice.mjs --phase <phaseN> --mode <phase|service-backed> [--inside-service-wrapper] [--json]`. | `make phase-slice`, `make service-backed-slice` | test/phase-slice | `PHASE`, `JSON`, `TEST_SERVICES_BIN`, Node/Go/pnpm/server/migrate env, scheduler resource env. | Missing `PHASE` is usage error; `service-backed` mode re-execs inside testservices when service requirements exist. | Target summary plus phase-slice scheduler summary/events. | May run backend, frontend, browser, and service-backed slice work for one phase. | Setup targets are computed before work units; scheduler owns work-unit order. | Resource limits from phase slice plan. | Usage exits `2`; setup/work-unit failure fails target. | `scripts/run-phase-slice.mjs`; `scripts/lib/phase-slice-plan.mjs`; `make explain-phase PHASE=phase0..phase6` | `observed/runtime_observed` | Phase7/phase8 remain planned missing surfaces. |
| EP-0013 | `scripts/run-harness-smoke.mjs --tier <fast|extended|full> [--jobs <count>] [--manifest <path>]`. | `run-harness-smoke-*`, `check-harness-smoke`, `ci`, `release-check` | harness_selftest | Tier, jobs, task-surface manifest, `TEST_OUTPUT_SCRIPT`, `NODE_BIN`. | `HARNESS_SMOKE_JOBS` defaults to Make value `6` for Make targets; script default is `1` if env omitted. | Run summary, target summary, child check summaries. | Runs logical harness self-tests; can create scratch outside repo through self-test scripts. | Runs checks in parallel until first failure; skips remaining checks after first failure. | Parallelism bounded by `--jobs`. | Unknown option or invalid jobs throws; child check failure fails tier summary. | `scripts/run-harness-smoke.mjs`; task surface report logical harness checks | `observed/runtime_observed` | Logical check list is summarized below. |
| EP-0014 | `scripts/run-make-node-tool.sh <target>` and `scripts/run-make-node-tool.mjs <target>` dispatch Make node tools. | Make investigation/maintenance targets | investigation/maintenance | Per-tool Make env such as `PHASE`, `TARGET`, `RESULTS_DIR`, `RUN_ID`, `DETAIL`, `JSON`, baseline paths. | Result-dir defaults vary by tool: required, current run default, or results root default. | Human output or `tool_run_summary.v2` depending on output policy. | Drift tools read retained artifacts; baseline update tools mutate committed baselines if executed. | None beyond per-tool validation. | Mostly read-only except update targets; parallel safety depends on target/output paths. | Usage errors exit `2`; child failure yields tool summary when wrapping applies. | `scripts/run-make-node-tool.mjs`; `scripts/lib/make-node-tools.mjs` | `observed` | Covers all generated `node_tool` Make targets. |
| EP-0015 | `cartulary-runner.mjs summary-target --target <target> --child-target <target> --status <pass|fail> [...]`. | Check wrapper targets | summary/projection | Target, child target, requested status, optional projection/phase label. | Phase label defaults to `<target> child <childTarget>`. | Parent target summary projected from child. | Runs child Make target. | Child target completes before parent summary. | Depends on child target. | Usage exits `2`; child failure forces parent fail. | `scripts/cartulary-runner.mjs`; generated `summary_target` recipes | `observed` | Covers five check-internal duration/harness summary wrappers. |
| EP-0016 | Root package scripts: `pnpm build`, `pnpm test`, `pnpm typecheck`; app scripts under `apps/web` for Vite, Vitest, Biome, and browser aliases. | developer/package script | package_script/test/build/dev/lint/browser | Node/pnpm workspace env; app scripts call Vite/Vitest or repo scripts. | Package scripts do not inherit Make result-root/output-policy setup unless invoked by Make. | Vite/Vitest/Playwright output; package-script artifacts depend on tool defaults. | `build` creates frontend assets; tests may create coverage/results; browser aliases start E2E flows. | `apps/web test:e2e` runs functional then stateful with shell `&&`. | Package scripts bypass Make scheduler resource policy unless scripts re-enter harness wrappers. | Tool/script failure propagates through pnpm. | `package.json`; `apps/web/package.json`; package manifests | `observed` | `packages/*` manifests currently declare no scripts. |
| EP-0017 | Provider-neutral CI scripts: `scripts/ci/verify.sh` and `scripts/ci/check-deployable-shape.sh`; public CI target is `make ci`. | external CI/developer | ci/release | Script env and Make env; `.github/**` unavailable. | `make ci` exports `CI=1`; provider workflow defaults unknown. | CI target/run summaries when `make ci` is used. | Runs `check` through the Make sequence; deployable-shape is a check-internal work unit. The stale extended harness smoke is no longer a blocking CI sequence step. | `ci` sequence runs `check`. | Parallelism delegated to `check` scheduler. | Script or child failure fails CI; provider workflow behavior unknown. | `scripts/ci/**`; `tools/task_surface.generated.mk`; `tools/task_surface_manifest.json`; source limit `SL-0001`; `MD-S7-0015`; `MD-S7-0016` | `observed/source_limit/maintainer_decision` | `.github/**` remains absent. `run-harness-smoke-extended` remains an explicit diagnostic target. |
| EP-0018 | Playwright direct config command from package/browser scripts: `pnpm --dir apps/web exec playwright test --config playwright.webserver-backed.config.ts` or default Playwright config. | browser wrappers/package scripts | browser/test | `CARTULARY_PLAYWRIGHT_FUNCTIONAL_GREP`, `CARTULARY_PLAYWRIGHT_FUNCTIONAL_FILES`, `CARTULARY_PLAYWRIGHT_SUPPORT_GREP`, `CARTULARY_PLAYWRIGHT_SUPPORT_FILES`, `CARTULARY_PLAYWRIGHT_EXTERNAL_SERVER`, `CARTULARY_WEB_E2E_PUBLIC_ORIGIN`, `PLAYWRIGHT_WORKERS`. | Shared config defaults public origin to `http://127.0.0.1:4173`, workers to `2`, trace to `retain-on-failure`; webserver-backed config requires grep/files env. | Playwright stdout, traces on failure, reports/test-results. | May start web server through Playwright `webServer` when external-server mode is not set. | Global setup before tests, global teardown after tests. | `fullyParallel=false`; workers configurable. | Missing webserver-backed env throws config error. | `apps/web/playwright.shared.config.ts`; `apps/web/playwright.webserver-backed.config.ts`; browser scripts | `observed` | Runtime failure bundles deferred to S3/S5. |
| EP-0019 | Vitest direct command from package/Make wrappers: `vitest`, `pnpm test --run`, or `scripts/run-frontend-unit.sh`. | Make/package script/developer | frontend_unit | `VITEST_FLAGS`, `VITEST_MAX_WORKERS`, Vite config, setup files. | Vite config includes `browser-unit` and `harness-node`; Make sets quiet flags in summary modes. | Vitest stdout and frontend target/phase summaries through Make wrapper. | May write coverage/cache depending on Vitest args; ordinary Make frontend-unit is validation. | Setup files run before tests. | Worker count controlled by `VITEST_MAX_WORKERS` in Make wrapper. | Tool failure propagates. | `apps/web/vite.config.ts`; `scripts/run-frontend-unit.sh`; package scripts | `observed` | Product assertions remain outside S2. |
| EP-0020 | Mutating local/maintenance commands: `make generate`, `make format`, `make phase-ledgers`, `make phase-schedules`, baseline refresh targets, `make clean`, `make distclean`. | developer/agent | fixture_update/generated_update/cleanup | Target-specific Make vars such as baseline paths, `RESULTS_DIR`, formatter/check flags, clean path vars. | Defaults are declared in `Makefile`; `agent-finalize` skips duration refresh when `RESULTS_DIR` is unset. | Generated files, formatted files, committed ledgers/schedules/baselines, or removed ignored artifacts. | Mutates repo-tracked generated/docs/baseline artifacts or deletes ignored runtime/build/tool artifacts. | Make prerequisites and script logic own order. | Do not run concurrently with verification or other writers unless owner says so. | Failures are target/script-specific; not executed in S2. | `Makefile`; `tools/task_surface.generated.mk`; update/check scripts | `observed/source_limit` | Documented because they are entrypoints, but not executed. |
| EP-0021 | Uninvoked candidate `scripts/test-run-go-target-fast.sh`. | unknown/manual | harness_selftest/unknown | Script-local scratch/test env if manually run. | No discovered caller. | Unknown as active harness entrypoint. | Unknown. | None discovered. | Unknown. | Not executed; no active task-surface row. | `uninvoked-surface-list.md`; S2 direct reference search inherited from S1 | `observed/source_limit` | Carry forward as uninvoked by discovered entrypoints. |

## Target row reconciliation

Every current task-surface target is represented by one command family below.
The family rows point back to the detailed command map above.

| Family | Entrypoint row | Count | Targets |
|---|---|---:|---|
| `print_help` | `EP-0001` | 2 | `help`, `help-all` |
| `phase_command` | `EP-0002` | 52 | `doctor`, `bootstrap`, `frontend-toolchain`, `frontend-install-ci`, `db-up`, `db-reset`, `services-up`, `postgres-wait`, `minio-wait`, `minio-init`, `dev`, `generate`, `generate-artifacts`, `generate-drift`, `generated-artifact-policy-check`, `json-shape-check`, `toolchain-drift`, `migration-drift`, `deployable-shape`, `phase-map-check`, `phase-ledgers`, `phase-ledger-drift`, `phase-schedules`, `phase-schedule-drift`, `agent-finalize`, `benchmark-claim-check`, `task-surface-check`, `backend-task-surface-check`, `service-backed-unit-check`, `test-service-images`, `run-harness-smoke-fast`, `run-harness-smoke-extended`, `run-harness-smoke-full`, `phase-test-name-check`, `frontend-typecheck`, `frontend-unit`, `frontend-task-surface-check`, `frontend-import-boundary-check`, `lint-biome`, `lint-scripts`, `lint-shell`, `format-go`, `format-frontend`, `browser-e2e-task-surface-check`, `lint-go-format`, `lint-go-vet`, `lint-go-staticcheck`, `go-vulncheck`, `go-gosec-targeted`, `go-gosec-audit`, `license-report`, `sbom` |
| `alias` | `EP-0003` | 19 | `bootstrap-node-runtime`, `frontend-install`, `playwright-install`, `services-wait`, `codegen-toolchain`, `go-lint-toolchain`, `govulncheck-toolchain`, `gosec-toolchain`, `go-security-toolchain`, `shell-lint-toolchain`, `format`, `test-local`, `lint`, `lint-go`, `check-frontend-install`, `build`, `build-server`, `build-migrate`, `build-web` |
| `go_target` | `EP-0004` | 1 | `backend-unit` |
| `service_backed_target` | `EP-0005` | 4 | `backend-store`, `backend-integration`, `backend-integration-support`, `backend-process` |
| `service_backed_schedule` | `EP-0006` | 3 | `test-service-backed`, `test-fast-service-backed`, `check-service-backed` |
| `check_schedule` | `EP-0007` | 1 | `check` |
| `sequence` | `EP-0008` | 4 | `test-fast`, `test`, `ci`, `release-check` |
| `browser_batch` | `EP-0009` | 8 | `browser-e2e`, `browser-e2e-webserver-backed`, `browser-e2e-functional`, `browser-e2e-support`, `browser-e2e-stateful`, `browser-e2e-resettable`, `browser-e2e-measurement`, `browser-e2e-visual` |
| `node_tool` | `EP-0014` | 21 | `task-surface-report`, `task-guide`, `phase-slice`, `service-backed-slice`, `target-plan`, `target-plan-json`, `fixture-report`, `explain-run`, `explain-phase`, `explain-target`, `go-test-duration-baselines`, `go-test-duration-baseline-coverage`, `go-test-duration-baseline-drift`, `browser-e2e-duration-baselines`, `browser-e2e-duration-baseline-drift`, `service-backed-make-target-duration-baselines`, `service-backed-make-target-duration-baseline-drift`, `harness-smoke-duration-baselines`, `harness-smoke-duration-baseline-drift`, `scheduler-event-order-drift`, `scheduler-summary-timing-drift` |
| `summary_target` | `EP-0015` | 5 | `check-harness-smoke`, `check-go-test-duration-baseline-drift`, `check-browser-e2e-duration-baseline-drift`, `check-service-backed-make-target-duration-baseline-drift`, `check-harness-smoke-duration-baseline-drift` |
| `cleanup` | `EP-0020` | 2 | `clean`, `distclean` |

## Aggregate flows

| Flow | Expansion | Evidence | Evidence status |
|---|---|---|---|
| `test-fast` | Parallel `test-local` with `jobs=3` produces `backend-unit`, `frontend-typecheck`, `frontend-unit`; then serial `test-fast-service-backed`. | `tools/task_surface_manifest.json` sequence `test-fast` | `observed` |
| `test` | Parallel `test-local` with `jobs=3`; then serial `test-service-backed`, which includes backend service-backed and browser leaves. | `tools/task_surface_manifest.json` sequence `test` | `observed` |
| `ci` | Serial `check`; target exports `CI=1`. | `tools/task_surface_manifest.json`; generated `ci` recipe; `MD-S7-0016` | `observed/maintainer_decision` |
| `release-check` | Serial `check`, `license-report`, `sbom`, `build`. | `tools/task_surface_manifest.json` sequence `release-check`; `MD-S7-0016` | `observed/maintainer_decision` |
| `check` | One check scheduler target with `96` work units, including nested service-backed work and browser-stage resource claims. | `tools/check_schedule_manifest.json`; `make explain-target TARGET=check` | `observed/runtime_observed` |
| `test-service-backed` | `8` work-unit sources: backend store/integration/support/process plus browser webserver-backed, stateful, measurement, visual. | `tools/service_backed_schedule_manifest.json` | `observed` |
| `test-fast-service-backed` | `4` work-unit sources: backend store, integration, integration-support, process. | `tools/service_backed_schedule_manifest.json` | `observed` |
| `check-service-backed` | `8` work-unit sources matching full service-backed backend and browser stage surface. | `tools/service_backed_schedule_manifest.json` | `observed` |

## Package scripts

| Package surface | Script | Declared command | Relationship to Make surface | Evidence | Evidence status |
|---|---|---|---|---|---|
| Root `package.json` | `build` | `pnpm --dir apps/web build` | Bypasses Make build wrappers and result summaries. | `package.json` | `observed` |
| Root `package.json` | `test` | `pnpm --dir apps/web test --run` | Runs Vitest directly, not `make frontend-unit`. | `package.json`; `apps/web/vite.config.ts` | `observed` |
| Root `package.json` | `typecheck` | `tsc -b tsconfig.json --noEmit --incremental false --pretty false` | Used by Make `frontend-typecheck` through `pnpm typecheck`. | `package.json`; generated recipe | `observed` |
| `apps/web/package.json` | `build` | `vite build` | Used indirectly by Make build-web through Vite exec, but direct script bypasses target summary. | `apps/web/package.json`; `Makefile` | `observed` |
| `apps/web/package.json` | `dev` | `vite` | Local frontend dev only. | `apps/web/package.json` | `observed` |
| `apps/web/package.json` | `format` | `bash ../../scripts/run-frontend-biome.sh write` | Mutating package alias; differs from Make `format-frontend` command mode `format`. | `apps/web/package.json` | `observed` |
| `apps/web/package.json` | `lint` | `bash ../../scripts/run-frontend-biome.sh check` | Similar child script to `make lint-biome`, but bypasses Make env and summary. | `apps/web/package.json` | `observed` |
| `apps/web/package.json` | `test` | `vitest` | Direct Vitest watch/default behavior; Make uses `scripts/run-frontend-unit.sh`. | `apps/web/package.json`; `apps/web/vite.config.ts` | `observed` |
| `apps/web/package.json` | `test:e2e` | `pnpm run test:e2e:functional && pnpm run test:e2e:stateful` | Package-level serial browser alias; bypasses Make aggregate sequence. | `apps/web/package.json` | `observed` |
| `apps/web/package.json` | `test:e2e:functional` | `bash ../../scripts/run-browser-e2e-functional.sh` | Enters browser wrapper without Make target summary setup. | `apps/web/package.json`; browser script | `observed` |
| `apps/web/package.json` | `test:e2e:stateful` | `bash ../../scripts/run-browser-e2e-stateful.sh` | Enters browser wrapper without Make target summary setup. | `apps/web/package.json`; browser script | `observed` |
| `apps/web/package.json` | `test:e2e:visual` | `bash ../../scripts/run-browser-e2e-visual.sh` | Enters browser wrapper without Make target summary setup. | `apps/web/package.json`; browser script | `observed` |
| `apps/web/package.json` | `typecheck` | `pnpm --workspace-root typecheck` | Delegates to root typecheck. | `apps/web/package.json` | `observed` |
| `packages/*/package.json` | none | No package-level scripts declared. | No direct package script entrypoints. | `packages/*/package.json` inspection | `observed` |

## Harness smoke checks

The task surface declares `47` logical harness smoke checks. They are invoked
through `scripts/run-harness-smoke.mjs` by tier, not as public Make targets.

| Tier surface | Checks | Evidence | Evidence status |
|---|---|---|---|
| Harness smoke logical checks | `harness-smoke-toolchain-pins`, `harness-smoke-bootstrap-node-runtime`, `harness-smoke-bootstrap-shellcheck`, `harness-smoke-lint-shell`, `harness-smoke-build-input-discovery`, `harness-smoke-check-migrations`, `harness-smoke-generate-drift`, `harness-smoke-generated-artifact-policy`, `harness-smoke-json-shapes`, `harness-smoke-run-go-staticcheck`, `harness-smoke-run-go-govulncheck`, `harness-smoke-run-go-gosec-targeted`, `harness-smoke-run-go-gosec-audit`, `harness-smoke-run-make-sequence-fast`, `harness-smoke-run-make-sequence`, `harness-smoke-release-task-surface`, `harness-smoke-sbom-license-evidence`, `harness-smoke-benchmark-claim-check`, `harness-smoke-task-surface-report`, `harness-smoke-make-node-tools`, `harness-smoke-execution-topology`, `harness-smoke-guidance-core`, `harness-smoke-guidance-matrix`, `harness-smoke-phase-slice`, `harness-smoke-go-test-duration-baselines`, `harness-smoke-service-backed-make-target-duration-baselines`, `harness-smoke-harness-duration-baselines`, `harness-smoke-browser-shard-plan`, `harness-smoke-phase-test-names`, `harness-smoke-run-phase`, `harness-smoke-run-go-target`, `harness-smoke-print-target-plan`, `harness-smoke-service-backed-scheduler-smoke`, `harness-smoke-service-backed-scheduler-matrix`, `harness-smoke-cartulary-runner-service-backed-target`, `harness-smoke-check-scheduler-smoke`, `harness-smoke-check-scheduler`, `harness-smoke-tool-output-real-targets`, `harness-smoke-run-playwright-phase`, `harness-smoke-run-playwright-manifest-phase`, `harness-smoke-run-playwright-webserver-batch`, `harness-smoke-run-vitest-phase`, `harness-smoke-run-vitest-manifest-phase`, `harness-smoke-run-frontend-unit`, `harness-smoke-frontend-import-boundaries`, `harness-smoke-web-e2e-lifecycle`, `harness-smoke-dev-stack-lifecycle` | `make task-surface-report TASK_SURFACE_REPORT_ARGS=--all`; `tools/task_surface_manifest.json` | `runtime_observed/observed` |

## Entry point questions answered

| Question | S2 answer | Evidence status |
|---|---|---|
| What are all current harness entrypoint categories? | Make target families, package scripts, core orchestration CLIs, service wrapper commands, browser batch/stage scripts, Playwright/Vitest configs, provider-neutral CI scripts, and one uninvoked candidate. | `observed` |
| Which surface is primary? | Make/task-surface is primary for developer, check, CI, release, and agent workflows. | `observed/runtime_observed` |
| Which package commands bypass Make? | Root `test` and app `test`, `build`, `lint`, `format`, and browser aliases can run without Make output/result policy. | `observed` |
| Which commands mutate state? | `generate`, `format`, ledgers/schedules, baseline refresh targets, release evidence generation, clean/distclean, local service commands, and browser/service wrappers can mutate generated, ignored, or external state. | `observed` |
| Which CI commands are recoverable? | `make ci`, `scripts/ci/verify.sh`, and `scripts/ci/check-deployable-shape.sh`; provider workflow files under `.github/**` are absent. | `observed/source_limit` |
| Which behavior remains unknown? | Runtime success/failure behavior for broad gates, live service lifecycle, provider CI workflow order, failure bundle schemas, and exact env precedence where multiple layers set the same names. | `source_limit` |
