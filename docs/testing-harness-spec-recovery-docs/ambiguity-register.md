---
doc_id: THR-S1-AMBIGUITY
title: Testing Harness Recovery Ambiguity Register
status: active
role: ambiguity-register
---

# Testing Harness Recovery Ambiguity Register

## Document role

This register records ambiguity, risk, and conflict points discovered during S1.
Rows should remain until resolved by later evidence or maintainer decision.

## Ambiguities

| Ambiguity ID | Surface | Ambiguity type | Conflicting or missing facts | Impact | Owner required | Current workaround | Decision prompt | Evidence | Evidence status | Resolution status | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| AMB-0001 | `.github/**` versus `scripts/ci/**` | `authority_gap` | CI workflow configuration was requested as recovery input, but `.github/` is absent; `scripts/ci/check-deployable-shape.sh` and `scripts/ci/verify.sh` exist. | S2 cannot map provider CI steps from repository workflow files. | Maintainer or repository owner | Treat `scripts/ci/**` as candidate provider-neutral CI scripts and keep `.github/**` as source limit. | Is CI intentionally external to this checkout, absent for now, or expected to be represented only by `scripts/ci/**`? | `.github` absence check; `find scripts/ci -type f`; `make help-all` names `ci`. | `source_limit` | `open` | Mirrors `SL-0001`. |
| AMB-0002 | `.cartulary/test-results/**` ignored status | `implicit_sequence` | `.cartulary/test-results/**` is ignored by the unanchored `test-results/` rule, while `.cartulary/release-artifacts/**` has an explicit ignore rule. | Artifact ownership is observable but ignore policy is indirect; S3 should clarify intentional retention and cleanup rules. | Harness owner | Record as ignored runtime artifact, not committed evidence. | Should the recovered spec name `.cartulary/test-results/**` as the canonical result root even though the ignore rule is indirect? | `git check-ignore -v .cartulary/test-results`; `.gitignore`; `Makefile` `CARTULARY_TEST_RESULTS_DIR`. | `observed` | `open` | No implementation change proposed in S1. |
| AMB-0003 | Committed generated artifacts | `authority_gap` | `internal/gen/**`, `packages/protocol-ts/src/generated/**`, and `tools/task_surface.generated.mk` are committed and generated. | Later specs must avoid treating generated outputs as behavior owners while still recording their execution impact. | Harness/spec owner | Use `tools/generated_artifact_policy.json` as owner evidence and mark generated outputs `evidence_only` or downstream. | Which generated surfaces may drive harness execution contracts, and which are only derived evidence? | `tools/generated_artifact_policy.json`; generated file markers. | `observed` | `open` | S3 should split codegen drift from artifact ownership. |
| AMB-0004 | Planned phase7/phase8 registry entries | `missing_default` | `tools/phase_registry.json` names phase7/phase8 manifest and ledger paths, but those files are absent. | `explain-phase` active coverage is phase0 through phase6; planned future phases must not be counted as missing active coverage unless owner says so. | Phase/harness owner | Treat phase7/phase8 as planned, not active, for S1 inventory. | Should planned phase registry entries be represented in the recovered harness spec now or deferred until the files exist? | `jq '.' tools/phase_registry.json`; `test -e` checks for phase7/phase8 paths. | `source_limit` | `open` | Also recorded as `SL-0005`. |
| AMB-0005 | Harness mechanics embedded in product tests | `authority_gap` | Product tests import harness utilities and sometimes define setup/teardown helpers locally; exact boundary between product evidence and harness contract is not explicit. | Future NLSpec could accidentally over-specify product assertions as harness requirements. | Harness/spec owner | Record representative embedded logic separately and defer preservation decisions. | Which embedded helpers should be recovered as harness behavior versus left as test implementation details? | `embedded-harness-logic-list.md`; `find cmd internal tools -name '*_test.go'`; `find apps/web/e2e`. | `observed` | `open` | Important for S5/S8. |
| AMB-0006 | `internal/app/test_runtime_reset.go` owner boundary | `authority_gap` | Test-only reset route is in `internal/app`, not under `internal/testutil`, and mutates database/object-store state for tests. | Could be product-runtime test hook, harness-only behavior, or an implementation detail that should not become public contract. | Maintainer/harness owner | Mark `owner_decision_required`; do not rewrite or specify normative behavior in S1. | Should test runtime reset be inside the recovered harness spec, and if so under what visibility/security boundary? | `sed -n '1,220p' internal/app/test_runtime_reset.go`. | `observed` | `open` | High-impact because it resets runtime state. |
| AMB-0007 | Service-backed lifecycle ownership | `implicit_sequence` | Service lifecycle spans Make, scheduler manifests, `tools/testservices`, `pgtest`, `s3test`, Playwright stack scripts, Docker/testcontainers, and retained artifacts. | S1 can inventory surfaces but cannot establish start/ready/reset/stop ordering without S4 tracing. | Harness owner | Defer lifecycle mapping to S4 and avoid guessing. | Which layer owns the normative lifecycle: Make target, scheduler manifest, `tools/testservices`, or testutil helpers? | `make explain-target`; `tools/testservices/main.go`; `pgtest.go`; `s3test.go`; `start-web-e2e.sh`. | `observed` | `open` | Not a blocker for S2. |
| AMB-0008 | Cleanup owner and idempotency | `resource_owner_unknown` | Cleanup behavior appears in `make clean`, `make distclean`, shell traps in script self-tests, `tools/testservices` cleanup/reaper, Playwright teardown, Go `t.Cleanup`, and package `TestMain`. | S3 must decide which cleanup behaviors are canonical, diagnostic, or best-effort. | Harness owner | Record surfaces only; defer cleanup lifecycle matrix. | Which cleanup paths are required on success/failure/timeout/interrupt, and which are local convenience cleanup? | `Makefile` cleanup macros; `scripts/test-*.sh`; `tools/testservices/main.go`; Playwright teardown files. | `observed` | `open` | S1 did not execute cleanup. |
| AMB-0009 | Local developer commands versus verification contract | `authority_gap` | `make help-all` lists local-dev, fast verification, full gates, phase maintenance, and release targets; some helper-only targets still create harness artifacts. | NLSpec boundary must not over-constrain local convenience behavior, but some local commands drive verification setup. | Harness/spec owner | Classify local-dev commands as `defer_to_later_sprint` unless they feed verification. | Which local-dev commands belong in the recovered harness contract, and which should remain operator guidance only? | `make help-all`; `make task-guide`; `task-surface-report --all`. | `runtime_observed` | `open` | S2 command map should retain help tier and included-in fields. |
| AMB-0010 | Runtime-only reports, traces, and failure bundles | `schema_unknown` | S1 found retained `.cartulary/test-results/**` summaries and configured Playwright trace retention, but did not run failures to inspect failure-only bundles. | Report schemas and stable consumer contracts remain incomplete. | Harness owner | Mark runtime-only gaps in source-limit log; recover in S3/S5 from retained artifacts and targeted non-mutating inspection. | Which output surfaces are machine-consumed stable interfaces versus diagnostics? | `.cartulary/test-results/**`; Playwright config `trace: "retain-on-failure"`; `scripts/lib/test-output.*`. | `observed/source_limit` | `open` | Not a blocker for S2 entrypoint tracing. |
| AMB-0011 | Package scripts versus Make task surface | `authority_gap` | Root and `apps/web` package scripts can invoke Vite, Vitest, Biome, and browser wrapper scripts directly, bypassing Make result-root, target-summary, and scheduler policy unless the child script re-enters the harness. | Later NLSpec work must decide whether package scripts are first-class harness contracts or developer convenience aliases. | Harness/spec owner | S2 records package scripts separately from Make targets and does not normalize them into Make behavior. | Should direct package scripts be specified as supported harness entrypoints, or should Make remain the canonical command surface? | `package.json`; `apps/web/package.json`; `entrypoint-command-map.md` package script table. | `observed` | `open` | `packages/*/package.json` currently declare no scripts. |
| AMB-0012 | Environment-variable precedence across Make, scripts, schedulers, and test configs | `missing_default` | S2 observed defaults and env names in Make, generated recipes, Node tools, Playwright config, Vitest config, scheduler scripts, and service wrappers, but did not prove precedence where multiple layers set or pass through the same variable. | A future spec could overstate override precedence or accidentally bless implementation-specific env filtering. | Harness owner | Record declared defaults and mark unresolved precedence as `TODO: precedence_unknown` in later detailed lifecycle/interface maps. | Which env vars are public override contracts, and what is their precedence across Make, package scripts, schedulers, and test runner configs? | `Makefile`; `tools/task_surface.generated.mk`; `scripts/lib/make-node-tools.mjs`; `apps/web/playwright*.config.ts`; `apps/web/vite.config.ts`. | `observed` | `open` | S2 did not execute override matrix tests. |
| AMB-0013 | Mutating maintenance commands in harness contract | `authority_gap` | `generate`, `format`, phase ledger/schedule generation, duration baseline refresh targets, release evidence generation, `clean`, and `distclean` are command entrypoints but intentionally mutate generated, committed, ignored, or external state. | S3/S8 must decide which writer commands are harness-owned update workflows versus local maintenance conveniences. | Harness/spec owner | S2 documents these as entrypoints but does not execute or normalize them as validation behavior. | Which mutating commands are normative harness update surfaces, and which are out-of-contract local maintenance tools? | `entrypoint-command-map.md` `EP-0020`; generated Make recipes; task surface help tiers. | `observed/source_limit` | `open` | No writer command was executed during S2. |
| AMB-0014 | Static command contracts versus runtime-observed failure behavior | `schema_unknown` | S2 recovered usage strings, declared child commands, output policies, and aggregate flows, but did not run broad gates, live service-backed targets, browser E2E, cleanup, format, generate, CI, release, or failure scenarios. | S5 must avoid treating static command declarations as fully observed failure contracts. | Harness owner | S2 labels runtime gaps as source limits and defers failure taxonomy to S5. | Which declared failure behaviors are stable public contracts versus incidental implementation details? | `entrypoint-command-map.md`; `sequencing-assumption-list.md`; `source-limit-log.md` `SL-0006`, `SL-0007`. | `observed/source_limit` | `open` | Completeness is row-level for entrypoints, not runtime proof. |
| AMB-0015 | Fixture, golden, and visual snapshot update authority | `authority_gap` | S3 found committed fixtures, goldens, and visual snapshots but no repository-owned update or refresh command for them in S2. | Later NLSpec work cannot specify mutation permissions without an owner decision. | Harness/spec owner | Mark rows `TODO: update_rule_unknown`; default until answered is that no refresh command is supported and committed fixtures/goldens change only by owner-reviewed source edits. | Are committed fixtures and goldens updated only by owner-reviewed source edits, or should a supported refresh command be created later? | `artifact-ownership-matrix.md` `ART-0001..ART-0003`, `ART-0005`; `entrypoint-command-map.md`; fixture/golden/snapshot directories. | `observed/source_limit` | `open` | S8 or explicit owner decision should close this before final NLSpec mutation rules. |
| AMB-0016 | Derived reports consumed by execution | `authority_gap` | Some derived reports are diagnostic-only, while schedules, ledgers, duration baselines, summaries, and service fixture summaries are consumed by drift, scheduling, reporting, or investigation tools. | NLSpec must not classify every derived output as non-authoritative diagnostics. | Harness owner | Record derived reports as non-authoritative unless consumed by a named entrypoint. | Which derived reports are stable machine interfaces versus human diagnostics? | `artifact-ownership-matrix.md` `ART-0009..ART-0017`, `ART-0029`; scheduler and fixture reporting scripts. | `observed` | `open` | S5 should refine observable interface stability. |
| AMB-0017 | Retained artifact freshness and run selection | `schema_unknown` | `.cartulary/test-results/**` retained runs exist locally, but S3 did not validate provenance, freshness, or completeness; artifact discovery and fixture-reporting code can choose newest candidates. | Tools may read stale local evidence if run ID or result dir selection is ambiguous. | Harness owner | Treat retained artifact provenance as source-limited; proposed rule is that explain/fixture tools may use newest only for human investigation and must print selected `run_id`, while drift and baseline tools require explicit `RESULTS_DIR` or active `CARTULARY_TEST_RUN_ID`. | Should investigation tools allow newest-run fallback, and what is the supported selection rule for retained artifacts used by explain, fixture, drift, and baseline tools? | `.cartulary/test-results/**`; `scripts/lib/artifact-discovery.mjs`; `scripts/lib/make-node-tools.mjs`; `scripts/lib/fixture-reporting.mjs`; `artifact-ownership-matrix.md` `ART-0013..ART-0017`. | `observed/source_limit` | `open` | S5 should close before observable-interface and baseline/drift schema recovery. |
| AMB-0018 | Cleanup behavior on timeout and interrupt | `schema_unknown` | Static source shows traps, process-group cleanup, contexts, and cleanup status recording, but S3 did not execute timeout or interrupt scenarios. | Future specs could overstate cleanup guarantees for browser and service-backed failures. | Harness owner | Mark timeout/interrupt cleanup fields as source-limited; default until S4/S6 evidence is that no timeout or interrupt cleanup path is a guaranteed contract. | Which cleanup paths are guaranteed on timeout or interrupt versus best-effort? | `cleanup-lifecycle-matrix.md`; `scripts/start-web-e2e.sh`; `tools/testservices/main.go`; `scripts/lib/process-lifecycle.sh`. | `observed/source_limit` | `open` | S4/S6 should recover runtime lifecycle and timing evidence if authorized. |
| AMB-0019 | External state cleanup and destructive janitor boundaries | `resource_owner_unknown` | Testservices can clean stale generated browser DB/bucket fixtures, but S3 did not execute janitor paths and relies on static generated-fixture checks. | A spec needs explicit boundaries before blessing destructive cleanup. | Harness owner | Record janitor as observed static behavior with S4/S6 handoff; default boundary is only generated web E2E names plus service-scope metadata. | Which DBs, buckets, and metadata files may stale-fixture janitors destroy, and what proves generated ownership? | `tools/testservices/main.go`; `cleanup-lifecycle-matrix.md` `CLN-0012`, `CLN-0013`; `shared-state-hazard-list.md` `HAZ-S3-0010`. | `observed/source_limit` | `open` | S4 should map lifecycle; S6 should classify destructive cleanup hazards. |
| AMB-0020 | Package-script artifact and cleanup authority | `authority_gap` | Package scripts can run Vitest, Playwright, Vite, Biome, and browser wrappers outside Make result-root and scheduler policy. | Direct package execution can produce artifacts with weaker cleanup/result contracts. | Harness/spec owner | Keep package-script artifacts separate with `TODO: owner_unknown`; default until answered is Make canonical and package scripts developer conveniences except where they re-enter harness wrappers. | Are direct package scripts first-class harness contracts? | `artifact-ownership-matrix.md` `ART-0030`; `cleanup-lifecycle-matrix.md` `CLN-0020`; S2 package script table. | `observed` | `open` | S8 should close with `preserve`, `exclude_from_contract`, or `authority_decision_required`. |
| AMB-0021 | External Go cache cleanup | `resource_owner_unknown` | Go cache defaults can point at `/tmp/cartulary-go-*`, outside repo cleanup and distclean. | Stale external cache behavior may affect local runs but may be outside harness contract. | Harness/spec owner | Proposed classification is tool-managed external caches, documented but not removed by `clean` or `distclean`. | Are `/tmp/cartulary-go-*` caches in the cleanup contract? | `artifact-ownership-matrix.md` `ART-0024`; S2 command map; generate-drift defaults. | `observed` | `open` | Low severity; S8 should classify as outside contract unless an owner expands cleanup policy. |
| AMB-0022 | Browser visual snapshot platform authority | `authority_gap` | Committed Playwright visual snapshots are Linux PNG baselines, but S3 did not find a platform/update policy or supported refresh command. | Visual tests may be platform-sensitive and mutation authority is unclear. | Harness/browser owner | Record visual snapshots as canonical snapshots with `TODO: update_rule_unknown`; default until answered is that snapshots are Linux canonical baselines and validation-only targets must not refresh them. | Which OS, browser version, and command may refresh Playwright visual snapshots? | `apps/web/e2e/workbook.visual.spec.ts-snapshots/**`; `apps/web/playwright.shared.config.ts`; browser target map. | `observed/source_limit` | `open` | This is a focused child of `AMB-0015`; S8 or browser owner decision required before snapshot mutation rules. |
| AMB-0023 | S4 static service lifecycle versus live readiness behavior | `schema_unknown` | S4 mapped readiness checks from source, but did not execute Docker, Postgres, MinIO, browser stack, reset route, or service-backed runtime paths. | Later specs could overstate readiness guarantees, retry timing, or failure output based only on static paths. | Harness owner | Treat `service-lifecycle-map.md` readiness fields as source-observed unless marked runtime-observed by a later sprint; keep `TODO: readiness_unknown` where no route-specific probe exists. | Which readiness behaviors are stable contracts after live service-backed and browser observation? | `service-lifecycle-map.md`; `source-limit-log.md` `SL-0012`; S1 `SL-0003`; S3 `SL-0011`. | `observed/source_limit` | `open` | Blocks final readiness/timing claims, not S5 static interface mapping. |
| AMB-0024 | Detached service reaper completion guarantee | `schema_unknown` | Source proves reaper scheduling and direct cleanup fallback, but not that a parent command observes detached reaper completion, success, or failure after scheduling. | Cleanup guarantees for suite-owned containers and retired browser fixtures could be overstated. | Harness owner | Classify detached reaper cleanup as `source_limited_best_effort` until controlled runtime evidence or owner decision proves stronger behavior. | Is detached reaper completion part of the caller-visible contract, or only best-effort cleanup? | `service-lifecycle-map.md` `SVC-0001`; `resource-allocation-register.md` `RES-0012`; `shared-state-hazard-list.md` `HAZ-S4-0003`; `source-limit-log.md` `SL-0014`. | `observed/source_limit` | `open` | S6 should recover timeout/interrupt/reaper timing if authorized. |
| AMB-0025 | Browser port allocation and `ss` platform behavior | `platform_specific` | Browser and dev stack scripts rely on `ss` for port collision/release checks when available, but source indicates checks can be unavailable and dynamic port allocation still has a bind race. | A normative spec may need to require `ss`, define best-effort behavior, or classify the race as unsupported/platform-dependent. | Harness/platform owner | Record configured-port conflicts and release checks as source-observed; mark no-`ss` and allocation-race behavior source-limited. | Is `ss` a required harness dependency, and what is the supported behavior when it is absent? | `resource-allocation-register.md` `RES-0017`, `RES-0018`; `shared-state-hazard-list.md` `HAZ-S4-0005`; `source-limit-log.md` `SL-0013`. | `observed/source_limit` | `open` | Important for S6 resource/timing hazards. |
| AMB-0026 | S4 environment precedence matrix remains unresolved | `missing_default` | S4 recorded assignments, defaults, and env reads, but did not execute override combinations across Make, generated recipes, schedulers, package scripts, shell wrappers, Playwright, Vite, and Go helpers. | A spec could incorrectly bless an override precedence that is only incidental to one call path. | Harness owner | `environment-contract-observations.md` marks unresolved precedence as `TODO: precedence_unknown`; use exact row evidence until S8 decides public env contracts. | Which env vars are public override contracts, and what precedence applies across Make, scripts, schedulers, and package scripts? | `environment-contract-observations.md`; `AMB-0012`; `source-limit-log.md` `SL-0015`. | `observed/source_limit` | `open` | Continues `AMB-0012` with S4 row-level evidence. |
| AMB-0027 | Destructive stale browser fixture janitor bounds | `resource_owner_unknown` | S4 statically observed generated-name guards, metadata files, cleanup summaries, leak checks, and bounded cleanup workers, but did not execute stale janitor paths. | A cleanup spec must know exactly which DBs/buckets/containers may be destroyed before calling stale cleanup normative. | Harness owner | Keep janitor behavior source-limited and tied to generated browser fixture names plus suite metadata; do not broaden cleanup authority in docs. | What source or runtime evidence is sufficient to prove stale DB/bucket/container ownership before deletion? | `service-lifecycle-map.md` `SVC-0011`; `resource-allocation-register.md` `RES-0013`, `RES-0014`, `RES-0016`; `AMB-0019`; `source-limit-log.md` `SL-0014`. | `observed/source_limit` | `open` | S6 should classify destructive cleanup hazards; S8 should settle authority. |
| AMB-0028 | Unsupported platform behavior for service and browser harness tools | `platform_specific` | S4 found assumptions around Linux shell tools, Docker/testcontainers, Compose, `setsid`, `curl`, `realpath`, localhost networking, Node/pnpm, and browser runtime, but no complete platform support matrix. | Final harness spec could overpromise support for non-Linux, no-Docker, restricted-network, or missing-tool environments. | Harness/platform owner | Use `TODO: platform_unknown` except where source explicitly fails, skips, or validates a tool. | What platforms and tool-availability profiles are supported by the testing harness? | `environment-contract-observations.md` `ENV-0024`; `source-limit-log.md` `SL-0013`; `resource-allocation-register.md` allocation gaps. | `observed/source_limit` | `open` | S8 or maintainer decision should decide supported platform statement. |

## S4 Follow-Up Routing Notes

These rows route S4 ambiguity follow-ups to completed recovery documentation
without resolving authority questions. Resolution remains `open` unless a row
already cites runtime evidence or a maintainer decision.

| Follow-up note ID | Ambiguity rows | S5 disposition | S6 disposition | S8 disposition | Resolution status | Evidence status | Notes |
|---|---|---|---|---|---|---|---|
| AMB-FU-0001 | `AMB-0006`, `AMB-0023`, `AMB-0027` | Reset-route call surfaces, response artifacts, failure modes, and cleanup-visible outputs are documented as interfaces only. | Reset timeout/readiness and destructive cleanup remain runtime hazards. | Reset-route authority/security boundary and stale-janitor destructive bounds remain owner-required in `harness-authority-map.md`. | `open` | `maintainer_decision_required` | No reset calls were run and no reset route behavior was made normative. |
| AMB-FU-0002 | `AMB-0011`, `AMB-0020` | Direct package-script output differences are separated from Make-owned result-root and scheduler outputs. | Package-script resource and cleanup bypass behavior is carried as a hazard. | Direct package scripts remain `maintainer_decision_required`: first-class harness entrypoints versus developer conveniences. | `open` | `maintainer_decision_required` | No package-script behavior was normalized into Make behavior. |
| AMB-FU-0003 | `AMB-0012`, `AMB-0026` | Environment variables are mapped as observable static interfaces only where source reads or assignments were inspected. | Override matrix and precedence timing remain source-limited. | Public env-var contracts and precedence across Make, generated recipes, schedulers, shell wrappers, package scripts, Go helpers, config files, and Playwright remain owner-required. | `open` | `observed/source_limit` | No override matrix was executed. |
| AMB-FU-0004 | `AMB-0025`, `AMB-0028` | Missing-tool and unsupported-platform failures are listed as caller-visible failure classes only where static source supports them. | Browser port allocation, optional `ss` behavior, Docker/Compose readiness, localhost networking, and browser runtime availability remain hazards. | Supported platform/tool profile remains owner-required for Docker, Compose, `ss`, `curl`, `setsid`, `realpath`, shell, localhost, Node/pnpm, and browser runtime. | `open` | `observed/source_limit` | Static platform assumptions are not runtime support guarantees. |
| AMB-FU-0005 | `AMB-0018`, `AMB-0019`, `AMB-0024`, `AMB-0027` | Cleanup summaries and cleanup-related failure outputs are documented as observable surfaces. | Timeout, interrupt, parent death, detached reaper completion, active DB connection cleanup, stale fixture janitor execution, and port release remain source-limited cleanup hazards. | Destructive cleanup bounds remain owner-required before any stronger preservation rule. | `open` | `observed/source_limit` | Cleanup strength is not upgraded beyond S4 static evidence. |
| AMB-FU-0006 | `AMB-0021` | External Go cache effects are included as possible failure/diagnostic context only. | Cache reuse/staleness remains a low-severity resource hazard. | External Go cache cleanup remains owner-required for contract inclusion or exclusion. | `open` | `maintainer_decision_required` | No cleanup scope was expanded to `/tmp/cartulary-go-*`. |
