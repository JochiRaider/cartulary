---
doc_id: THR-S1-AMBIGUITY
title: Testing Harness Recovery Ambiguity Register
status: active
role: ambiguity-register
---

# Testing Harness Recovery Ambiguity Register

## Document role

This register records ambiguity, risk, and conflict points discovered during S1.
Rows should remain until resolved by later evidence or maintainer decision.

## Ambiguities

| Ambiguity ID | Surface | Ambiguity type | Conflicting or missing facts | Impact | Owner required | Current workaround | Decision prompt | Evidence | Evidence status | Resolution status | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| AMB-0001 | `.github/**` versus `scripts/ci/**` | `authority_gap` | CI workflow configuration was requested as recovery input, but `.github/` is absent; `scripts/ci/check-deployable-shape.sh` and `scripts/ci/verify.sh` exist. | S2 cannot map provider CI steps from repository workflow files. | Maintainer or repository owner | Treat `scripts/ci/**` as candidate provider-neutral CI scripts and keep `.github/**` as source limit. | Is CI intentionally external to this checkout, absent for now, or expected to be represented only by `scripts/ci/**`? | `.github` absence check; `find scripts/ci -type f`; `make help-all` names `ci`. | `source_limit` | `open` | Mirrors `SL-0001`. |
| AMB-0002 | `.cartulary/test-results/**` ignored status | `implicit_sequence` | `.cartulary/test-results/**` is ignored by the unanchored `test-results/` rule, while `.cartulary/release-artifacts/**` has an explicit ignore rule. | Artifact ownership is observable but ignore policy is indirect; S3 should clarify intentional retention and cleanup rules. | Harness owner | Record as ignored runtime artifact, not committed evidence. | Should the recovered spec name `.cartulary/test-results/**` as the canonical result root even though the ignore rule is indirect? | `git check-ignore -v .cartulary/test-results`; `.gitignore`; `Makefile` `CARTULARY_TEST_RESULTS_DIR`. | `observed` | `open` | No implementation change proposed in S1. |
| AMB-0003 | Committed generated artifacts | `authority_gap` | `internal/gen/**`, `packages/protocol-ts/src/generated/**`, and `tools/task_surface.generated.mk` are committed and generated. | Later specs must avoid treating generated outputs as behavior owners while still recording their execution impact. | Harness/spec owner | Use `tools/generated_artifact_policy.json` as owner evidence and mark generated outputs `evidence_only` or downstream. | Which generated surfaces may drive harness execution contracts, and which are only derived evidence? | `tools/generated_artifact_policy.json`; generated file markers. | `observed` | `open` | S3 should split codegen drift from artifact ownership. |
| AMB-0004 | Planned phase7/phase8 registry entries | `missing_default` | `tools/phase_registry.json` names phase7/phase8 manifest and ledger paths, but those files are absent. | `explain-phase` active coverage is phase0 through phase6; planned future phases must not be counted as missing active coverage unless owner says so. | Phase/harness owner | Treat phase7/phase8 as planned, not active, for S1 inventory. | Should planned phase registry entries be represented in the recovered harness spec now or deferred until the files exist? | `jq '.' tools/phase_registry.json`; `test -e` checks for phase7/phase8 paths. | `source_limit` | `open` | Also recorded as `SL-0005`. |
| AMB-0005 | Harness mechanics embedded in product tests | `authority_gap` | Product tests import harness utilities and sometimes define setup/teardown helpers locally; exact boundary between product evidence and harness contract is not explicit. | Future NLSpec could accidentally over-specify product assertions as harness requirements. | Harness/spec owner | Record representative embedded logic separately and defer preservation decisions. | Which embedded helpers should be recovered as harness behavior versus left as test implementation details? | `embedded-harness-logic-list.md`; `find cmd internal tools -name '*_test.go'`; `find apps/web/e2e`. | `observed` | `open` | Important for S5/S8. |
| AMB-0006 | `internal/app/test_runtime_reset.go` owner boundary | `authority_gap` | Test-only reset route is in `internal/app`, not under `internal/testutil`, and mutates database/object-store state for tests. | Could be product-runtime test hook, harness-only behavior, or an implementation detail that should not become public contract. | Maintainer/harness owner | Mark `owner_decision_required`; do not rewrite or specify normative behavior in S1. | Should test runtime reset be inside the recovered harness spec, and if so under what visibility/security boundary? | `sed -n '1,220p' internal/app/test_runtime_reset.go`. | `observed` | `open` | High-impact because it resets runtime state. |
| AMB-0007 | Service-backed lifecycle ownership | `implicit_sequence` | Service lifecycle spans Make, scheduler manifests, `tools/testservices`, `pgtest`, `s3test`, Playwright stack scripts, Docker/testcontainers, and retained artifacts. | S1 can inventory surfaces but cannot establish start/ready/reset/stop ordering without S4 tracing. | Harness owner | Defer lifecycle mapping to S4 and avoid guessing. | Which layer owns the normative lifecycle: Make target, scheduler manifest, `tools/testservices`, or testutil helpers? | `make explain-target`; `tools/testservices/main.go`; `pgtest.go`; `s3test.go`; `start-web-e2e.sh`. | `observed` | `open` | Not a blocker for S2. |
| AMB-0008 | Cleanup owner and idempotency | `resource_owner_unknown` | Cleanup behavior appears in `make clean`, `make distclean`, shell traps in script self-tests, `tools/testservices` cleanup/reaper, Playwright teardown, Go `t.Cleanup`, and package `TestMain`. | S3 must decide which cleanup behaviors are canonical, diagnostic, or best-effort. | Harness owner | Record surfaces only; defer cleanup lifecycle matrix. | Which cleanup paths are required on success/failure/timeout/interrupt, and which are local convenience cleanup? | `Makefile` cleanup macros; `scripts/test-*.sh`; `tools/testservices/main.go`; Playwright teardown files. | `observed` | `open` | S1 did not execute cleanup. |
| AMB-0009 | Local developer commands versus verification contract | `authority_gap` | `make help-all` lists local-dev, fast verification, full gates, phase maintenance, and release targets; some helper-only targets still create harness artifacts. | NLSpec boundary must not over-constrain local convenience behavior, but some local commands drive verification setup. | Harness/spec owner | Classify local-dev commands as `defer_to_later_sprint` unless they feed verification. | Which local-dev commands belong in the recovered harness contract, and which should remain operator guidance only? | `make help-all`; `make task-guide`; `task-surface-report --all`. | `runtime_observed` | `open` | S2 command map should retain help tier and included-in fields. |
| AMB-0010 | Runtime-only reports, traces, and failure bundles | `schema_unknown` | S1 found retained `.cartulary/test-results/**` summaries and configured Playwright trace retention, but did not run failures to inspect failure-only bundles. | Report schemas and stable consumer contracts remain incomplete. | Harness owner | Mark runtime-only gaps in source-limit log; recover in S3/S5 from retained artifacts and targeted non-mutating inspection. | Which output surfaces are machine-consumed stable interfaces versus diagnostics? | `.cartulary/test-results/**`; Playwright config `trace: "retain-on-failure"`; `scripts/lib/test-output.*`. | `observed/source_limit` | `open` | Not a blocker for S2 entrypoint tracing. |
