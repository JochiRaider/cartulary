# internal-app Module Refactoring Tracker and Handoff

## 1. Current Scope, Authority, and Source Hierarchy

| Field | Value |
| --- | --- |
| Target path | `internal/app` |
| Target label | `internal-app` |
| Output path | `docs/handoffs/internal-app-module-refactor-tracker.md` |
| Current tracker status | Active remediation control artifact for the internal-app module cleanup authorized on 2026-07-06. |
| Allowed changes for this iteration | Tracker, implementation, tests, implementation-support scripts, harness owner inputs, generated companions through Make, and supporting docs needed to complete WS-01 through WS-07. |
| Implementation posture | Implementation authorized. Update this tracker after every completed workstream and before starting the next workstream. |
| Current boundary thesis | `internal/app` should be an application assembly and CLI facade boundary. It should not own recovery semantics, migration-history evidence semantics, restore workbook probe semantics, object-store migration semantics, view/workbook semantics, or platform adapter policy. |

Authority and source hierarchy:

- Core 00 through Core 04 own current Base Profile product behavior. Core 05 applies only to claim-bearing timed, benchmark, fixture-sensitive, or publication evidence.
- Core 01 section 12.2.1 owns operator recovery CLI logical command grammar, result schema, progress schema, timeout defaults, exit-code mapping, backup selection rules, and operator recovery error vocabulary.
- Core 04 owns local-operator recovery authority, the no-listener/no-browser boundary, recovery-operation exclusion, restore-target preflight, encrypted journal requirements, safe administrative-audit summaries, and redaction expectations.
- `docs/testing-harness-nlspec.md` owns harness mechanics and evidence routing. It classifies legacy `deployment_admin` recovery tests as negative-only and migration-history evidence as database-contract or migration-evidence evidence.
- `docs/domain.md` owns vocabulary and concept-boundary interpretation. Backup/restore remains deployment-local operator-facing behavior, not workbook or public route behavior.
- `internal/modules/*` owns domain and application behavior. `internal/platform/*` owns runtime plumbing, storage/config adapters, auth primitives, and database support. `contracts/*` is the derived contract layer.

Repository state inspected for this iteration:

- `internal/app/*`
- `internal/modules/recovery/operatorcli/*`
- `internal/modules/recovery/operatorops/*`
- `internal/modules/recovery/workbook_probe.go`
- `internal/platform/postgres/migrationevidence/*`
- `cmd/operator/operator_phase10_test.go`
- `cmd/server/main_phase10_recovery_sentinel_test.go`
- `tools/phase0_test_map.json`
- `tools/phase10_test_map.json`
- `docs/testing/phase10_coverage_ledger.md`
- `docs/testing-harness-nlspec.md`
- Core 00, Core 01, Core 04, and `docs/domain.md`

## 2. Current Inventory

### `internal/app`

| Path | Current responsibility | Boundary diagnosis | Tests and evidence posture | Next action |
| --- | --- | --- | --- | --- |
| `internal/app/runtime.go` | Builds the server runtime: validates config, bootstraps telemetry, opens Postgres/object store, runs bootstrap preflight, seeds reference data, wires jobs, WebSocket hub, auth keys, pagination codec, module route registrars, readiness, and HTTP handler. | Legitimate `internal/app` assembly facade. Keep thin and reject domain behavior. | `runtime_phase0_test.go`, `runtime_phase0_integration_test.go`, server/process tests, and testutil callers. | Keep. Add assembly characterization only before any future runtime wiring movement. |
| `internal/app/migrate.go` | CLI facade for migration command parsing, config load, SQL open, embedded SQL source selection, `postgres.Migrate`, and remediation report emission. | Acceptable CLI facade over `platform/postgres`; optional future simplification only. | `migrate_test.go`; Phase 0 accounting uses app package tests. | Defer. Do not move unless app facade thinning needs it. |
| `internal/app/operator.go` | Shared operator runner, object-store init, JSON output helpers, and storage/config opening helpers. | Correct after WS-06. Legacy recovery, canonical recovery orchestration, and object-store migration are removed from this file. | `operator_test.go`; Phase 10 operator recovery process rows live in canonical `cmd/operator` tests and recovery-owned packages. | Keep thin. Do not reintroduce support-only migration behavior here. |
| `internal/app/operator_recovery.go` | Thin bridge from the operator binary facade into recovery-owned `operatorcli.Runner` and `operatorops.Service`; injects config, Postgres, object-store, backup-storage, clock, key loader, and projection rebuilder adapters. | Correct app facade shape after WS-04. App composes adapters and no longer owns recovery flow. | `operator_recovery_test.go`; recovery `operatorcli` tests; Phase 10 process and service-backed slices. | Keep thin. Avoid adding recovery semantics here. |
| `internal/app/operator_migration_evidence.go` | Thin CLI wrapper around `internal/platform/postgres/migrationevidence.Build`; no app-local result type aliases. | Correct facade after WS-05. App only loads config/DB and emits the platform-owned evidence result. | `operator_migration_evidence_test.go`; `operator_migration_evidence_integration_test.go`; Phase 0 migration-evidence rows. | Keep thin. Do not reintroduce app-owned evidence DTOs. |
| `internal/app/recovery_probe.go` | Removed. Restore workbook probe moved to `internal/modules/recovery/workbook_probe.go`. | Closed remediation item. Not current inventory. | `internal/modules/recovery/workbook_probe_test.go`; server/browser restore callers now use recovery owner. | No app work. Preserve owner routing in future changes. |

### Adjacent Extracted Owners and Accounting Inputs

| Path | Current responsibility | Owner | Notes |
| --- | --- | --- | --- |
| `internal/modules/recovery/operatorcli/cli.go` | Canonical recovery CLI parsing, result/progress schemas, timeout bounds, negative-only legacy rejection, error mapping, and final stdout/stderr behavior. | Recovery module, subordinate to Core 01/Core 04. | App should call this package rather than owning CLI grammar. |
| `internal/modules/recovery/operatorcli/journal.go` | Encrypted recovery journal append and safe administrative-audit summary append. | Recovery module, subordinate to Core 04. | App currently supplies DB/key-loading adapter. |
| `internal/modules/recovery/operatorops/operations.go` | Recovery-owned implementation of canonical operator backup inspect/create, restore, restore verification, operation lock, target preflight, target marker validation, basis digest, journal/audit closeout, and safe operation summaries. | Recovery module, subordinate to Core 01/Core 04. | App supplies platform adapters only; Phase 10 process and service-backed slices validate behavior. |
| `internal/modules/recovery/workbook_probe.go` | Restore-verification workbook probe using a deterministic incident lookup and owner-registered timeline workbook query. | Recovery module, subordinate to Core 01 probe contract. | The old app probe is gone. |
| `internal/platform/postgres/migrationevidence/evidence.go` | Migration manifest/source/goose ledger evidence construction with neutral schema `cartulary.migration_history_evidence.v1` and neutral exported names: `Result`, `DatabaseBinding`, `ManifestSummary`, `SourceAudit`, `GooseLedger`, `GooseState`, and `Finding`. | Platform Postgres / migration-evidence support. | Should not close operator recovery conformance. |
| `cmd/operator/operator_phase10_test.go::TestSupportPhaseF_SeaweedFSMigrationPreservation*` | Backend-process support-only generation of retained Phase F SeaweedFS migration-preservation pass/mismatch artifacts. | Harness/release support, not operator/app surface and not Phase 10 recovery conformance. | Writes existing `backend-process/phase-f-object-store-migration/{pass,mismatch}` artifact paths for SeaweedFS release evidence. |
| `db/migrations/00003_deployment_admin_and_recovery.sql` | Authored baseline SQL input for encrypted operator recovery journal storage. | Migration SQL input; generated SQL models are downstream. | Do not hand-edit generated SQL model output. |
| `tools/schema_object_ownership_manifest.json` | Owner input assigning the recovery journal table/index ownership. | Harness/schema ownership input. | Edit this owner input before regenerated or drift outputs when schema ownership changes. |
| `tools/phase0_test_map.json` | Phase 0 owner input for runtime/bootstrap and migration-evidence rows. | Harness owner input. | Update before generated ledgers/schedules if tests move. |
| `tools/phase10_test_map.json` | Phase 10 owner input. E-10-01 now points at canonical operator recovery process evidence; legacy `deployment_admin` recovery tests are negative-only. | Harness owner input. | Update before generated ledgers/schedules when evidence ownership or symbols change. |

## 3. Boundary Diagnosis by Responsibility

| Responsibility | Current location | Intended owner | Diagnosis | Keep / move / remove |
| --- | --- | --- | --- | --- |
| Server application assembly | `runtime.go` | `internal/app` | Correct. App composes platform services and module route registrars. | Keep. |
| Runtime route semantics | Owning modules registered by `runtime.go` | `internal/modules/*` and `platform/httpapi` | App should only wire registrars and dependency set. | Keep assembly only. |
| Migration CLI facade | `migrate.go` | `internal/app` facade over `platform/postgres` | Acceptable. Migration mechanics belong to Postgres platform support. | Keep or optionally thin later. |
| Canonical recovery CLI grammar/result/progress | `internal/modules/recovery/operatorcli`; app delegates through `operator_recovery.go` | Core 01/Core 04 via recovery module | Correct owner after remediation. | Keep outside app. |
| Canonical recovery operation orchestration | `internal/modules/recovery/operatorops` with app adapter wiring in `operator_recovery.go` | `internal/modules/recovery` service/package with app/platform adapter ports | Closed in WS-04. Recovery owns operation bodies and app composes adapters. | Keep outside app. |
| Legacy recovery commands and DTOs | Removed from `operator.go`; invalid-command negative coverage remains in `operatorcli` tests. | No current conformance owner. | Closed in WS-03. These commands no longer execute or produce legacy DTOs. | Keep removed. Preserve only negative rejection tests. |
| Backup metadata inspection legacy support | Removed from `operator.go`; canonical `backup inspect latest` is owned by recovery `operatorcli`. | Recovery module. | Closed in WS-03. Phase 10 conformance now uses canonical inspection evidence. | Keep removed. |
| Object-store init | `operator.go` | `internal/app` CLI facade over `platform/objectstore` | Deployment-local support command; not recovery conformance. | Keep thin or move to platform support if it grows. |
| Object-store migration | Recovery helpers plus backend-process support tests | Harness/release support only | Closed in WS-06. The operator/app command is removed; retained Phase F artifacts are generated by support-only tests for SeaweedFS release evidence. | Keep out of app/operator command surface and Phase 10 conformance. |
| Migration-history evidence | `operator_migration_evidence.go` facade; `platform/postgres/migrationevidence` owner | `internal/platform/postgres/migrationevidence` | Correct owner and neutral naming after WS-05. | Keep owner and schema stable. |
| Restore workbook probe | `internal/modules/recovery/workbook_probe.go` | `internal/modules/recovery` | Closed. No longer app-owned. | Keep outside app. |
| Recovery journal and safe audit | `operatorcli/journal.go`; operation closeout in `operatorops`; app supplies key/database adapters | Recovery/Core 04 | Correct owner after WS-04. | Keep outside app. |
| Phase/accounting evidence | Phase owner inputs and generated ledgers | `docs/testing-harness-nlspec.md` and Make-owned generation | Evidence maps are not architecture. | Owner-input first; no generated hand edits. |

## 4. Gap Register

| Gap | Remediation | Affected area | Rationale | Expected long-term benefit | Compatibility or migration impact | Risk if left unresolved | Validation criteria |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Stale tracker sections contradicted current repo. | Rewrite sections 1-12 from current repository state; archive W0-W8 remediation history. | Documentation | Prevent rediscovery and avoid reopening completed recovery work. | Another agent can resume from current facts. | No runtime impact. | Future agents may follow obsolete `recovery_probe.go` or old `deployment_admin` recovery guidance. | `make lint-markdown`; record result in this tracker. |
| Legacy recovery handlers and DTOs remained in `operator.go`. | Closed in WS-03 by removing `backup capture`, `backup-metadata latest`, legacy restore, legacy restore-verify parsing/DTOs, legacy result schemas, quiescence-proof-only backup creation, deployment-admin recovery authorization, and the old process-test helper. | Implementation, tests, documentation | These commands were negative-only and should not become compatibility burden. | Smaller app facade and less accidental conformance drift. | Canonical commands stay unchanged; legacy invocations fail through the canonical invalid-command envelope. | If reintroduced, accidental non-conformant recovery behavior can return. | Completed: `make build-operator`; `make backend-unit`; `make backend-process`; `make phase-slice PHASE=phase10`; `make backend-module-boundary-check`. |
| Canonical recovery orchestration executed inside `operator_recovery.go`. | Closed in WS-04 by adding recovery-owned `internal/modules/recovery/operatorops.Service`, implementing `operatorcli.Operations`, and reducing app to adapter injection. | Implementation, tests | App should compose the CLI facade, not own backup/restore algorithms. | Recovery behavior is easier to test, extend, and reason about for later phases. | Internal movement only; canonical stdout, progress, exit codes, errors, journal/audit behavior, target preflight, and marker validation are preserved. | If reintroduced, app again becomes a mixed domain/platform coordinator. | Completed: `make build-operator`; `make backend-module-boundary-check`; `make backend-unit`; `make backend-store`; `make backend-process`; `make service-backed-slice PHASE=phase10`; `make phase-slice PHASE=phase10`. |
| Phase 10 E-10-01 was blocked around legacy operator evidence. | Closed in WS-02 by replacing blocked legacy recovery evidence with canonical Core 01/Core 04 process evidence; WS-03 removed the old aliases from app execution. | Harness accounting, tests, documentation | Conformance evidence must match owner docs, not old command aliases. | AC-402, AC-427, and AC-428 can close on current commands. | `tools/phase10_test_map.json` and generated ledgers/schedules changed through Make. | If legacy tests re-enter E-10-01, recovery conformance becomes misleading again. | Completed: `make json-shape-check`; `make phase-ledgers`; `make phase-schedules`; `make phase-ledger-drift`; `make phase-schedule-drift`; `make phase-slice PHASE=phase10`. |
| Migration-evidence names carried `Operator*` terminology under the platform owner. | Closed in WS-05 by renaming platform result/helper symbols to neutral names, removing app re-export aliases, and dropping the deprecated no-op `-deployment-admin-email` flag from `migration-evidence capture`. | Implementation, tests, documentation | Database-contract evidence should not look like operator recovery behavior. | Cleaner ownership and easier evidence classification. | Internal API/test churn only; JSON schema `cartulary.migration_history_evidence.v1` remains stable. Deprecated admin flag is now rejected for this support command. | If reintroduced, future evidence may be misclassified as recovery conformance. | Completed: `make build-operator`; `make json-shape-check`; `make backend-unit`; `make backend-integration`; `make phase-slice PHASE=phase0`. |
| MVP/release support scripts still invoke legacy recovery aliases. | Update `deploy/mvp` scripts, README, and `standup-operational-recovery-smoke` to use canonical recovery commands and parse `cartulary.operator_recovery_result.v1`. | Implementation-support scripts, tests, documentation | Package guidance must teach the product contract, not compatibility aliases. | Local deployment automation stays aligned with future recovery phases. | Operators must use `--source-config-file`, `--target-config-file`, canonical result schema, and no deployment-admin email. | Standup smoke and package docs keep legacy behavior alive. | `make lint-shell`; `make lint-markdown`; `make standup-operational-recovery-smoke`. |
| Object-store migration remained support-only but large in `operator.go`. | Closed in WS-06 by removing `object-store-migration run` from the operator/app surface and retaining SeaweedFS migration-preservation artifact generation as backend-process support-only tests with existing Phase F artifact paths. | Implementation, tests, harness accounting, documentation | Avoid carrying migration-era behavior in app while preserving current release-gate value. | Less unsupported surface in app and clearer release-support boundary. | The operator command is removed. `backend-process/phase-f-object-store-migration/{pass,mismatch}` artifacts are preserved for release-gate compatibility. Go duration-baseline refresh is deferred to WS-07 scheduler-backed finalization. | If reintroduced, app keeps indefinite support behavior that complicates later expansion. | Completed: `make backend-process`; `make seaweedfs-release-evidence`; `make seaweedfs-release-gate`; `make backend-module-boundary-check`; `make phase-slice PHASE=phase10`; `make json-shape-check`; drift checks. |

## 5. Recommended Next Workstreams

| Workstream | Order | Dependencies | Risk | Exit criteria |
| --- | ---: | --- | --- | --- |
| WS-01 Tracker and spec baseline | 1 | None | Low | Current tracker records active remediation scope, default removal decisions, and validation result. |
| WS-02 Canonical recovery evidence and package alignment | 2 | WS-01 | High | E-10-01 no longer depends on negative-only legacy recovery tests; package scripts and owner inputs use canonical commands. |
| WS-03 Legacy recovery surface removal | 3 | WS-02 | High | Old recovery aliases/DTOs/handlers are absent; canonical commands unchanged. |
| WS-04 Recovery operation extraction | 4 | WS-03 | High | `internal/app/operator_recovery.go` becomes facade wiring; recovery module owns operation bodies and tests. |
| WS-05 Migration-evidence neutralization | 5 | WS-01 | Medium | Platform migration-evidence package exposes neutral type names; app aliases and deprecated admin flag are removed. |
| WS-06 Object-store migration rehome | 6 | WS-03 | Medium | Operator/app command is removed and SeaweedFS migration-preservation evidence remains release-support only. |
| WS-07 Final validation and handoff | 7 | WS-02 through WS-06 | High | Narrow and broad validation is complete and tracker records final evidence, skipped checks, and residual risks. |

## 6. Workstream Sequencing Detail

| Workstream | Implementation notes | Generated-file handling | Rollback scope |
| --- | --- | --- | --- |
| WS-01 | Documentation tracker edit only. Core 01/Core 04 need no behavior edits because the canonical command contract already rejects compatibility aliases. | Not applicable. | Revert this tracker file only. |
| WS-02 | Add canonical process tests for the five Core logical commands; update MVP scripts and standup smoke to canonical flags/results; update Phase 10 owner input before generated companions. | Edit `tools/phase10_test_map.json`; run Make generators and drift checks. Do not hand-edit generated ledgers/schedules. | Revert tests, scripts, docs, and owner-input/generated companion changes together. |
| WS-03 | Delete old recovery parser branches, DTOs, result constants, legacy support functions, and legacy process tests after canonical coverage exists. | Update phase owner input first if test paths or row evidence change. | Revert removal as one app/operator slice. |
| WS-04 | Introduce a recovery-owned operation service with ports for config loading, Postgres/object-store opening, backup storage creation, projection rebuilder, clock, locking, journal, and audit. App supplies adapter values only. | No generated files unless new SQL/schema ownership is added. | Revert service extraction and app wiring together. |
| WS-05 | Rename migration-evidence types and helpers to remove `Operator` prefix in platform package; keep JSON schema `cartulary.migration_history_evidence.v1`; remove deprecated admin flag. | No generated files expected. Update Phase 0 owner input only if test identifiers or paths change. | Revert rename, parser/test, and app facade changes together. |
| WS-06 | Remove `object-store-migration run` from operator/app. Preserve SeaweedFS migration-preservation artifacts as release/harness support, not Phase 10 operator evidence. | Closed. Updated topology and Phase 10 owner inputs first, then regenerated companions through Make. | Revert command/test/accounting changes together. |
| WS-07 | Run final validation targets from narrow to broad, including `make agent-finalize`; pass `RESULTS_DIR` only when a successful full warm check run is available. | Generated companions through Make only. | Revert only failed finalization metadata if needed; do not revert validated implementation slices. |

## 7. Validation Plan

| Scenario | Make-owned target | When to run | Notes |
| --- | --- | --- | --- |
| Tracker-only edit | `make lint-markdown` | Required for this pass. | Result recorded in Section 10. |
| App/operator build | `make build-operator` | Any operator facade or recovery command change. | Confirms binary composition after app changes. |
| Narrow unit coverage | `make backend-unit` | Any app, recovery CLI, migration-evidence, object-store support, or parser change. | Includes app and recovery unit coverage selected by Make. |
| Service-backed process evidence | `make backend-process` | Any operator process or server process evidence change. | Baseline before this edit passed at `.cartulary/test-results/20260706T113639Z-p2681164`. |
| Recovery phase slice | `make phase-slice PHASE=phase10` | Recovery/operator evidence, phase map, restore, journal, or support-only cleanup. | Required before claiming E-10-01 progress. |
| Recovery service-backed slice | `make service-backed-slice PHASE=phase10` | Restore/verify behavior or app-to-recovery orchestration movement. | Use after WS-04 or high-risk recovery changes. |
| Phase 0 slice | `make phase-slice PHASE=phase0` | Migration-evidence movement, naming, or accounting changes. | Migration evidence is Phase 0/database-contract evidence. |
| Generated/artifact policy | `make generated-artifact-policy-check`; `make json-shape-check` | Any owner-input, manifest, schema, or generated-policy-adjacent change. | Must pass before generated drift claims. |
| Phase generated companions | `make phase-ledgers`; `make phase-schedules`; `make phase-ledger-drift`; `make phase-schedule-drift` | When phase owner inputs change. | Generated ledgers/schedules are downstream. |
| Boundary guardrails | `make backend-module-boundary-check` | Package movement, new imports, or ownership extraction. | Use especially after WS-03, WS-04, or WS-06. |

## 8. Generated-File and Owner-Input Handling Rules

- Do not hand-edit generated roots declared by `tools/generated_artifact_policy.json`: `internal/gen/**`, `packages/protocol-ts/src/generated/**`, and `packages/ui-contracts/src/generated/**`.
- Do not hand-edit generated harness/topology outputs, generated phase ledgers, generated schedules, or generated topology/render indexes.
- For phase-map or accounting changes, edit owner inputs first, such as `tools/phase0_test_map.json` or `tools/phase10_test_map.json`, then run the Make-owned generation and drift targets.
- For schema ownership changes, edit owner manifests such as `tools/schema_object_ownership_manifest.json` before running `make json-shape-check`.
- For SQL changes, author migrations under `db/migrations`, update migration-history owner inputs as required, and regenerate downstream artifacts only through `make generate` or the relevant Make target.
- Do not hand-edit `go.sum`, `pnpm-lock.yaml`, or tool-managed dependency/install artifacts.

## 9. Handoff Log Format

Future sessions must append one row per meaningful work slice:

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `<UTC timestamp>` | `<agent/session>` | `<what changed or what was learned>` | `<paths>` | `<Make targets and focused reads>` | `<pass/fail plus run root when available>` | `<none or concrete blocker>` | `<next dependency-aware action>` |

Final reports should state planning summary, files inspected or changed, substantive edits, verification commands and results, and skipped checks with reasons.

## 10. Current Handoff Log

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-06T11:41:55Z | Codex tracker reconciliation | Started current-state tracker update after recovery remediation. | Inspected this tracker, `internal/app`, recovery `operatorcli`, recovery workbook probe, platform migration evidence, Phase 10 ledger/map, harness NLSpec, Core 01/Core 04, and domain vocabulary. Touched only this tracker. | `git status --short`; `sed`; `find`; `rg`; baseline `make lint-markdown`; baseline `make backend-process`. | Baseline `make lint-markdown` passed; baseline `make backend-process` passed at `.cartulary/test-results/20260706T113639Z-p2681164`. | None. | Run final `make lint-markdown` after tracker edit and record the result. |
| 2026-07-06T11:44:21Z | Codex tracker reconciliation | Current-state tracker rewrite completed. | Touched only `docs/handoffs/internal-app-module-refactor-tracker.md`. | `make lint-markdown`; final rerun of `make lint-markdown`. | Both passed. | None. | Handoff complete for tracker-only iteration. |
| 2026-07-06T12:08:28Z | Codex WS-01 tracker and spec baseline | Completed active remediation tracker baseline; recorded refined decisions to remove legacy recovery aliases, canonicalize package recovery scripts, neutralize migration evidence naming, and remove object-store migration from the operator/app surface while retaining release-support artifacts. | Inspected and touched `docs/handoffs/internal-app-module-refactor-tracker.md`. | `git status --short`; `sed`; `date -u`; `make lint-markdown`. | PASS: `make lint-markdown`. | None. | Start WS-02 canonical recovery evidence and package alignment. |
| 2026-07-06T12:32:53Z | Codex WS-02 canonical recovery evidence and package alignment | Completed canonical E-10-01 evidence and package alignment. Added process coverage for `backup inspect latest`, `backup create`, `restore latest`, `restore-verify latest`, and `restore-verify due`; switched MVP scripts and standup smoke to canonical flags/results; fixed due ordering and per-backup due artifact refs; mapped key/preflight failures to closed recovery errors. | Touched `cmd/operator/operator_phase10_test.go`, `internal/modules/recovery/operatorcli/cli.go`, `internal/modules/recovery/catalog.go`, `internal/app/operator_recovery.go`, `db/queries/recovery_phase10.sql`, generated SQL, MVP scripts/docs/env example, standup smoke, harness NLSpec, `tools/phase10_test_map.json`, and generated phase ledger/schedule companions. | `gofmt`; `make generate`; `make build-operator`; `make phase-ledgers phase-schedules`; `make json-shape-check`; `make lint-shell`; `make lint-markdown`; `make phase-ledger-drift`; `make phase-schedule-drift`; `make generated-artifact-policy-check`; `make phase-slice PHASE=phase10`; `make backend-unit`; `make backend-process`; `make standup-operational-recovery-smoke`. | PASS. Key run roots: generate `.cartulary/test-results/20260706T122605Z-p2707960`; build-operator `.cartulary/test-results/20260706T122613Z-p2708777`; phase-ledgers `.cartulary/test-results/20260706T122900Z-p2712515`; phase-schedules `.cartulary/test-results/20260706T122903Z-p2712830`; phase10 slice `.cartulary/test-results/20260706T123036Z-p2715116`; backend-unit `.cartulary/test-results/20260706T123128Z-p2732331`; backend-process `.cartulary/test-results/20260706T123128Z-p2732355`; standup smoke `.cartulary/test-results/20260706T123208Z-p2747969`. | None. | Run `make lint-markdown` after this tracker row, then start WS-03 legacy recovery surface removal. |
| 2026-07-06T12:43:29Z | Codex WS-03 legacy recovery surface removal | Removed legacy recovery parser branches, DTO/result schema constants, backup quiescence-proof capture path, deployment-admin recovery authorization, old restore/verify handlers, and the legacy Phase 10 process helper. Package-facing MVP backup wording now describes canonical backup creation. | Touched `internal/app/operator.go`, `internal/app/operator_recovery.go`, `cmd/operator/operator_phase10_test.go`, `deploy/mvp/README.md`, and MVP backup systemd units. | `git status --short`; `sed`; `rg`; `gofmt`; `go test ./internal/app` quick compile check; `make build-operator`; `make backend-module-boundary-check`; `make backend-unit`; `make backend-process`; `make phase-slice PHASE=phase10`. | PASS. Key run roots: build-operator `.cartulary/test-results/20260706T124143Z-p2761657`; boundary check `.cartulary/test-results/20260706T124143Z-p2761679`; backend-unit `.cartulary/test-results/20260706T124155Z-p2764529`; backend-process `.cartulary/test-results/20260706T124155Z-p2764559`; phase10 slice `.cartulary/test-results/20260706T124242Z-p2781869`. | None. | Run `make lint-markdown` after this tracker row, then start WS-04 recovery operation extraction. |
| 2026-07-06T12:55:10Z | Codex WS-04 recovery operation extraction | Moved canonical recovery operation bodies into `internal/modules/recovery/operatorops.Service`; app now only wires `operatorcli.Runner` with platform adapters, recovery key loading, clock, and projection rebuilder factory. Target preflight, marker validation, basis digest, operation lock, journal/audit closeout, and backup object helpers are recovery-owned. | Touched `internal/modules/recovery/operatorops/operations.go`, `internal/app/operator_recovery.go`, `internal/app/operator.go`, and `cmd/operator/operator_phase10_test.go`. | `git status --short`; `sed`; `rg`; `gofmt`; `go test ./internal/modules/recovery/operatorops ./internal/app` quick compile check; `make build-operator`; `make backend-module-boundary-check`; `make backend-unit`; `make backend-store`; `make backend-process`; `make service-backed-slice PHASE=phase10`; `make phase-slice PHASE=phase10`. | PASS. Key run roots: build-operator `.cartulary/test-results/20260706T125130Z-p2799613`; boundary check `.cartulary/test-results/20260706T125130Z-p2799654`; backend-unit `.cartulary/test-results/20260706T125138Z-p2802506`; backend-store `.cartulary/test-results/20260706T125138Z-p2802513`; backend-process `.cartulary/test-results/20260706T125139Z-p2802612`; service-backed phase10 `.cartulary/test-results/20260706T125232Z-p2830287`; phase10 slice `.cartulary/test-results/20260706T125316Z-p2844656`. | None. | Run `make lint-markdown` after this tracker row, then start WS-05 migration-evidence neutralization. |
| 2026-07-06T13:01:34Z | Codex WS-05 migration-evidence neutralization | Neutralized platform migration-evidence naming, removed app re-export aliases, and made `migration-evidence capture -deployment-admin-email` an invalid flag instead of a deprecated no-op. The JSON schema remains `cartulary.migration_history_evidence.v1`. | Touched `internal/platform/postgres/migrationevidence/evidence.go`, `internal/platform/postgres/migrationevidence/evidence_test.go`, `internal/app/operator_migration_evidence.go`, `internal/app/operator_migration_evidence_test.go`, `internal/app/operator_migration_evidence_integration_test.go`, and `internal/app/operator.go`. | `git status --short`; `sed`; `rg`; `gofmt`; `go test ./internal/platform/postgres/migrationevidence ./internal/app` quick compile check; `make build-operator`; `make json-shape-check`; `make backend-unit`; `make backend-integration`; `make phase-slice PHASE=phase0`. | PASS. Key run roots: build-operator `.cartulary/test-results/20260706T125858Z-p2861360`; json-shape `.cartulary/test-results/20260706T125858Z-p2861382`; backend-unit `.cartulary/test-results/20260706T125858Z-p2861545`; backend-integration `.cartulary/test-results/20260706T125934Z-p2869670`; phase0 slice `.cartulary/test-results/20260706T130031Z-p2880635`. | None. | Run `make lint-markdown` after this tracker row, then start WS-06 object-store migration rehome. |
| 2026-07-06T13:26:40Z | Codex WS-06 object-store migration rehome | Removed `object-store-migration run` from the operator/app command surface. Replaced the old operator-binary migration evidence tests with backend-process support-only `TestSupportPhaseF_SeaweedFSMigrationPreservation*` tests that call recovery migration helpers directly and retain the existing Phase F pass/mismatch artifact paths for SeaweedFS release evidence. Added backend-process support-row handling in topology and phase-accounting harness code. | Touched `internal/app/operator.go`, `internal/app/operator_test.go`, `internal/app/operator_migration_evidence_test.go`, `cmd/operator/operator_phase10_test.go`, `tools/execution_topology_manifest.json`, `tools/phase10_test_map.json`, generated topology/phase companions, `tools/harness/backend/go-shard-plan.mjs`, `tools/harness/backend/tests/test-run-go-target.sh`, `tools/harness/generated-artifacts/render-phase-ledger.mjs`, and `tools/harness/phase-accounting/phase-manifest-constants.mjs`. | `git status --short`; `rg`; `sed`; `gofmt`; `go test ./internal/app ./cmd/operator -run 'TestOperatorObjectStoreMigrationCommand_U_RemovedFromOperatorSurface\|TestSupportPhaseF_SeaweedFSMigrationPreservation'`; `make generate-artifacts phase-ledgers phase-schedules`; `make backend-process`; `make seaweedfs-release-evidence seaweedfs-release-gate SEAWEEDFS_MIGRATION_PASS_DIR=.cartulary/test-results/20260706T132223Z-p2900850/backend-process/phase-f-object-store-migration/pass`; `make json-shape-check`; `make generated-artifact-policy-check`; `make phase-ledger-drift`; `make phase-schedule-drift`; `make lint-shell`; `make build-operator`; `make backend-module-boundary-check`; `make phase-slice PHASE=phase10`. | PASS after two expected intermediate generator/coverage corrections. Key run roots: phase-ledgers `.cartulary/test-results/20260706T131919Z-p2898883`; phase-schedules `.cartulary/test-results/20260706T131921Z-p2899198`; backend-process `.cartulary/test-results/20260706T132223Z-p2900850`; seaweedfs-release-evidence `.cartulary/test-results/20260706T132352Z-p2913675`; seaweedfs-release-gate `.cartulary/test-results/20260706T132352Z-p2913677`; json-shape `.cartulary/test-results/20260706T132453Z-p2916199`; generated-artifact-policy `.cartulary/test-results/20260706T132453Z-p2916239`; phase-ledger-drift `.cartulary/test-results/20260706T132453Z-p2916226`; phase-schedule-drift `.cartulary/test-results/20260706T132453Z-p2916255`; build-operator `.cartulary/test-results/20260706T132513Z-p2917523`; boundary `.cartulary/test-results/20260706T132513Z-p2917566`; phase10 slice `.cartulary/test-results/20260706T132518Z-p2920115`. | Go duration-baseline coverage still needs a scheduler-backed refresh because `make go-test-duration-baselines RESULTS_DIR=.cartulary/test-results/20260706T132223Z-p2900850` correctly rejected a single-target run without `scheduler-summary.json`. | Run `make lint-markdown` after this tracker row, then start WS-07 final validation and handoff. |
| 2026-07-06T14:00:57Z | Codex WS-07 final validation and handoff | Completed final validation, duration-baseline cleanup, generated schedule refresh, and handoff. Legacy object-store-migration recovery symbols are absent from non-archive authored code/docs and `tools/go_test_duration_baselines.json`; retained SeaweedFS release evidence remains available through backend-process Phase F support artifacts. | Touched this tracker plus final generated/accounting outputs: `tools/go_test_duration_baselines.json` and `tools/scheduler_manifest.json`. Inspected final `git status`, diff stat, finalizer summary, and stale-symbol searches. | Final validation included `make phase-ledgers`; `make phase-schedules`; `make phase-ledger-drift`; `make phase-schedule-drift`; `make build-operator`; `make backend-module-boundary-check`; `make generated-artifact-policy-check`; `make json-shape-check`; `make backend-unit`; `make backend-integration`; `make backend-process`; `make phase-slice PHASE=phase0`; `make phase-slice PHASE=phase10`; `make service-backed-slice PHASE=phase10`; `make standup-operational-recovery-smoke`; `make seaweedfs-release-gate SEAWEEDFS_MIGRATION_PASS_DIR=.cartulary/test-results/20260706T133013Z-p2957284/backend-process/phase-f-object-store-migration/pass`; `make test-fast`; `make check-service-backed`; `make go-test-duration-baselines RESULTS_DIR=.cartulary/test-results/20260706T135515Z-p3233102 PRUNE_OBSERVED_PACKAGES=1`; `make go-test-duration-baselines RESULTS_DIR=.cartulary/test-results/20260706T134051Z-p3062102`; `make go-test-duration-baseline-coverage`; `make agent-finalize`; `git diff --check`; stale-symbol `rg`. | PASS. Key final roots: build-operator `.cartulary/test-results/20260706T132827Z-p2938107`; boundary `.cartulary/test-results/20260706T132827Z-p2938130`; generated-artifact-policy `.cartulary/test-results/20260706T132827Z-p2938169`; json-shape `.cartulary/test-results/20260706T132827Z-p2938217`; backend-unit `.cartulary/test-results/20260706T132833Z-p2941202`; backend-integration `.cartulary/test-results/20260706T132915Z-p2946558`; backend-process `.cartulary/test-results/20260706T133013Z-p2957284`; phase0 slice `.cartulary/test-results/20260706T133121Z-p2968937`; phase10 slice `.cartulary/test-results/20260706T133221Z-p2976168`; service-backed phase10 `.cartulary/test-results/20260706T133335Z-p2990663`; standup smoke `.cartulary/test-results/20260706T133504Z-p3004812`; seaweedfs gate `.cartulary/test-results/20260706T133544Z-p3014078`; final phase-schedules `.cartulary/test-results/20260706T135806Z-p3272797`; final schedule drift `.cartulary/test-results/20260706T135812Z-p3272958`; final test-fast `.cartulary/test-results/20260706T135836Z-p3274563`; final agent-finalize `.cartulary/test-results/20260706T140041Z-p3317265`. `make agent-finalize` reported retained-run maintenance skipped because `RESULTS_DIR` was unset. | No remediation blocker. `make test-service-backed` was attempted for pruning and failed once on stale scheduler output before schedule regeneration, then failed at `.cartulary/test-results/20260706T135133Z-p3172831` on unrelated `browser-e2e-visual` screenshot mismatch in `workbook.visual.spec.ts`. Invalid `make agent-finalize RESULTS_DIR=<test-fast-root>` attempts failed retained-run preflight because those roots were not full warm `make check` roots. | Run `make lint-markdown` after this tracker row, then final response. |

## 11. Completion Criteria for This Tracker Iteration

| Criterion | Status | Evidence |
| --- | --- | --- |
| Current `internal/app` inventory matches the repository. | PASS | Section 2 lists thin `operator_recovery.go`, support-only `operator.go`, and removes `recovery_probe.go` from current inventory. |
| Adjacent extracted packages are inventoried. | PASS | Section 2 lists `operatorcli`, `operatorops`, recovery workbook probe, migration evidence, journal migration, and owner inputs. |
| Each remaining app responsibility has a boundary diagnosis. | PASS | Section 3 classifies keep, move, remove, and support-only responsibilities. |
| Gaps include remediation, affected area, rationale, benefit, compatibility impact, risk, and validation. | PASS | Section 4 gap register. |
| Workstreams are ordered by dependency and risk. | PASS | Sections 5 and 6. |
| Validation plan uses Make-owned targets. | PASS | Section 7. |
| Generated-file and owner-input rules are explicit. | PASS | Section 8. |
| Handoff log format is explicit. | PASS | Section 9. |
| Required tracker validation is recorded. | PASS | `make lint-markdown` passed after the tracker rewrite, after each workstream row, and after the final WS-07 handoff row. |

## 12. Completed Recovery Remediation Log

The following archive preserves the completed W0-W8 recovery remediation history. It is historical evidence, not the current work queue.

| Workstream | Status | Started | Completed | Evidence |
| --- | --- | --- | --- | --- |
| W0 Tracker bootstrap | DONE | 2026-07-06T02:32:27Z | 2026-07-06T02:34:22Z | Tracker scope changed for the remediation run; `make lint-markdown` passed. |
| W1 Spec/docs/accounting cleanup | DONE | 2026-07-06T02:34:22Z | 2026-07-06T02:37:02Z | Updated Core 01 probe/alias text, dev guide alias guidance, Phase 0/10 owner inputs, and Make-regenerated ledgers/schedules. Validation passed: `make phase-ledgers`, `make phase-schedules`, `make json-shape-check`, `make lint-markdown`, `make phase-ledger-drift`, `make phase-schedule-drift`. |
| W2 Canonical recovery CLI contract | DONE | 2026-07-06T02:37:02Z | 2026-07-06T02:44:43Z | Added canonical recovery CLI interception for the five Core logical commands, typed `cartulary.operator_recovery_result.v1` and `cartulary.operator_recovery_progress.v1` emitters, timeout/default parsing, closed exit/error mapping, and negative-only legacy rejection. Validation passed: `make build-operator`, `make backend-unit`. |
| W3 Recovery orchestration extraction | DONE | 2026-07-06T02:44:43Z | 2026-07-06T02:51:58Z | Added `internal/modules/recovery/operatorcli` as recovery-owned contract/dispatch; app delegates canonical parsing/progress/result/timeout/error mapping while supplying process adapters. Validation passed: `make build-operator`, `make backend-unit`, `make backend-module-boundary-check`. |
| W4 Security, preflight, lock, journal, audit | DONE | 2026-07-06T02:51:58Z | 2026-07-06T03:00:18Z | Added shared recovery-operation advisory locking, encrypted operator recovery journal persistence, safe administrative-audit summaries, journal/audit error mapping, and generated SQL model refresh. The recovery journal is now expressed in the production DDL rebaseline deployment-admin-and-recovery slice. Validation passed: `make build-operator`, `make backend-unit`, `make generate`, `make migration-drift`, `make backend-store`, `make backend-process`. |
| W5 Restore probe ownership move | DONE | 2026-07-06T03:00:18Z | 2026-07-06T03:07:45Z | Moved `RestoreVerificationWorkbookProbe` from `internal/app` into `internal/modules/recovery`, updated operator/server/browser restore call sites, and changed empty workbook-query results from probe failure to success. Validation passed: `make build-operator`, `make backend-unit`, `make backend-integration`, `make build-server`, `make service-backed-slice PHASE=phase10`. |
| W6 Migration-evidence owner move | DONE | 2026-07-06T03:07:45Z | 2026-07-06T03:17:58Z | Added `internal/platform/postgres/migrationevidence` as migration-history evidence owner with schema `cartulary.migration_history_evidence.v1`; reduced app to a CLI facade; removed deployment-admin authorization from the command path and real PostgreSQL test. Validation passed: `make build-operator`, `make backend-unit`, `make backend-integration`, `make phase-slice PHASE=phase0`, `make backend-module-boundary-check`, `make lint-markdown`. |
| W7 Support-command split and boundary guardrails | DONE | 2026-07-06T03:17:58Z | 2026-07-06T03:23:46Z | `object-store init` remains local support tooling; `object-store-migration run` remains support-only, no longer requires or authorizes a deployment-admin credential, records fixed `local_os_execution`, and retains safe-output process coverage. Validation passed: `make build-operator`, `make backend-unit`, `make backend-process`, `make backend-module-boundary-check`. |
| W8 Final validation and handoff | DONE | 2026-07-06T03:23:46Z | 2026-07-06T03:30:28Z | Final validation passed: `make build-operator`, `make generated-artifact-policy-check`, `make json-shape-check`, `make backend-module-boundary-check`, `make lint-markdown`, `make phase-slice PHASE=phase0`, `make phase-slice PHASE=phase10`, `make service-backed-slice PHASE=phase10`, `make agent-finalize`, and `make test-fast`. Initial final `make json-shape-check` failed because `tools/schema_object_ownership_manifest.json` lacked the new recovery journal table/index ownership; fixed by adding a recovery-owned manifest entry. `make agent-finalize` reported retained-run maintenance skipped because `RESULTS_DIR` was unset. |
