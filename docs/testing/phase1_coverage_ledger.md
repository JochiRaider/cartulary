# Phase 1 Coverage Ledger

This ledger is the authoritative repo-local map for Phase 1 test evidence.

- Scope: local authentication, session lifecycle, TOTP bootstrap or replacement, deployment-local user administration, and deployment-admin credential actions only.
- Normative owners: Core 01 `§3.3.2`, `§3.3.2.2`, `§3.3.5.1`, `§3.3.10`; Core 04 `§1`, `§3`.
- Support-only evidence: `cmd/server/main_phase1_process_test.go` remains process-smoke evidence only. It runs through `backend-process`, `make test`, and `make check`, but it is not counted as the authoritative `E-*` surface below.

## Unit

| Row | Evidence | Task target | Real services | Major assertions | Remaining gap |
| --- | --- | --- | --- | --- | --- |
| `U-1-01` | `internal/modules/auth/phase1_request_test.go::TestPhase1_LoginRequestShape_U_1_01` | `backend-unit phases` | None | Login request-shape validation rejects unknown top-level members, unsupported `second_factor.kind`, and malformed TOTP assertions with the route-owned invalid-auth surface. | Does not execute the full login route or session creation path. |
| `U-1-02` | `internal/platform/authn/phase1_primitives_test.go::TestPhase1_LoginNormalizationAndPasswordExactness_U_1_02` | `backend-unit platform` | None | Email normalization trims and case-folds the login key while password verification preserves exact post-decode bytes against real Argon2id helpers. | Does not prove route-owned JSON decode, replay, or persisted route-created password hashes. |
| `U-1-03` | `internal/platform/authn/phase1_primitives_test.go::TestPhase1_SessionTiming_U_1_03` | `backend-unit platform` | None | Session timing helpers compute `session_expires_at=min(idle, absolute)` and slide fields deterministically. | Still helper-level only; it does not prove one successful login creates one durable session row. |
| `U-1-04` | `internal/modules/auth/phase1_request_test.go::TestPhase1_SessionInspectionContracts_U_1_04` | `backend-unit phases` | None | Singleton session inspection rejects pagination members and keeps `GET /auth/session` out of idle-slide behavior. | Returned singleton session resource shape remains primarily proven at integration and browser layers. |
| `U-1-05` | `internal/platform/authn/phase1_primitives_test.go::TestPhase1_ConcurrencyLimitRevokesLRUNonCurrent_U_1_05` | `backend-unit platform` | None | LRU non-current victim selection and the `concurrency_limit` reason code are deterministic. | Durable revocation rows and emitted audit evidence are still proven at integration and process layers. |
| `U-1-06` | `internal/platform/authn/phase1_primitives_test.go::TestPhase1_RevocationScopes_U_1_06` | `backend-unit platform` | None | Revocation-action to revocation-scope mapping stays stable. | Route-owned consequences for logout, password change, TOTP reset, and revoke-all still rely on integration and browser evidence. |
| `U-1-07` | `internal/modules/auth/phase1_users_test.go::TestPhase1_UserCreateDefaultsAndSafeShape_U_1_07` | `backend-unit phases` | None | User-create defaults set `mfa_required=true`, `is_deployment_admin=false`, and keep the safe user resource secret-free. | Persisted Argon2id storage and replay behavior are covered at integration rather than unit level. |
| `U-1-08` | `internal/modules/auth/phase1_users_test.go::TestPhase1_UserPatchAndLastAdminGuard_U_1_08` | `backend-unit phases` | None | Missing `base_user_version` is rejected and last-active-deployment-admin guard logic is deterministic. | Real stale-version conflicts and route-owned patch results are still integration or browser evidence. |
| `U-1-09` | `internal/modules/auth/phase1_request_test.go::TestPhase1_CSRFFailClosed_U_1_09` | `backend-unit phases` | None | Cookie-authenticated state-changing requests fail closed without matching CSRF proof, while bearer auth and safe methods stay exempt. | Concrete route surfaces remain integration or process verified. |
| `U-1-10` | `internal/modules/auth/phase1_request_test.go::TestPhase1_CredentialStateInspection_U_1_10` | `backend-unit phases` | None | Credential-state builders expose the safe password or TOTP shape and keep the declared `totp.state` vocabulary closed. | Full route-owned rejection and lifecycle consequences remain stronger at integration and browser layers. |
| `U-1-11` | `internal/modules/auth/phase1_request_test.go::TestPhase1_PasswordChangeRequest_U_1_11` | `backend-unit phases` | None | Password-change decode preserves exact `current_password` and `new_password` values and rejects malformed second-factor payloads. | TOTP-required logic, replay ordering, all-session revocation, and secret-safe persisted idempotency state are integration-proven. |
| `U-1-12` | `internal/modules/auth/phase1_request_test.go::TestPhase1_TOTPBootstrapAndSetupRules_U_1_12` | `backend-unit phases` | None | Bootstrap-route payload builders and first-vs-replacement revoke decisions stay deterministic. | Exactly-one-auth-mode enforcement, begin replay boundaries, bootstrap consumption, and begin-vs-complete secret visibility are integration and browser proven. |
| `U-1-13` | `internal/modules/auth/phase1_users_test.go::TestPhase1_AdminCredentialActionsRequireDeploymentAdmin_U_1_13` | `backend-unit phases` | None | Deployment-admin authorization helpers and admin password-reset decode stay fail-closed. | Route-owned replay, versioning, revocation, and audit consequences still rely on integration and browser evidence. |

## Integration

| Row | Evidence | Task target | Real services | Major assertions | Remaining gap |
| --- | --- | --- | --- | --- | --- |
| `I-1-01` | `internal/modules/auth/phase1_integration_test.go::TestPhase1_LoginSessionLifecycle_I_1_01` | `backend-integration phases` | PostgreSQL + real runtime | Real login creates one durable session row, session inspection exposes the singleton resource without idle slide, touch slides idle expiry, logout revokes the current session, idle expiry fails closed with `session_expired`, and sixth-login concurrency revokes the LRU non-current session with one deployment-local audit event. | None. |
| `I-1-02` | `internal/modules/auth/phase1_integration_test.go::TestPhase1_SessionRevocationClosesAttachedSocket_I_1_02` | `backend-integration phases` | PostgreSQL + real runtime + real WebSocket boundary | A real socket on `/ws/v1/test/session-lifecycle` receives `session_revoked` and then closes after logout. The Phase 1 shared socket helpers now route through `internal/testutil/phase1test` and `internal/testutil/wstest`. | None. |
| `I-1-03` | `internal/modules/auth/phase1_integration_test.go::TestPhase1_UserAdminLifecycle_I_1_03`, `TestPhase1_UserAdminAudit_I_1_03`, `TestPhase1_UserCreateReplayReturnsOriginalCommittedResource_I_1_03` | `backend-integration phases` | PostgreSQL + real runtime | User create applies defaults, stores Argon2id password hashes, persists safe `response_json`, replays the original committed resource even after later mutation, rejects divergent replay with `client_txn_conflict`, keeps one idempotency row, proves stale-version and last-admin guard behavior, and records secret-safe deployment-admin audit before/after payloads. | Unit-level handler orchestration for create and patch remains thinner than the integration evidence. |
| `I-1-04` | `internal/modules/auth/phase1_integration_test.go::TestPhase1_CredentialStateAndBootstrapFlows_I_1_04`, `TestPhase1_CredentialStateTransitions_I_1_04`, `TestPhase1_BootstrapEnrollmentConsumption_I_1_04`, `TestPhase1_PasswordChangeReplayAndStoredPayload_I_1_04` | `backend-integration phases` | PostgreSQL + real runtime | Credential-state transitions prove first enrollment, replacement enrollment, begin replay and divergent conflict, bootstrap-token consumption, password change revocation, Argon2id password-hash replacement, exact replay of password change before fresh invalid-current-password evaluation, secret-safe stored `route_idempotency.response_json`, and one-row replay stability. | None for the declared Phase 1 contract. |
| `I-1-05` | `internal/modules/auth/phase1_integration_test.go::TestPhase1_AdminCredentialActions_I_1_05`, `TestPhase1_AdminCredentialAuditAndScope_I_1_05`, `TestPhase1_AdminPasswordResetReplayReturnsOriginalCommittedResource_I_1_05`, `TestPhase1_AdminTOTPResetAndRevokeAllReplay_I_1_05` | `backend-integration phases` | PostgreSQL + real runtime + real WebSocket boundary | Admin password reset, TOTP reset, and revoke-all revoke attached sockets, preserve deployment-local authorization boundaries, store only secret-safe replay payloads, reject divergent replay with `client_txn_conflict`, keep one idempotency row per committed request, return original committed replay payloads, prove password-reset replay after later user mutation, and prove persisted Argon2id password-hash replacement on admin reset. | None for the declared Phase 1 contract. |
| `I-1-06` | `internal/modules/auth/phase1_integration_test.go::TestPhase1_BootstrapTokenRouteBoundaries_I_1_06` | `backend-integration phases` | PostgreSQL + real runtime + real WebSocket boundary | Bootstrap tokens work only on `totp/begin` and `totp/complete`, are rejected on ordinary session, credential-state, incident, and test-auth routes, and are rejected on the real WebSocket boundary with `credential_bootstrap_rejected`. | None. |

## Browser E2E

| Row | Evidence | Task target | Real services | Major assertions | Remaining gap |
| --- | --- | --- | --- | --- | --- |
| `E-1-01` | `apps/web/e2e/phase1.spec.ts::E-1-01 logs in as a local user and inspects the singleton session resource` | `browser-e2e` via `make test` and `make check` | Real browser + Go server + PostgreSQL + Vite debug harness | Browser login exposes the Phase 1 session resource, including `provider_type='local'`, singleton expiry fields, and `memberships[]`. | None. |
| `E-1-02` | `apps/web/e2e/phase1.spec.ts::E-1-02 requires MFA when the account has an active factor, rejects wrong codes, and accepts a valid TOTP code` | `browser-e2e` via `make test` and `make check` | Real browser + Go server + PostgreSQL + Vite debug harness | Browser-visible MFA-required login rejects missing and wrong TOTP assertions and accepts a valid code against real backend state. | None. |
| `E-1-03` | `apps/web/e2e/phase1.spec.ts::E-1-03 rejects invalid credentials without issuing a session cookie` | `browser-e2e` via `make test` and `make check` | Real browser + Go server + PostgreSQL + Vite debug harness | Invalid credentials leave no session cookie and expose the route-owned browser-visible failure code. | None. |
| `E-1-04` | `apps/web/e2e/phase1.spec.ts::E-1-04 forces the idle expiry boundary and requires a fresh login afterwards` | `browser-e2e` via `make test` and `make check` | Real browser + Go server + PostgreSQL + Vite debug harness | Idle expiry fails closed and requires a fresh login before the browser can read the singleton session again. | Uses the test-only expiry trigger rather than waiting for wall-clock TTL expiry. |
| `E-1-05` | `apps/web/e2e/phase1.spec.ts::E-1-05 lets deployment admins create and patch users, rejects stale versions, and proves the last-admin guard through the harness` | `browser-e2e` via `make test` and `make check` | Real browser + Go server + PostgreSQL + Vite debug harness | Browser admin create and patch flows work, stale version conflicts surface in the harness, and the harness now actually attempts both last-admin demotion and last-admin deactivation and observes `last_deployment_admin`. | None. |
| `E-1-06` | `apps/web/e2e/phase1.spec.ts::E-1-06 follows the bootstrap-token enrollment sequence for first enrollment and post-reset re-enrollment, and proves completion alone issues no session` | `browser-e2e` via `make test` and `make check` | Real browser + Go server + PostgreSQL + Vite debug harness | Browser flow proves first-time bootstrap enrollment, completion without implicit session issuance, later login with active TOTP, admin-driven TOTP reset, second bootstrap enrollment, and post-reset re-login with the replacement secret. | None. |
| `E-1-07` | `apps/web/e2e/phase1.spec.ts::E-1-07 requires the current password and current TOTP code, revokes the session immediately, and requires re-login with the new password` | `browser-e2e` via `make test` and `make check` | Real browser + Go server + PostgreSQL + Vite debug harness | Browser password change requires exact current password and current TOTP, revokes the current session immediately, rejects the old password, and accepts the new password. | None. |
| `E-1-08` | `apps/web/e2e/phase1.spec.ts::E-1-08 keeps credential actions deployment-admin only and denies the same routes to a non-deployment-admin incident admin` | `browser-e2e` via `make test` and `make check` | Real browser + Go server + PostgreSQL + Vite debug harness | Browser harness proves deployment-admin-only authorization for password reset, TOTP reset, and revoke-all, and proves incident-admin scope does not widen into deployment-admin credential authority. | It is an authorization row only; route-owned target-state consequences are integration evidence. |

## Shared Harness Coverage

| Harness | Phase 1 evidence |
| --- | --- |
| Envelope consistency and browser/admin API helpers | `internal/testutil/httptestx/httptestx.go` plus `apps/web/e2e/authRuntime.ts` and `apps/web/e2e/helpers.ts` keep the Phase 1 browser helpers on one shared request surface instead of per-spec ad hoc API wiring. |
| Real runtime, Postgres, and object-store harness | `internal/testutil/phase1test/runtime.go` wraps real PostgreSQL, real MinIO, and real runtime startup with test routes enabled for the Phase 1 integration suite. |
| Auth HTTP, bootstrap, and session helpers | `internal/testutil/phase1test/phase1test.go` now centralizes local-user seeding, login, bootstrap enrollment, session inspection helpers, count helpers, audit lookup, and credential-flow utilities shared by Phase 1 integration and process-smoke suites. |
| WebSocket lifecycle helpers | `internal/testutil/phase1test.ConnectSessionSocket`, `ExpectSessionRevoked`, and `RequireBootstrapWebsocketRejected`, built on `internal/testutil/wstest`, cover the real WebSocket boundary without test-local raw socket code. |
| Mutation attribution and secret-safe payload checks | `internal/testutil/httptestx/crosscutting.go::RequireMutationAttribution`, `RequireDivergentReplayRejected`, and `RequireSecretSafePayload` are exercised by the Phase 1 integration tests for deployment-admin audit, replay, and stored payload safety. |
| Replay and divergent replay proof | `TestPhase1_UserCreateReplayReturnsOriginalCommittedResource_I_1_03`, `TestPhase1_PasswordChangeReplayAndStoredPayload_I_1_04`, `TestPhase1_AdminPasswordResetReplayReturnsOriginalCommittedResource_I_1_05`, and `TestPhase1_AdminTOTPResetAndRevokeAllReplay_I_1_05` now cover exact replay, divergent replay, original committed payload reuse, and one-row idempotency stability for the bounded credential routes and admin credential-action routes. |
