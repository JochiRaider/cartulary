# Phase 1 Coverage Ledger

This ledger is generated from `tools/phase1_test_map.json`. Update the manifest row metadata first, then regenerate this file.

- Scope: local authentication, session lifecycle, TOTP bootstrap or replacement, deployment-local user administration, and deployment-admin credential actions only.
- Normative owners: Core 01 `§3.3.2`, `§3.3.2.2`, `§3.3.5.1`, `§3.3.10`; Core 04 `§1`, `§3`.
- Authority: `tools/phase1_test_map.json` is the enforced Phase 1 traceability source. This ledger is a rendered companion and does not control the mechanical row inventory.
- Explicit `N/A`: view-schema field-key conformance, projection-rebuild determinism, and measurement or timed-completion validation under claim-bearing timing criteria.

## Authoritative Execution

- `backend-unit` selects authoritative `U-1-*` rows through `cartulary-runner.mjs go-target backend-unit`, with target-plan selection constrained by the Phase 1 manifest and `backend_unit` execution dependency.
- `backend-store` selects store-backed authoritative `U-1-*` rows through `cartulary-runner.mjs go-target backend-store`, with target-plan selection constrained by the Phase 1 manifest and `backend_store` execution dependency.
- `frontend-unit` selects authoritative `U-1-*` ordinary-shell Vitest rows only through the Phase 1 Vitest manifest for `frontend_unit`.
- `backend-integration` selects authoritative `I-1-*` rows through `cartulary-runner.mjs go-target backend-integration`, with target-plan selection constrained by the Phase 1 manifest and `backend_integration` execution dependency.
- `tools/execution_topology_manifest.json` owns browser batch grouping and renders `tools/browser_e2e_batch_manifest.json` for Phase 1: `browser-e2e-webserver-backed` carries authoritative functional `E-1-*` rows through duration-balanced Playwright manifest-entry shards for `test` and `check`, while direct `browser-e2e-webserver-backed` and helper-only `browser-e2e-functional` select the same rows through manifest-driven `browser_functional` shard planning. Service-backed scheduling runs isolated browser evidence as leaf targets while direct `browser-e2e` remains the local aggregate for stateful, measurement, and visual browser batches.
- `tools/execution_topology_manifest.json` also keeps `browser-e2e-stateful` as the isolated Phase 1 browser batch for authoritative stateful `E-1-*` rows selected through the Phase 1 Playwright manifest for `browser_stateful`.

## Support-Only Execution

- `internal/modules/auth/support_test.go` runs through `backend-unit` with `^(?:TestAdminCredentialActionsRequireDeploymentAdmin|TestCSRFHelpers|TestCredentialStateBuilders|TestPasswordChangeDecode|TestSessionInspectionHelpers|TestTOTPBootstrapHelpers|TestUserCreateDefaultsAndSafeShape)$` and is forbidden from claiming `U-1-*` identifiers.
- `internal/modules/auth/support_integration_test.go` runs through `backend-integration-support` with `^(?:TestAuditAttribution|TestAuthorizationReDerivation|TestBootstrapBoundaries|TestCSRFProtection|TestReplayAndStoredPayloadSafety|TestRequestContracts|TestSessionRevocation|TestSurfaceEnvelope|TestUserListContinuationUsesLiveRows)$` and is forbidden from claiming `I-1-*` identifiers.
- `apps/web/src/app/App.auth.support.test.tsx` remains smoke-only support coverage and is forbidden from claiming `U-1-*` identifiers.
- `internal/app/serverprocess/process_test.go` remains process-smoke evidence only. It is supplemental manifest coverage in the `backend-process` execution family, but it is not counted as the authoritative `E-*` surface below.

## Unit

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `U-1-01` | `implemented` | `internal/modules/auth/request_test.go::TestLoginRequestShape_Unit` | `backend_unit` | Login request-shape validation rejects unknown top-level members, unsupported `second_factor.kind`, and malformed TOTP assertions with the route-owned invalid-auth surface, and the full login route returns the error envelope before user lookup, session creation, bootstrap issuance, or auth-cookie emission. | Successful route login and durable session creation are covered by adjacent unit and integration rows. |
| `U-1-02` | `implemented` | `internal/modules/auth/handlers_test.go::TestLoginNormalizationAndPasswordExactness_Unit` | `backend_unit` | The real login handler trims and case-folds the username for lookup while exact post-JSON-decoded password bytes reach verification unchanged, so trimmed or normalized password variants fail before session creation. | Durable route-created password-hash persistence remains integration evidence. |
| `U-1-03` | `implemented` | `internal/modules/auth/handlers_test.go::TestLoginCreatesSessionAndResource_Unit` | `backend_unit` | The real login handler creates exactly one session via the store, returns the singleton session resource, and emits the ordinary cookie and envelope shape with the required auth-cookie attributes. | Durable session-row persistence and expiry math remain integration evidence. |
| `U-1-04` | `implemented` | `internal/modules/auth/handlers_test.go::TestSessionInspectionRoute_Unit` | `backend_unit` | The real `GET /auth/session` route rejects pagination members, returns the singleton resource envelope, and proves the handler does not slide idle expiry. | Durable non-sliding inspection is still integration-proven against PostgreSQL. |
| `U-1-05` | `implemented` | `internal/modules/auth/store_test.go::TestConcurrencyLimitRevokesLRUNonCurrent_Unit` | `backend_store` | The real login handler creates the sixth session through the real store, revokes the deterministic least-recently-used non-current victim, publishes `concurrency_limit` to the session hub, persists the durable revocation row, and records the matching deployment-local audit event while still returning the new session resource. | Browser-visible session loss and shared-runtime socket closure remain higher-layer evidence. |
| `U-1-06` | `implemented` | `internal/modules/auth/store_test.go::TestRouteRevocationConsequences_Unit` | `backend_store` | One route matrix proves logout, password change, TOTP replacement completion, account disablement, admin password reset, admin TOTP reset, and explicit revoke-all drive the correct current-only or all-session revocation scope, publish the right hub fanout, clear auth cookies only when the actor loses the current session, and persist the resulting session-row state. | Socket closure on revoked attached sessions remains integration evidence. |
| `U-1-07` | `implemented` | `internal/modules/auth/handlers_test.go::TestUserCreateRouteDefaults_Unit` | `backend_unit` | User-create rejects undeclared `is_active`, rejects unsupported `auth_kind`, normalizes writable `email` and `display_name`, forces server-managed activation, keeps default `mfa_required` and `is_deployment_admin` behavior stable, and returns the safe user resource without secret echo. | Persisted Argon2id storage and replay behavior are covered at integration rather than unit level. |
| `U-1-08` | `implemented` | `internal/modules/auth/store_test.go::TestUserPatchAndLastAdminGuard_Unit` | `backend_store` | The real patch route rejects missing `base_user_version`, normalizes writable `email` and `display_name`, translates stale-version conflicts to `user_version_conflict`, and proves the last-active-deployment-admin demotion or deactivation guard against durable database state without publishing revocations or applying partial mutation. | Browser-visible administration flows remain browser evidence. |
| `U-1-09` | `implemented` | `internal/modules/auth/handlers_test.go::TestCSRFProtectionRoutes_Unit` | `backend_unit` | An inventory-driven loop covers every cookie-authenticated Phase 1 mutator and proves missing or invalid CSRF proof fails closed before session lookup, mutation, replay, or revocation fan-out. | Cross-route persistence consequences remain integration evidence. |
| `U-1-10` | `implemented` | `internal/modules/auth/handlers_test.go::TestCredentialStateRoute_Unit` | `backend_unit` | The real credential-state route returns the safe response shape, keeps `totp.state` closed, and rejects bootstrap-token access with the route-owned failure code. | Credential lifecycle transitions and bootstrap consumption remain integration-proven. |
| `U-1-11` | `implemented` | `internal/modules/auth/handlers_test.go::TestPasswordChangeRouteContracts_Unit` | `backend_unit` | The password-change route proves exact current-password verification, active-TOTP enforcement, all-session revocation consequences, replay behavior, and secret-safe handler-boundary response storage. | Durable replay storage and audit persistence remain integration evidence. |
| `U-1-12` | `implemented` | `internal/modules/auth/handlers_test.go::TestTOTPRouteContracts_Unit` | `backend_unit` | The TOTP begin and complete handlers enforce exactly one auth mode, reject mixed session-plus-bootstrap auth on both routes, prove same-scope replay and divergent replay conflict, keep setup material begin-only, and prove bootstrap-token consumption after successful complete. | Durable enrollment storage and post-complete credential transitions remain integration evidence. |
| `U-1-13` | `implemented` | `internal/modules/auth/handlers_test.go::TestAdminCredentialActionGuards_Unit` | `backend_unit` | Deployment-admin-only credential routes fail closed for non-deployment-admin sessions and keep route-local revoke and reset consequences stable. | True incident-admin denial, audit persistence, replay storage, and WebSocket revocation remain integration evidence. |
| `U-1-14` | `implemented` | `apps/web/src/app/App.auth.test.tsx::ordinary shell keeps the anonymous login surface, sends the login request, refreshes session and credential state after success, and keeps deployment-user controls denied for non-admin sessions`, `ordinary shell blocks authenticated bootstrap until credential state loads and renders credential public errors without private details` | `frontend_unit` | The ordinary shell starts anonymous, sends the Phase 1 login request with the ordinary client payload, refreshes session plus credential-state fetches after success, blocks authenticated bootstrap until credential state loads, renders credential public errors without private details, and keeps deployment-user controls denied for a non-deployment-admin session. | Real cookies, durable session rows, and route-owned invalid-credential cases remain integration or browser evidence. |
| `U-1-15` | `implemented` | `apps/web/src/app/App.auth.test.tsx::ordinary shell follows mfa_setup_required through totp begin and complete, sends bootstrap-token requests, and proves completion alone does not issue a session` | `frontend_unit` | The ordinary login shell consumes `mfa_setup_required`, sends bootstrap-token `totp/begin` and `totp/complete` requests on the ordinary client surface, and proves completion alone does not refresh into an authenticated session. | Real bootstrap-token issuance and later login with an enrolled factor remain browser E2E evidence. |
| `U-1-16` | `implemented` | `apps/web/src/app/App.auth.test.tsx::ordinary account-security controls issue password-change and totp-enrollment requests, surface failures on the shell, and refresh back to anonymous state after success` | `frontend_unit` | The ordinary account-security panel issues TOTP begin and password-change requests with the expected payloads, keeps route-owned failure codes visible on the shell, and returns the user to the anonymous shell after a successful password change. | Real session revocation, credential persistence, and replacement-enrollment storage remain integration or browser evidence. |
| `U-1-17` | `implemented` | `apps/web/src/app/App.auth.test.tsx::ordinary deployment-admin controls create and load users, send versioned patch requests, and surface user_version_conflict plus last_deployment_admin on the shell` | `frontend_unit` | The ordinary deployment-admin panel sends create, load, and versioned patch requests, then surfaces `user_version_conflict` and `last_deployment_admin` on the ordinary shell without leaning on debug-only harness views. | Durable patch persistence, audit rows, and deployment-admin authorization re-derivation remain backend integration evidence. |

## Integration

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `I-1-01` | `implemented` | `internal/modules/auth/integration_test.go::TestLoginSessionLifecycle_Integration` | `backend_integration` | Real login creates one durable session row, session inspection exposes the singleton resource without idle slide, touch slides idle expiry, logout revokes the current session, idle expiry fails closed with `session_expired`, and sixth-login concurrency revokes the LRU non-current session with one deployment-local audit event. | None. |
| `I-1-02` | `implemented` | `internal/modules/auth/integration_test.go::TestSessionRevocationClosesAttachedSocket_Integration` | `backend_integration` | Real sockets on canonical `/ws/v1/incidents/{incident_id}` connections receive `session_revoked` and then close after logout and sixth-login concurrency revocation, with incident membership seeded only to reach the public WebSocket boundary. | None. |
| `I-1-03` | `implemented` | `internal/modules/auth/integration_test.go::TestUserAdminLifecycle_Integration`, `TestUserAdminAudit_Integration`, `TestUserCreateReplayReturnsOriginalCommittedResource_Integration` | `backend_integration` | User create applies defaults, stores Argon2id password hashes, persists safe `response_json`, replays the original committed resource even after later mutation, rejects divergent replay with `client_txn_conflict`, keeps one idempotency row, proves stale-version and last-admin guard behavior, and records secret-safe deployment-admin audit before/after payloads. | Unit-level handler orchestration for create and patch remains thinner than the integration evidence. |
| `I-1-04` | `implemented` | `internal/modules/auth/integration_test.go::TestCredentialStateAndBootstrapFlows_Integration`, `TestCredentialStateTransitions_Integration`, `TestBootstrapEnrollmentConsumption_Integration`, `TestPasswordChangeReplayAndStoredPayload_Integration` | `backend_integration` | Credential-state transitions prove first enrollment, replacement enrollment, begin replay and divergent conflict, bootstrap-token consumption, password change revocation, Argon2id password-hash replacement, exact replay of password change before fresh invalid-current-password evaluation, secret-safe stored `route_idempotency.response_json`, and one-row replay stability. | None for the declared Phase 1 contract. |
| `I-1-05` | `implemented` | `internal/modules/auth/integration_test.go::TestAdminCredentialActions_Integration`, `TestAdminCredentialAuditAndScope_Integration`, `TestAdminPasswordResetReplayReturnsOriginalCommittedResource_Integration`, `TestAdminTOTPResetAndRevokeAllReplay_Integration` | `backend_integration` | Admin password reset, TOTP reset, and revoke-all revoke attached sockets, preserve deployment-local authorization boundaries including true incident-admin denial, store only secret-safe replay payloads, reject divergent replay with `client_txn_conflict`, keep one idempotency row per committed request, return original committed replay payloads, prove password-reset replay after later user mutation, and prove persisted Argon2id password-hash replacement on admin reset. | None for the declared Phase 1 contract. |
| `I-1-06` | `implemented` | `internal/modules/auth/integration_test.go::TestBootstrapTokenRouteBoundaries_Integration` | `backend_integration` | Bootstrap tokens work only on `totp/begin` and `totp/complete`, are rejected on ordinary session, credential-state, incident, and test-auth routes, and are rejected on canonical `/ws/v1/incidents/{incident_id}` with `credential_bootstrap_rejected`. | None. |

## Browser E2E

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `E-1-01` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::signs in as a local user and inspects the ordinary session surface` | `browser_functional` | Browser sign-in exposes the ordinary session surface, including `provider_type='local'`, singleton expiry fields, and `memberships[]`, without relying on the debug harness façade. | None. |
| `E-1-02` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::requires MFA on the ordinary login surface, rejects wrong codes, and accepts a valid TOTP code` | `browser_functional` | The ordinary login UI rejects missing and wrong TOTP assertions and accepts a valid code against real backend state. | None. |
| `E-1-03` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::rejects invalid credentials without issuing a session cookie` | `browser_functional` | Invalid credentials leave no session cookie and expose the route-owned browser-visible failure code on the normal login surface. | None. |
| `E-1-04` | `implemented` | `apps/web/e2e/clock.spec.ts::advances the shared clock past idle expiry and requires a fresh login afterwards` | `browser_stateful` | Idle expiry fails closed after the shared test clock advances past the normative idle TTL, and the ordinary account surface drops back to login before a fresh sign-in succeeds. | None. |
| `E-1-05` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::lets deployment admins create and patch users, rejects stale versions, and shows the last-admin guard on the ordinary shell` | `browser_stateful` | Browser deployment-user administration works on the ordinary shell, stale version conflicts surface in the normal admin panel, and the last-admin demotion and deactivation guard remain visible there. | None. |
| `E-1-06` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::follows the bootstrap-token enrollment sequence on the ordinary login shell and proves completion alone issues no session` | `browser_functional` | The ordinary login UI proves first-time bootstrap enrollment, completion without implicit session issuance, later login with active TOTP, admin-driven TOTP reset, second bootstrap enrollment, and post-reset re-login with the replacement secret. | None. |
| `E-1-07` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::requires the current password and current TOTP code, revokes the session immediately, and requires re-login with the new password` | `browser_functional` | The ordinary account-security UI requires exact current password and current TOTP, revokes the session immediately, rejects the old password, and accepts the new password. | None. |
| `E-1-08` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::keeps deployment-user administration on deployment-admin sessions and hides it from an incident admin on the ordinary shell` | `browser_functional` | The ordinary shell keeps deployment-user administration on deployment-admin sessions, shows a deny-by-UI note for an incident admin, and the authenticated incident-admin browser session receives `403 authorization_denied` with `required_capability=deployment_admin` when it attempts password reset, TOTP reset, or revoke-all directly. | Audit persistence and route-owned state consequences remain integration evidence. |
| `E-1-09` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::creates an incident from the landing screen, lists it, and opens the workbook as incident admin` | `browser_functional` | The ordinary authenticated landing screen creates an incident through `POST /api/v1/incidents`, lists the created incident, opens it by `incident_id`, and renders the workbook shell with current role `admin`. | Workbook layout and mutation replay remain later-phase evidence. |
| `E-1-10` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::clears a stale selected incident after membership removal while preserving the account session` | `browser_functional` | A selected incident whose membership is removed fails closed back to the landing screen, disappears from the visible incident list, preserves the account session, and still allows opening another visible incident. | Broader membership administration UX remains Phase 2 evidence. |
| `E-1-11` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::observes current-role authorization on a stale reviewer edit through the public incident error envelope` | `browser_functional` | A reviewer-visible incident edit control fails with `403 authorization_denied` after the user's current role is demoted before submit, and the ordinary shell renders only the public error code before reloading the current editor role. | Private diagnostic filtering has frontend-unit route-boundary support coverage; this row observes the browser E2E consequence. |
| `E-1-12` | `implemented` | `apps/web/e2e/auth-and-incident-directory.spec.ts::returns a revoked target browser to login and allows re-authentication with unchanged incident membership` | `browser_functional` | Deployment-admin revoke-all invalidates a target browser session, the next public session refresh returns the ordinary shell to login, and a fresh login can reopen the user's unchanged visible incident membership. | Admin credential-action audit persistence remains integration evidence. |

## Shared Harness Coverage

| Harness | Phase 1 evidence |
| --- | --- |
| Envelope consistency and browser/admin API helpers | `internal/testutil/httptestx/httptestx.go` plus `apps/web/e2e/authRuntime.ts` and `apps/web/e2e/helpers.ts` keep the Phase 1 browser helpers on one shared request surface instead of per-spec ad hoc API wiring. |
| Real runtime, Postgres, and object-store harness | `internal/testutil/appsupport` owns composed PostgreSQL, object-store, database, and app-server startup; `internal/modules/auth/testsupport/flowtest/runtime.go` selects auth route privilege and dependencies, while `internal/modules/auth/testsupport/storetest/runtime.go` owns the non-cyclic `StartStore` harness used by service-backed Phase 1 unit rows that exercise handlers directly against a real `authn.Store`. |
| Auth HTTP, bootstrap, and session helpers | `internal/modules/auth/testsupport/flowtest/flow.go` centralizes HTTP, bootstrap, credential-flow, and shared auth-cookie helpers for the integration and process-smoke suites, while `internal/modules/auth/testsupport/storetest/store.go` centralizes local-user seeding, active-TOTP seeding, deterministic session and pending-enrollment seeding, session inspection queries, counts, and audit lookup used by the Phase 1 service-backed unit rows. |
| Public-route inventory governance | `internal/modules/auth/testsupport/routetest/routes.go` plus `internal/modules/auth/support_integration_test.go` apply `surface_envelope`, `bootstrap_boundary`, `csrf`, `replay_stored_payload`, `mutation_audit`, `session_revocation`, `authorization_rederivation`, and `request_contracts` support sweeps from one auth route inventory rather than scattered ad hoc selections. |
| Ordinary-shell frontend-unit ownership | `apps/web/src/app/App.auth.test.tsx`, `tools/phase1_test_map.json`, and `make frontend-unit` keep the Phase 1 ordinary shell on the authoritative `frontend-unit` task surface instead of leaving Vitest coverage as unmapped smoke. |
| WebSocket lifecycle helpers | `internal/modules/collaboration/testsupport/incidentwstest` owns the canonical `/ws/v1/incidents/{incident_id}` handshake, presence-snapshot, bootstrap-rejection, and `session_revoked` assertions; `internal/modules/auth/testsupport/flowtest` wraps those helpers for auth session-lifecycle evidence. |
| Mutation attribution and secret-safe payload checks | `internal/testutil/auditassert.RequireMutationAttribution`, `internal/testutil/contractassert.RequireDivergentReplayRejected`, and `internal/testutil/securityassert.RequireSecretSafePayload` are exercised by the Phase 1 integration tests for deployment-admin audit, replay, and stored payload safety. |
| Replay and divergent replay proof | `TestUserCreateReplayReturnsOriginalCommittedResource_Integration`, `TestPasswordChangeReplayAndStoredPayload_Integration`, `TestAdminPasswordResetReplayReturnsOriginalCommittedResource_Integration`, and `TestAdminTOTPResetAndRevokeAllReplay_Integration` now cover exact replay, divergent replay, original committed payload reuse, and one-row idempotency stability for the bounded credential routes and admin credential-action routes. |

## Support-Only Evidence

- `internal/modules/auth/support_test.go` keeps helper-level regression coverage for session inspection helpers, CSRF helpers, credential-state builders, password-change decode, TOTP bootstrap helpers, user-create defaults, and deployment-admin credential-action guards. It does not replace `U-1-*`.
- `internal/modules/auth/support_integration_test.go` keeps inventory-driven user-list pagination, success-envelope, bootstrap-boundary, CSRF, replay-stored-payload, mutation-audit, session-revocation, authorization re-derivation, and request-contract sweeps on the support runtime surface. It does not replace `I-1-*`.
- `apps/web/src/app/App.auth.support.test.tsx` remains ordinary-shell smoke-only coverage and is not completion evidence for Phase 1.
- `internal/app/serverprocess/process_test.go` remains supplemental process-smoke evidence for startup and runtime confidence and is not authoritative `E-1-*` evidence.
