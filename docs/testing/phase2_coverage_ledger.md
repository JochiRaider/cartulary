# Phase 2 Coverage Ledger

This ledger is generated from `tools/phase2_test_map.json`. Update the manifest row metadata first, then regenerate this file.

- Scope: incident create or list or get or patch, bootstrap membership and workbook preferences, incident membership administration, incident-scoped authorization, extension discovery and reserved-family dispatch, plus the ordinary landing and incident-shell Phase 2 UI.
- Normative owners: Core 01 `§3.3.1`, `§3.3.3`, `§3.3.5.1`, `§3.3.5.2`, `§3.3.5.3`; Core 02 `§4.5`; Core 04 `§2`, `§3`.
- Authority: `tools/phase2_test_map.json` is the enforced Phase 2 traceability source. This ledger is a rendered companion and does not control the mechanical row inventory.

## Authoritative Execution

- `backend-unit` selects authoritative `U-2-*` rows through `cartulary-runner.mjs go-target backend-unit`, with target-plan selection constrained by the Phase 2 manifest and `backend_unit` execution dependency.
- `backend-store` selects store-backed authoritative `U-2-*` rows through `cartulary-runner.mjs go-target backend-store`, with target-plan selection constrained by the Phase 2 manifest and `backend_store` execution dependency.
- `backend-integration` selects authoritative `I-2-*` rows through `cartulary-runner.mjs go-target backend-integration`, with target-plan selection constrained by the Phase 2 manifest and `backend_integration` execution dependency.
- `frontend-unit` selects authoritative `U-2-*` rows only through the Phase 2 Vitest manifest for `frontend_unit`.
- `tools/execution_topology_manifest.json` owns browser batch grouping and renders `tools/browser_e2e_batch_manifest.json` for Phase 2: `browser-e2e-webserver-backed` carries authoritative `E-2-*` rows through duration-balanced Playwright manifest-entry shards for `test` and `check`, while direct `browser-e2e-webserver-backed` and helper-only `browser-e2e-functional` select the same rows through manifest-driven `browser_functional` shard planning. Service-backed scheduling runs isolated browser evidence as leaf targets while direct `browser-e2e` remains the local aggregate for stateful, measurement, and visual browser batches.

## Support-Only Execution

- `internal/modules/incidents/request_test.go` runs through `backend-unit` with `TestOpenAPIWorkbookPreferencesExposeGetAndPutContracts` and is forbidden from claiming `U-2-*` identifiers.
- `internal/modules/incidents/support_test.go` runs through `backend-unit` with `TestUnit_MembershipPatchAndDeleteDecodeAndLastAdminGuardStayStable` and is forbidden from claiming `U-2-*` identifiers.
- `internal/modules/workbook/startup/api_test.go` runs through `backend-unit` with `^(?:TestWorkbookPreferencesPutDecodersCanonicalizeSheetRefs|TestWorkbookPreferencesPutDecodersRejectInvalidPayloads)$` and is forbidden from claiming `U-2-*` identifiers.
- `internal/modules/workbook/startup/store_test.go` runs through `backend-unit` with `TestWorkbookStartupPreferencesBootstrapAndUpsert` and is forbidden from claiming `U-2-*` identifiers.
- `internal/modules/incidents/extra_integration_test.go` runs through `backend-integration-support` with `^(?:TestIncidentPatchWritesAuditBeforeAfter|TestMembershipCreateReplayReturnsOriginalAndDivergentConflict|TestMembershipMutationsWriteAuditBeforeAfter)$` and is forbidden from claiming `I-2-*` identifiers.
- `internal/modules/incidents/http_conformance_test.go` runs through `backend-integration-support` with `^(?:TestIncidentCreateBootstrapsCreatorAndWorkbookPreferencesHTTPConformance|TestIncidentCreateIdempotencyUsesActorAndNormalizedReplayHTTPConformance|TestIncidentCreateReturnsStableLocationHeaderHTTPConformance|TestMembershipCreateRequiresOneSelectorClosedRolesAndNoInvitationFieldsHTTPConformance|TestMembershipPatchAndDeleteEnforceBaseVersionAndLastAdminGuardHTTPConformance)$` and is forbidden from claiming `I-2-*` identifiers.
- `internal/modules/incidents/http_conformance_test.go` runs through `backend-integration-support` with `^(?:TestPublicRouteInventoryExtensionDiscoveryEnvelopes|TestPublicRouteInventoryIncidentCoreEnvelopes|TestPublicRouteInventoryMembershipAdminEnvelopes|TestPublicRouteInventoryWorkbookPreferencesEnvelopes)$` and is forbidden from claiming `I-2-*` identifiers.
- `internal/modules/incidents/http_conformance_test.go` runs through `backend-integration-support` with `^(?:TestControlBoundaryIncidentCoreDeploymentAdminWithoutMembershipDenied|TestControlBoundaryMembershipAdminDeploymentAdminWithoutMembershipDenied|TestControlBoundaryTimelineRecordAndLiveDeploymentAdminWithoutMembershipDenied|TestControlBoundaryWorkbookPreferencesDeploymentAdminWithoutMembershipDenied|TestControlBoundaryWorkbookQueriesDeploymentAdminWithoutMembershipDenied)$` and is forbidden from claiming `I-2-*` identifiers.
- `internal/modules/incidents/pagination_integration_test.go` runs through `backend-integration-support` with `^(?:TestIncidentListContinuationOmitsRevokedMembership|TestIncidentListContinuationUsesLiveMembershipQuery|TestIncidentListUsesLiveFirstPageIndependentlyOfTestClock|TestMembershipListContinuationUsesLiveRows)$` and is forbidden from claiming `I-2-*` identifiers.
- `backend-integration-support` emits a shared-capture support report rather than a second independent runtime.
- `make browser-e2e-support` is a helper slice also exercised inside the `browser-e2e-webserver-backed` browser batch and does not provide additional authoritative confirmation.
- Phase 2 process smoke now runs as supplemental manifest coverage inside the `backend-process` execution family instead of a separate helper target.

## Unit

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `U-2-01` | `implemented` | `internal/modules/incidents/request_test.go::TestIncidentCreateAcceptsDeclaredMembersAndNormalizesIncidentKey_Unit` | `backend_unit` | Incident create decode rejects undeclared members, rejects `initial_memberships`, and normalizes `incident_key` with trim plus NFC before uniqueness. | Durable create persistence remains integration evidence. |
| `U-2-02` | `implemented` | `internal/modules/incidents/store_test.go::TestStoreCreateIncidentCommitsBootstrapAdminAndWorkbookPreferences_Unit` | `backend_store` | Store.CreateIncident commits the bootstrap `admin` membership plus both incident-wide and per-user workbook-preference rows, and workbook-preference PUT upserts preserve structural no-op timestamps and default actors. | HTTP envelope and header emission remain support coverage. |
| `U-2-03` | `implemented` | `internal/modules/incidents/store_test.go::TestStoreCreateIncidentReturnsStableLocationValue_Unit` | `backend_store` | The committed create result returns the stable `/api/v1/incidents/{incident_id}` location value. | Actual `Location` header emission remains support coverage. |
| `U-2-04` | `implemented` | `internal/modules/incidents/store_test.go::TestStoreCreateIncidentReplayPreservesDurableSideEffectsAndScopesByActor_Unit` | `backend_store` | Incident create idempotency scopes by actor plus normalized request, exact replay returns the original committed result, divergent replay rejects, and replay keeps the incident row, bootstrap membership, workbook-preference rows, owner mutation artifacts, and route idempotency stable. | HTTP replay envelope proof remains support coverage. |
| `U-2-05` | `implemented` | `internal/modules/incidents/request_test.go::TestIncidentPatchAllowsPromotedFieldsAndKeepsNoOpVersionStable_Unit` | `backend_unit` | Incident patch decode allows only `description`, `severity`, `tlp`, `current_phase`, and `primary_external_case_ref`; requires `base_incident_version`; rejects immutable fields plus invalid TLP values; and keeps structural no-ops version-stable. | Persisted patch consequences remain integration evidence. |
| `U-2-06` | `implemented` | `internal/modules/incidents/request_test.go::TestMembershipCreateUsesLookupOnlyForUserOrEmailTargets_Unit` | `backend_unit` | Membership create requires exactly one selector, resolves targets by lookup-only `user_id` or `email`, returns canonical `user_not_found` or `user_inactive`, and rejects invitation-only fields without any auto-create path. | Live HTTP envelope proof remains support coverage. |
| `U-2-07` | `implemented` | `internal/modules/incidents/store_test.go::TestStoreMembershipPatchAndDeleteRejectStaleBaseVersion_Unit` | `backend_store` | Store-backed membership patch and delete reject stale `base_membership_version` values without mutating durable membership state, and same-role membership patch keeps `membership_version` stable. | Live HTTP same-role patch evidence is covered by I-2-07; pure decoder shape and `last_incident_admin` guard coverage stay on the executed support-only unit surface. |
| `U-2-08` | `implemented` | `internal/modules/incidents/unit_test.go::TestIncidentAccessDecisionUsesNotFoundForMissingMembershipAndDeniedForInsufficientRole_Unit` | `backend_unit` | The incident access decision helper returns `incident_not_found` for no membership and `authorization_denied` for insufficient incident role. | Deployment-admin-without-membership route breadth remains paired support or integration inventory coverage. |
| `U-2-09` | `implemented` | `internal/modules/incidents/request_test.go::TestExtensionDiscoveryReturnsExactSingletonProfileShape_Unit` | `backend_unit` | Extension discovery keeps the singleton route shape, exact profile fields, canonical ordering, and no pagination members. | Runtime auth and leak checks remain integration evidence. |
| `U-2-10` | `implemented` | `internal/modules/incidents/request_test.go::TestReservedExtensionDispatchHonorsBaseRoutesClaimedFamiliesAndOutsideFallback_Unit` | `backend_unit` | Reserved-family dispatch preserves base-route precedence, claimed-family routing, reserved-but-unclaimed canonical 404s, and ordinary outside-family 404s. | Real authenticated dispatch remains integration evidence. |
| `U-2-11` | `implemented` | `apps/web/src/app/App.landing.test.tsx::ordinary landing shell creates an incident, refreshes session-visible membership, routes to the workbook by incident_id, and falls back when a stale incident selection is no longer visible` | `frontend_unit` | The ordinary landing shell creates an incident, refreshes session-visible membership on workbook entry, stores the selected `incident_id` in the route, and returns cleanly to landing when a stale selection is no longer visible. | Real browser confirmation stays `E-2-01` and `E-2-02`. |
| `U-2-12` | `implemented` | `apps/web/src/app/IncidentAdminPanel.test.tsx::ordinary incident shell gates promoted-field controls by incident role, hides membership-admin controls from non-admin members, and returns to landing when incident access is lost` | `frontend_unit` | The ordinary incident shell exposes patchable metadata controls for `description`, `severity`, `tlp`, `current_phase`, and `primary_external_case_ref` only for reviewer or admin roles, hides membership-administration controls from non-admin members, and triggers the landing fallback when incident access disappears. | Real browser visibility remains `E-2-02` and `E-2-03`. |
| `U-2-13` | `implemented` | `apps/web/src/app/IncidentAdminPanel.test.tsx::ordinary incident shell issues membership create, patch, and delete requests with versioned payloads and refreshes session role after each mutation` | `frontend_unit` | The ordinary incident shell emits the expected membership mutation payloads, includes versioned patch or delete bodies, and refreshes the session-visible incident role after each mutation. | Real browser mutation flow remains `E-2-03`. |
| `U-2-14` | `implemented` | `internal/modules/incidents/store_test.go::TestStoreIncidentPatchReturnsTypedVersionConflictDetails_Unit` | `backend_store` | Store.UpdateIncident returns a typed IncidentVersionConflictError with incident_id, base_incident_version, and current_incident_version details for stale base versions, without mutating durable incident state. | HTTP envelope mapping, authorization, CSRF, and incident-metadata persistence remain integration evidence under I-2-04. |

## Integration

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `I-2-01` | `implemented` | `internal/modules/incidents/integration_test.go::TestIncidentCreatePersistsBootstrapStateAndRollsBackAtomically_Integration` | `backend_integration` | Incident create persists the incident row, bootstrap membership, workbook preferences, and owner-level mutation artifacts on the current Phase 2 audit substrate atomically, while hook-forced rollback leaves no surviving Phase 2 mutation artifacts. | `change_sets` migration remains an owner/docs follow-on, not Phase 2 evidence. |
| `I-2-02` | `implemented` | `internal/modules/incidents/integration_test.go::TestIncidentCreateReplayAndDuplicateKeyConflictUseNormalizedState_Integration` | `backend_integration` | Real create with trimmed decomposed input normalizes the response key, replay with the composed form returns the original committed resource, divergent replay rejects, opposite-form duplicates on distinct requests fail with `incident_key_conflict`, and the incident row, bootstrap membership, workbook-preference rows, owner mutation artifacts, and route idempotency remain stable. | None for the declared replay and normalization contract. |
| `I-2-03` | `implemented` | `internal/modules/incidents/integration_test.go::TestControlBoundaryIncidentCoreReDerivesAuthorizationImmediately_Integration`, `TestControlBoundaryMembershipAdminReDerivesAuthorizationImmediately_Integration`, `TestControlBoundaryTimelineRecordAndLiveReDerivesAuthorizationImmediately_Integration`, `TestControlBoundaryWorkbookPreferencesReDerivesAuthorizationImmediately_Integration`, `TestControlBoundaryWorkbookQueriesReDerivesAuthorizationImmediately_Integration` | `backend_integration` | One same-session role progression `no membership -> viewer -> reviewer -> admin -> removed` re-derives access immediately for every `ControlBoundaryInventory()` route, including workbook-preference reads and writes, membership-admin routes, and the Timeline websocket boundary. | Deployment-admin-without-membership breadth is paired with the support inventory sweep. |
| `I-2-04` | `implemented` | `internal/modules/incidents/integration_test.go::TestIncidentPatchPersistsOnlyPromotedFieldsAndAdvancesOnMaterialChange_Integration` | `backend_integration` | Real incident patch persists only patchable metadata fields, covers set, replace, clear, invalid TLP, and stale-version cases, and advances `incident_version` only on material change. | None for the declared patch contract. |
| `I-2-05` | `implemented` | `internal/modules/incidents/integration_test.go::TestExtensionDiscoveryReturnsExactZeroMembershipShapeWithoutLeaks_Integration` | `backend_integration` | Extension discovery succeeds for an authenticated zero-membership session and leaks no provider secrets or live claim payloads. | None for the declared discovery contract. |
| `I-2-06` | `implemented` | `internal/modules/incidents/integration_test.go::TestUnclaimedReservedFamiliesReturnCanonical404AndOutsidePathsDoNot_Integration` | `backend_integration` | Reserved but unclaimed family roots and descendants return canonical `extension_profile_not_claimed`, while outside-family paths preserve ordinary 404 handling. | None for the declared dispatch contract. |
| `I-2-07` | `implemented` | `internal/modules/incidents/integration_test.go::TestMembershipPatchSameRoleReturnsOKWithoutVersionOrMutationArtifact_Integration` | `backend_integration` | Live membership patch to the current role returns `200 OK`, keeps `membership_version` and durable role stable, and writes no membership mutation artifact. | Role-change authorization re-derivation remains I-2-03 evidence. |

## Browser E2E

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `E-2-01` | `implemented` | `apps/web/e2e/incident-administration.spec.ts::creates an incident, bootstraps the creator as admin, and lands on the workbook surface` | `browser_functional` | Real browser incident create bootstraps the creator as incident admin and lands on the ordinary workbook shell. | None for the declared browser create-flow contract. |
| `E-2-02` | `implemented` | `apps/web/e2e/incident-administration.spec.ts::shows incident discovery, raw querystring deep-link retrieval, and promoted-field-only patching on the ordinary incident shell` | `browser_functional` | The ordinary shell exposes incident discovery, raw `/?incident_id=...` deep-link retrieval, and patchable incident metadata controls on the real browser surface. | None for the declared browser patch-flow contract. |
| `E-2-03` | `implemented` | `apps/web/e2e/incident-administration.spec.ts::lets incident admins manage memberships and hides those controls from non-admin members on the ordinary shell` | `browser_functional` | Incident admins manage memberships on the ordinary shell and non-admin incident members do not receive those controls. | None for the declared browser membership contract. |

## Shared Harness Coverage

| Harness | Phase 2 evidence |
| --- | --- |
| Public-route inventory ownership | Owner route inventories under incidents, workbook, Timeline, records, entities, indicators, and extensions compose the Phase 2 HTTP success-envelope coverage through `internal/testutil/routeinventory`; no product facade owns another module's rows. |
| Role-aware control boundary inventory | Each semantic owner contributes its role-aware routes to `I-2-03` and the deployment-admin-without-membership sweep; incident support retains only incident and membership rows. |
| Runtime and route inventory groups | `internal/testutil/appsupport` owns composed PostgreSQL, object-store, app-server, and incident-store wiring. `internal/modules/incidents/testsupport/scenariotest` supplies incident scenarios while owner route packages remain independently schedulable. |
| Runtime-owner classification | Workbook preference/startup rows exercised from Phase 2 incidents tests are workbook/startup-owned behavior, and extension discovery rows are extensions-owned behavior. Their current incidents-package test location is harness history and must not be cited as runtime module ownership evidence. |
| Owner-aware mutation helpers | `internal/modules/incidents/testsupport/mutationtest` keeps Phase 2 tests tied to incident resource or membership mutation ownership on the current audit substrate instead of naming `deployment_admin_audit_events` directly. |
| Store-domain incident helpers | `internal/modules/incidents/testsupport/storetest` owns rollback-backed incident store behavior; auth fixtures come from auth `storetest`, and `internal/testutil/appsupport.NewIncidentStore` owns cross-module composition. |
| Reusable incident route fixture helpers | `internal/modules/incidents/inventory_helpers_test.go` centralizes the seeded incident, membership, and record fixture setup plus route execution helpers shared by the Phase 2 support and integration matrices. |
| Frontend ordinary-shell component coverage | `apps/web/src/app/App.landing.test.tsx` and `apps/web/src/app/IncidentAdminPanel.test.tsx` keep the authoritative frontend-unit evidence on the ordinary landing or incident shell rather than the debug-only `Phase2Harness`. |
| Audit-substrate follow-on | Phase 2 currently proves owner-level mutation attribution through the existing audit substrate only. Whether Phase 2 incident mutations should migrate onto `change_sets` is a separate owner decision and is not asserted by this ledger. |

## Support-Only Evidence

- `internal/modules/incidents/http_conformance_test.go::TestSupportPhase2_PublicRouteInventory*Envelopes` loops grouped `PublicRouteInventory*()` entries and proves the success-envelope contract stays attached to the shared inventory rather than scattered route lists.
- `internal/modules/incidents/http_conformance_test.go::TestSupportPhase2_ControlBoundary*DeploymentAdminWithoutMembershipDenied` loops grouped `ControlBoundaryInventory*()` entries and proves deployment-admin-without-membership denial across the same incident-scoped surface that `I-2-03` re-derives through same-session role changes.
- `internal/modules/incidents/request_test.go::TestOpenAPIWorkbookPreferencesExposeGetAndPutContracts` keeps workbook-preference contract-shape regressions on the executed `backend-unit` support surface; `internal/modules/workbook/startup/api_test.go::TestSupportPhase2_WorkbookPreferencesPut*` keeps workbook-preference PUT decoder regressions with the startup owner.
- `internal/modules/incidents/request_test.go::TestOpenAPIExtensionDiscoveryExposesClosedContract` and extension discovery HTTP support rows remain extensions-owned evidence even while retained in the incidents package for Phase 2 accounting stability.
- `internal/modules/workbook/startup/store_test.go::TestWorkbookStartupPreferencesBootstrapAndUpsert` keeps create-time workbook preference bootstrap and no-op upsert behavior with the startup storage owner.
- `internal/modules/incidents/support_test.go::TestUnit_MembershipPatchAndDeleteDecodeAndLastAdminGuardStayStable` keeps pure decoder-shape and `last_incident_admin` guard regression coverage on the executed `backend-unit` support surface. It does not replace `U-2-07`.
- `internal/modules/incidents/extra_integration_test.go` keeps membership replay and before-or-after mutation payload regressions that strengthen confidence but do not replace the authoritative `I-2-*` rows.
- `apps/web/e2e/incident.support.spec.ts` and browser-authenticated request probes remain supplemental browser evidence for request-shape regressions and reserved-route behavior.
- `apps/web/src/app/Phase2Harness.tsx` remains a debug-only probe surface behind `?debug=harness` and is not completion evidence.
