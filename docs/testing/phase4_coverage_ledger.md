# Phase 4 Coverage Ledger

This ledger is generated from `tools/phase4_test_map.json`. Update the manifest row metadata first, then regenerate this file.

- Scope: entity mentions, resolve and merge routes, entity-origin Host and Identity creation, indicator create and query surfaces, Parties and coordination workbook surfaces, object-blob creation, evidence handle issuance, Timeline auto-resolution and manual relationship HTTP behavior, and the owned Phase 4 browser inspector flows.
- Normative owners: Core 01 `§3.3.5`; Core 02 `§6` through `§10`, `§13`, `§19`; Core 03 `§8.4`, `§9`, `§16`, `§20`; Core 04 `AC-017`, `AC-019` through `AC-023`, `AC-077` through `AC-079`, `AC-128`, `AC-186`, `AC-188` through `AC-190`, `AC-205`, `AC-209`, `AC-221` through `AC-225`, `AC-251`, `AC-277`, `AC-281` through `AC-284`, `AC-388` through `AC-397`.
- Authority: `tools/phase4_test_map.json` is the enforced Phase 4 traceability source. This ledger is a rendered companion and does not control the mechanical row inventory.
- Binding-mode note: contract-derived binding-mode, row-field, and projection-determinism assertions are absorbed into the owning authoritative rows instead of creating separate helper-only completion evidence.

## Authoritative Execution

- `backend-unit` selects authoritative `U-4-*` decoder rows through `cartulary-runner.mjs go-target backend-unit`, with target-plan selection constrained by the Phase 4 manifest and `backend_unit` execution dependency.
- `backend-store` selects authoritative store-backed `U-4-*` rows through `cartulary-runner.mjs go-target backend-store`, with target-plan selection constrained by the Phase 4 manifest and `backend_store` execution dependency.
- `frontend-unit` selects authoritative Phase 4 workbook component rows through the Phase 4 manifest for `frontend_unit`, while shared package tests remain support or regression coverage.
- `backend-integration` selects authoritative `I-4-*` rows through `cartulary-runner.mjs go-target backend-integration`, with target-plan selection constrained by the Phase 4 manifest and `backend_integration` execution dependency.
- `tools/execution_topology_manifest.json` owns browser batch grouping and renders `tools/browser_e2e_batch_manifest.json` for Phase 4: `browser-e2e-webserver-backed` carries authoritative `E-4-*` rows through duration-balanced Playwright manifest-entry shards for `test` and `check`, while direct `browser-e2e-webserver-backed` and helper-only `browser-e2e-functional` select the same rows through manifest-driven `browser_functional` shard planning. `browser-e2e-visual` carries authoritative `V-4-*` visual rows through `browser_visual`.

## Support-Only Execution

- `internal/modules/entities/resolution_integration_test.go` runs through `backend-integration-support` with `TestSupportPhase4Integration_` and is forbidden from claiming `I-4-*` identifiers.
- `internal/modules/entities/support_integration_test.go` runs through `backend-integration-support` with `TestSupportPhase4Integration_` and is forbidden from claiming `I-4-*` identifiers.
- `internal/platform/postgres/entity_alias_migration_test.go` runs through `backend-integration-support` with `TestSupportPhase4_EntityAliasMigration31` and is forbidden from claiming `I-4-*` identifiers.
- `apps/web/src/workbook/WorkbookShell.phase4.support.test.tsx` runs through `frontend-unit` and is forbidden from claiming `U-4-*`, `I-4-*`, or `E-4-*` identifiers.

## Unit

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `U-4-01` | `implemented` | `internal/modules/timeline/unit_test.go::TestBindingMode_Unit` | `backend_store` | Timeline mention fields prove `mention_origin` binding while Host and Identity entity-origin writes create concrete entity rows, and the store writes mentions versus entity rows without synthesizing the other substrate. | HTTP route behavior and browser-visible resolution flows remain integration or browser evidence. |
| `U-4-02` | `implemented` | `internal/modules/timeline/unit_test.go::TestDuplicateMentionProvenance_Unit` | `backend_store` | Repeated identical mention tokens persist as separate durable mentions with distinct provenance locators, mention ids, and source-row attachment. | Interactive resolve and dismiss route behavior remains integration and browser evidence. |
| `U-4-03` | `implemented` | `internal/modules/entities/unit_test.go::TestCreateFromMention_Unit` | `backend_store` | Create-from-mention store paths promote unresolved mentions into durable Host or Identity rows with stable lineage and immediate relationship state. | The live resolve route and current-state HTTP readback remain integration evidence. |
| `U-4-04` | `implemented` | `internal/modules/entities/unit_test.go::TestDismissRestoreMentionLifecycle_Unit` | `backend_store` | Dismiss and restore store operations preserve raw mention text, mutate durable mention status correctly, and couple link removal or restoration to the lifecycle transition. | Route idempotency and authorization re-derivation remain integration evidence. |
| `U-4-05` | `implemented` | `internal/modules/entities/unit_test.go::TestExactMatchPrecedence_Unit` | `backend_store` | Exact-match precedence resolves reuse decisions through the owned identifier hierarchy and keeps suggestion-only aliases out of authoritative reuse. | HTTP conflict payload shape and browser-visible merge prompts remain higher-layer evidence. |
| `U-4-06` | `implemented` | `internal/modules/entities/unit_test.go::TestExplicitEntityMerge_Unit` | `backend_store` | Explicit merge store behavior repoints live mentions, links, and dependent rows onto the survivor while preserving loser lineage and replay stability. | HTTP merge route envelopes, websocket invalidation, and auth checks remain integration evidence. |
| `U-4-07` | `implemented` | `internal/modules/indicators/unit_test.go::TestIndicatorObservationSeparation_Unit` | `backend_store` | Indicator storage keeps source-bound observations separate while fanning them into one canonical indicator record and lifecycle projection state. | Current-state route readback and live authorization remain integration evidence. |
| `U-4-08` | `implemented` | `internal/modules/timeline/request_test.go::TestAutoResolutionEligibility_Unit` | `backend_unit` | Timeline request decoding preserves authoritative raw mention text, applies shared mention-token normalization, and accepts suppressor or forbidden rewrite tokens without silently rewriting them. | Durable mention persistence and auto-match side effects remain store or integration evidence. |
| `U-4-09` | `implemented` | `internal/modules/timeline/request_test.go::TestManualTimelineConfidenceNull_Unit` | `backend_unit` | Timeline create and patch decoding accepts manual relationship actions only when client confidence or provenance metadata is omitted and fails closed on override attempts. | Committed relationship mutations, replay, and current-state route readback remain integration evidence. |
| `U-4-WB-01` | `implemented` | `apps/web/src/workbook/WorkbookShell.assessments.test.tsx::Phase 4 U-4-WB-01 maps band-first assessment create payloads without submitting the derived band` | `frontend_unit` | The workbook assessment payload builder maps band-first UI state to the owned create contract, deduplicates support refs, and does not submit derived confidence-band fields. | Durable assessment history and real browser ordering remain integration and browser evidence. |
| `U-4-WB-02` | `implemented` | `apps/web/src/workbook/WorkbookShell.assessments.test.tsx::Phase 4 U-4-WB-02 submits assessment creates through the workbook UI` | `frontend_unit` | The ordinary workbook assessment panel submits the Phase 4 create route payload through app-level UI controls and renders the returned assessment grid row. | Service-backed append-only assessment history remains backend and browser evidence. |
| `U-4-WB-03` | `implemented` | `apps/web/src/workbook/WorkbookShell.surfaces.test.tsx::Phase 4 U-4-WB-03 issues opaque evidence preview and download handles from the evidence surface` | `frontend_unit` | The ordinary Evidence workbook surface issues preview and download handle requests with the required empty-object body and consumes opaque returned hrefs rather than deriving object storage access. | Handle issuance validation, redeem behavior, and object-store access remain backend integration evidence. |
| `U-4-WB-04` | `implemented` | `apps/web/src/workbook/WorkbookShell.surfaces.test.tsx::Phase 4 U-4-WB-04 builds required creates with direct values, timestamps, and explicit clears` | `frontend_unit` | The generic workbook mutation builder emits create payloads for required Phase 4 surfaces with trimmed direct values, timestamp fields, and explicit null clears. | Route-level validation and persistence for each surface remain backend integration evidence. |
| `U-4-WB-05` | `implemented` | `apps/web/src/workbook/WorkbookShell.surfaces.test.tsx::Phase 4 U-4-WB-05 builds direct clears and typed collection actions` | `frontend_unit` | The generic workbook mutation builder maps direct clears plus token, party, record, and risk collection edits to typed Phase 4 collection action payloads. | Typed collection conflict payloads and foreign-target rejection remain backend integration evidence. |

## Integration

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `I-4-01` | `implemented` | `internal/modules/entities/resolution_integration_test.go::TestResolveRoute_Integration` | `backend_integration` | The mention resolve route persists durable resolve, dismiss, and revert transitions through the live HTTP surface with authoritative replay/divergent-replay conformance, attributed mutation history, websocket invalidation, row-version conflict handling, and live target validation. | Browser inspector workflows remain browser evidence. |
| `I-4-02` | `implemented` | `internal/modules/entities/resolution_integration_test.go::TestEntityOriginUpsert_Integration`, `TestEntityCreateAuthAndCSRFFailBeforeMalformedBody_Integration` | `backend_integration` | Host and Identity create routes reuse exact matches, keep suggestion-only aliases non-authoritative, expose contract-conformant current-state rows, reject unauthenticated or CSRF-invalid malformed create requests before body parsing without durable or websocket side effects, and rebuild deterministic entity projections through the real HTTP surface. | End-user browser create flows remain browser evidence. |
| `I-4-03` | `implemented` | `internal/modules/entities/resolution_integration_test.go::TestExplicitMergeRoute_Integration` | `backend_integration` | The explicit merge route repoints live fan-out, preserves survivor reuse semantics, emits attributed history, and keeps loser lineage intact on Hosts and Identities. | Browser-visible inspector merge choreography remains browser evidence. |
| `I-4-07` | `implemented` | `internal/modules/indicators/resolution_integration_test.go::TestIndicatorsRoute_Integration` | `backend_integration` | Indicator create and query routes persist canonical indicators, expose contract-conformant current-state rows, and rebuild deterministic indicator projections without collapsing source-bound observations. | Browser-visible indicator discovery remains out of scope for this phase. |
| `I-4-08` | `implemented` | `internal/modules/timeline/resolution_integration_test.go::TestAutoResolutionEligibility_Integration` | `backend_integration` | Timeline patch and query routes auto-resolve only eligible exact matches, keep ineligible tokens unresolved, prove generic patch `resolve_item` behavior for `timeline.host_refs`, and return contract-conformant current-state rows before and after projection rebuilds. | Browser-visible chip rendering and inspector affordances remain browser or support-only frontend evidence. |
| `I-4-09` | `implemented` | `internal/modules/timeline/resolution_integration_test.go::TestManualTimelineConfidenceNull_Integration` | `backend_integration` | Timeline create and patch routes persist manual relationship actions with null confidence only, reject override payloads, and expose contract-conformant current-state rows across create, patch, and create-from-mention paths. | Browser inspector workflows remain browser or support-only frontend evidence. |
| `I-4-COLLECTION-01` | `implemented` | `internal/modules/workbook/workbook_mutation_integration_test.go::TestGenericCollectionPatch_Integration` | `backend_integration` | Generic record patch collection fields use stable record anchors, `field_key`, `row_version`, `base_row_version`, `collection_actions_v1`, and `collection_value_v1` semantics for add/remove/replace actions without accepting legacy array-shaped writes. | Keyboard paste expansion, queued multi-row commands, and broader grid command translation remain Phase 9 evidence. |
| `I-4-BLOB-01` | `implemented` | `internal/modules/evidence/evidence_integration_test.go::TestObjectBlobCreate_Integration` | `backend_integration` | The object-blob create route enforces request shape, authorization re-derivation, and declared-size limits, records the normalized accepted contract, returns stable idempotent replay, rejects divergent replay, and creates no blob state on rejected requests. | Browser upload UX and attach route lifecycle coverage remain separate evidence. |
| `I-4-COORD-01` | `implemented` | `internal/modules/workbook/workbook_mutation_integration_test.go::TestCoordinationDefaults_Integration` | `backend_integration` | Communications Log, Handoff, Status Review, and Lesson creates enforce their minimum semantic fields, apply server defaults, expose immutable server ids as read-only fields, distinguish clearable from non-clearable scalars, and include authoritative create replay/history conformance without partial rows on rejected creates. | End-user browser flows for coordination surfaces remain future browser evidence. |
| `I-4-COORD-02` | `implemented` | `internal/modules/workbook/workbook_mutation_integration_test.go::TestCoordinationCollections_Integration` | `backend_integration` | Coordination collection fields round-trip typed record, party, and risk references, coalesce duplicates, reject raw arrays and nulls, reject wrong or foreign targets, and return typed collection conflict payloads for stale same-field writes. | Bulk browser editing of coordination collections remains browser evidence when UI ownership is introduced. |
| `I-4-COORD-03` | `implemented` | `internal/modules/workbook/workbook_integration_test.go::TestCoordinationProjectionQueries_Integration` | `backend_integration` | Parties and coordination system views query route-created Parties and coordination rows plus seeded support rows through projection-backed or projection-shaped state with contract-declared filters, sorts, grouping values, row ids, and cell values. | Optional artifact-backed surfaces outside the base profile remain outside this Phase 4 row. |
| `I-4-HANDLE-01` | `implemented` | `internal/modules/evidence/evidence_integration_test.go::TestEvidenceHandles_Integration`, `TestDownloadHandleBlobMissingDoesNotConsumeHandle_Integration` | `backend_integration` | Preview and download handle issuance accepts only an empty JSON object body, rejects zero-length, null, non-object, unknown, and client_txn_id bodies for both routes without side effects, re-derives session authorization, returns distinct preview versus download handle contracts, and consumes single-use download handles only after a successful redeem starts byte delivery. | Detailed redeem expiry, revocation, and browser preview behavior remain separate evidence. |
| `I-4-PARTIES-01` | `implemented` | `internal/modules/workbook/workbook_mutation_integration_test.go::TestPartiesSurface_Integration` | `backend_integration` | The Parties system view is discoverable as a base-profile system view, fetchable by singleton schema route, incident-scoped in query behavior, rejects incomplete creates without partial state, and creates normalized party rows with authoritative replay/history conformance. | Inspector-driven party linking and browser party creation affordances remain browser or later route evidence. |

## Browser E2E

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `E-4-01` | `implemented` | `apps/web/e2e/phase4.mentions.resolve.spec.ts::E-4-01 resolves and creates entities from Timeline mentions in the inspector` | `browser_functional` | The browser inspector resolves existing entities and creates new entities from Timeline mentions through the real end-user flow. | Lower-level route replay and store substrate assertions remain backend evidence. |
| `E-4-02` | `implemented` | `apps/web/e2e/phase4.mentions.lifecycle.spec.ts::E-4-02 dismisses and ordinarily restores a mention without relinking` | `browser_functional` | The browser inspector dismisses a mention and restores it to unresolved state without silently relinking it. | Mention substrate mutation details remain backend evidence. |
| `E-4-03` | `implemented` | `apps/web/e2e/phase4.merge.spec.ts::E-4-03 merges duplicate entities from the inspector and preserves survivor identity` | `browser_functional` | The browser inspector merges duplicate entities through the end-user flow while preserving the survivor identity in the UI. | Detailed fan-out repointing and projection substrate assertions remain backend evidence. |
| `E-4-04` | `implemented` | `apps/web/e2e/phase4.autoresolve.spec.ts::E-4-04 auto-resolves only eligible exact-match Timeline tokens` | `browser_functional` | The browser workbook shows auto-resolution only for eligible exact-match Timeline tokens and leaves ineligible tokens unresolved. | Decoder normalization details and route-level projection rebuild assertions remain backend evidence. |
| `E-4-05` | `implemented` | `apps/web/e2e/phase4.workbook.assessments.spec.ts::E-4-05 creates append-only assessment history through the workbook UI` | `browser_functional` | The browser workbook drives the Assessments surface through ordinary user controls with band-first confidence, rationale, support refs, default ordering, and state or confidence filters. | Append-only assessment storage, projection determinism, idempotency, validation, and audit history remain backend evidence. |
| `E-4-06` | `implemented` | `apps/web/e2e/phase4.workbook.generic.spec.ts::E-4-06 creates and edits required workbook mutation surfaces through typed generic controls` | `browser_functional` | The browser workbook exercises Parties, Notes, Task Requests, Decisions, Evidence, Communications Log, Handoff, Status Review, and Lesson surfaces through real typed controls, including direct values, reference selectors, collection actions, and visible required-field validation. | Durable mutation history, replay/idempotency, authorization, projection rebuild, and detailed lifecycle-transition assertions remain backend integration evidence. |

## Visual Regression

| Row | Claim status | Evidence | Execution | Claim | Out of scope |
| --- | --- | --- | --- | --- | --- |
| `V-4-GRID-01` | `implemented` | `apps/web/e2e/workbook.visual.spec.ts::V-4-GRID-01 captures Timeline unresolved and resolved mention chips in the workbook grid` | `browser_visual` | The visual harness captures unresolved mention and resolved entity chip presentations on the live Timeline workbook grid. | Inspector resolution choreography remains functional browser evidence. |
| `V-4-GRID-02` | `implemented` | `apps/web/e2e/workbook.visual.spec.ts::V-4-GRID-02 captures Evidence access affordances on the required Evidence surface` | `browser_visual` | The visual harness captures Evidence surface access controls and blocked-access messaging through the ordinary workbook grid. | Opaque handle issuance and object access are owned by backend and frontend-unit evidence. |
| `V-4-GRID-03` | `implemented` | `apps/web/e2e/workbook.visual.spec.ts::V-4-GRID-03 captures Task Requests system view fields through the generic workbook grid` | `browser_visual` | The visual harness captures a required coordination system view rendered through the same generic workbook grid surface. | Detailed generic mutation behavior remains functional browser and component evidence. |

## Shared Harness Coverage

| Harness | Phase 4 evidence |
| --- | --- |
| Real runtime and route helpers | `internal/modules/workbook/testsupport/scenariotest` centralizes the Postgres + object-store runtime boot path, bootstrap-admin login helpers, inventory-driven route metadata, and authoritative replay/history conformance helpers for owned Phase 4 HTTP routes, while `internal/modules/records/testsupport/storetest` owns rollback-backed entity or mention seed helpers and contract-derived field-surface helpers for store rows. |
| Cross-cutting HTTP and projection helpers | `internal/testutil/httptestx` owns success or error envelope checks, replay scaffolding, authorization re-derivation assertions, mutation attribution checks, closed-vocabulary checks, field-key conformance, and projection determinism assertions shared across the Phase 4 backend suite. |
| Entity and Timeline substrate inspection | `internal/modules/records/testsupport/assertx`, `internal/modules/timeline/testsupport/asserttest`, and Phase 4 package-local lookup helpers inspect durable mention, link, change-set, projection, and observation state that the authoritative Phase 4 rows rely on. |
| Browser helper fixtures | `apps/web/src/testing/timelineWorkbookTestSupport.ts` and `apps/web/src/workbook/timeline/services/workbookShellPhase4.ts` provide the mocked workbook row, websocket, and mention helper scaffolding used by the support-only Phase 4 workbook tests. |

## Support-Only Evidence

- `internal/modules/entities/support_integration_test.go` consumes the centralized Phase 4 route-family inventory through `TestSurfaceEnvelope`, `..._CSRFProtection`, `..._ReplayAndDivergentConflict`, `..._AuthorizationReDerivation`, `..._DefaultQueryMetaAndFieldKeyConformance`, and `..._ProjectionAndWebsocketConsequences`. These tests are diagnostic smoke coverage for mention, merge, entity-origin, indicator, Timeline, object-blob, evidence-handle, Parties, assessment, coordination, and generic workbook routes; authoritative completion claims must live in the owned `I-4-*` rows.
- `apps/web/src/workbook/WorkbookShell.phase4.support.test.tsx` keeps mocked helper and component regression coverage for Phase 4 workbook chips, payload builders, inspector mention derivation, and auto-resolution notices. It remains support-only and is not completion evidence for Phase 4.
