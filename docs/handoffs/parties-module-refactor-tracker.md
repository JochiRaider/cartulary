# parties Module Refactoring Tracker and Handoff

## 1. Scope and Source Posture

- **Target path:** `internal/modules/parties`
- **Target label:** `parties`
- **Output path:** `docs/handoffs/parties-module-refactor-tracker.md`
- **Status:** Authorized implementation complete. S-00 through S-09, WF-08, and PT-001 through PT-015 are `DONE`; all four implementation blockers are resolved and the clean cutover is ready for review and handoff. The target existed with 24 files at the implementation baseline.
- **Allowed change in this session:** The user authorized serial execution of S-00 through S-09, including owner specifications, authored contracts, generated projections, migrations, runtime, tests, harness inputs, and this tracker.
- **Non-goals:** The explicit Section 4 and user-plan non-goals remain controlling: no Party merge, phone normalization, directory synchronization, new Party field, `custom_attrs`, frontend relocation, generic identity service, or compatibility layer.
- **Cutover posture:** The repository is pre-production. Every later authorized slice MUST update all callers and owner inputs atomically, MUST remove predecessor imports, exports, registrations, expectations, versions, and test-row identities in that slice, and MUST NOT add compatibility aliases, dual package paths, dual writers, dual registration, fallback readers, or a deprecation period.
- **Implementation authorization:** Granted by the user's 2026-08-23 implementation request. Each slice remains gated by its recorded dependencies and exit criteria.

The source hierarchy used for this tracker is:

1. Adopted subsystem NLSpecs within their named scopes.
2. Core 00 through Core 04 for implementation-conformance behavior.
3. Core 05 only for claim-bearing timed or fixture-sensitive publication; it is not applicable to this planning task.
4. `docs/domain.md` and implementation-support guides for vocabulary, navigation, package boundaries, and harness mechanics.
5. Current repository code and tests for implementation state.
6. Prior plans, handoffs, and the planning framework as evidence and method only.

No owner-to-owner contradiction was found. The mismatches recorded below are downstream implementation, test, machine-projection, or owner-completeness findings. This tracker MUST NOT resolve them by treating repository behavior, analysis notes, or planning prose as authority.

### 1.1 Requirement modality

The capitalized terms in this tracker have the following scope. They make the plan testable without elevating the tracker into an owner document.

| Marker | Meaning | Normative effect |
| --- | --- | --- |
| `CURRENT` | A fact established from live repository inspection. | Descriptive only; it does not establish required product behavior. |
| `ADOPTED` | A requirement already stated by an owner in the authority hierarchy above. | Controlling within that owner's scope. |
| `PROPOSED_OWNER_AMENDMENT` | Exact language or behavior that a later authorized owner change must adopt before implementation may depend on it. | Non-authoritative until adopted; affected slices remain `BLOCKED`. |
| `PLANNED_IMPLEMENTATION` | A future implementation, test, projection, migration, or harness obligation derived from adopted owners. | Requires later authorization and MUST NOT begin before its owner gates pass. |

`MUST`, `MUST NOT`, `SHOULD`, and `MAY` in a `PROPOSED_OWNER_AMENDMENT` or `PLANNED_IMPLEMENTATION` row govern the proposed plan only. They do not supersede Core, an adopted subsystem NLSpec, or an authored machine projection.

Owner and planning documents inspected:

- `docs/handoffs/cartulary_modular_refactor_planning_framework.md`, read first and used only as planning doctrine and template;
- `temp/analysis-notes.md`, used as design analysis and closure recommendations rather than as instruction or authority;
- `docs/research/nlspec-spec.md`, used only as a writing-quality reference for behavioral completeness, explicit defaults, mappings, and binary acceptance criteria;
- `docs/spec/00_document_set_status_and_precedence.md`;
- relevant Party, string-contract, view-schema, projection, import, portability, revision, collaboration, authorization, and acceptance sections of Core 01 through Core 04, including Core 02 REQ-02-060, REQ-02-061, REQ-02-063, and REQ-02-230;
- relevant source-boundary and Party sections of `docs/reporting-subsystem-nlspec.md` (`status: adopted/current`);
- relevant owner, row-selection, generation, and evidence-accounting sections of `docs/testing-harness-nlspec.md` (`status: adopted/current`);
- `docs/domain.md`, especially Party, Coordination, Workbook, projection, import, and module-boundary vocabulary.

Repository files inspected include every file listed in Section 2 and the following live callers and projections:

- application composition under `internal/app/workbookassembly`, `internal/app/importassembly`, `internal/app/incidentportabilityassembly`, `internal/app/projectionassembly`, `internal/app/revisionassembly`, and `internal/app/recoveryassembly`;
- generic Workbook routes under `internal/modules/workbook`, Collaboration WebSocket registration, Reporting materialization, Projections runtime/storage adapters, and `internal/testutil/appsupport/workbook.go`;
- Party-related frontend workflow/model files under `apps/web/src/workbook` and the shared view-contract surface;
- `db/migrations/00002_auth.sql`, `db/migrations/00009_parties.sql`, and the route-idempotency migration precedent in `db/migrations/00035_assessment_create_idempotency_v1.sql`;
- `internal/modules/imports/ownerfacade/owner_create.go`, `internal/modules/imports/owner_errors.go`, `contracts/imports/schemas.v1.json`, and the Party Imports adapter;
- `internal/modules/incidentbundles/sourceport/sourceport.go`, `contracts/incident-bundles/source_catalog.json`, and the current version-2 bundle grammar;
- `contracts/network-flow/timezone/tzdb-2026c.provenance.json`, inspected only as evidence that reproducible packaged tzdb provenance exists; Network Flow does not own Party timezone semantics;
- `tools/schema_object_ownership_manifest.json`, `tools/backend_module_boundaries.json`, and the Party view, import, projection, portability, revision, recovery, OpenAPI, protocol, verification, and test-family contract inputs.

Framework/repository mismatch: the framework describes a reusable modular-refactor method, but the live target is neither a single thin facade nor proof that `parties` is the correct permanent physical boundary. The code establishes a legitimate Party source owner while also colocating several supporting-owner adapters and cross-owner tests. This tracker adapts the framework to that live shape.

Additional live findings that control this revision are:

- `CURRENT`: the Party table has no `custom_attrs` column, the Party view declares no retention surface for it, and the Party import target rejects unmapped values. The proposed portable Party row therefore forbids `custom_attrs`; it does not add new Party state.
- `CURRENT`: the Party view-schema projection declares all six optional writable Party strings `clearable=false`, while the current owner text and nullable storage establish an incomplete or defective projection that requires owner clarification and regeneration.
- `CURRENT`: the Incident Bundle source-port grammar admits versions per path but has one global bundle version and one global source-catalog contract major. It cannot version only the Party family.
- `CURRENT`: the repository pins `tzdb-2026c` for Network Flow. The bytes and provenance are a reproducibility precedent only; a Party/Core-owned registry requires separate owner adoption and projection.

## 2. Current-State Repository Inventory

| Path | Current responsibility | Exported/public symbols or package surface | Inbound callers | Outbound dependencies | Tests touching it | Generated artifacts or contracts touched | Suspected target owner module | Risk level | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `internal/modules/parties/conflict_snapshot.go` | Maps writable Party view fields to the canonical Party revision snapshot for same-field conflict analysis. | No exported symbol; constructs a `RevisionSnapshotProjector` internally. | `NewMutationFacade` in `workbook_facade.go`; conflict-window construction. | `internal/modules/revisions/conflicts`. | Party patch/conflict coverage in Workbook and Party tests; no direct unit test. | Party view schema and `cartulary.revisions.snapshot.party.v1`. | Parties, consuming Revisions mechanics. | High | Field-map drift can change conflict detection without changing the view schema. |
| `internal/modules/parties/deleterestore/provider.go` | Supplies Party snapshots, view identity, and delete preflight; blocks deletion while active Evidence, Task, or communication-log references exist. | `ActiveIncomingPartyReferenceReason`, `Source`, `NewSource`, and `DeleteRestoreSource` methods. | `RevisionProviderContribution`; Revisions delete/restore runtime and tests. | `revisions/deleterestorecontract`, PostgreSQL queries over records, Evidence, Tasks, and active links. | `internal/modules/workbook/parties_integration_test.go`; `internal/modules/revisions/delete_restore_test.go`. | Revision provider registry/snapshot contract; backend boundary manifest. | Party source semantics behind Revisions. | High | Cross-owner reference rules are legitimate Party delete semantics but need explicit characterization before movement. |
| `internal/modules/parties/import_create.go` | Implements the Imports owner facade for Party create-or-reuse and finalizes the revision/collaboration outcome inside the caller transaction. | `ImportCreateCommand`, `NewImportCreateFacade`, `Store.CreateImportRowTx`. | `internal/app/importassembly/owner_registry.go` and its tests. | Imports `ownerfacade`, Records, Revisions, Party projection port, PostgreSQL. | Import assembly tests and Party exact-match tests. | `contracts/imports/view-targets.v1.json`, import owner registry, Party view schema. | Parties for source mutation; Imports for orchestration. | High | Reuses the same exact-match path as Workbook create and inherits RB-001 and RB-002. |
| `internal/modules/parties/import_projection.go` | Refreshes and loads a Party projection row after an import mutation. | `Store.RefreshImportRowTx`. | `CreateImportRowTx`. | Party `workbookprojection.Rows`. | Import assembly tests indirectly. | Projection provider descriptor and Party view schema. | Parties adapter to Projections. | Medium | Must not acquire ownership of projection storage or generic import lifecycle. |
| `internal/modules/parties/incident_bundle_portability.go` | Exports `data/parties.ndjson` and imports it into the authored `parties` table with attribution. | `ExportIncidentBundleFiles`, `ImportIncidentBundleFilesTx`. | `NewIncidentBundleSourcePort`. | `internal/modules/incidentportability`, PostgreSQL. | General Incident Bundle integration coverage; no Party-specific invariant suite found. | Incident Bundle source catalog and source-row format. | Parties source adapter to Incident Portability. | High | Fixed import requires only `record_id` and `incident_id`; declared invariants need direct evidence under RB-003. |
| `internal/modules/parties/incident_bundle_source_port.go` | Declares and adapts the Party Incident Bundle source port, dependencies, path, stable identity, and four invariants. | `NewIncidentBundleSourcePort`. | `internal/app/incidentportabilityassembly/catalog.go`. | `incidentbundles/sourceport`, Party portability helpers, direct validation SQL. | General Incident Bundle catalog/import tests. | `contracts/incident-bundles/source_catalog.json` and generated catalog projections. | Parties contribution; Incident Portability orchestration. | High | Validation visibly checks envelope/type scope; lifecycle and normalization enforcement are not fully demonstrated. |
| `internal/modules/parties/incident_bundle_subtype_presence.go` | Contributes Party subtype-presence bindings for portable record-envelope validation. | `IncidentBundleSubtypeContribution`; unexported source implements the subtype port. | Incident Portability assembly catalog. | Records `subtypepresence`, PostgreSQL. | Incident Bundle catalog/presence tests indirectly. | Incident Bundle subtype/source catalog. | Parties contribution to Records/Incident Portability. | Medium | Thin source-owned contribution; preserve its record type and ordering. |
| `internal/modules/parties/mutation_admission.go` | Admits closed create, patch, and conflict-resolution JSON into immutable owner commands carrying one normalized-value set and the named request hash. | `ConflictClaims`, `CreateAdmission`, `PatchAdmission`, `ConflictResolveAdmission`, `AdmitCreateJSON`, `AdmitPatchJSON`, `AdmitConflictResolveJSON`. | `internal/app/workbookassembly/party_adapters.go`; Party facade and unit tests. | Generated Party policy, private Party policy, platform RFC-8785 canonical JSON, `httpapi`. | Admission/hash/boundary tests; Workbook mutation and conflict tests. | `cartulary.parties.field_registry.v1`, `cartulary.timezone_name_registry.v1`, `cartulary.parties.mutation_request_hash.v1`. | Parties semantic admission; HTTP failure representation remains transport-adjacent until S-07. | High | Closed in S-03: no caller hash, host tzdb, generic line/note inference, or predecessor decoder remains. |
| `internal/modules/parties/mutation_admission_test.go` | Exercises strict request admission, exact field bounds/clears, timezone membership, normalization equivalence, canonical ordering, and three hash goldens. | Four `TestPartyMutation*` unit tests. | Current `module.parties` unit harness row; immutable row split remains S-05. | Public immutable Party admissions, UUID fixtures. | Self plus the generated normalization corpus and canonical-JSON vectors. | `tools/test_families/module.parties.json` behavior row. | Parties. | Medium | Causal assertions are complete for S-03; S-05 still owns immutable harness-accounting repair. |
| `internal/modules/parties/parties_test.go` | Exercises Party create/reuse, raw-text preservation, incident/deletion scope, and ambiguous matches using a service-backed harness. | `TestPartyExactMatchReuseAndRawTextPreservation_Unit`. | `module.parties` store harness row. | Party facade, app projection assembly, Revisions, Evidence fixtures, Postgres test support. | Self. | Party behavior verification row. | Parties. | High | Current ambiguous-match expectation creates another Party and conflicts with Core 02 REQ-02-063. |
| `internal/modules/parties/party_refs_test.go` | Tests direct Party references plus a Decision reference decoder and cross-owner Evidence/Task/Workbook behavior. | Three exported test functions. | One `module.parties.store` harness row. | Evidence, Tasks/Decisions, Workbook, Parties, platform auth/postgres, shared test support. | Self. | Party behavior and Incident Portability verification routing. | Split candidate: Evidence, Tasks/Decisions, Workbook, and Parties collaborators. | High | Filename/package ownership is not sufficient; the row's postconditions are mixed and portability verification is not exercised. |
| `internal/modules/parties/projectionprovider/contribution.go` | Constructs the Party projection contribution from the Party source reader. | `NewContribution`. | Projection assembly and projection test support. | Party `workbookprojection`. | Projection assembly catalog tests. | Projection provider index and backend boundary manifest. | Parties source-provider adapter. | Medium | Thin adapter; candidate for an internal provider boundary while retaining the typed contribution. |
| `internal/modules/parties/projectionprovider/source.go` | Reads active Party source facts for one-row refresh and bounded incident rebuild pages. | `Source`, `NewSource`, `LoadProjectionInputTx`, `ListProjectionInputsTx`. | Party projection contribution; Projections runtime via assembly. | Party projection DTO/port, PostgreSQL, `records` and `parties` tables. | Projection runtime/query tests and source-ownership guard. | Projection descriptor/index and schema ownership manifest. | Parties source reader; Projections owns projection storage. | High | Direct source SQL is legitimate here; pagination, active-record filtering, and null preservation are frozen behavior. |
| `internal/modules/parties/recovery_state.go` | Declares `parties` as an authoritative Recovery table for `module.parties`. | `RecoveryStateContribution`. | Recovery assembly state catalog. | Platform `recoverystate`. | Recovery catalog/contract checks indirectly. | Recovery authored table catalog and generated recovery projections. | Parties contribution to Recovery. | Medium | Thin source-owned declaration; generic Recovery mechanics must remain outside Parties. |
| `internal/modules/parties/reportingprovider/provider.go` | Reads active Party source rows into Reporting provider output with `parties` paths and `party:` disclosure partitions. | `CollectFieldsTx`, `CollectFactsTx`. | `internal/modules/reporting/export_materializer.go`. | Reporting `exportprovider`, PostgreSQL. | Reporting materializer/boundary tests indirectly; no Party-local test. | Reporting provider/materialization contracts and snapshot behavior. | Parties source adapter; Reporting owns export-model semantics. | High | Preserve active-only selection, logical record identity, provider key, content class, and disclosure partition prefix. |
| `internal/modules/parties/revision_append_port.go` | Defines a private narrow append port and adapts the concrete Revisions appender. | No exported symbols. | Party Store and mutation facade construction. | Revisions types and appender, PostgreSQL. | Party mutation/import tests indirectly. | Revision change-set, record-mutation, snapshot, and collaboration-intent contracts. | Parties-owned dependency port to Revisions. | Medium | Good narrowing direction, but facade constructors still expose the concrete Revisions appender. |
| `internal/modules/parties/revision_provider_contribution.go` | Registers the Party snapshot, delete/restore, rollback, and record-view routing contribution with Revisions. | `RevisionProviderContribution`. | Revision assembly. | Parties delete/restore and rollback providers; Revisions registry types. | Revision assembly and delete/restore tests. | Revision provider catalog and Party snapshot registry. | Parties source contribution to Revisions. | High | Contribution identity and snapshot schema are public coordination contracts. |
| `internal/modules/parties/rollbackprovider/party.go` | Validates retained Party source snapshots and restores present source fields without treating omission as clear. | `PartyProvider`, `NewPartyProvider`, `ValidateRollbackValue`, `RestoreTx`. | `RevisionProviderContribution`; Revisions rollback runtime. | Revisions `rollbackcontract`, PostgreSQL. | `rollbackprovider/party_test.go`; broader rollback-route coverage. | Party revision snapshot schema and rollback provider registry. | Parties source semantics behind Revisions. | High | Presence semantics, required-field validation, and timestamp behavior must remain exact. |
| `internal/modules/parties/rollbackprovider/party_test.go` | Verifies retained-value presence and required Party rollback validation. | `TestPartySourceForRollbackValuePreservesPresence`, `TestPartyProviderRejectsInvalidRequiredValues`. | Direct package test; no Party owner row found. | Rollback contract and Party provider. | Self. | Harness accounting is incomplete for this package. | Parties/Revisions collaborator. | Medium | Add or relocate owner routing based on the externally visible rollback postcondition. |
| `internal/modules/parties/store.go` | Owns Party source persistence, create validation, exact-match lookup, inserts, direct field patches, timestamps, and projection/revision dependencies. | `ViewSchemaID`, `Store`, `FieldValue`, `CreateParams`, `ValidationError`, `NewStore`, source mutation methods, and validators. | Workbook facade, Imports facade, direct Party tests, projection runtime query tests. | Records, Revisions, Party projection port, PostgreSQL; authored `parties` table. | Party exact-match tests plus Workbook/import/projection integration coverage. | Party view schema, import target, schema ownership manifest, revision snapshot. | Parties. | High | The source table is legitimately Party-owned; the root type combines repository and coordination concerns and contains RB-001. |
| `internal/modules/parties/workbook_conflict.go` | Resolves `keep_saved`, `use_unsaved`, and merged-value conflict outcomes with idempotency and Party patch execution. | `ConflictCommand`; `MutationFacade.ResolveConflict`. | Workbook assembly Party conflict provider. | Revisions conflict mechanics, platform auth idempotency, viewschema, Party facade. | Workbook conflict-route and Party admission tests. | OpenAPI conflict route, error envelope, conflict-token and revision contracts. | Parties semantic resolution behind Workbook routing. | High | Preserve token binding, replay, row version, status, and no-write `keep_saved` behavior. |
| `internal/modules/parties/workbook_facade.go` | Coordinates Party create/reuse/patch transactions, incident-open checks, Records envelopes, projections, revisions, conflict windows, idempotency, and mutation results. | `MutationFacade`, request/command/result/error/conflict types, `NewMutationFacade`, `Create`, `Patch`. | Workbook assembly, Party test support, Workbook/Assessment tests. | Incidents admission, Records, Revisions/conflicts, Party projection, platform auth/postgres, HTTP status constants. | Party store tests and broad Workbook mutation/conflict tests. | OpenAPI operations, protocol envelopes, Party view schema, revision/collaboration contracts. | Parties application facade, with transport/platform coupling to reduce. | Critical | This is the main behavior-preservation seam and the highest-risk clean-cutover point. |
| `internal/modules/parties/workbookprojection/contribution.go` | Defines the public typed Party projection facade, source facts, rows/rebuilder ports, descriptor, and view-surface intent. | `Rows`, `ProjectionInput`, `ProjectionInputPage`, `SourceReader`, `Rebuilder`, `Ports`, `Contribution`, `NewContribution`, `Descriptor`, `SurfaceIntent`, and methods. | Parties store/import/facade; Projection assembly, adapters, runtime, storage, and tests. | Projections `providercontract`, platform `viewschema`, PostgreSQL transaction type. | `workbookprojection/contribution_test.go`; projection runtime and assembly tests. | `contracts/projection-providers/index.json`; generated provider catalog. | Parties-owned typed provider facade; Projections-owned mechanics/storage. | High | Intentional cross-module API. Preserve or replace atomically; do not create a second DTO package. |
| `internal/modules/parties/workbookprojection/contribution_test.go` | Verifies source requirement, descriptor ownership, surface intent, defensive copies, and typed source retention. | Four exported test functions. | `module.projections` currently maps only the unknown-schema test. | Party projection contract and test stub. | Self. | Projection harness/catalog accounting. | Parties and Projections collaborator; row ownership requires review. | Medium | Several tests are not visibly represented by the current catalog row selection. |

No target file is out of scope.

## 3. Module Boundary Diagnosis

The current target is a legitimate Party source owner and application facade, a mutation coordinator, a view/projection participant, and a persistence-adjacent adapter. It is also a mixed-responsibility package because multiple supporting-owner adapters and cross-owner tests are exposed beside the root mutation surface. It is not evidence that every current subpackage or public symbol is a valid permanent boundary. It is not a frontend shell/controller package or a grid-vendor integration layer.

| Responsibility found | Current location | Correct owner candidate | Keep / move / split / defer | Evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| Party vocabulary, source fields, kinds, exact-match precedence, and source-table semantics | `store.go` and Core types embedded in the root package | Parties | keep | Core 01 Party view addendum; Core 02 Party model and exact-match rules; `public.parties` ownership manifest | Keep semantic ownership even if repository mechanics become private. |
| Workbook Party create, reuse, patch, and conflict application | `workbook_facade.go`, `workbook_conflict.go` | Parties application facade | split | Workbook assembly delegates Party mutations; target owns Party-specific source effects | Keep a deep facade; separate source repository and supporting ports behind it. |
| HTTP-shaped admission errors and status codes | `mutation_admission.go`, `workbook_facade.go`, `workbook_conflict.go` | Workbook transport/assembly for translation; Parties for semantic validation | split | Parties imports `httpapi` and `net/http`; Workbook already owns generic route envelopes and failure translation | Preserve error codes and statuses while moving translation, not behavior. |
| Authentication/idempotency store and incident-open checks inside mutation coordination | `workbook_facade.go` | Parties application facade through narrow ports; platform/auth owners retain mechanics | split | Facade constructs `authn.Store` and incident checker from `postgres.DB` | Route authorization correctly remains in Workbook; idempotency and open-state dependencies should be injected narrowly. |
| Party source SQL | `store.go`, delete/restore, rollback, projection source, portability/reporting providers | Parties repositories/providers | keep | Schema ownership assigns `public.parties` to Parties | Direct SQL is not itself a violation; hide it behind source-owner boundaries. |
| Party grid projection facts and contribution descriptor | `projectionprovider/**`, `workbookprojection/**` | Parties source/provider facade; Projections runtime and storage | split | Descriptor says source owner `parties`, storage owner `projections`; `public.party_grid_projection` is rebuildable Projection state | Preserve `workbookprojection` as the sole typed cross-module facade during a clean cutover. |
| Import create-or-reuse | `import_create.go`, `import_projection.go` | Parties target facade; Imports orchestration | keep | Import registry binds `module.parties@1`; create occurs in caller transaction | Reuse Party source rules and generic import finalization without owning import lifecycle. |
| Incident Bundle Party file, invariants, and subtype presence | `incident_bundle_*` | Parties source contribution; Incident Portability orchestration | split | Source descriptor, subtype contribution, and portability assembly registration | Provider placement may change; declared invariants must first be characterized. |
| Revision snapshot, delete/restore, conflict, and rollback source semantics | root conflict/revision files plus `deleterestore` and `rollbackprovider` | Parties source contribution; Revisions mechanics | split | Revision contribution identifies source owner `parties` and canonical snapshot ID | Internalize providers while preserving one typed contribution entry. |
| Recovery authoritative-table declaration | `recovery_state.go` | Parties contribution; Recovery orchestration | keep | Contribution names `module.parties` and table `parties` | Thin, legitimate source-owner registration. |
| Reporting Party source facts | `reportingprovider/provider.go` | Parties source adapter; Reporting export-model semantics | defer | Reporting materializer imports the provider; Reporting NLSpec owns export derivation after source admission | Decide the final adapter placement in WF-05; do not move reporting semantics into Parties. |
| Cross-owner direct-reference tests | `party_refs_test.go` | Owner of each externally visible postcondition, with collaborators | split | Test imports and assertions span Evidence, Tasks/Decisions, Workbook, and Parties | Harness ownership must follow the normative postcondition, not the filename. |
| Frontend Party-link creation and Workbook controller behavior | `apps/web/src/workbook/**` | Web/Workbook | keep | Live frontend consumers use generic view and mutation contracts | No frontend move is supported; only contract characterization is in scope. |
| Grid-vendor integration | `packages/grid-adapter` only | Grid adapter | keep | No direct Party-target or Party frontend import of the grid vendor was found | `intentional/no_action`. |

### 3.1 Required semantic boundaries for the later redesign

| Boundary | Required posture | Forbidden outcome | Authority gate |
| --- | --- | --- | --- |
| Party active identity | Parties owns exact-match meaning and derived claims. | A generic identity registry, an Entities dependency, or query collation defining Party identity. | Core 02 Party-specific claim amendment. |
| Party field admission | Parties owns the immutable field-to-contract binding and normalized semantic result; shared primitives implement adopted contracts only. | A platform helper inferring Party semantics from field names, columns, or UI labels. | Core 01 field/default clarification. |
| Workbook transport | Workbook owns route authentication, authorization, HTTP envelopes, and translation of typed Party failures. | Party source code retaining transport status or exposing storage failures. | Core 01 route/error amendment. |
| Imports | Imports owns unit orchestration and the existing outer failure envelope; Parties owns source admission and match failures. | Parser reinterpretation, row skip, fallback create, or a second Party normalizer. | Core 01 Imports mapping amendment. |
| Incident Bundles | Incident Bundles owns version selection, catalog validation, ordering, and publication; Parties owns its exact portable row and invariants. | Descriptor-derived SQL, generic conflict-ignore persistence, or Party code choosing bundle versions. | Core 01 Incident Portability and Core 04 amendments. |
| Claims and recovery | Party claims are derived, non-public, excluded from portable/authoritative backup state, and deterministically rebuildable. | Claim rows becoming Party records, workbook rows, portable source rows, or an independent authority. | Core 02 plus Recovery owner projection. |
| Projections | Parties provides typed source facts; Projections owns storage, refresh, rebuild, and query mechanics. | Parties owning `party_grid_projection` mechanics or Projections owning `public.parties`. | Existing owner split; no amendment required. |
| Supporting providers | Party-specific portability, reporting, revision, rollback, delete/restore, projection, and recovery adapters remain source-owner contributions behind the Party boundary. | Generic orchestration moving into Parties or external owners importing Party implementation packages. | WF-05 physical-topology review after semantic gates. |

The final directory layout of internal Party providers remains `defer`. The semantic interfaces in this section are closed; the source-tree realization is not an owner-level product contract.

## 4. Public Contract and Behavior Freeze Map

| Contract | Current owner | Evidence | Existing tests | Required characterization tests | Refactor risk | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `cartulary.view.parties.v1` identity, field set, field contracts, visibility, sort/filter fields, and default layout | Core 01 / view-schema owner; Parties supplies source semantics | `contracts/view-schemas/cartulary.view.parties.v1.json`, generated Go/TypeScript projections | Workbook surface and projection tests; frontend registry tests | Exact field-contract boundary matrix, null/omission behavior, default query/layout, and generated projection parity | Critical | Generated outputs are downstream and must never be hand-edited. |
| `POST /api/v1/incidents/{incident_id}/views/{view_schema_id}/query` for Parties | Workbook route; Projections query implementation | Workbook route and OpenAPI owner source; Party descriptor | Workbook Party and query tests | Party-specific pagination, sort/filter, deleted-row exclusion, saved-view replay, and authorization parity | High | Query requires incident membership; no Party-specific ACL exists. |
| `POST /api/v1/incidents/{incident_id}/views/{view_schema_id}/rows` for Parties | Workbook route plus Parties mutation facade | Workbook route, Party adapter, OpenAPI operation `createViewRow` | Party exact-match test, Workbook integration, browser sentinel | Fresh create, replay, unique reuse, ambiguous match, closed incident, role matrix, revision, projection, and event parity | Critical | Current ambiguous-match behavior is blocked by RB-001. |
| `PATCH /api/v1/records/{record_id}` for Party records | Workbook route plus Parties mutation facade | Workbook route, Party adapter, Party facade | Workbook mutation/conflict tests | Every writable field, omission/null/no-op, stale disjoint patch, same-field conflict, replay, and deleted/wrong-type target | Critical | Preserve 32-change bound, canonical hashing, row version, and error envelope. |
| `POST /api/v1/records/{record_id}/conflicts/{conflict_token}/resolve` | Workbook route plus Parties conflict semantics | Workbook conflict provider and Party conflict facade | Generic Workbook conflict tests | Party `keep_saved`, `use_unsaved`, `merged_value`, invalid token/field/record, replay, and side-effect counts | Critical | Keep token claims, snapshot field mapping, and no duplicate revisions/intents. |
| `DELETE /api/v1/records/{record_id}` for Party records | Revisions route; Parties delete preflight | Party delete/restore source and generic OpenAPI route | Workbook Party delete tests; Revisions matrix | Each Evidence, Task, audience, and attendee reference blocker; unreferenced delete; wrong incident/type | High | Authorization remains route-owned. |
| `POST /api/v1/records/{record_id}/restore` | Revisions route plus Party source contribution | Revision contribution and delete/restore source | Revisions delete/restore tests | Party snapshot restore, projection refresh, event/revision parity, idempotency, and authorization | High | Source row has no separate delete flag; Records envelope owns deletion. |
| `POST /api/v1/records/{record_id}/rollback` | Revisions route plus Party rollback provider | Party rollback provider and snapshot schema | Party provider unit tests; broader rollback tests | Presence-sensitive restoration for every field, required-value rejection, projection/event parity | High | Do not convert absent snapshot members into clears. |
| Import application through `module.parties@1` | Imports orchestration plus Parties create facade | Import target registry and import assembly owner registry | Import assembly tests | Create/reuse parity with Workbook, ambiguous key, field contracts, revision/intent, row result, cancellation/rollback | Critical | Uses caller transaction and must not own import lifecycle. |
| Incident Bundle `parties` source and `data/parties.ndjson` | Incident Portability orchestration plus Parties source port | Source descriptor, source catalog, subtype contribution | Generic bundle tests | Each declared invariant, strict shape/identity, lifecycle, normalization, attribution, and round-trip | Critical | RB-003 blocks safe behavior changes or provider movement. |
| Party projection refresh/rebuild/query | Projections storage/runtime plus Parties source/provider facade | Provider descriptor, source reader, schema ownership manifest | Projection contribution/runtime/query tests | Single refresh, incident rebuild paging, null preservation, deleted exclusion, restore rebuild, descriptor parity | High | `party_grid_projection` remains Projections-owned rebuildable state. |
| Revision change sets, snapshots, idempotency, and conflict windows | Revisions mechanics plus Parties mutation/source contributions | Party facade, revision port/contribution, snapshot ID | Workbook/Party/Revisions tests | Exact operation kind (`create`, `reuse`, `patch`), changed fields, row versions, replay, and rollback safety | Critical | Preserve one transaction and one durable outcome per accepted mutation. |
| `record_changed` over `GET /ws/v1/incidents/{incident_id}` | Collaboration | Revision append-and-intent path and Collaboration protocol | Workbook WebSocket tests and browser sentinel | Party create/reuse/patch/conflict/delete/restore/rollback event presence or intentional absence, payload, replay, and auth rederivation | High | Reuse and rejected/no-op operations require explicit event posture. |
| Reporting Party provider output | Reporting after source admission; Parties source adapter | `reportingprovider/provider.go`, Reporting materializer and NLSpec | Reporting integration/boundary tests | Active-row selection, field allowlist posture, logical identities, deterministic ordering, support refs, and `party:` partitions | High | Do not query live state after immutable export-model materialization. |
| Saved views and view-schema discovery for Parties | Workbook/view-schema owners | Party view schema and generic saved-view/startup routes | Workbook startup/saved-view tests | Party saved-view create/patch/query/default-layout and schema list/get parity | Medium | No saved-view implementation is in the target, but its schema is a consumer contract. |
| Frontend Party create/link and raw-text preservation | Web/Workbook with source owners for Party/Evidence/Task fields | Party link workflow, mutation command ports, Workbook model and policy files | Browser sentinel and frontend model tests | Selector/action continuity, create-from-text, exact raw text plus supplemental ID, error/conflict handling | High | No frontend ownership move is planned. |
| Harness/test accounting | Testing Harness mechanics; product postcondition owner per row | Party verification contract and test-family manifest | Four active Party rows | Correct postcondition owner/collaborators, direct portability evidence, rollback/projection test accounting, full-owner closure | High | Routing is evidence accounting and does not define runtime architecture. |

### 4.1 Closure requirement registry

These stable tracker IDs define the proposed closure. They are not substitutes for adopted Core requirement IDs.

| Requirement ID | Modality | Requirement | Required owner adoption | Execution gate |
| --- | --- | --- | --- | --- |
| `PTR-REQ-001` | `PLANNED_IMPLEMENTATION` | Every later slice MUST obey the authority and modality rules in Section 1.1. | None; tracker process rule. | All slices. |
| `PTR-REQ-002` | `PLANNED_IMPLEMENTATION` | Every later slice MUST use the clean-cutover posture and MUST leave no predecessor or compatibility path active. | None; user-authorized pre-production posture. | All slices. |
| `PTR-REQ-101` | `PROPOSED_OWNER_AMENDMENT` | Party matching MUST normalize and resolve every supplied exact-match key before precedence and MUST fail ambiguous or divergent results. | Core 02. | S-02. |
| `PTR-REQ-102` | `PROPOSED_OWNER_AMENDMENT` | Parties MUST own one derived active-claim relation with exact lifecycle and recovery semantics. | Core 02; Recovery projection follows. | S-02. |
| `PTR-REQ-103` | `PROPOSED_OWNER_AMENDMENT` | Exact replay MUST precede fresh Party-state evaluation; divergent replay MUST remain `client_txn_conflict`; reuse MUST NOT enrich source fields. | Core 01 and Core 02. | S-02, S-03. |
| `PTR-REQ-104` | `PROPOSED_OWNER_AMENDMENT` | Workbook and Imports MUST expose the closed safe Party-match failure mappings in Section 4.4. | Core 01; Core 04 acceptance. | S-02, S-07. |
| `PTR-REQ-201` | `PROPOSED_OWNER_AMENDMENT` | Parties MUST own one immutable field registry with the exact defaults, contracts, limits, equality, claim, and conflict mappings in Section 4.5. | Core 01; Core 02 identity reference. | S-03. |
| `PTR-REQ-202` | `PROPOSED_OWNER_AMENDMENT` | Every Party mutation and portability path MUST consume one normalized-value interface and MUST NOT use field-name or line-versus-note inference. | Core 01; Core 04 acceptance. | S-03, S-04. |
| `PTR-REQ-203` | `PROPOSED_OWNER_AMENDMENT` | `timezone_name_v1` MUST use the packaged registry policy in Section 4.7. | Core 01. | S-01, S-03. |
| `PTR-REQ-204` | `PROPOSED_OWNER_AMENDMENT` | Party mutation hashes and replay-state cutover MUST satisfy Section 4.8. | Core 01; Auth owns generic storage mechanics. | S-03. |
| `PTR-REQ-301` | `PROPOSED_OWNER_AMENDMENT` | Incident Bundle version 3, source-catalog contract major 2, and the exact Party row in Section 4.9 MUST be adopted atomically. | Core 01 Incident Portability; Core 04; source owners for coordinated version adoption. | S-04. |
| `PTR-REQ-302` | `PROPOSED_OWNER_AMENDMENT` | The Party source port MUST implement the exact prepare, apply, validate, failure, and atomicity contract in Section 4.10. | Core 01 and Core 04. | S-04. |
| `PTR-REQ-401` | `PLANNED_IMPLEMENTATION` | Test ownership MUST follow the normative postcondition and direct causal evidence, never the file or package location. | Existing Testing Harness NLSpec is sufficient. | S-05. |
| `PTR-REQ-402` | `PLANNED_IMPLEMENTATION` | New immutable rows MUST replace mixed or unaccounted rows with no alias, duplicate selector, double counting, or stale verification mapping. | Authored verification and test-family inputs. | S-05. |

### 4.2 Exact-match key contract

| Key kind | Accepted source field | Authoritative stored value | Equality and claim value | Case posture | Precedence |
| --- | --- | --- | --- | --- | --- |
| `primary_email` | `party.primary_email` | `email_address_v1` normalized value; letter case MAY be preserved. | Locale-independent case-insensitive form produced from the normalized value. | Case-insensitive. | First. |
| `external_ref` | `party.external_ref` | `locator_text_v1` normalized value. | Exact Unicode code-point sequence of the stored normalized value. | Case-sensitive; query filtering does not redefine identity. | Second. |

For create, explicit create-from-text, patch, restore, rollback, Imports, Incident Bundle import, and recovery, the later adopted algorithm MUST execute in this order:

1. Complete field admission for every supplied field.
2. Return an exact committed replay before reading current Party or claim state.
3. Reject a divergent replay as `client_txn_conflict` before current-state evaluation.
4. Normalize every supplied non-null exact-match field.
5. Restrict claim resolution to active Parties in the same incident.
6. Resolve all supplied keys before applying precedence.
7. If one key resolves to multiple active Parties, fail with `ambiguous_exact_match`.
8. If supplied keys resolve to different Parties, fail with `cross_key_exact_match`.
9. If all non-empty matches converge on one Party, reuse that Party. Precedence selects only the internal `matched_by` value.
10. If no key matches, acquire all proposed claims and then create the Party.
11. A uniqueness race or an existing Party acquiring another Party's key fails with `exact_match_key_claimed`.
12. On any failure, commit no Party, Records envelope change, claim, revision, projection refresh, collaboration event, idempotent success, or Imports outcome.

The internal result MAY carry `matched_by=primary_email`, `matched_by=external_ref`, or `matched_by=null`. It MUST NOT add a new public Workbook or Imports member without a separately adopted public contract. Reuse retains the current `reuse` revision posture and MUST NOT overwrite or enrich the existing Party.

### 4.3 Party-owned active-claim contract

The logical identity is exactly:

`(incident_id, party_key_kind, normalized_value) -> party_record_id`

| Rule | Required behavior |
| --- | --- |
| Key vocabulary | `party_key_kind` is exactly `primary_email` or `external_ref`. |
| Presence | A claim exists if and only if the Party is active and its corresponding field is non-null. |
| Uniqueness | One claim tuple maps to exactly one Party. Multiple canonical copies on one Party remain one logical claim. |
| Locking | Old, current, and proposed tuples are locked in bytewise ascending order over the complete tuple. |
| Clear | Clearing a field releases its claim in the same transaction. |
| Delete | Soft deletion releases both claims in the same transaction. |
| Restore and rollback | The operation reacquires the exact resulting claim set or fails atomically with `exact_match_key_claimed`. |
| Imports and Incident Bundles | Both consume the same Party semantics; neither owns or reinterprets claims. |
| Recovery | Claim bytes are not restored. Recovery rebuilds deterministically from authoritative Records and Party rows, validates exact set equality, and blocks readiness on duplicates or drift. |
| Publication | Claims are not records, workbook rows, links, public resources, Party portable rows, or authoritative backup members. |
| Migration | Preflight reports conflicting record IDs, field keys, and safe reasons to operators, blocks the migration, and never selects, merges, deletes, or edits a winner. |

### 4.4 Failure-interface mapping

#### Workbook mapping

| Member | Exact value or rule |
| --- | --- |
| HTTP status | `409` |
| `error.code` | `party_match_conflict` |
| `error.message` | Stable non-value-bearing Party-match conflict message selected by Workbook. |
| `error.retryable` | `false` |
| `error.details.reason_code` | Exactly `ambiguous_exact_match`, `cross_key_exact_match`, or `exact_match_key_claimed`. |
| `error.details.conflicting_field_keys` | Non-empty, sorted, duplicate-free subset of `party.primary_email` and `party.external_ref`. |
| Forbidden output | Party IDs, names, emails, external references, SQL, constraint names, relation names, claim-table details, driver text, and storage topology. |

#### Imports mapping

| Layer | Exact value or rule |
| --- | --- |
| Unit `error_code` | Existing `import_apply_blocked`. |
| Unit `reason_code` | Existing `owner_create_validation_failed`. |
| Owner code | Existing `owner_create_validation_failed`. |
| Owner retryability | `false`. |
| Owner safe reason | `party_match_conflict`. |
| Owner Party reason | One closed Workbook Party reason from the preceding table. |
| Owner field keys | The same sorted, duplicate-free safe field-key set. |
| Forbidden behavior | Parser classification, silent skip, fallback create, generic internal substitution for a recognized Party conflict, or exposure of a source value. |

### 4.5 Immutable Party field registry

| Field key | Required/default posture | Value contract and bound | Stored/equality posture | Claim role | Conflict class |
| --- | --- | --- | --- | --- | --- |
| `party.display_name` | Required; no default; `null` or normalized-empty is invalid. | `display_name_line_v1`; maximum 256 Unicode scalar values. | Contract-normalized storage and equality. | None. | `text_compare_merge` |
| `party.party_kind` | Required; no default. | Exact token `person`, `team`, `organization`, `distribution_list`, or `other`; no trimming or case folding. | Exact token. | None. | `atomic_replace` |
| `party.organization_name` | Optional; create omission defaults to `null`; explicit `null` or normalized-empty clears; patch omission is unchanged. | `display_name_line_v1`; maximum 256. | Contract-normalized storage and equality. | None. | `text_compare_merge` |
| `party.role_title` | Optional; create omission defaults to `null`; explicit `null` or normalized-empty clears; patch omission is unchanged. | `display_name_line_v1`; maximum 256. | Contract-normalized storage and equality. | None. | `text_compare_merge` |
| `party.primary_email` | Optional; create omission defaults to `null`; explicit `null` or normalized-empty clears; patch omission is unchanged. | `email_address_v1`; maximum 320. | Storage MAY preserve case; equality and claim comparison are case-insensitive. | `primary_email` | `atomic_replace` |
| `party.timezone_name` | Optional; create omission defaults to `null`; explicit `null` or normalized-empty clears; patch omission is unchanged. | Revised `timezone_name_v1`; maximum 128. | Preserve the exact admitted registry identifier. | None. | `atomic_replace` |
| `party.external_ref` | Optional; create omission defaults to `null`; explicit `null` or normalized-empty clears; patch omission is unchanged. | `locator_text_v1`; maximum 1024. | Exact code-point equality after normalization. | `external_ref` | `atomic_replace` |
| `party.notes` | Optional; create omission defaults to `null`; explicit `null` or normalized-empty clears; patch omission is unchanged. | `multiline_body_v1`; maximum 16384. | NFC, outer trim, and `CRLF`/`CR` to `LF`; preserve admitted interior whitespace and line breaks. | None. | `text_compare_merge` |

The authored Party view-schema projection MUST set `clearable=true` for the six optional fields after Core 01 adopts this explicit Party binding. `party.display_name` and `party.party_kind` remain non-clearable.

### 4.6 Normalized-value interface and consumers

One Party-owned admission operation MUST return the semantic equivalent of:

| Result member | Meaning |
| --- | --- |
| `stored_value` | Authoritative value or `null` written to Party source state. |
| `equality_value` | Value used for no-op detection, revision changed-field calculation, and semantic replay equality. |
| `exact_match_claim_value` | Normalized Party claim value, or absent for a non-identity field. |
| `canonical_hash_value` | JSON-domain value supplied to the named Party request-hash algorithm. |

Workbook create/patch/conflict admission, Imports owner create, source persistence, no-op detection, revisions, same-field conflicts, Party claims, request hashing, and Incident Bundle normalization validation MUST consume the same registry result. A generic platform normalizer MUST NOT infer a contract from a Party field name, database column, visible label, or whether a value contains a newline.

### 4.7 Timezone registry policy

`cartulary.timezone_name_registry.v1` is the proposed exhaustive machine projection. Its initial reviewed snapshot ID is `iana.tzdb.2026c`.

| Rule | Required behavior |
| --- | --- |
| Admission | A non-null value is valid only when its exact, case-sensitive normalized identifier is a member of the packaged registry. The registry, not runtime library inference, is exhaustive. |
| Normalization | Apply NFC and trim outer Unicode whitespace; preserve admitted interior characters and identifier spelling exactly. |
| Zone and Link posture | The registry MAY contain reviewed IANA Zone and Link spellings. An admitted Link remains that spelling and is not rewritten to its target. |
| Explicit exclusions | Abbreviation-only identifiers, numeric offsets, POSIX expressions, filesystem paths, malformed traversal-like values, case variants, and every non-member are invalid. |
| Runtime inputs | No host tzdb, locale, network lookup, latest selector, or Network Flow runtime package is authoritative. |
| Row shape | Party rows do not store the registry snapshot ID. |
| Update rule | A later registry revision MUST retain all previously admitted identifiers unless a separately adopted migration contract disposes them. |

The existing Network Flow `tzdb-2026c` provenance is a packaging precedent only. The later owner amendment MUST assign the new registry to the Core/view-contract projection boundary and MUST NOT make Parties depend on Network Flow.

### 4.8 Mutation request-hash contract

The proposed algorithm ID is `cartulary.parties.mutation_request_hash.v1`. It produces the raw 32-byte SHA-256 digest of RFC-8785 canonical JSON over these closed preimages:

| Operation | Required preimage members | Normalization/default rules |
| --- | --- | --- |
| `workbook.rows.create` | `algorithm_id`, `operation_id`, `view_schema_id`, and `fields` containing all eight Party field keys. | Required fields carry admitted values. Each omitted, explicit-null, or normalized-empty optional field is canonical `null`. `client_txn_id`, actor, and incident scope are lookup-key members and are excluded. |
| `workbook.records.patch` | `algorithm_id`, `operation_id`, `view_schema_id`, `base_row_version`, and `changes`. | `changes` is non-empty and sorted by `field_key`; omission means unchanged and is absent, while a clear is represented as `value=null`. Record scope and `client_txn_id` are excluded. |
| `workbook.records.conflicts.resolve` | `algorithm_id`, `operation_id`, `record_id`, `view_schema_id`, `field_key`, `current_row_version`, `conflict_token`, `resolution_kind`, and `resolved_value`. | `resolved_value` is canonical `null` when the resolution does not carry a value. `client_txn_id` is excluded. |

Object members are canonicalized by the named algorithm. Email case variants, equivalent NFC inputs, and equivalent notes line endings MUST hash equally after admission. External-reference case variants MUST hash differently. Invalid inputs never enter the hash domain.

Because existing Party hashes have no owner-adopted algorithm identity and cannot be recomputed from stored responses, the clean cutover MUST delete only deterministically identified Party replay rows:

- create rows whose route is `workbook.rows.create` and whose scope/view identifies `cartulary.view.parties.v1`;
- patch and conflict-resolution rows whose route is `workbook.records.patch` or `workbook.records.conflicts.resolve` and whose scope resolves to a Party record.

The migration MUST leave every unrelated owner's replay row unchanged. A pre-cutover Party replay is not replayable after the cutover. No old hasher, dual comparison, fallback, or compatibility reader remains.

### 4.9 Incident Bundle version and Party row contract

`PROPOSED_OWNER_AMENDMENT`: the live grammar cannot version one family independently. The closure therefore requires one coordinated clean cutover:

| Contract item | Exact post-cutover value |
| --- | --- |
| Sole admitted bundle version | Numeric `3`. |
| Retired version behavior | Version `2` and every other integer fail as `unsupported_bundle_version`; malformed or non-integer versions retain malformed-manifest classification. |
| Source catalog schema | `cartulary.incident_bundle_source_catalog.v4`. |
| Source catalog contract major | `2`. |
| Source-port contract major | `2`. |
| Path versions | Every required source and special-consumer path uses only `[3]`. |
| Party row schema path | `contracts/incident-bundles/parties.row.v1.schema.json`. |
| Party row schema ID | `cartulary.incident_bundle.parties.row.v1`. |
| Compatibility | No version-2 reader, translator, converter, registry, feature flag, or dual decoder. |

Each nonblank line of `data/parties.ndjson` MUST be one closed JSON object with exactly these ten required members:

| Member | Required portable value |
| --- | --- |
| `record_id` | Canonical lowercase hyphenated UUID; stable and unique in the Party path. |
| `incident_id` | Canonical lowercase hyphenated UUID equal to the immutable import context. |
| `display_name` | Non-null canonical `display_name_line_v1` value. |
| `party_kind` | One exact admitted Party-kind token. |
| `organization_name` | Canonical admitted string or explicit JSON `null`. |
| `role_title` | Canonical admitted string or explicit JSON `null`. |
| `primary_email` | Canonical admitted `email_address_v1` stored value or explicit JSON `null`. |
| `timezone_name` | Exact admitted timezone-registry identifier or explicit JSON `null`. |
| `external_ref` | Canonical `locator_text_v1` value or explicit JSON `null`. |
| `notes` | Canonical `multiline_body_v1` value or explicit JSON `null`. |

Omitted optional members, aliases, unknown members, duplicate object members, wrong types, noncanonical values, blank rows, and trailing content are invalid. `custom_attrs`, row version, timestamps, actors, deletion tuples, idempotency values, claim values, projection values, and every other member are forbidden. Records and Actors remain the sole envelope and attribution authorities.

The closed Party invariant precedence is:

| Order | Invariant ID | Exact responsibility |
| --- | --- | --- |
| 1 | `parties.source_identity_admitted` | `record_id` and `incident_id` are present, canonical, in scope, and unique. Missing or duplicate stable identity selects this invariant before other row defects. |
| 2 | `parties.version_shape_exact` | The row has exactly the version-3 member set, types, nullability, line framing, and closed JSON shape. |
| 3 | `parties.envelope_type_scope` | Exactly one same-incident Records envelope exists with `record_type=party`; no orphan, wrong type, duplicate subtype, or cross-incident binding exists. |
| 4 | `parties.identity_lifecycle` | Required Party fields and lifecycle are legal, and active Party claim state is exact. |
| 5 | `parties.normalization_exact` | Every string equals its owner-defined stored representation, including email, timezone, external-reference, and notes rules. |

The pre-production default is that retained version-2 bundles are disposable. If contrary operational evidence appears, implementation MUST stop and add a blocker; it MUST NOT invent compatibility in an implementation slice.

### 4.10 Party source-port behavior

| Phase | Required behavior | Forbidden behavior |
| --- | --- | --- |
| Export | Read only authoritative Party source state; emit `record_id` ascending; emit canonical UTF-8 JSON and exactly one LF after each row; use explicit nulls; produce an empty file for zero rows. | Projection-derived identity, timestamps or actors in Party rows, ambient normalization, unordered output, or `custom_attrs`. |
| Prepare import | Decode bounded closed rows, reject duplicate members and trailing content, validate canonical identity and every field, and return an opaque value bound to the port and operation. Perform no database or publication mutation. | Generic map-to-row persistence, silent normalization/repair, attribution mutation, or visible state. |
| Apply import transaction | Accept only the matching prepared value, use fixed Party-owned SQL or SQLC, write only Party authoritative state and derived claims, and require exact affected-row equality. | Descriptor-derived identifiers or SQL, generic `ON CONFLICT DO NOTHING`, value-dependent table selection, or partial success. |
| Validate import transaction | Compare admitted rows with transaction state and prove exact row equality, envelope scope/type, lifecycle, claim set, attribution boundary, row completeness, and all five invariants before publication. | Selecting an invariant from database text, defaulting to the first invariant, or treating generic bundle success as Party proof. |
| Failure | Use `incident_bundle_import_rejected`, `reason_code=source_family_invalid`, `retryable=false`, `source_family_id=parties`, and the selected declared invariant. | Imported values, Party identity, SQL, relations, paths, storage references, credentials, actor descriptions, or topology in public/durable diagnostics. |
| Atomicity | Failure or cancellation leaves no visible imported incident, Party, claim, membership, audit success, projection row, attribution, terminal success, or final object reference beyond the already adopted non-visible job/staging posture. | Partial publication or a compensating-success path. |

### 4.11 Binary acceptance matrices

#### Exact-match acceptance

| Acceptance ID | Scenario | Required result |
| --- | --- | --- |
| `PTR-AC-101` | Unique normalized email match. | Reuse the active same-incident Party; internal `matched_by=primary_email`; no enrichment. |
| `PTR-AC-102` | Email differs only by case. | Same result as `PTR-AC-101`. |
| `PTR-AC-103` | Unique external-reference match. | Reuse with internal `matched_by=external_ref`; no enrichment. |
| `PTR-AC-104` | External reference differs only by case. | Treat as a distinct unclaimed key; create only if every supplied key is unclaimed. |
| `PTR-AC-105` | Both keys resolve to the same Party. | Reuse once; `matched_by=primary_email`. |
| `PTR-AC-106` | Keys resolve to different Parties. | `cross_key_exact_match`; no durable effects. |
| `PTR-AC-107` | One email key resolves to multiple active Parties. | `ambiguous_exact_match`; no durable effects. |
| `PTR-AC-108` | One external-reference key resolves to multiple active Parties. | `ambiguous_exact_match`; no durable effects. |
| `PTR-AC-109` | Matching value exists only in another incident. | It is not a match. |
| `PTR-AC-110` | Matching value exists only on a deleted Party. | It is not a match. |
| `PTR-AC-111` | Patch retains the target Party's own key. | No claim conflict; ordinary no-op/change rules apply. |
| `PTR-AC-112` | Patch acquires another active Party's key. | `exact_match_key_claimed`; no durable effects. |
| `PTR-AC-113` | A key is cleared and later used by another Party. | Clear and release are atomic; later acquisition succeeds. |
| `PTR-AC-114` | Concurrent same-key creates. | Exactly one Party is created; the concurrent peer reuses that Party; no duplicate active claim or recognized conflict is published. |
| `PTR-AC-115` | Restore collides with an active claim. | Restore fails atomically with `exact_match_key_claimed`. |
| `PTR-AC-116` | Rollback would restore a claimed key. | Rollback fails atomically with `exact_match_key_claimed`. |
| `PTR-AC-117` | Exact replay after Party state changes. | Return the original committed result before fresh claim evaluation. |
| `PTR-AC-118` | Same idempotency key, different normalized request. | `client_txn_conflict`. |
| `PTR-AC-119` | Equivalent Workbook and Imports inputs. | Same normalized Party outcome; Imports retains its outer envelope. |
| `PTR-AC-120` | Every recognized rejection. | Zero Party, envelope, claim, revision, projection, event, success-idempotency, and Imports effects. |

#### Field-contract acceptance

Every one of the eight fields MUST cover wrong JSON type, create omission, patch omission, explicit null, normalized-empty input, NFC equivalence, outer whitespace, interior whitespace policy, allowed/forbidden controls, exact maximum, maximum plus one, stored representation, equality/no-op behavior, hash equality or inequality, conflict class, Workbook/Imports parity, and zero side effects on rejection.

| Acceptance ID | Field-specific cases |
| --- | --- |
| `PTR-AC-201` | `display_name`: required/no-default, NFC, controls, 256/257, and clear rejection. |
| `PTR-AC-202` | `party_kind`: all five tokens, unknown token, case variant, whitespace variant, null, and omission. |
| `PTR-AC-203` | `organization_name` and `role_title`: optional defaults/clears, NFC, and 256/257. |
| `PTR-AC-204` | `primary_email`: zero/multiple `@`, empty component, interior whitespace, case equality, claim equality, and 320/321. |
| `PTR-AC-205` | `timezone_name`: registered Zone, registered Link, unknown, case variant, abbreviation, offset, POSIX/path-like input, 128/129, and packaged-snapshot reproducibility. |
| `PTR-AC-206` | `external_ref`: trim/NFC, case inequality, claim collision, and 1024/1025. |
| `PTR-AC-207` | `notes`: CRLF/CR/LF equality, allowed tab/LF, forbidden controls, internal blank lines, and 16384/16385. |
| `PTR-AC-208` | Registry parity: Workbook, Imports, revision/no-op/conflict, claim, hash, and portability consumers produce the same normalized forms. |

#### Incident Bundle and harness acceptance

| Acceptance ID | Required proof |
| --- | --- |
| `PTR-AC-301` | Empty Party set; every Party kind; null and populated optionals; deterministic ordering; import/re-export semantic byte equality. |
| `PTR-AC-302` | Missing, extra, duplicate, aliased, wrong-type, noncanonical, omitted-optional, blank-line, trailing-content, and duplicate-identity cases select the required shape/identity invariant. |
| `PTR-AC-303` | Missing, wrong-type, duplicate-subtype, cross-incident, and reverse-orphan Records-envelope cases select `parties.envelope_type_scope`. |
| `PTR-AC-304` | Required-field, kind, active/deleted lifecycle, duplicate claim, cross-key claim, missing claim, and deleted-claim cases select `parties.identity_lifecycle`. |
| `PTR-AC-305` | NFC, outer whitespace, email storage/comparison, timezone admission, external reference, and notes line-ending defects select `parties.normalization_exact`. |
| `PTR-AC-306` | Affected-row mismatch, attribution failure, cancellation at every phase, fixed-SQL failure, and publication failure leave no prohibited partial state. |
| `PTR-AC-307` | Archive-member and source-row permutations do not alter selected invariant, public reason, or deterministic export. Public and retained diagnostics contain only allowed stable identifiers. |
| `PTR-AC-308` | Version 3 succeeds; version 2 has no successful reader or compatibility path and fails as unsupported. |
| `PTR-AC-401` | Every assertion in the mixed reference row has exactly one semantic owner and destination selector. |
| `PTR-AC-402` | Direct Party portability evidence alone carries the Party portability verification and fails when a Party invariant sentinel is broken. |
| `PTR-AC-403` | Rollback-provider and Party projection-source tests are accounted for under the owner of their normative postcondition. |
| `PTR-AC-404` | Generated owner explanation, target plan, row results, and retained-evidence audit show one result per active row, no predecessor result, no alias, no duplicate selector, and no stale mapping. |

## 5. Coupling and Boundary Findings

| Finding | Evidence | Risk | Classification | Proposed owner | Required planning action |
| --- | --- | --- | --- | --- | --- |
| Ambiguous exact-match values create another active Party instead of failing as a merge/conflict case. | Core 02 REQ-02-063 versus `findUniqueReusablePartyByFieldTx` behavior and `TestPartyExactMatchReuseAndRawTextPreservation_Unit`. | Duplicate active semantic identity and nonconformance. | `must_fix` | Parties | Characterize the owner-required failure, define its stable error mapping, and implement only in a separately authorized behavior-change slice. |
| Party admission dispatches only multiline versus generic line normalization. | `decodePartyValue`; Core 01 REQ-01-487..498 and REQ-01-501; Party view schema bindings. | Invalid email/timezone/length/control input may be accepted, and canonical hashes may diverge. | `must_fix` | Parties semantic admission, using owner-defined platform primitives where appropriate | Build a field-by-contract test matrix before changing decoding or replay hashes. |
| Party Incident Bundle declares four invariants but visibly validates only stable identity preparation and envelope/type scope. | `NewIncidentBundleSourcePort`, portability helper behavior, and absence of Party-specific invariant tests. | Malformed or noncanonical source state may cross a portability boundary. | `must_fix` | Parties for source invariants; Incident Portability for orchestration | Characterize every declared invariant and correct each missing enforcement path under later authorization. |
| Optional Party string fields are projected as non-clearable. | Core 01 optional-string/default rules, nullable Party columns, and `contracts/view-schemas/cartulary.view.parties.v1.json` with `clearable=false`. | Workbook and Imports cannot express the proposed authoritative clear semantics consistently. | `must_fix` | Core 01 Party binding; view-schema projection follows | Adopt the exact Section 4.5 defaults, set only the six optional fields clearable, regenerate, and prove parity. |
| Party mutation hashes have deterministic current bytes but no adopted algorithm identity or complete semantic-normalization contract. | `CreateRequestHash`, `PatchRequestHash`, `ConflictResolveRequestHash`, generic route-idempotency storage, and narrow existing unit coverage. | A field correction could silently alter replay equivalence or invalidate committed replay state. | `must_fix` | Core 01 Party route semantics; Auth retains generic replay storage | Adopt `cartulary.parties.mutation_request_hash.v1`, preflight and remove only Party replay rows, and prohibit a dual hasher. |
| `timezone_name_v1` depends on an ambient runtime database and does not close Zone/Link or alias behavior. | Core 01 REQ-01-498 and the absence of a Party/Core timezone registry; Network Flow alone pins `tzdb-2026c`. | Host-image drift can change Party admission and portable validation. | `must_fix` | Core 01 and its machine projection | Adopt the packaged registry in Section 4.7; do not import Network Flow ownership. |
| The Party portable shape is derived with `to_jsonb(parties)` and generic record population rather than a closed Party row schema. | `incident_bundle_portability.go`, Party table shape, and source catalog path without a Party `schema_id`. | Table changes can change bundle bytes, duplicate envelope state, or admit unknown members without a version change. | `must_fix` | Core 01 Incident Portability and Parties source contribution | Adopt the exact ten-member version-3 row and coordinated catalog-major cutover before changing the port. |
| The source catalog cannot version the Party family independently. | Global `bundle_version`, global source-port `ContractMajor`, and path `Versions` in the live catalog grammar. | Adding `parties.version_shape_exact` under unchanged version identity would make old and new bytes indistinguishable. | `must_fix` | Incident Portability owner with every source-owner contribution | Use the coordinated version-3/catalog-major-2 clean cutover in Section 4.9; version 2 becomes unsupported. |
| The root mutation facade constructs concrete platform stores/checkers from `postgres.DB`. | `NewMutationFacade` and `MutationFacade` fields. | Difficult isolation and shallow boundaries; higher clean-cutover risk. | `should_fix` | Parties application facade over narrow borrowed ports | WF-05 must define the minimum transaction/idempotency/open-state ports before movement. |
| Party admission returns `httpapi.APIError` and mutation results carry HTTP status codes. | `mutation_admission.go`, `workbook_facade.go`, `workbook_conflict.go`; Workbook already translates owner failures. | Transport vocabulary leaks into owner semantics. | `should_fix` | Workbook assembly translates; Parties owns typed semantic failures/results | Move translation in one clean-cutover slice while freezing wire envelopes. |
| Party source, provider adapters, and public contribution surfaces are colocated at several visibility levels. | Root files plus `projectionprovider`, `workbookprojection`, `reportingprovider`, `deleterestore`, and `rollbackprovider`. | Future callers may bind to implementation packages and widen the module surface. | `should_fix` | Parties deep module boundary | Decide one facade and internal provider topology in WF-05; do not add aliases. |
| Direct SQL appears across the Party store and source-owner providers. | Store, delete/restore, rollback, projection, reporting, and portability files. | SQL semantics can drift across adapters. | `should_fix` | Parties source repositories/providers | Consolidate only where semantics are identical; do not create a generic transaction facade or move Party schema meaning to platform. |
| `party_refs_test.go` mixes Party and non-Party postconditions. | Imports and three test bodies span Evidence, Tasks/Decisions, Workbook, and Parties. | Incorrect ownership, duplicated coverage, and misleading refactor safety. | `should_fix` | Owner of each externally visible postcondition | Split tests and allocate new immutable row IDs with a migration crosswalk where ownership changes. |
| The Party Incident Portability verification points at direct-reference tests. | `module.parties.verification.incident_portability` is attached to the mixed reference row. | Harness can report routed evidence that does not assert the named behavior. | `must_fix` | Parties product owner plus Testing Harness mechanics | Add actual portability tests and move the verification reference; generated harness outputs follow authored inputs. |
| Some rollback and projection contribution tests are not visibly covered by Party-owned rows. | Current Party and Projections family manifests. | Full-owner evidence may miss target subpackage behavior. | `should_fix` | Normative postcondition owner with collaborators | Audit exact selectors and add/move catalog rows without inferring ownership from paths. |
| Workbook performs authentication and incident-role checks before Party dispatch. | `routes_query.go`, `routes_mutation.go`. | Moving checks into Parties would duplicate or alter authorization. | `intentional/no_action` | Workbook/Incidents authorization | Freeze query membership and mutation `editor|reviewer|admin` behavior; Parties keeps only incident-open source admission. |
| `public.parties` is Party-owned while `public.party_grid_projection` is Projections-owned. | Schema object ownership manifest and provider descriptor. | Blurring the distinction would transfer source or storage authority. | `intentional/no_action` | Parties and Projections respectively | Preserve the source/projection split and typed `workbookprojection.Rows` facade. |
| No Party code directly imports the grid vendor outside the grid adapter. | Repository import search. | None currently. | `intentional/no_action` | Grid adapter / Web | Keep a boundary guard; no grid-related slice is justified. |
| Exact internal provider directory topology is not established by owners. | Owners define semantics and ports, not the final source tree. | Premature movement could create another accidental boundary. | `defer` | WF-05 redesign authority | Select the topology in the later authorized redesign, using current module patterns and a clean cutover. |

## 6. Refactor Workstreams

| Workflow ID | Name | Class: root/chain/parallel | Required previous workflows | Required subsequent workflows | Goal | Files likely involved | Validation | Handoff checkpoint |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| WF-00 | Session/source bootstrap and tracker initialization | root | None | WF-01 | Establish authority, live-repository posture, safe label/path, clean-cutover preference, and planning-only boundary. | This tracker; owner documents | Read-only status, framework-first inspection, `make task-guide ROLE=module-author OWNER=module.parties` | `DONE`: target and authority confirmed; tracker initialized. |
| WF-01 | Target inventory | chain | WF-00 | WF-02, WF-04 | Account for all 24 target files, callers, dependencies, tests, and machine projections. | `internal/modules/parties/**`; app callers; contracts and harness manifests | File inventory, import/caller searches, exact source inspection | `DONE`: Section 2 is exhaustive. |
| WF-02 | Contract-owner mapping | chain | WF-01 | WF-03, WF-04 | Map every observable Party-adjacent contract to its normative and implementation owner. | Core/Subsystem owner docs; Party view/import/projection/revision/portability contracts | Human owner review against Section 4 | `DONE`: no owner contradiction found; downstream mismatches identified. |
| WF-03 | Characterization test gap analysis | parallel | WF-02 | WF-05, WF-06 | Define binary current-state and owner-required evidence for mutation, matching, fields, portability, revision, projection, reporting, frontend, authorization, and harness behavior. | Target and collaborator tests; Section 4 acceptance registry; verification inputs | Human completeness review plus executable owner slices | `DONE`: behavioral tests and causal retained routing through `PTR-AC-404` pass. |
| WF-04 | Boundary/coupling scan | parallel | WF-01, WF-02 | WF-05 | Classify source ownership, transport/platform coupling, provider placement, direct SQL, frontend, grid, and test leakage. | Target, app assembly, boundary manifests, frontend imports | `make backend-module-boundary-check`; conditional frontend boundary check | `DONE`: Section 5 records current findings. |
| WF-05 | Facade or ownership redesign plan | chain | WF-03, WF-04 | WF-06 | Select the deep Party facade, narrow ports, internal providers, and clean-cutover caller map without moving behavior between semantic owners. | Parties root/subpackages; application assembly; collaborator facades | Design review against Sections 3–5 after owner adoption; baseline owner slices | `DONE`: one root mutation/import application boundary, private policy/source implementation, root supporting-owner contributions, and internal providers were selected; S-06 implements the facade/source half and S-07 owns the selected provider moves. |
| WF-06 | Slice sequencing plan | chain | WF-03, WF-04 | WF-07, WF-08 | Define the smallest independently reviewable authority, behavior, harness, structural, generation, and validation cutovers. | Files and owner surfaces named by Section 7 | Per-slice dependency, rollback, validation, and exit review | `DONE`: S-00 through S-09 executed serially with a tracker checkpoint before every successor slice. |
| WF-07 | Harness/test/accounting update plan | parallel | WF-03, WF-06 | WF-08 | Reassign mixed tests by postcondition owner, add causal portability evidence, and refresh generated harness projections from authored inputs. | Tests; `contracts/verification/**`; `tools/test_families/**`; owner catalogs | `make generate`, drift checks, complete affected-owner slices, and retained-evidence audit | `DONE`: semantic rows, causal sentinels, topology, owner slices, and evidence-class gates pass with no active predecessor or duplicate selector. |
| WF-08 | Validation and final handoff | chain | WF-06, WF-07 | None | Complete focused, browser, static, drift, and broad validation and leave current handoff evidence. | Repository-wide validation surfaces and tracker | Section 8; `make agent-finalize` before broad end-of-run verification | `DONE`: every focused/service-backed owner pair, browser, drift/static/frontend, fast, finalize, and 655-unit broad gate passes; retained-run maintenance was explicitly skipped because no valid pre-existing full warm-check root was supplied. |

## 7. Refactor Slice Execution Plan

The user authorized serial execution of every slice below. S-00 through S-05 include owner, behavior, version, test, schema, or harness changes and are not behavior-preserving. S-06 and S-07 are behavior-preserving only after those controlling amendments and corrections pass. Every slice MUST satisfy `PTR-REQ-002`: remove the predecessor in the same cutover, with no forwarding alias, dual registration, dual writer, compatibility reader, retired expectation, or active predecessor row.

| Slice ID | Depends on | Intended change | Files/packages likely involved | Contract risks | Tests to add or preserve | Validation command | Rollback note | Completion criterion |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S-00 | WF-03, WF-04 | **DONE:** adopted the exact Core 01, Core 02, Core 04, and traceability amendments for `PTR-REQ-101` through `PTR-REQ-302`. | `docs/spec/01_architecture_storage_and_view_contracts.md`, `docs/spec/02_domain_model_schema_and_history.md`, `docs/spec/04_security_deployment_and_conformance.md`, `docs/spec/F_source_traceability_matrix.md` | Closed. The independent second-implementer review found and drove correction of traceability and exact-interface omissions before returning GO. | Two-implementer review completed; every new requirement maps to binary acceptance and selected verification owners. | Independent owner review GO; `git diff --check` passed; `make lint-markdown` passed at `.cartulary/test-results/20260823T220407Z-p859024`. | Revert the complete owner amendment together if a later owner review discovers a contradiction; no implementation may bypass the adopted text. | DONE: adopted owners state every Section 4 algorithm, default, failure, version, invariant, and atomicity rule without contradiction. Domain vocabulary unchanged. |
| S-01 | S-00 | **DONE:** authored and generated the Party field/normalization/safe-error projections, neutral timezone registry and provenance, exact Party bundle row, catalog v4/major 2/version 3, optional clearability, and owner-derived traceability. | `contracts/{parties,string-contracts,incident-bundles,view-schemas,errors,imports}/**`; contract generator and schemas; generated Go/TypeScript projections; schema attachments/topology index | Closed for projection authoring. S-03 removed abbreviation/POSIX aliases discovered during runtime parity validation; the reviewed snapshot now has 578 sorted Zone/Link identifiers. Runtime remains behind only the global bundle contract until S-04. | Closed schemas and generator semantic checks enforce the eight-field registry, six clears, 578 reviewed identifiers plus digest, exact safe errors, ten-member Party row, v4 catalog, and AC-561..AC-563 traceability. | `make generate` passed at `.cartulary/test-results/20260823T221925Z-p875226`; `make generate-drift` passed at `.cartulary/test-results/20260823T222005Z-p878622`; policy and JSON checks passed at `.cartulary/test-results/20260823T222020Z-p881619` and `.cartulary/test-results/20260823T222020Z-p881622`; checkpoint Markdown passed at `.cartulary/test-results/20260823T222156Z-p882965`. | Revert authored inputs and regenerate as one unit if an owner-projection defect is found; generated roots remain generator-owned. | DONE: authored and generated projections match adopted owners, active catalog inputs have no v2 path, and S-02 is unblocked. Domain vocabulary unchanged. |
| S-02 | S-00, S-01 | **DONE:** introduced database-enforced Party active claims, bounded duplicate/invalid-state preflight and backfill, convergent all-key matching, tuple locks, exact lifecycle/recovery maintenance, and typed Workbook/Imports/rollback failures. | Migration `00039`; Party claim/store/facade/delete-restore/rollback/recovery code and tests; Workbook/Imports/Revisions adapters; Recovery and DDL catalogs/generation; current recovery generation fixtures; generated error/recovery/topology projections | Closed. Malformed or competing upgrades block without repair; create, patch, clear, delete, restore, rollback, Imports, and recovery now share the claim boundary. Runtime normalizers are least-privilege executable while claim writers remain private. | `PTR-AC-101` through `PTR-AC-120`; clean Up/Down/reapply/backfill/grants, bounded preflight, concurrent convergence, key case posture, create/reuse/cross-key, patch/clear/delete/restore/rollback, exact recovery-set equality, safe HTTP/Imports errors, and zero-effect rejections. New direct claim tests remain intentionally unrouted until S-05 assigns immutable owner rows. | `make migration-drift` passed at `.cartulary/test-results/20260823T230550Z-p1206712`; final Party, Workbook, Imports, Revisions, Recovery, Database Migrations, and Operator slices passed at `.cartulary/test-results/20260823T232701Z-p1669088`, `.cartulary/test-results/20260823T231020Z-p1304314`, `.cartulary/test-results/20260823T232349Z-p1619083`, `.cartulary/test-results/20260823T230857Z-p1255274`, `.cartulary/test-results/20260823T232017Z-p1514617`, `.cartulary/test-results/20260823T232139Z-p1567055`, and `.cartulary/test-results/20260823T232306Z-p1583453`; direct claim package diagnostics passed in 67.909 seconds; final generate/drift/policy/JSON roots are `.cartulary/test-results/20260823T232636Z-p1665776`, `.cartulary/test-results/20260823T232757Z-p1708663`, `.cartulary/test-results/20260823T232543Z-p1664083`, and `.cartulary/test-results/20260823T232653Z-p1668600`; checkpoint Markdown passed at `.cartulary/test-results/20260823T233001Z-p1712266`. | Roll back migration, claim runtime, recovery-generation identity, and failure translations together; never retain the removed lookup or automatically select a duplicate winner. | DONE: exact claims and concurrency converge; active lifecycle and recovery sets are exact; failures are safe and atomic; predecessor lookup and generic Party-to-Entity rollback reason are absent. Domain vocabulary unchanged. |
| S-03 | S-00, S-01, S-02 | **DONE:** cut over immutable Party admissions, the generated field/timezone registries, one normalized-value interface, RFC-8785 request hashes, and Party-only replay disposition migration `00040`. | Party private policy/admission/store/facade/imports/claims and tests; Workbook adapter; neutral canonical-JSON primitive; timezone authored/generated projection; migration catalog/baselines and `00040` | Closed. Required fields reject null/empty; all six optional fields clear; email uses case-insensitive equality/claim/hash while preserving storage case; external references remain exact; timezone admission is packaged and host-independent; callers cannot supply a Party hash. | `PTR-AC-201` through `PTR-AC-208`; generated normalization corpus, scalar max/max-plus-one and clear matrices, RFC-8785 vectors, three hash goldens and equivalence/inequality tests, semantic no-op evidence, exact replay precedence, Party-only delete and unrelated-row byte preservation. | Final focused Parties, Workbook, Imports, Revisions, Projections, and Database Migrations roots: `.cartulary/test-results/20260824T000930Z-p2134864`, `.cartulary/test-results/20260823T235501Z-p1814220`, `.cartulary/test-results/20260823T235725Z-p1874604`, `.cartulary/test-results/20260823T235839Z-p1917196`, `.cartulary/test-results/20260823T235948Z-p1962473`, `.cartulary/test-results/20260824T000228Z-p1995971`; Party, Workbook, Imports, and Database Migrations service-backed roots: `.cartulary/test-results/20260823T235355Z-p1774614`, `.cartulary/test-results/20260824T000602Z-p2070230`, `.cartulary/test-results/20260824T000449Z-p2028378`, `.cartulary/test-results/20260824T000339Z-p2012320`; migration drift `.cartulary/test-results/20260823T235234Z-p1730361`; final generate/drift/policy/JSON/boundary roots `.cartulary/test-results/20260824T000827Z-p2127460`, `.cartulary/test-results/20260824T000844Z-p2130364`, `.cartulary/test-results/20260824T000844Z-p2130382`, `.cartulary/test-results/20260824T000844Z-p2130396`, `.cartulary/test-results/20260824T000845Z-p2130654`. | Roll back policy/runtime and migration `00040` together on disposable data; Down cannot reconstruct old requests, and no legacy hasher or replay reader may be restored. | DONE: every live mutation/import/claim/store consumer shares the immutable policy result; only deterministic Party replay rows are removed; predecessor decoders/hashers and ambient timezone lookup are absent. Domain vocabulary unchanged. |
| S-04 | S-00, S-01, S-02, S-03 | **DONE:** cut over every Incident Bundle source and consumer to version 3/catalog major 2; implemented the exact ten-member Party row, strict prepare/apply/validate phases, claim and exact-state validation, five-invariant precedence, safe failures, and no-v2 admission. | Incident Bundle coordinator/catalog/source port and tests; every source descriptor; Party portability/source port and direct tests; Revisions version projection; bundle fixtures/schema descriptions; owner example; boundary manifest and stale descriptor/head assertions | Closed. Version 2 is rejected before source preparation; Party wire bytes no longer derive from table topology; no dual reader, converter, fallback, or partial publication path exists. | `PTR-AC-301` through `PTR-AC-308`; zero/every-kind/null/full corpus, duplicate-aware strict shape, normalization, lifecycle, envelope, claims, exact-state, cancellation/failure atomicity, safe diagnostics, semantic byte round trip, and causal sentinel failures. New direct Party tests remain intentionally unrouted until S-05. | Final focused and service-backed slices for all fourteen changed owners passed at the roots in Section 10. `make generate` passed at `.cartulary/test-results/20260824T004117Z-p2311991`; drift/policy/JSON/migration/boundary gates passed at `.cartulary/test-results/20260824T012224Z-p3436852`, `.cartulary/test-results/20260824T012233Z-p3439759`, `.cartulary/test-results/20260824T012234Z-p3440169`, `.cartulary/test-results/20260824T012237Z-p3440575`, and `.cartulary/test-results/20260824T012245Z-p3443457`; executable audit found zero production v2 success symbols and four negative-test references. | Revert the coordinated catalog/runtime/test cutover as one unit if later evidence fails; never restore a dual reader or select a partial source-family rollback. | DONE: version 3 is the sole success path; exact Party rows round-trip; all five invariants have causal evidence; every changed source owner passes; RB-003 is resolved. Domain vocabulary unchanged. |
| S-05 | S-01, S-04 | **DONE:** retired the mixed Party/Evidence/Tasks row, allocated nine owner-specific immutable rows, routed direct claim/portability/rollback/projection-source evidence, and regenerated exact harness topology. | Party, Incident Bundles, and Projections tests; Evidence, Tasks/Decisions, Parties, and Projections family inputs; row migration and PostgreSQL fixture policy; harness contract expectation; Operator migration-evidence golden | Closed. Every assertion is selected once by its normative postcondition owner; fixture capabilities and public evidence-class projections account for the expanded catalog exactly. | `PTR-AC-401` through `PTR-AC-404`; direct invariant sentinels, exact result roster, owner explanations, complete destination-owner slices, and public unit/integration/security routing. | Focused/service-backed roots for Parties, Evidence, Tasks/Decisions, Projections, Workbook, and Revisions are recorded in Section 10. Generation/drift/policy/JSON passed at `.cartulary/test-results/20260824T014147Z-p3563954`, `.cartulary/test-results/20260824T015720Z-p4076016`, `.cartulary/test-results/20260824T015732Z-p4078949`, and `.cartulary/test-results/20260824T015737Z-p4079404`; harness contract, backend unit, and backend integration passed at `.cartulary/test-results/20260824T015835Z-p4081427`, `.cartulary/test-results/20260824T020208Z-p4110194`, and `.cartulary/test-results/20260824T020258Z-p4126724`; checkpoint Markdown passed at `.cartulary/test-results/20260824T020859Z-p4148275`. | Revert authored rows/tests and regenerate together if a later accounting defect is found; never reactivate the predecessor or add an alias. | DONE: one semantic owner/result per assertion; causal Party portability/claims/rollback/projection evidence is active; no temporary crosswalk, duplicate selector, active predecessor ID, or stale mapping remains. Domain vocabulary unchanged. |
| S-06 | S-02, S-03, S-04, WF-05 | **DONE:** replaced the shallow Store/facade construction with root mutation/import contributions, injected semantic capabilities, immutable admitted commands, transport-neutral results, a typed replay union, and private Party source functions. | Party facade/capabilities/admission/import code and new `internal/source`; Workbook/Imports/appsupport assembly; exported-surface and composition/idempotency tests; Party family input and backend boundary input | Closed for the facade cutover. Transaction/replay order, exact wire behavior, claims, projection/revision calls, conflict keep-saved semantics, and caller construction all pass after the move. | Preserved all owner-required behavior and added exact constructor dependency, exported-surface, and route-idempotency result evidence. | Final focused/service-backed Parties roots `.cartulary/test-results/20260824T024856Z-p445217` and `.cartulary/test-results/20260824T024948Z-p485295`; Workbook roots `.cartulary/test-results/20260824T025038Z-p524934` and `.cartulary/test-results/20260824T025255Z-p582377`; Imports, Revisions, and Projections roots recorded in Section 10; boundary root `.cartulary/test-results/20260824T025514Z-p639543`; generation/static roots recorded in Section 10; checkpoint Markdown passed at `.cartulary/test-results/20260824T025711Z-p641014`. | Revert the root facade, private source, assembly, tests, and authored harness/boundary inputs together if a later regression is found; deleted predecessor files and constructors must not return. | DONE: all mutation/import callers use root contributions and narrow capabilities; commands carry `ActorUserID`; Party results are semantic; `Store`, `NewStore`, `NewMutationFacade`, concrete platform-store construction, and bridge packages are absent. Domain vocabulary unchanged. |
| S-07 | S-06 | **DONE:** moved Party projection, reporting, Incident Bundle, delete/restore, and rollback implementations under `internal/providers`; exposed root contribution constructors; moved admission and keep-saved HTTP/API translation entirely into Workbook. | Party internal providers/root contributions/admission; Workbook mutation contract and Party adapters; projection/reporting/portability/revision assemblies and tests; strict JSON primitive; authored harness/boundary inputs and generated topology | Closed. Stable provider/source/snapshot IDs, paging/null/deleted selection, reporting paths/partitions, rollback presence, delete blockers, recovery identity, v3 portability, and exact public envelopes pass unchanged. | Complete provider/destination suites and exact root-surface/static predecessor audits now cover the selected topology. | Final focused/service-backed owner roots are recorded in Section 10. Generate/drift/policy/JSON/harness passed at `.cartulary/test-results/20260824T030911Z-p689764`, `.cartulary/test-results/20260824T032421Z-p1146645`, `.cartulary/test-results/20260824T032432Z-p1149598`, `.cartulary/test-results/20260824T032436Z-p1150047`, and `.cartulary/test-results/20260824T032443Z-p1150564`; final boundary passed at `.cartulary/test-results/20260824T032510Z-p1151204`; checkpoint Markdown passed at `.cartulary/test-results/20260824T032703Z-p1152305`. | Revert internal moves, root contribution names, all assembly imports, transport translation, tests, and authored boundary/harness inputs together if a later defect is found; no forwarding package or old constructor may return. | DONE: production supporting owners import only the Party root or `workbookprojection`; Party production imports no HTTP/API/auth-record surface; root contributions are the only provider constructors; translation occurs once in Workbook. Domain vocabulary unchanged. |
| S-08 | S-05, S-06, S-07 | **DONE:** swept all remaining callers, boundary/ownership/recovery/source inputs, generated topology, registrations, versions, and row identities; removed four empty predecessor provider directories. | Current authored/runtime/generated inputs and application/catalog/startup construction; no additional product behavior or generated bytes changed beyond the S-07 authored cutover | Closed. No current predecessor path, caller, constructor, schema version, registration, or active row identity remains. Historical prose, the exact retirement-ledger entry, a static forbidden-symbol sentinel, and the v2 rejection test are non-success evidence only. | Targeted Projection/Revisions/Incident Bundle/Recovery/Workbook catalog rows and server startup composition pass. | Catalog/startup roots `.cartulary/test-results/20260824T032931Z-p1155642`, `.cartulary/test-results/20260824T033008Z-p1171382`, `.cartulary/test-results/20260824T033015Z-p1171773`, `.cartulary/test-results/20260824T033022Z-p1172181`, `.cartulary/test-results/20260824T033029Z-p1172579`, and `.cartulary/test-results/20260824T033038Z-p1172980`; generate/drift/policy/JSON/boundary roots `.cartulary/test-results/20260824T033048Z-p1173400`, `.cartulary/test-results/20260824T033100Z-p1176323`, `.cartulary/test-results/20260824T033112Z-p1179275`, `.cartulary/test-results/20260824T033115Z-p1179709`, and `.cartulary/test-results/20260824T033121Z-p1180184`; target-plan digest `sha256:331ebafe21f3e8bce91567bcbdc9207c0976836b22fffe271c8f7b434326bd24`; checkpoint Markdown passed at `.cartulary/test-results/20260824T033303Z-p1181560`. | If S-09 exposes a stale identity, reopen S-08, fix the authored owner input/caller, regenerate, and repeat downstream checks; never patch a generated result or add a compatibility registration. | DONE: all current callers and registrations use final identities; generated/static/catalog/startup checks pass; old row/version/symbol occurrences are exclusively negative or historical records. Domain vocabulary unchanged. |
| S-09 | S-08 | **DONE:** completed owner closure, browser/static/frontend/fast/finalize/broad validation, repaired the two causal findings from the first broad run, and completed the implementation handoff. | All changed owner/runtime/contract/test/harness surfaces; lowercase Party errors in `internal/modules/parties/import_create.go` and `tools/contractgen/party_policy.go`; `contracts/otel/error_class_registry.json`; this tracker | Closed. The first `make check` passed 653/655 units but correctly rejected eight capitalized Go error strings and the unmapped `party_match_conflict` telemetry class. Authored fixes pass direct gates and the full corrected graph. | Every active affected row, owner closure, evidence-class gate, browser sentinel, migration/drift/static gate, and broad check. | Focused/service-backed pairs: Parties `.cartulary/test-results/20260824T033507Z-p1188412` / `.cartulary/test-results/20260824T033557Z-p1228042`; Workbook `.cartulary/test-results/20260824T033646Z-p1267725` / `.cartulary/test-results/20260824T033859Z-p1324779`; Imports `.cartulary/test-results/20260824T034112Z-p1381825` / `.cartulary/test-results/20260824T034221Z-p1423974`; Incident Bundles `.cartulary/test-results/20260824T034331Z-p1465633` / `.cartulary/test-results/20260824T034427Z-p1481572`; Database Migrations `.cartulary/test-results/20260824T034523Z-p1497512` / `.cartulary/test-results/20260824T034627Z-p1513715`; Recovery `.cartulary/test-results/20260824T034731Z-p1529587` / `.cartulary/test-results/20260824T034846Z-p1581221`; Revisions `.cartulary/test-results/20260824T035000Z-p1632842` / `.cartulary/test-results/20260824T035103Z-p1677117`; Projections `.cartulary/test-results/20260824T035208Z-p1721400` / `.cartulary/test-results/20260824T035245Z-p1737471`; Reporting `.cartulary/test-results/20260824T035322Z-p1753540` / `.cartulary/test-results/20260824T035403Z-p1769310`; Artifacts `.cartulary/test-results/20260824T035443Z-p1785084` / `.cartulary/test-results/20260824T035521Z-p1801213`; Assessments `.cartulary/test-results/20260824T035559Z-p1816927` / `.cartulary/test-results/20260824T035656Z-p1859684`; Entities `.cartulary/test-results/20260824T035752Z-p1901506` / `.cartulary/test-results/20260824T035944Z-p1956094`; Evidence `.cartulary/test-results/20260824T040138Z-p2009756` / `.cartulary/test-results/20260824T040254Z-p2059950`; Extensions `.cartulary/test-results/20260824T040408Z-p2108877` / `.cartulary/test-results/20260824T040456Z-p2148813`; Incidents `.cartulary/test-results/20260824T040543Z-p2188585` / `.cartulary/test-results/20260824T040709Z-p2233653`; Indicators `.cartulary/test-results/20260824T040831Z-p2277850` / `.cartulary/test-results/20260824T040916Z-p2294876`; Links `.cartulary/test-results/20260824T041000Z-p2310864` / `.cartulary/test-results/20260824T041043Z-p2350586`; Records `.cartulary/test-results/20260824T041126Z-p2389896` / `.cartulary/test-results/20260824T041202Z-p2405851`; Reference Data `.cartulary/test-results/20260824T041238Z-p2421674` / `.cartulary/test-results/20260824T041323Z-p2461525`; Saved Views `.cartulary/test-results/20260824T041408Z-p2500925` / `.cartulary/test-results/20260824T041519Z-p2549735`; Tasks/Decisions `.cartulary/test-results/20260824T041629Z-p2598366` / `.cartulary/test-results/20260824T041715Z-p2638207`; Timeline `.cartulary/test-results/20260824T041801Z-p2677640` / `.cartulary/test-results/20260824T042243Z-p2736895`. Browser 58/58 `.cartulary/test-results/20260824T042723Z-p2794242`; drift/policy/JSON/migration/boundary `.cartulary/test-results/20260824T043139Z-p2847118`, `.cartulary/test-results/20260824T043154Z-p2850077`, `.cartulary/test-results/20260824T043159Z-p2850526`, `.cartulary/test-results/20260824T043206Z-p2850983`, `.cartulary/test-results/20260824T043218Z-p2853901`; frontend type/unit/import `.cartulary/test-results/20260824T043223Z-p2854327`, `.cartulary/test-results/20260824T043237Z-p2854861`, `.cartulary/test-results/20260824T043408Z-p2889401`; first broad failure `.cartulary/test-results/20260824T043506Z-p2896534`; corrected telemetry 6/6 `.cartulary/test-results/20260824T044139Z-p3025391`; corrected fast 429/429 `.cartulary/test-results/20260824T044309Z-p3038591`; corrected finalize `.cartulary/test-results/20260824T044357Z-p3045424`; corrected check 655/655 `.cartulary/test-results/20260824T044416Z-p3048348`; `git diff --check` and final predecessor/version audits pass. `make agent-finalize` ran without `RESULTS_DIR`; retained-run maintenance was skipped because no valid pre-existing successful full warm-check root existed. | If later operational evidence disproves pre-production disposability, stop deployment and adopt a new owner migration; do not restore v2, legacy replay, or another compatibility path. | DONE: all required commands pass, the single skip is justified, compatibility disposition is explicit, all handoff areas are current, and valid evidence roots are recorded. Domain vocabulary unchanged. |

## 8. Validation Plan

| Validation layer | Command | Scope | Required before implementation? | Notes |
| --- | --- | --- | --- | --- |
| unit | `make test-slice OWNER=module.parties ROWS=module.parties.unit.party_mutation_admission_and_replay_hashing_are_o_7e9a31d4c2` | Current Party mutation admission and replay hashing | yes | Establish the fast baseline before changing admission or public types. |
| integration | `make service-backed-test-slice OWNER=module.parties` | All three current service-backed Party rows, including the webserver-backed browser row selected by owner policy | yes | Current routing has two Postgres integration rows and one Playwright row; RB-004 means this is not yet complete portability evidence. |
| affected owners | `make task-guide ROLE=module-author OWNER=<owner-id>` followed by complete `make test-slice OWNER=<owner-id>` and, when selected, `make service-backed-test-slice OWNER=<owner-id>` | Parties, Workbook, Imports, Incident Bundles, Database Migrations, Recovery, Revisions, Projections, and every source owner changed by bundle v3 | yes | Discover the exact current owner rows before each slice. New row IDs MUST come from authored catalogs and MUST NOT be guessed or copied from this tracker. |
| e2e/browser | `make browser-e2e-webserver-backed` | Workbook Party create/link raw-text and selector behavior | yes | Required before and after any route, Party-link, view schema, or frontend-adjacent change. |
| generated drift | `make generate-drift`; `make generated-artifact-policy-check`; `make json-shape-check` | Generated contracts, policy, JSON owner inputs, and harness projections | yes | Establish a clean baseline; after owner-input changes run `make generate` first. Never hand-edit generated roots. |
| migration | `make migration-drift` | Party claims, replay-state disposition, recovery metadata, and database rebaseline posture | yes | Required before and after S-02 and S-03. A duplicate-claim preflight failure blocks the slice and does not authorize remediation. |
| import-boundary/static | `make backend-module-boundary-check` | Backend module imports, source ownership, provider access, and SQL boundaries | yes | If frontend code changes, also run `make frontend-import-boundary-check`, `make frontend-typecheck`, and `make frontend-unit`; otherwise those are not required. |
| harness accounting | `make explain-test-owner OWNER=module.parties`; `make target-plan`; generated evidence-class gates selected by the new rows | Immutable row ownership, selector uniqueness, direct portability evidence, complete-owner closure, and retained evidence | no | After S-05, repeat owner explanation for every destination owner. Exact generated gates are discovered from the authored rows; broad `check` is not a substitute. |
| full check | `make check` | Repository-wide end-of-run verification | no | Required at the end of the authorized implementation, not before the first slice. Run `make agent-finalize` first; if `RESULTS_DIR` is unset, report retained-run maintenance as skipped. |
| documentation | `make lint-markdown` | This tracker and repository Markdown | no | Required for this tracker-only task after the file is written. |

Command discovery used the current public Make surface and the read-only outputs of `make task-guide ROLE=module-author OWNER=module.parties` and `make explain-test-owner OWNER=module.parties`. No command in this table was invented from a direct runner invocation.

Final S-09 completion Markdown validation passed at `.cartulary/test-results/20260824T045306Z-p3160200`; the evidence-only tracker edit that records this root is validated by the required final rerun.

## 9. Top-Level Work Tracker

| ID | Work item | Workstream | Status | Depends on | Evidence or artifact | Exit condition |
| --- | --- | --- | --- | --- | --- | --- |
| PT-001 | Complete exhaustive live target inventory | WF-01 | DONE | WF-00 | Section 2, 24 file rows | Every target file is accounted for exactly once. |
| PT-002 | Freeze Party-adjacent public contracts and owners | WF-02 | DONE | PT-001 | Sections 3–4 | Every discovered contract has an owner, evidence, test posture, and risk. |
| PT-003 | Close exact-match and claim planning | WF-03 | DONE | PT-002 | `PTR-REQ-101` through `PTR-REQ-104`; `PTR-AC-101` through `PTR-AC-120` | Algorithm, defaults, failure mappings, lifecycle, replay, migration, and acceptance are decision-complete. |
| PT-004 | Close Party field, timezone, and hash planning | WF-03 | DONE | PT-002 | `PTR-REQ-201` through `PTR-REQ-204`; `PTR-AC-201` through `PTR-AC-208` | Registry, normalized forms, defaults, timezone policy, hash preimages, cutover, and acceptance are decision-complete. |
| PT-005 | Close Party Incident Bundle planning | WF-03 | DONE | PT-002 | `PTR-REQ-301` through `PTR-REQ-302`; `PTR-AC-301` through `PTR-AC-308` | Version, catalog major, row shape, invariants, port phases, failure, atomicity, and acceptance are decision-complete. |
| PT-006 | Close test/harness ownership planning | WF-07 | DONE | PT-002 | `PTR-REQ-401` through `PTR-REQ-402`; `PTR-AC-401` through `PTR-AC-404` | Assertion ownership, migration, direct portability, sentinels, and retained-evidence audit are decision-complete. |
| PT-007 | Adopt required owner amendments | WF-02 | DONE | PT-003, PT-004, PT-005 | S-00; REQ-01-668..REQ-01-670; REQ-02-271; AC-561..AC-563; independent review GO | Core 01, Core 02, Core 04, and traceability adopt the closure without contradiction. |
| PT-008 | Update authored projections and acceptance fixtures | WF-02 | DONE | PT-007 | S-01; generated evidence roots recorded in Section 10 | Owner inputs project the adopted requirements and generation/drift checks pass. |
| PT-009 | Implement Party active claims and failure cutover | WF-06 | DONE | PT-008 | S-02; RB-001; evidence roots in the S-02 row and Section 10 | `PTR-AC-101` through `PTR-AC-120` pass and no predecessor behavior remains. |
| PT-010 | Implement Party field/normalization/hash cutover | WF-06 | DONE | PT-008, PT-009 | S-03; RB-002; evidence roots in Sections 7 and 10 | `PTR-AC-201` through `PTR-AC-208` pass; only Party replay history is dispositioned. |
| PT-011 | Implement Incident Bundle version-3 cutover | WF-06 | DONE | PT-008, PT-009, PT-010 | S-04; RB-003; exact evidence roots in Sections 7 and 10 | `PTR-AC-301` through `PTR-AC-308` pass and no version-2 success path remains. |
| PT-012 | Repair test and harness ownership/accounting | WF-07 | DONE | PT-008, PT-011 | S-05; RB-004; exact evidence roots in Sections 7 and 10 | `PTR-AC-401` through `PTR-AC-404` pass and no temporary or active predecessor crosswalk remains. |
| PT-013 | Select and implement the deep Party facade/provider topology | WF-05, WF-06 | DONE | PT-009, PT-010, PT-011 | S-06, S-07; exact roots in Sections 7 and 10 | All callers use approved root contributions; implementation providers are private; Party transport/platform coupling and predecessor packages are absent. |
| PT-014 | Complete callers, owner inputs, generation, and boundary cutover | WF-06 | DONE | PT-012, PT-013 | S-08; exact roots in Sections 7 and 10 | No current predecessor path, registration, version, projection, or active row ID remains; generated/static/catalog/startup checks pass. |
| PT-015 | Complete validation and implementation handoff | WF-08 | DONE | PT-014 | S-09; Section 8; final broad root `.cartulary/test-results/20260824T044416Z-p3048348` | Every focused and broad required command passes; the only skip is justified; compatibility and migration disposition, evidence roots, and handoff guidance are current. |

## 10. Session Handoff Log

### Scope and authority

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-23T20:02:58Z | Codex planning inspection | Target exists; label and output path are safe; tracker created as the sole write; clean-cutover posture recorded. | Inspected planning framework, Core 00–04, Reporting and Testing Harness NLSpecs, `docs/domain.md`; touched only this tracker. | `sed`, `rg`, `find`, `wc`, `git status --short --branch` | Authority and repository posture established; Core 05 not applicable; no owner contradiction found. | None for tracker completion. | Authorize WF-03 characterization and WF-05 boundary redesign. |
| 2026-08-23T21:17:56Z | Codex NLSpec-style tracker revision | Planning decisions for RB-001 through RB-004 are closed; owner adoption and implementation remain unauthorized. | Inspected `temp/analysis-notes.md`, `docs/research/nlspec-spec.md`, this tracker, relevant Core/owner text, and live repository evidence; touched only this tracker. | `sed`, `rg`, `find`, `jq`, `wc`, `git status --short`, `date -u`, `make lint-markdown` | Modality, stable requirements, defaults, mappings, acceptance, sequencing, and binary closure criteria added; Markdown lint passed. | RB-001 through RB-004 block execution, not tracker completion. | Authorize S-00 owner-amendment adoption; do not start implementation from the tracker alone. |
| 2026-08-23T22:02:24Z | Codex S-00 owner adoption | User authorization is active; S-00 owner adoption is complete and S-01 is next. Domain vocabulary unchanged. | Changed Core 01, Core 02, Core 04, Appendix F, and this tracker; inspected exact Party, Incident Bundle, and profile traceability sections. | `rg`, `sed`, `git diff`, `git diff --check`; independent second-implementer review; `make lint-markdown` | Adopted REQ-01-668..REQ-01-670, REQ-02-271, AC-561..AC-563; independent review returned GO; Markdown passed at `.cartulary/test-results/20260823T220407Z-p859024`. | Runtime and projections intentionally remain behind adopted owners until S-01 through S-04; the branch is non-deployable. | Begin S-01 authored projections. |
| 2026-08-23T22:20:42Z | Codex S-01 machine projections | S-01 is complete and S-02 is the only next slice. Domain vocabulary unchanged. | Added Party/string-contract inputs and closed schemas; changed Party view/errors/Imports/bundle catalog/traceability; changed contract generation and schema-shape validation; generated declared Go and TypeScript outputs. | `make generate`, `make generate-drift`, `make generated-artifact-policy-check`, `make json-shape-check`, `git diff --check` | All required S-01 checks pass at the run roots recorded in the S-01 row. The first generator run exposed an incomplete strict provenance decoder and the first JSON runs exposed byte-order/topology staleness; both were corrected before closure. | S-02 through S-04 runtime parity remains intentionally open on this non-deployable branch. | Implement migration `00039`, active claims, convergent matching, recovery exactness, and safe Workbook/Imports failures in S-02. |

| 2026-08-23T23:28:16Z | Codex S-02 active claims and matching | S-02 and PT-009 are complete; S-03 is the only next slice. Domain vocabulary unchanged. | Added migration `00039`; changed Party mutation/recovery/delete-restore/rollback code, Workbook and Imports translations, Revisions error adaptation, Recovery/DDL catalogs, exact tests, generated projections, and this tracker. | Required owner slices, direct claim diagnostics, migration scratch/apply/drift, generation/drift/policy/JSON, predecessor searches, and `git diff --check` | Exact active claims, concurrency convergence, lifecycle/recovery equality, typed safe errors, and clean predecessor removal pass at the roots in the S-02 row. | RB-001 resolved. RB-002 is the active S-03 implementation gap; RB-003 and RB-004 remain scheduled. Branch remains non-deployable through S-04. | Implement the field/timezone/hash and Party-only replay disposition cutover in S-03. |
| 2026-08-24T00:10:38Z | Codex S-03 field/timezone/hash cutover | S-03 and PT-010 are complete; S-04 is the only next slice. Domain vocabulary unchanged. | Added canonical JSON and Party private policy packages plus migration `00040`; changed Party admission/facade/store/import/claims, Workbook adapters, timezone registry/provenance, migration ownership/baselines, generated projections, exact tests, and this tracker. | Focused/service-backed affected-owner slices, migration drift, generation/drift/policy/JSON/boundary gates, canonical-JSON diagnostic, predecessor/ambient-dependency searches, `git diff --check` | Immutable normalized commands and named hashes pass; Party-only replay rows are disposed while unrelated bytes remain exact; no old decoder/hasher, alternate Party caller hash, host tzdb, or Network Flow dependency remains. | RB-002 resolved. RB-003 is the active S-04 runtime gap; RB-004 remains scheduled for S-05. The branch remains non-deployable through S-04. | Implement the coordinated Incident Bundle v3 and strict Party source-port cutover in S-04. |
| 2026-08-24T01:23:55Z | Codex S-04 Incident Bundle v3 cutover | S-04 and PT-011 are complete; S-05 is the only next slice. Domain vocabulary unchanged. | Changed all source descriptors and Incident Bundle consumers to v3/major 2; replaced Party topology-derived portability with strict explicit mapping; added direct Party and public-failure tests; updated authored fixtures/example, Revisions projection, boundary inputs, and stale owner assertions. | Fourteen focused plus fourteen service-backed owner slices; generate/drift/policy/JSON/migration/boundary gates; direct Party portability diagnostics; no-v2 executable audit; `git diff --check` | Version 3 is the sole success path, Party export/import is schema-bound and exact, claim/state validation and five-invariant precedence are causal, failures are atomic and safe, and every changed source owner passes. | RB-003 resolved. RB-004 is the active S-05 harness-accounting gap. The branch remains non-deployable until S-09. | Repair semantic test ownership and immutable harness rows in S-05. |
| 2026-08-24T02:06:39Z | Codex S-05 harness accounting cutover | S-05 and PT-012 are complete; S-06 is the only next slice. Domain vocabulary unchanged. | Changed Party/Projections tests, four owner family inputs, row migration and fixture policy, harness fixture-count contract, Operator migration-evidence golden, generated topology, and this tracker. | Owner explanations; six focused plus six service-backed owner slices; generate/drift/policy/JSON; target-plan parity; harness contract; backend unit/integration; predecessor searches | Nine immutable semantic rows replace one mixed row; each active assertion has one owner/result, causal Party portability evidence is retained, and all catalog/evidence gates pass. | RB-004 resolved. No compatibility row, duplicate selector, or temporary crosswalk remains; structural facade/provider work remains S-06/S-07. | Begin S-06 deep Party facade extraction. |

| 2026-08-24T02:55:49Z | Codex S-06 deep Party facade cutover | S-06 is complete; the S-06 portion of PT-013 is DONE and S-07 is the only next slice. Domain vocabulary unchanged. | Replaced Party Store/facade/import construction, added private source and semantic capabilities, updated Workbook/Imports/appsupport assembly, added facade/idempotency/surface tests, and updated authored harness/boundary inputs plus generated topology. | Complete affected owner slices; constructor/export/idempotency rows; generation/drift/policy/JSON/harness gates; boundary and predecessor searches | One root facade owns mutation/import application entry, all dependencies are explicit narrow capabilities, and Party commands/results contain no auth record or HTTP response envelope. | No S-06 blocker remains. The branch is non-deployable until S-09; exposed provider packages and residual Party admission transport construction remain assigned to S-07. | Begin S-07 provider internalization and transport-translation move. |

| 2026-08-24T03:25:54Z | Codex S-07 provider and transport cutover | S-07 and PT-013 are complete; S-08 is the only next slice. Domain vocabulary unchanged. | Internalized five provider families, added four final root contribution names, updated all app/supporting-owner callers, moved Party admission errors to a semantic type, centralized strict object decoding, and updated authored harness/boundary inputs plus generated topology. | Six complete focused/service-backed destination-owner pairs; generation/static/harness gates; exact predecessor/import/transport searches | The selected deep topology is implemented without an alias, forwarding package, dual registration, or wire change. | No S-07 blocker remains. The branch remains non-deployable until S-09. | Begin S-08 repository-wide caller/manifest/generated/stale-identity sweep. |

| 2026-08-24T03:31:56Z | Codex S-08 caller/manifest/generated sweep | S-08 and PT-014 are complete; S-09 is the only next slice. Domain vocabulary unchanged. | Audited all current code, contracts, tools, recovery/source indexes, registrations, generated topology, and row migration inputs; removed four empty Party predecessor directories; changed no additional product behavior. | Targeted catalogs/startup; generate/drift/policy/JSON/boundary; target plan; exact predecessor/version/row searches | Final identities are complete and singular across current authored/runtime/generated surfaces. | No S-08 blocker remains. The branch remains non-deployable pending S-09 broad validation. | Begin S-09 in the exact prescribed validation order. |

| 2026-08-24T04:49:00Z | Codex S-09 validation and handoff | S-00 through S-09 and PT-001 through PT-015 are complete. Domain vocabulary unchanged; `docs/domain.md` was not edited. | Final repository status/diff and every changed owner surface; implementation scope spans adopted Core owners, authored/generated contracts, migrations 00039/00040, Party/Workbook/Imports/Incident Bundle/Recovery/Revisions/Projections runtime, test/harness inputs, and this tracker. | Twenty-two final focused/service-backed owner pairs; browser; static/drift/frontend; corrected fast/finalize/check sequence; final clean-cutover and diff audits | All authorized specification, projection, implementation, validation, and handoff work is complete with the evidence in S-09. | None. Retained-run maintenance was intentionally skipped because `RESULTS_DIR` was unset and no valid pre-existing full warm-check root existed. | Review and merge the complete cutover as one unit; do not split, deploy, or release an intermediate slice. |

### Backend module boundary

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-23T20:02:58Z | Codex planning inspection | Parties is a legitimate source owner and facade with mixed supporting-provider responsibilities. | Inspected all 24 target files, app assembly callers, Workbook routes, Projections/Reporting/Revisions consumers, test support, schema ownership, and backend boundary inputs; touched only this tracker. | `find`, `rg`, `sed`, `jq` | Exhaustive inventory and boundary diagnosis completed. | RB-001, RB-002, RB-003 affect safe redesign sequencing. | Define the deep facade and provider topology in WF-05 after characterization. |
| 2026-08-23T21:17:56Z | Codex NLSpec-style tracker revision | Semantic Party boundaries are closed; physical internal-provider topology remains deferred to WF-05. | Reinspected Party store/facade/admission, claim precedent, Imports adapter, delete/restore, rollback, recovery, projection, reporting, and application assembly; touched only this tracker. | `sed`, `rg` | Defined Party-owned claims and field registry, typed failure boundary, supporting-owner contribution posture, and structural sequencing after behavior correction. | RB-001 through RB-003 and S-00 adoption gate. | Adopt owners, implement S-02 through S-04, then select the S-06/S-07 topology. |
| 2026-08-23T22:20:42Z | Codex S-01 machine projections | Generated immutable Party field and neutral timezone lookup packages now exist; production Party packages have not consumed them yet. | Changed `tools/contractgen`; added `internal/gen/{partypolicy,stringcontracts}` plus generic family projections. | `make generate`, `make generate-drift`, generated-artifact policy | Runtime-neutral generation passes and generated lookups expose no Network Flow import or host tzdb dependency. | Active-claim and runtime normalization/hash consumers remain for S-02/S-03. | Begin S-02 without introducing a lookup fallback or a Network Flow dependency. |

| 2026-08-23T23:28:16Z | Codex S-02 active claims and matching | Parties now owns the single exact-match coordination boundary; Workbook and Imports consume typed semantic conflicts, while Revisions coordinates restore/rollback without owning Party identity. | Changed Party root store/facades and provider implementations plus Workbook/Imports/Revisions/Recovery adapters; added the Party claim relation and private routines. | Party, Workbook, Imports, Revisions, Recovery, Database Migrations, and Operator owner slices; migration drift; predecessor import/symbol searches | No lookup fallback, dual writer, compatibility constructor, Entities dependency, or generic identity service was introduced. Claim writers remain database-private; runtime sees read-only claims and narrow refresh/release routines. | S-06/S-07 facade/provider physical topology is intentionally unchanged until semantic S-03/S-04 cutovers pass. | Preserve the exact claim boundary while replacing temporary S-02 normalization with the generated Party policy in S-03. |
| 2026-08-24T00:10:38Z | Codex S-03 field/timezone/hash cutover | Parties now admits immutable commands carrying one private policy value per field and its owner-derived digest; the facade no longer accepts a Party request hash. | Added `internal/modules/parties/internal/policy`; changed mutation admission, store, claims, import facade, conflict execution, and Workbook assembly; added neutral `internal/platform/canonicaljson`. | Parties/Workbook/Imports/Revisions/Projections slices; backend boundary check; predecessor-symbol and dependency searches | Persistence, semantic no-op, conflicts, claims, Imports, and hashes share one result. Exact replay remains before current-state checks, and the final S-06 admission interface is already in place without an alias. | Provider/facade physical internalization remains S-06/S-07; Incident Bundle consumes the policy in S-04. | Preserve immutable admissions while implementing strict Party portability in S-04. |
| 2026-08-24T01:23:55Z | Codex S-04 Incident Bundle v3 cutover | Party portability is an explicit schema-bound source adapter using the Party policy and claims; Incident Bundles owns orchestration and the source-port contract. | Replaced Party export/import helpers; changed the Party source contribution and coordinator/catalog version bindings; updated the exact backend boundary allowlist. | All affected source-owner slices; `make backend-module-boundary-check`; descriptor parity and predecessor searches | Party code no longer serializes table topology, and all source families use one version/major. The exact boundary check passes; no compatibility port or cross-owner ownership transfer was added. | The final internal-provider topology remains S-06/S-07; S-05 must first repair causal evidence routing. | Preserve strict portability while moving only test ownership in S-05. |
| 2026-08-24T02:06:39Z | Codex S-05 harness accounting cutover | Production module boundaries are unchanged; test ownership now follows semantic postconditions rather than the package containing the assertion. | Changed authored owner rows and direct Projections/Party test selectors; regenerated topology. No production Party or supporting-owner import moved. | Complete Parties, Evidence, Tasks/Decisions, Projections, Workbook, and Revisions slices plus public backend unit/integration gates | Cross-package selectors are owned exactly once, Party portability/claims/rollback remain Party-owned, and projection storage/query evidence remains Projections-owned. | S-06 must now select and enforce the deep root facade without weakening these owner assignments. | Introduce narrow facade capabilities and remove shallow constructors atomically in S-06. |

| 2026-08-24T02:55:49Z | Codex S-06 deep Party facade cutover | WF-05 topology is resolved: root `NewMutationContribution`/`NewImportContribution`, private `internal/policy` and `internal/source`, root supporting-owner constructors, and `internal/providers` for S-07. | Deleted `store.go`, `import_projection.go`, and `revision_append_port.go`; added capability and private-source files; updated Workbook/Imports assembly and exact Records SQL boundary allowlist. | `make backend-module-boundary-check`; exact exported-surface test; production searches for predecessor constructors, concrete platform stores, auth records, statuses, payloads, and HTTP imports | Boundary passed at `.cartulary/test-results/20260824T025514Z-p639543`. Production Party mutation/import code has no concrete Auth/Records/Incidents/Revisions construction and no predecessor facade/store symbol. | Direct `deleterestore` and `rollbackprovider` packages remain intentionally visible only until S-07; no compatibility package was added. | Move each Party supporting provider to the selected internal topology and update every assembly caller atomically. |

| 2026-08-24T03:25:54Z | Codex S-07 provider and transport cutover | Party root contributions now exclusively expose projection, reporting, Incident Bundle, Revisions, Imports, mutations, and Recovery; implementations are private. | Moved providers to `internal/providers/{projection,reporting,incidentbundle,deleterestore,rollback}`; changed Projection, Reporting, Incident Portability, Revisions, and test-support consumers to the root; updated exact static allowlists. | `make backend-module-boundary-check`; production import and removed-path searches; exact Party exported-surface lock | Final boundary passed at `.cartulary/test-results/20260824T032510Z-p1151204`. Outside Party, production imports resolve only the root or `workbookprojection`; Party production has no HTTP/API/auth-record/status/payload imports or fields. | The initial boundary run `.cartulary/test-results/20260824T030621Z-p653611` found four stale source-port allowlist paths; the authored manifest was corrected, and no compatibility allowance remains. | S-08 must sweep every non-Go manifest/source index and remove any stale physical identity before declaring the cutover complete. |

| 2026-08-24T03:31:56Z | Codex S-08 caller/manifest/generated sweep | No current implementation import, provider package, constructor, or registration bypasses the Party root or `workbookprojection`. | Audited current Go imports, exact static allowlists, source SQL ownership paths, app assemblies, test support, and empty predecessor directories. | Final boundary `.cartulary/test-results/20260824T033121Z-p1180184`; `git diff --check`; exact `rg`/`find` audits | Zero old provider paths, zero old Party calls in executable code, zero empty predecessor directories, and one intentionally retained forbidden `parties.NewStore(` string in the Imports static guard. | None. S-09 broad checks may still reveal a non-Party boundary regression elsewhere in the accumulated branch. | Reopen the owning slice for any broad failure; do not weaken the allowlist. |

| 2026-08-24T04:49:00Z | Codex S-09 validation and handoff | The Party root is the only application/contribution boundary; policy, source, and five provider families are private; Workbook owns transport translation. | Final Party root/internal packages, application assembly, boundary manifest, supporting-owner callers, and predecessor searches. | `make backend-module-boundary-check` at `.cartulary/test-results/20260824T043218Z-p2853901`; `make check` 655/655 at `.cartulary/test-results/20260824T044416Z-p3048348`; final searches and `git diff --check` | No production old provider import, old constructor, Party HTTP/API dependency, dual registration, or compatibility package remains. Historical tracker text and static forbidden-symbol sentinels are the only expected predecessor tokens. | None. | Preserve root-only contribution imports and the `workbookprojection` cross-owner contract during review. |

### Frontend module boundary

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-23T20:02:58Z | Codex planning inspection | Frontend Party behavior is an indirect consumer of generic Workbook/view contracts; no frontend move or direct grid-vendor breach is supported. | Inspected Party-link workflow, mutation command ports, Workbook model/policies, view-contract consumers, grid-adapter import boundary; touched only this tracker. | `rg`, `sed` | Frontend contract risks recorded; grid integration remains adapter-owned. | None unless a later slice changes frontend code or selectors. | Preserve browser sentinel and run conditional frontend checks only if frontend changes. |
| 2026-08-23T21:17:56Z | Codex NLSpec-style tracker revision | Frontend ownership remains unchanged; Party failures stay typed until Workbook translation and grid-vendor integration remains adapter-owned. | Reused the prior frontend inspection and current view-contract evidence; touched only this tracker. | `rg`, `sed` | No frontend relocation or selector change is proposed; browser evidence remains mandatory for route/view changes. | None beyond backend owner adoption; frontend checks become required only if frontend files change. | Preserve the browser sentinel through S-07 and run conditional frontend checks only on a frontend diff. |
| 2026-08-23T22:20:42Z | Codex S-01 machine projections | The generated shared view contract now marks exactly the six optional writable Party fields clearable; no authored frontend source changed. | Changed the authored Party view schema and regenerated `packages/view-contracts/src/generated/view-contract-projection.ts`. | `make generate`, `make generate-drift` | Generated parity is exact; frontend runtime behavior is deferred to the generic contract consumer and later end-to-end validation. | No frontend-specific blocker; branch-wide runtime remains non-deployable through S-04. | Preserve these semantics while S-02 implements backend match failures. |

| 2026-08-23T23:28:16Z | Codex S-02 active claims and matching | No authored frontend file or selector changed; Workbook remains the sole HTTP translator for Party conflicts. | Generated protocol error projections changed downstream of the authored error registry; no authored frontend source changed. | Party and Workbook owner slices include the webserver-backed Party sentinel; generate/drift/policy checks pass. | Real runtime-role Party create now succeeds and value-free `409 party_match_conflict` translation is covered. Frontend type/unit/import checks were not required because no authored frontend code changed. | Global branch remains non-deployable until the S-04 bundle cutover; no frontend-specific blocker exists. | Keep frontend untouched in S-03 unless generated view-contract consumption exposes a real authored-code change. |
| 2026-08-24T00:10:38Z | Codex S-03 field/timezone/hash cutover | No authored frontend code, selector, or public Party member changed; the generic Workbook wire remains stable. | Generated contract artifacts changed only through authored timezone/migration inputs; no authored frontend file changed. | Complete Workbook focused/service-backed slices; generation/drift/policy checks | HTTP create/patch/conflict behavior remains transport-owned; optional null clears and normalized errors pass existing wire tests. Frontend type/unit/import checks were skipped because there is no authored frontend diff. | Branch-wide non-deployability is now limited to S-04 Bundle runtime parity. | Keep frontend unchanged during S-04 unless a real bundle-facing authored frontend consumer is discovered. |
| 2026-08-24T01:23:55Z | Codex S-04 Incident Bundle v3 cutover | No authored frontend file, selector, public Party response, or browser-owned bundle behavior changed. | Backend source-port/catalog/runtime/tests and generated backend contract projections only; no authored frontend path changed. | Complete changed backend owner slices and generated/static checks | Frontend type/unit/import checks were skipped because the diff contains no authored frontend code. The global wire cutover is an operator portability boundary, not a Workbook UI change. | No frontend-specific blocker; RB-004 is harness-only. | Keep frontend unchanged in S-05 unless moving a test exposes a genuine frontend owner assertion. |
| 2026-08-24T02:06:39Z | Codex S-05 harness accounting cutover | No authored frontend file, selector, browser row, or public response changed. | Backend tests and harness inputs only; generated execution topology changed downstream. | Complete Workbook focused/service-backed slices and harness/evidence-class gates | Existing Workbook wire/auth evidence remains active once and passes; frontend type/unit/import checks were skipped because no authored frontend source changed. | No frontend-specific blocker. S-06/S-07 must preserve wire behavior while moving transport-neutral Party internals. | Keep frontend unchanged unless the structural slices touch authored frontend code. |

| 2026-08-24T02:55:49Z | Codex S-06 deep Party facade cutover | No authored frontend file or public Workbook wire contract changed. | Backend Party/Workbook/Imports code, tests, harness, and boundary inputs only. | Complete Workbook focused/service-backed slices and exact idempotency payload adapter tests | Workbook still selects public status/envelope once at its edge; all 65 focused and 37 service-backed rows pass. Frontend checks were skipped because no authored frontend source changed. | No frontend-specific blocker. S-07 must preserve the same envelopes while moving remaining admission/error construction. | Keep frontend unchanged unless S-07 unexpectedly touches authored frontend code. |

| 2026-08-24T03:25:54Z | Codex S-07 provider and transport cutover | No authored frontend file or public Workbook response changed; translation moved only between backend ownership boundaries. | Backend Party/Workbook/provider/assembly code and tests only. | Workbook focused/service-backed roots `.cartulary/test-results/20260824T031120Z-p772663` and `.cartulary/test-results/20260824T031338Z-p830560` | All create/patch/conflict statuses, payloads, closed failures, authentication, authorization, CSRF, and publication behavior pass. Frontend checks were skipped because no authored frontend source changed. | No frontend-specific blocker. | Keep frontend untouched in S-08 unless the stale-caller sweep discovers an authored frontend dependency. |

| 2026-08-24T03:31:56Z | Codex S-08 caller/manifest/generated sweep | No authored frontend source or generated frontend contract required an S-08 change. | Audited frontend/package references to Party final identities; no stale dependency found. | Current generated/static checks plus repository searches | Frontend checks remain intentionally deferred because no authored frontend file changed; S-09 will run diff-triggered frontend checks only if that fact changes. | None. | Preserve this skip rationale in S-09 unless the final diff gains an authored frontend file. |

| 2026-08-24T04:49:00Z | Codex S-09 validation and handoff | No authored frontend source moved or changed; generated protocol/view projections and the new Party conflict envelope are compatible with the existing Workbook UI. | Generated TypeScript contract outputs plus Workbook browser/runtime integration; no authored `apps/web` edit. | Browser 58/58 `.cartulary/test-results/20260824T042723Z-p2794242`; frontend typecheck `.cartulary/test-results/20260824T043223Z-p2854327`; unit 390/390 `.cartulary/test-results/20260824T043237Z-p2854861`; import boundary `.cartulary/test-results/20260824T043408Z-p2889401` | Authentication, membership/role authorization, CSRF, HTTP envelopes, and WebSocket publication remain Workbook-owned and pass end to end. | None. | No frontend migration is required; carry generated projections with the atomic backend cutover. |

### Contract and codegen

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-23T20:02:58Z | Codex planning inspection | Party view, import, projection, portability, revision, recovery, OpenAPI, protocol, and generated surfaces are mapped; generated roots remain downstream. | Inspected authored contract inputs, schema ownership, generated references, and boundary manifests; touched only this tracker. | `rg`, `jq`, `sed`, filtered `make help-all` | Owner-input-first generation and drift commands recorded. | RB-002 and RB-003 may require authorized owner-input changes. | Update authored inputs first and use `make generate`; never hand-edit generated files. |
| 2026-08-23T21:17:56Z | Codex NLSpec-style tracker revision | Proposed machine identities and clean-cutover versions are exact but non-authoritative pending owner adoption. | Inspected Party view schema, error/import schemas, route idempotency, Incident Bundle source-port/catalog grammar, Party table, and `tzdb-2026c` provenance; touched only this tracker. | `sed`, `rg`, `jq` | Closed clearability, timezone, hash, replay disposition, bundle v3/catalog-major 2, ten-member Party row, and invariant precedence; generated roots remain owner-input-first. | RB-002, RB-003, and S-00 owner adoption. | Adopt owner text, author projections in S-01, then run `make generate`; never hand-edit generated roots. |
| 2026-08-23T22:02:24Z | Codex S-00 owner adoption | Party field/hash/failure contracts and the global Bundle v3 source contract are adopted; generated/runtime projections are intentionally stale until S-01. | Changed Core 01 and Appendix F; reviewed the authored view, Imports error, source catalog, row-schema, timezone, and generator input boundaries without changing them. | `rg`, `sed`, `git diff --check`; independent review GO | Fixed field registry identity, six optional clears, packaged `iana.tzdb.2026c`, RFC-8785 hash preimages, exact Workbook/Imports failures, catalog v4/major 2/version 3, Party schema identity, zero-row behavior, and five invariants. | Expected projection/runtime parity failures are deferred only to S-01 through S-04; no compatibility input is authorized. | Author authored registries, schemas, catalogs, corpora, and traceability fixtures in S-01; generate rather than hand-edit outputs. |
| 2026-08-23T22:20:42Z | Codex S-01 machine projections | Owner-derived machine inputs and generated projections are current. | Added `contracts/parties/**`, `contracts/string-contracts/**`, Party row schema, catalog v4 schema, six schema attachments, two contract families, generator projections, and regenerated downstream artifacts; removed catalog schema v3. | Required S-01 Make targets plus `rg` predecessor audits and `git diff --check` | Field registry, normalization corpus, safe-error fixture, reviewed 578-member timezone snapshot/digest after S-03 alias correction, exact Party row, v4/major2/version3 catalog, and AC-561..563 traceability are projected without drift. | Runtime parity is explicitly deferred to S-02 claims/errors, S-03 field/hash admission, and S-04 bundle execution. | Start S-02 only after the Markdown checkpoint records S-01 as DONE. |

| 2026-08-23T23:28:16Z | Codex S-02 active claims and matching | Migration 00039, DDL ownership, current Recovery generation, Party failure reasons, and generated projections are coherent and drift-free. | Changed migration history/schema-object ownership generators and manifests; Recovery catalog/generation/binding/current journal fixtures; authored error registry; generated Go/TypeScript/topology projections; clarified the owner rollback reason registry under REQ-02-271/AC-561. | `make generate`, `make generate-drift`, `make generated-artifact-policy-check`, `make json-shape-check`, `make migration-drift`, migration scratch apply | Final roots are recorded in the S-02 row. Historical Recovery generations remain byte-for-byte frozen; only the current generation advanced for the new rebuildable table. | S-01 field/timezone projections are not yet runtime-consumed, and Incident Bundle runtime remains behind catalog v4 until S-03/S-04. | Consume generated Party policy and author migration 00040 in S-03; never edit generated roots directly. |
| 2026-08-24T00:10:38Z | Codex S-03 field/timezone/hash cutover | The reviewed `iana.tzdb.2026c` projection contains 578 exact Zone/Link names after removing abbreviation/POSIX aliases; migration history is current through v40. | Changed authored timezone registry/provenance, generator outputs, migration owner inputs/manifests, and current DDL hash baselines; added `00040`. | Final generate/drift/policy/JSON/migration-drift roots in S-03 row | Generated lookup/digest parity and schema shapes pass; v40 hash is `7609c33e820cb85080a4c48279043bc020b7b15a888325ff6ca437431e365f94`. No generated root was hand-edited. | Catalog v4 inputs are owner-current but Incident Bundle runtime is still S-04 work. | Change all source descriptors/runtime together in S-04 and regenerate once. |
| 2026-08-24T01:23:55Z | Codex S-04 Incident Bundle v3 cutover | Authored catalog v4, contract major 2, path version `[3]`, Party row schema, bundle fixtures, and runtime descriptors are exact and current. | Changed Revisions version input, bundle fixture coverage labels and row-schema descriptions, the Core manifest example, source descriptors, and generated Revisions projection through `make generate`. | `make generate`, `make generate-drift`, generated-artifact policy, JSON shape, migration drift | Generation is reproducible and drift-free. No generated root was hand-edited; version 2 remains only in explicit negative tests. | Harness ownership for the new causal Party tests remains S-05. | Update authored verification/test-family inputs and regenerate in S-05. |
| 2026-08-24T02:06:39Z | Codex S-05 harness accounting cutover | Authored test-family, row-migration, and fixture-policy inputs are current; generated topology projects the nine new rows exactly. | Changed four test-family files, `tools/test_catalog_row_migrations.json`, `tools/postgres_fixture_policy_registry.json`, and the authored harness fixture-count assertion; regenerated declared outputs only through `make generate`. | Generate `.cartulary/test-results/20260824T014147Z-p3563954`; drift/policy/JSON roots `.cartulary/test-results/20260824T015720Z-p4076016`, `.cartulary/test-results/20260824T015732Z-p4078949`, `.cartulary/test-results/20260824T015737Z-p4079404`; target-plan digest `sha256:f64f26c2d2120b00536f77eb12364027cdca9cb8e51bc0a0f23343500deca106` | Catalog, fixture counts, generated topology, row migrations, selectors, and JSON schemas agree. No generated root was hand-edited. | Structural source movement in S-06/S-07 may require later authored boundary/topology regeneration in S-08. | Preserve authored-input-first generation during S-06 and defer global cleanup to S-08. |

| 2026-08-24T02:55:49Z | Codex S-06 deep Party facade cutover | The Party owner now has 11 immutable rows, including direct composition/export-surface and route-idempotency semantics; generated topology and fixture policy are current. | Added two Party family rows and Workbook assembly idempotency test; updated the PostgreSQL transaction approval/count and generated topology; no adopted product contract changed. | Format `.cartulary/test-results/20260824T024809Z-p424880`; generate/drift `.cartulary/test-results/20260824T024439Z-p416275` and `.cartulary/test-results/20260824T024453Z-p419201`; generated policy/JSON/harness `.cartulary/test-results/20260824T024506Z-p422164`, `.cartulary/test-results/20260824T024618Z-p422969`, and `.cartulary/test-results/20260824T024624Z-p423518`; `make explain-test-owner OWNER=module.parties` reports 11 rows | Authored rows, 87 PostgreSQL transaction fixtures, generated topology, and harness schemas agree; no generated root was hand-edited. | S-07 source movement may require boundary manifest edits but should not require new product contracts or harness identities. | Preserve both new immutable rows and update only authored generator inputs if S-07 changes topology. |

| 2026-08-24T03:25:54Z | Codex S-07 provider and transport cutover | Authored Party row selectors now follow the internal Incident Bundle/rollback test locations, and generated topology matches without changing row identities. | Updated `tools/test_families/module.parties.json`, backend boundary policy, projection/reporting boundary assertions, source-ownership paths, and generated execution topology; added neutral `strictjson.DecodeObject`. | Generate `.cartulary/test-results/20260824T030911Z-p689764`; drift/policy/JSON/harness `.cartulary/test-results/20260824T032421Z-p1146645`, `.cartulary/test-results/20260824T032432Z-p1149598`, `.cartulary/test-results/20260824T032436Z-p1150047`, and `.cartulary/test-results/20260824T032443Z-p1150564` | Immutable row IDs, fixture capabilities, provider paths, static policy, and generated topology agree; no generated root was hand-edited. | S-08 must verify no stale predecessor source path remains in catalogs or generated indexes beyond historical tracker prose. | Sweep authored inputs first, regenerate, then audit generated/static parity. |

| 2026-08-24T03:31:56Z | Codex S-08 caller/manifest/generated sweep | Current catalog is v4/major 2 with every family and special-consumer path exactly `[3]`; row/topology and recovery/source projections are current. | Regenerated from authored inputs, audited schema attachments, source catalog, recovery catalogs, boundary policy, test rows, row migration ledger, and generated topology. | Generate `.cartulary/test-results/20260824T033048Z-p1173400`; drift/policy/JSON `.cartulary/test-results/20260824T033100Z-p1176323`, `.cartulary/test-results/20260824T033112Z-p1179275`, `.cartulary/test-results/20260824T033115Z-p1179709`; target-plan digest `sha256:331ebafe21f3e8bce91567bcbdc9207c0976836b22fffe271c8f7b434326bd24` | Zero current v3 catalog-schema identity, zero non-v3 path, and no active retired row. The retired Party row appears only once in `tools/test_catalog_row_migrations.json`, as required. | None. | S-09 repeats generation/static drift after focused/browser evidence. |

| 2026-08-24T04:49:00Z | Codex S-09 validation and handoff | Adopted Core owners, Party/string/bundle/error/import/recovery/revision contracts, and generated projections are aligned on claims, normalization, tzdb 2026c, hash v1, and Bundle v3/catalog v4/major 2. | Owner specs, Appendix F, authored contract/schema/catalog inputs, generator sources, declared generated roots, migrations, and final identity searches. | Drift `.cartulary/test-results/20260824T043139Z-p2847118`; generated policy `.cartulary/test-results/20260824T043154Z-p2850077`; JSON `.cartulary/test-results/20260824T043159Z-p2850526`; migration `.cartulary/test-results/20260824T043206Z-p2850983`; telemetry 6/6 `.cartulary/test-results/20260824T044139Z-p3025391`; full check `.cartulary/test-results/20260824T044416Z-p3048348` | Generated output matches authored owners. Every active bundle path is `[3]`; no v2 success reader, old Party hasher, or non-clearable optional Party projection remains. | None. | Apply migrations 00039 then 00040 and deploy the generated/runtime cutover together; do not introduce a converter or dual reader. |

### Tests and harness

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-23T20:02:58Z | Codex planning inspection | Current Party owner has four rows: one unit, two Postgres integration, and one browser row; ownership/evidence gaps are documented. | Inspected Party and collaborator tests, `contracts/verification/owners/module.parties.json`, and `tools/test_families/module.parties.json`; touched only this tracker. | `make task-guide ROLE=module-author OWNER=module.parties`; `make explain-test-owner OWNER=module.parties`; `jq`; `rg` | Both read-only Make discovery commands passed and reported four rows, three service-backed. No test or validation suite was run. | RB-004. | Split tests by normative postcondition, add direct portability coverage, update authored rows, and regenerate. |
| 2026-08-23T21:17:56Z | Codex NLSpec-style tracker revision | Test-design and ownership migration are decision-complete; no executable tests or harness inputs changed. | Inspected Party verification/family manifests, mixed reference tests, rollback-provider tests, projection contribution tests, and Imports/Incident Bundle tests; touched only this tracker. | `sed`, `rg`, `jq`, `make lint-markdown` | Added `PTR-AC-101` through `PTR-AC-404`, semantic owner mapping, causal portability sentinel, immutable-row migration, and retained-evidence rules. Markdown lint passed; no product suite ran. | RB-004 remains blocked pending authored test/harness work. | Allocate new row IDs from live catalogs in S-05, remove the predecessor atomically, regenerate, and run complete owner slices/gates. |
| 2026-08-23T22:02:24Z | Codex S-00 owner adoption | Binary Core acceptance now owns Party matching/claims, fields/timezone/hash, and v3 portability outcomes; harness accounting remains correctly deferred to its existing owner and S-05. | Changed Core 04 and Appendix F; reviewed every `PTR-AC-101` through `PTR-AC-308` against AC-561 through AC-563. | Primary review plus independent second-implementer review; `git diff --check` | Added direct concurrency, lifecycle, normalization, hash, replay-cutover, v3 corpus, invariant, atomicity, and no-v2-path acceptance with bidirectional requirement mapping. | Executable evidence does not exist yet; S-01 authors fixtures, S-02 through S-04 implement product behavior, and S-05 repairs harness ownership. | Begin S-01 only after the Markdown checkpoint; do not claim runtime conformance from owner prose. |
| 2026-08-23T22:20:42Z | Codex S-01 machine projections | Acceptance fixtures now encode field normalization, timezone membership, safe failures, and Incident Bundle traceability; executable product evidence is still owned by later slices. | Added normalization/safe-error corpora and AC-561..563 traceability; updated schema attachments and generated topology input hashes. | `make json-shape-check`, `make generate-drift` | Fixture and schema infrastructure passes; no test row was reassigned or double-counted in S-01. | RB-004 correctly remains for S-05; PTR acceptance remains unproven until S-02 through S-05. | Add causal claims/matching tests in S-02 without preempting the S-05 accounting migration. |

| 2026-08-23T23:28:16Z | Codex S-02 active claims and matching | Direct causal claims/matching tests exist and all currently routed affected-owner slices pass; harness reassignment remains correctly deferred to S-05. | Added Party migration/concurrency/safe-error/recovery integration tests, lifecycle tests, rollback collision evidence, and Imports safe-detail characterization; changed existing Party expectations from case-insensitive external references to owner-required exact case. | Final owner slices and direct `go test ./internal/modules/parties -run TestPartyActiveKeyClaims -count=1`; generated/static checks | Concurrent same-email creation converges; preflight is bounded/no-winner; cross-key, patch, restore, rollback, recovery, and safe-error rejection effects are proven. Direct package diagnostics passed in 67.909 seconds. | New direct tests are deliberately not claimed as retained harness evidence until S-05 allocates immutable rows; RB-004 remains. | S-03 adds normalization/hash causal tests without modifying test-row ownership early. |
| 2026-08-24T00:10:38Z | Codex S-03 field/timezone/hash cutover | Causal field, timezone, hash, semantic no-op, and replay-disposition evidence exists; current owner accounting is unchanged until S-05. | Expanded Party admission tests; added generated-corpus policy tests, RFC-8785 vectors, v40 Up/Down/preservation evidence, email equality no-op/zero-effect assertion; updated caller fixtures to immutable admissions. | All focused/service-backed roots in S-03 row; direct canonical-JSON package diagnostic passed | `PTR-AC-201` through `PTR-AC-208` are behaviorally covered and affected owners pass. The initial migration-owner run failed only on the expected canonical DDL hash, which was rebaselined and then passed `9/9`. | New assertions remain deliberately unrouted until S-05 assigns immutable row ownership; RB-004 remains. | Add direct v3 portability rows and repair all ownership/accounting in S-05 after S-04 behavior passes. |
| 2026-08-24T01:23:55Z | Codex S-04 Incident Bundle v3 cutover | Direct causal Party portability and safe public-failure tests exist; existing owner rows pass, but the new tests are deliberately not retained evidence until S-05 assigns immutable rows. | Added Party preparation/invariant unit coverage, exact-state/claims/failure integration coverage, and Incident Bundles public failure/atomicity coverage; strengthened full catalog parity and corrected stale source-owner version assertions. | Direct Party and Incident Bundles diagnostics; fourteen focused and fourteen service-backed owner slices; source-catalog parity | `PTR-AC-301` through `PTR-AC-308` are behaviorally covered. Focused roots, in owner order Parties through Saved Views, are `.cartulary/test-results/20260824T004159Z-p2317914`, `.cartulary/test-results/20260824T004247Z-p2357801`, `.cartulary/test-results/20260824T004344Z-p2373838`, `.cartulary/test-results/20260824T004621Z-p2435975`, `.cartulary/test-results/20260824T004705Z-p2451827`, `.cartulary/test-results/20260824T005150Z-p2511568`, `.cartulary/test-results/20260824T005508Z-p2583697`, `.cartulary/test-results/20260824T005813Z-p2633790`, `.cartulary/test-results/20260824T005856Z-p2649819`, `.cartulary/test-results/20260824T005943Z-p2689983`, `.cartulary/test-results/20260824T010058Z-p2740028`, `.cartulary/test-results/20260824T010154Z-p2782641`, `.cartulary/test-results/20260824T010237Z-p2822249`, and `.cartulary/test-results/20260824T010341Z-p2868299`. Service-backed roots in the same order are `.cartulary/test-results/20260824T010456Z-p2917099`, `.cartulary/test-results/20260824T010542Z-p2956310`, `.cartulary/test-results/20260824T010637Z-p2972187`, `.cartulary/test-results/20260824T010800Z-p3016318`, `.cartulary/test-results/20260824T010835Z-p3032066`, `.cartulary/test-results/20260824T011308Z-p3088334`, `.cartulary/test-results/20260824T011502Z-p3141728`, `.cartulary/test-results/20260824T011546Z-p3157640`, `.cartulary/test-results/20260824T011624Z-p3174726`, `.cartulary/test-results/20260824T011711Z-p3214214`, `.cartulary/test-results/20260824T011825Z-p3263032`, `.cartulary/test-results/20260824T011921Z-p3304780`, `.cartulary/test-results/20260824T012004Z-p3344022`, and `.cartulary/test-results/20260824T012108Z-p3388257`. | RB-004 remains: causal tests must be routed once by semantic owner, and mixed predecessor accounting must be removed. | Perform the S-05 assertion split and immutable-row cutover. |
| 2026-08-24T02:06:39Z | Codex S-05 harness accounting cutover | `PTR-AC-401` through `PTR-AC-404` are retained causal evidence; the mixed predecessor is retired and no assertion is double-counted. | Added nine immutable semantic rows across Parties, Evidence, Tasks/Decisions, and Projections; renamed the Party reference integration test accurately; added direct projection-source coverage; updated fixture policy and row migration. | Owner explanations for Parties/Evidence/Tasks/Workbook/Revisions/Projections; focused roots `.cartulary/test-results/20260824T014258Z-p3582970`, `.cartulary/test-results/20260824T014443Z-p3662288`, `.cartulary/test-results/20260824T014718Z-p3761111`, `.cartulary/test-results/20260824T014857Z-p3840214`, `.cartulary/test-results/20260824T015019Z-p3872650`, `.cartulary/test-results/20260824T015455Z-p3987014`; service-backed roots `.cartulary/test-results/20260824T014349Z-p3622764`, `.cartulary/test-results/20260824T014601Z-p3712114`, `.cartulary/test-results/20260824T014807Z-p3800821`, `.cartulary/test-results/20260824T014938Z-p3856557`, `.cartulary/test-results/20260824T015239Z-p3929971`, `.cartulary/test-results/20260824T015604Z-p4031569`; harness/backend gates `.cartulary/test-results/20260824T015835Z-p4081427`, `.cartulary/test-results/20260824T020208Z-p4110194`, `.cartulary/test-results/20260824T020258Z-p4126724` | Every active row produced one result; a Party invariant sentinel causally fails its Party portability row; unit/integration/security projections include the new rows; stale predecessor search is empty outside the closed row-migration record. Initial failures at `.cartulary/test-results/20260824T013545Z-p3460602`, `.cartulary/test-results/20260824T013746Z-p3516554`, `.cartulary/test-results/20260824T015757Z-p4080426`, and `.cartulary/test-results/20260824T015933Z-p4082976` exposed missing fixture dependency, multi-statement fixture setup, stale fixture totals, and a stale migration-evidence golden; all were corrected before final passes. | None for S-05; RB-004 is resolved. | Preserve row identities and ownership while S-06 changes internal structure. |

| 2026-08-24T02:55:49Z | Codex S-06 deep Party facade cutover | Constructor, exported-surface, semantic idempotency, and all affected-owner evidence pass after removal of the shallow facade. | Added private source tests/ports, exact root declaration lock, dependency-null matrix, semantic replay persistence tests, and updated all Party caller fixtures to admitted commands with `ActorUserID`. | Party focused/service roots `.cartulary/test-results/20260824T024856Z-p445217` and `.cartulary/test-results/20260824T024948Z-p485295`; Workbook `.cartulary/test-results/20260824T025038Z-p524934` and `.cartulary/test-results/20260824T025255Z-p582377`; Imports `.cartulary/test-results/20260824T023726Z-p208294` and `.cartulary/test-results/20260824T023841Z-p251004`; Revisions `.cartulary/test-results/20260824T023957Z-p292729` and `.cartulary/test-results/20260824T024107Z-p337933`; Projections `.cartulary/test-results/20260824T024215Z-p382250` and `.cartulary/test-results/20260824T024259Z-p398838` | All affected owner rows pass. Initial failures at `.cartulary/test-results/20260824T022253Z-p4164376`, `.cartulary/test-results/20260824T022315Z-p4164960`, `.cartulary/test-results/20260824T022633Z-p4174344`, `.cartulary/test-results/20260824T022706Z-p4174984`, and `.cartulary/test-results/20260824T024341Z-p414982` exposed the moved view constant, an unused predecessor field, an incomplete projection fake, the intentionally empty surface allowlist, and an exact boundary path; each was corrected before final passes. | None for S-06. Provider contract suites remain S-07 validation. | Re-run every provider/destination owner affected by S-07 and retain exact descriptor/wire parity. |

| 2026-08-24T03:25:54Z | Codex S-07 provider and transport cutover | Provider, wire, and recovery behavior pass through final root contribution surfaces. | Moved Party provider tests with their implementations; updated root consumers and exact descriptor/type assertions; preserved all existing immutable rows. | Parties `.cartulary/test-results/20260824T030925Z-p692781` and `.cartulary/test-results/20260824T031018Z-p733053`; Projections `.cartulary/test-results/20260824T030713Z-p655095` and `.cartulary/test-results/20260824T031642Z-p903534`; Reporting `.cartulary/test-results/20260824T030802Z-p673324` and `.cartulary/test-results/20260824T031555Z-p887697`; Revisions `.cartulary/test-results/20260824T031723Z-p919654` and `.cartulary/test-results/20260824T031833Z-p965739`; Incident Bundles `.cartulary/test-results/20260824T031945Z-p1010086` and `.cartulary/test-results/20260824T032045Z-p1026204`; Recovery `.cartulary/test-results/20260824T032144Z-p1042171` and `.cartulary/test-results/20260824T032301Z-p1094874` | Descriptor parity, projection page limits/nulls/deleted filtering, reporting Party partitions, rollback presence/claims, delete blockers, v3 prepare/apply/validate, recovery identity, and exact Workbook envelopes all pass. | None for S-07. S-08 static sweeps may reveal stale non-executable identities but must not change these semantics. | Preserve every provider key/schema/source/snapshot ID while removing only stale identities in S-08. |

| 2026-08-24T03:31:56Z | Codex S-08 caller/manifest/generated sweep | Catalog and startup construction use only final Party contributions and active immutable rows. | Ran narrow code-backed catalogs for Projections, Revisions, Incident Bundles, Recovery, Workbook, and server startup composition. | Roots `.cartulary/test-results/20260824T032931Z-p1155642`, `.cartulary/test-results/20260824T033008Z-p1171382`, `.cartulary/test-results/20260824T033015Z-p1171773`, `.cartulary/test-results/20260824T033022Z-p1172181`, `.cartulary/test-results/20260824T033029Z-p1172579`, `.cartulary/test-results/20260824T033038Z-p1172980` | Each catalog/startup path constructs with final provider IDs and no duplicate/missing registration; generated plan has 655 units, 500 cacheable units, and 124 fixtures. | None. Final full-suite accounting belongs to S-09. | Re-run focused/service-backed owner closure, then browser and broad gates in S-09. |

| 2026-08-24T04:49:00Z | Codex S-09 validation and handoff | Owner-accounted evidence is causally complete: twenty-two focused/service-backed owner pairs, direct Party claim/portability/rollback/projection rows, browser, fast, and broad gates pass. | Authored verification/family/fixture/topology inputs, generated schedules, direct owner tests, and retained run artifacts named in S-09. | Owner roots are enumerated in S-09; browser 58/58 `.cartulary/test-results/20260824T042723Z-p2794242`; corrected fast 429/429 `.cartulary/test-results/20260824T044309Z-p3038591`; finalize `.cartulary/test-results/20260824T044357Z-p3045424`; check 655/655 `.cartulary/test-results/20260824T044416Z-p3048348` | One result exists per active row; no stale predecessor selector or retained result is active. The first broad run `.cartulary/test-results/20260824T043506Z-p2896534` causally found two omissions, both fixed before the green rerun. | None. Retained-run maintenance was skipped only because no valid pre-existing successful full warm-check root existed. | Use the final check root as handoff evidence; do not count the failed run or retired row as success evidence. |

### Security and authorization

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-23T20:02:58Z | Codex planning inspection | Workbook authenticates and rechecks incident access; Party facade checks incident-open state and uses auth storage for route idempotency, not Party-specific ACLs. | Inspected Workbook query/mutation routes, Party facade, Core 04 authorization sections, and Collaboration WebSocket route; touched only this tracker. | `rg`, `sed`, `jq` | Existing owner split recorded as `intentional/no_action`; authorization and event semantics are frozen. | Behavior corrections must not bypass route checks or leak record existence. | Add role, closed-incident, replay, and WebSocket characterization before moving dependencies. |
| 2026-08-23T21:17:56Z | Codex NLSpec-style tracker revision | Existing route authorization remains frozen; new Party and portability failures have closed safe-detail allowlists. | Reinspected Workbook failure translation, Imports owner-error envelopes, Party admission/idempotency, Incident Bundle public failure rules, and claim-data exposure risks; touched only this tracker. | `sed`, `rg`, `jq` | Specified non-retryable safe errors, forbidden value/topology disclosure, exact replay precedence, no partial effects, and operator-only duplicate preflight details. | Owner adoption and negative security evidence remain required. | Adopt Core 01/Core 04 mappings in S-00 and prove safe diagnostics/no-effects in S-02 through S-04. |
| 2026-08-23T22:02:24Z | Codex S-00 owner adoption | Safe Party conflict and Incident Bundle diagnostic surfaces are now owner-adopted; authorization ownership is unchanged. | Changed Core 01, Core 02, Core 04, and Appendix F; inspected current Imports owner-error shape before fixing exact nested member names. | `rg`, `sed`; independent review GO | Workbook exposes only `reason_code` and `conflicting_field_keys`; Imports retains its exact outer/owner envelope; portability diagnostics remain family/invariant-only; rejected mutations have zero effects. | Negative runtime evidence remains required in S-02 through S-04. | Project the error shapes in S-01, then implement and test the typed failures without moving Workbook authorization. |
| 2026-08-23T22:20:42Z | Codex S-01 machine projections | Safe Party-match reason and field-key allowlists are projected for Workbook and Imports without source values or storage topology. | Changed error and Imports contracts; added the Party safe-error fixture; regenerated protocol/import projections. | `make generate`, `make json-shape-check`, safe-token inspection | Closed error shapes and all three reason codes project successfully; authorization behavior itself is unchanged. | Runtime error construction/translation and rejection side-effect proof remain S-02/S-07 work. | Implement typed semantic conflicts in Parties and exact outer translation in Workbook/Imports during S-02. |

| 2026-08-23T23:28:16Z | Codex S-02 active claims and matching | Party conflict and migration diagnostics are closed, value-free, and least-privilege; existing Workbook auth/authorization/CSRF ownership is unchanged. | Changed typed Party/Workbook/Imports failure data, database grants, duplicate preflight, and rollback reason mapping. | Runtime-role browser sentinel; negative forbidden-token scan; Imports fail-closed safe-detail test; DDL privilege/parity tests; affected owner slices | Public errors expose only a closed reason and sorted Party field keys. Preflight exposes bounded record IDs/field keys/counts but no source values and never chooses a winner. Normalizers are executable by runtime/recovery; claim mutation remains behind security-definer routines. | S-03 must replace the temporary claim normalizer with the adopted generated field policy without widening privileges or accepting host tzdb input. | Implement S-03 admission/hash/replay cutover and repeat negative side-effect/security evidence. |
| 2026-08-24T00:10:38Z | Codex S-03 field/timezone/hash cutover | Admission is deterministic and host-independent; Party hashes cannot be caller-substituted; replay disposition is narrowly scoped. | Changed Party adapters/facade and private policy; added canonical JSON; added the data-only v40 migration and exact preservation test. | Safe rejection/no-effect tests, complete affected slices, migration test/drift, dependency and predecessor searches | Invalid fields/timezones never enter hashes or source state; email equality no-op leaves source/version/replay unchanged; v40 preserves every unrelated hash, payload, status, and timestamp. Workbook auth, membership, CSRF, and error translation remain unchanged. | S-04 must retain safe invariant-only diagnostics and atomic publication for bundle input. | Implement strict prepare/apply/validate phases without adding any compatibility reader. |
| 2026-08-24T01:23:55Z | Codex S-04 Incident Bundle v3 cutover | Untrusted Party bundle bytes are duplicate-aware, strictly typed, normalized through Party policy, and expose only the selected safe family/invariant on failure. | Changed Party preparation/apply/validation and Incident Bundles failure tests; no authorization, membership, CSRF, or public Party mutation surface changed. | Safe-diagnostic direct integration tests, cancellation/failure rollback tests, complete Incident Bundles and source-owner slices | Hostile values, IDs, paths, SQL topology, claims, and constraints do not reach terminal public diagnostics. Duplicate identity, shape, lifecycle, normalization, envelope, claim, and state failures are atomic. | Retained causal row ownership is S-05; security behavior itself is closed. | Route the safe-failure assertions to their normative owners without duplicating selectors. |
| 2026-08-24T02:06:39Z | Codex S-05 harness accounting cutover | Safe Party portability failure/atomicity assertions now have one retained Party-owned security row; authorization behavior remains unchanged. | Routed `TestPartyIncidentBundleFailuresAreClosedAndAtomic_Integration` with dedicated PostgreSQL/object-store dependencies; changed no auth, membership, role, CSRF, or public error implementation. | Complete Party/Workbook owner slices; public backend integration gate includes the dedicated security row | Closed failure families, atomic rollback, and value/topology redaction are now causally retained without borrowing Incident Bundles or Workbook ownership. | S-07 still moves HTTP translation physically into Workbook; semantics and authorization must remain exact. | Preserve the closed semantic error through the transport-neutral S-06 facade. |

| 2026-08-24T02:55:49Z | Codex S-06 deep Party facade cutover | Workbook owns route-idempotency storage encoding/status and semantic keep-saved translation; Parties sees only typed replay and Party row results. Existing auth, membership, role, CSRF, and incident-open responsibilities remain unchanged. | Changed Party commands/results and Workbook capability adapters; added hash-before-decode, stored-operation-kind, uniqueness, and semantic replay tests. | Complete Parties/Workbook slices; semantic idempotency row; searches for `authn.UserRecord`, HTTP status/payload, and concrete stores in Party production mutation/import code | Divergent hashes fail before payload decode, stored operation mismatch fails closed, uniqueness maps to the semantic client-transaction conflict, and no current-state evaluation precedes replay. | S-07 still owns physical relocation of remaining HTTP/API admission failure construction from Parties to Workbook. | Move transport translation once without changing the closed public error or authorization envelope. |

| 2026-08-24T03:25:54Z | Codex S-07 provider and transport cutover | Parties returns only `AdmissionError`, semantic mutation errors, and semantic results; Workbook constructs public mutation failures, statuses, payloads, and keep-saved translations exactly once. | Removed `net/http`/`httpapi` from Party production, added Party-to-Workbook semantic admission translation, and kept authentication/authorization/CSRF/incident-open responsibilities unchanged. | Complete Workbook/Party slices; exact production dependency searches; boundary gate | Unknown/duplicate/limit admission, replay conflicts, match conflicts, row conflicts, and keep-saved outcomes retain closed Workbook envelopes without exposing source values or storage topology. | No security blocker. S-09 still owns browser and broad end-to-end confirmation. | S-08 must not reintroduce transport vocabulary while cleaning manifests/callers. |

| 2026-08-24T03:31:56Z | Codex S-08 caller/manifest/generated sweep | No cleanup reintroduced a transport, auth, compatibility, or v2 success path. | Audited Party production imports, final constructor uses, bundle grammar, and negative/historical exceptions; changed no authorization or public error implementation. | Exact searches plus catalog/startup/static gates | The only `BundleVersion: 2` is the direct rejection test; the only old Party row is its retirement ledger; the only old constructor token is a static forbidden-symbol sentinel. | None. | S-09 browser and broad checks provide final security/integration confirmation. |

| 2026-08-24T04:49:00Z | Codex S-09 validation and handoff | Safe matching, claim, replay, and portability failures remain value-free and atomic; Party code has no transport/auth object coupling; Workbook authorization responsibilities are unchanged. | Claim migration/grants, semantic Party errors, Workbook/Imports translation, strict bundle preparation, recovery validation, browser and security rows, telemetry error-class registry. | All security-bearing owner pairs in S-09; browser `.cartulary/test-results/20260824T042723Z-p2794242`; telemetry `.cartulary/test-results/20260824T044139Z-p3025391`; broad `.cartulary/test-results/20260824T044416Z-p3048348` | Rejections have zero side effects and expose only closed reasons/sorted field keys or safe invariant families. `party_match_conflict` is classified as `concurrency_conflict`; auth, membership, role, CSRF, and incident-open checks remain at their adopted boundaries. | None. | Preserve least-privilege claim routines and Workbook-only public error translation during deployment review. |

### Open risks and next session

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-23T20:02:58Z | Codex planning inspection | Tracker is planning-complete; implementation is intentionally unauthorized. | Inspected repository status and all evidence named above; touched only this tracker. | Discovery commands above; one malformed quoted `rg` search failed, was corrected, and had no repository effect. | Four stable implementation blockers remain; repository was clean before the tracker write. | RB-001 through RB-004. | Authorize characterization and boundary-redesign work, beginning with S-00 and keeping S-06A/B/C separate. |
| 2026-08-23T21:17:56Z | Codex NLSpec-style tracker revision | Tracker is NLSpec-style planning-complete; no owner, runtime, test, schema, migration, generated, or harness change is authorized or performed. | Inspected the tracker and every additional evidence class named in Section 1; touched only this tracker. | Discovery commands above; `make lint-markdown` | Markdown lint passed at `.cartulary/test-results/20260823T212632Z-p842787`; four implementation blockers remain with closed planning dispositions. | RB-001 through RB-004; S-00 owner adoption is the first gate. | Authorize S-00 owner amendments, then follow S-01 through S-09 without compatibility paths. |
| 2026-08-23T22:02:24Z | Codex S-00 owner adoption | Owner adoption is complete after correction of independent-review findings; S-01 is the only next slice. Domain vocabulary unchanged. | Changed four owner/traceability documents and this tracker; no generated, runtime, migration, test, or harness file changed. | Owner searches, diffs, `git diff --check`, independent second-implementer GO; `make lint-markdown` | No owner contradiction remains; Markdown passed at `.cartulary/test-results/20260823T220407Z-p859024`. Appendix F has complete new forward/reverse navigation and updated profiles. | RB-001 through RB-003 are owner-unblocked but implementation-incomplete; RB-004 remains blocked until S-05. The branch is non-deployable. | Begin S-01. |
| 2026-08-23T22:20:42Z | Codex S-01 machine projections | S-01 is closed; PT-008 is DONE and PT-009 is READY. Domain vocabulary unchanged. | Authored and generated contract surfaces listed in the S-01 row; no production runtime, migration, or test-row change was made. | Required S-01 Make targets; `git diff --check`; `make lint-markdown` | Generation/drift/policy/JSON gates pass. Markdown passed at `.cartulary/test-results/20260823T222156Z-p882965`. Active catalog inputs contain only version 3, and no compatibility alias or v2 catalog schema remains. | RB-001 is now the active S-02 implementation gap; RB-002/RB-003 await S-03/S-04; RB-004 awaits S-05. | Begin S-02 active claims and matching. |

| 2026-08-23T23:28:16Z | Codex S-02 active claims and matching | S-02 is closed; PT-009 is DONE and PT-010 is READY. Domain vocabulary unchanged. | Claim/runtime/migration/recovery/error/test files named in the S-02 row; generated outputs; this tracker. | All S-02 validation and audits recorded above; checkpoint Markdown passed at `.cartulary/test-results/20260823T233001Z-p1712266`. | RB-001 is resolved with no compatibility path. Known branch-wide mismatch is limited to scheduled S-03 field/hash runtime and S-04 global Bundle v3 runtime. | RB-002 active; RB-003 and RB-004 scheduled. | Begin S-03 field, timezone, hash, and Party-only replay cutover. |
| 2026-08-24T00:10:38Z | Codex S-03 field/timezone/hash cutover | S-03 is closed; PT-010 is DONE and PT-011 is READY. Domain vocabulary unchanged. | Field/timezone/hash/runtime/migration/test files named in the S-03 row; generated outputs; this tracker. | All S-03 validations and audits recorded above; checkpoint Markdown passed at `.cartulary/test-results/20260824T001453Z-p2176058`. | RB-002 is resolved without an old hasher, dual comparison, caller override, or host tzdb. The only intentional owner/runtime divergence is the S-04 global bundle cutover. | RB-003 active; RB-004 scheduled. Branch remains non-deployable until S-04 closes. | Begin S-04. |
| 2026-08-24T01:23:55Z | Codex S-04 Incident Bundle v3 cutover | S-04 is closed; PT-011 is DONE and PT-012 is READY. Domain vocabulary unchanged. | Runtime/catalog/source/test/boundary files named in the S-04 row; generated Revisions projection; this tracker. | All S-04 validations and audits recorded above; checkpoint Markdown passed at `.cartulary/test-results/20260824T012623Z-p3445162`. Pre-closure failures at `.cartulary/test-results/20260824T002503Z-p2181532`, `.cartulary/test-results/20260824T002625Z-p2235306`, `.cartulary/test-results/20260824T004509Z-p2419049`, `.cartulary/test-results/20260824T005347Z-p2566222`, `.cartulary/test-results/20260824T005557Z-p2600219`, and `.cartulary/test-results/20260824T005710Z-p2616791` were related stale version/startup/boundary assertions and were corrected before the final passing roots. | RB-003 is resolved with no reader, converter, fallback, or partial source-family cutover. The exact no-v2 audit passes and every changed source owner closes. | RB-004 is active; the branch remains non-deployable until S-09. | Begin S-05 test and harness ownership/accounting repair. |
| 2026-08-24T02:06:39Z | Codex S-05 harness accounting cutover | S-05 is closed; PT-012 is DONE and PT-013 is READY. Domain vocabulary unchanged. | Test/harness/fixture/topology files named in the S-05 row; direct Projections source test; Operator golden; this tracker. | All S-05 owner/generation/harness/evidence-class validation recorded above; predecessor and selector searches | RB-004 is resolved without alias, duplicate selector, compatibility row, or retained predecessor result. All four implementation blockers are now resolved; the remaining work is the authorized structural and final-validation sequence. | The branch remains non-deployable until S-09. S-06 transaction/replay ordering and exported-surface removal are the next risks. | Begin S-06 deep Party facade extraction; do not start S-07 before its tracker checkpoint. |

| 2026-08-24T02:55:49Z | Codex S-06 deep Party facade cutover | S-06 is closed; WF-05 is DONE and PT-013 is IN_PROGRESS with only S-07 outstanding. Domain vocabulary unchanged. | Facade/source/assembly/test/harness/boundary files named in the S-06 row; this tracker. | All S-06 validation roots and predecessor audits recorded above | Transaction/replay order and public wire behavior are preserved through one semantic Party boundary; no bridge constructor/package or removed Store remains. | Branch remains non-deployable until S-09. S-07 risks are provider ID/query parity and exact one-time Workbook error translation. | Begin S-07; do not start S-08 before its tracker checkpoint. |

| 2026-08-24T03:25:54Z | Codex S-07 provider and transport cutover | S-07 is closed; PT-013 is DONE and PT-014 is READY. Domain vocabulary unchanged. | Provider/root/transport/assembly/test/harness/boundary files named in S-07; this tracker. | All S-07 roots and exact searches recorded above | No old provider import, old root constructor, direct Party HTTP/API surface, compatibility package, or duplicate registration remains in executable code. | Branch remains non-deployable until S-09. S-08 risk is stale identity in less-visible authored/generated manifests or empty predecessor paths. | Begin S-08 and do not begin S-09 until its tracker checkpoint passes. |

| 2026-08-24T03:31:56Z | Codex S-08 caller/manifest/generated sweep | S-08 is closed; PT-014 is DONE, WF-08/PT-015 are READY, and S-09 is the only remaining slice. Domain vocabulary unchanged. | Current repository inputs and generated outputs named above; empty directories; this tracker. | All S-08 validations and audits recorded above | No current predecessor or dual path remains; final broad validation can begin from a coherent cutover. | Branch remains non-deployable until S-09 completes. Any S-09 failure reopens its owning slice. | Run S-09 exactly in the user-prescribed order and finish the tracker handoff. |
| 2026-08-24T04:41:39Z | Codex S-09 corrective checkpoint | S-09 remains `IN_PROGRESS`; domain vocabulary unchanged. Focused, service-backed, browser, static, frontend, fast, and finalize gates passed before broad validation. | Corrected lowercase error construction in Party import/generation code; classified `party_match_conflict` in the authored telemetry error-class registry; updated this tracker. | First `make check` failed 653/655 at `.cartulary/test-results/20260824T043506Z-p2896534`; `make explain-run`; corrected `make lint-go`; corrected `make otel-conformance` passed 6/6 at `.cartulary/test-results/20260824T044139Z-p3025391`. | Failures were causal cutover omissions, not environmental or unrelated regressions. Both owning concerns now pass directly without compatibility behavior. | The corrected downstream fast/finalize/broad sequence has not yet passed, so PT-015 and WF-08 remain open. | Run checkpoint Markdown lint, then repeat downstream S-09 validation and complete all seven handoff areas only after a green broad check. |

| 2026-08-24T04:49:00Z | Codex S-09 validation and handoff | Implementation and validation are complete; domain vocabulary unchanged. | Final diff/status, compatibility-token searches, all changed files summarized in S-09, and this tracker. | Corrected fast/finalize/check roots plus `git diff --check` and exact predecessor/version searches | No unresolved implementation blocker, unjustified skip, alias, dual writer, fallback reader, v2 success path, deprecated active row, old hasher, or parallel registration remains. Expected historical tracker references, one static forbidden-constructor sentinel, and one v2 rejection test are not executable compatibility paths. | Operational assumption: pre-cutover Party replay rows and v2 bundles are disposable. If that assumption is false in a target environment, deployment must stop for new owner adoption; this implementation must not be weakened with compatibility behavior. | Review the atomic diff, apply migrations in order, and use `.cartulary/test-results/20260824T044416Z-p3048348` as the final broad evidence root. |

## 11. Open Questions and Blockers

No owner contradiction was discovered, so no `BLOCKED: owner contradiction` entry is present. S-00 adopted the required owner behavior after independent review, and S-01 through S-09 closed every runtime, projection, migration, executable-evidence, structural, and handoff gate.

| ID | Question or blocker | Why it matters | Needed authority or evidence | Current status |
| --- | --- | --- | --- | --- |
| RB-001 | The predecessor ambiguous Party lookup could create another Party and had no database-enforced active-key integrity. | Structural work must not preserve the defect or bypass the adopted error, concurrency, lifecycle, or replay behavior. | S-02 implementation and `PTR-AC-101` through `PTR-AC-120`; evidence roots in Sections 7 and 10. | RESOLVED — migration 00039, convergent claim-only matching, lifecycle/recovery maintenance, safe failures, and predecessor removal pass. |
| RB-002 | Party field admission, optional clearability, timezone admission, normalized forms, and request hashing were incomplete or inconsistent downstream of the adopted owner. | Acceptance, stored values, identity, no-op detection, revisions, conflicts, portable validation, and replay equivalence could diverge. | S-01/S-03 implementation and `PTR-AC-201` through `PTR-AC-208`; evidence roots in Sections 7 and 10. | RESOLVED — immutable policy values, packaged timezone membership, named hashes, caller-hash removal, and Party-only replay disposition pass. |
| RB-003 | The adopted Party source family now has an exact versioned row and global v3 cutover, but authored projections and runtime previously implemented v2 and incomplete invariants. | Without the atomic cutover, portable admission could diverge by source family, table topology, or host state and partially publish invalid incidents. | S-01/S-04 implementation and `PTR-AC-301` through `PTR-AC-308`; evidence roots in Sections 7 and 10. | RESOLVED — v3/major-2-only runtime, exact Party row and state/claim validation, causal invariants, atomic safe failures, and no-v2 executable audit pass. |
| RB-004 | Party harness ownership/accounting misclassifies mixed reference tests, lacks causal Party portability evidence, and incompletely accounts for rollback/projection tests. | Passing routed evidence does not prove each named postcondition or complete-owner closure. | Implement `PTR-REQ-401` and `PTR-REQ-402` through S-05; allocate immutable IDs from live authored catalogs; pass `PTR-AC-401` through `PTR-AC-404`. | RESOLVED — mixed accounting is retired; nine immutable semantic rows, exact fixture policy, causal sentinels, complete destination-owner slices, and public evidence-class gates pass without aliases or duplicate selectors. |

The following assumptions are explicit defaults, not open blockers:

- retained pre-production Incident Bundle version-2 files and pre-cutover Party replay rows are disposable;
- Party merge, automatic remediation, new Party fields, `custom_attrs`, phone normalization, directory synchronization, and frontend relocation are out of scope;
- if evidence disproves the disposability assumption, the affected slice MUST stop and add a blocker rather than introduce compatibility.

## 12. Binary Completion Criteria

- [x] Every file in `internal/modules/parties` is inventoried; no target file is marked out of scope.
- [x] Every discovered public contract risk has an owner and test posture in Section 4.
- [x] Current repository facts, adopted requirements, proposed owner amendments, and planned implementation obligations are explicitly distinguished.
- [x] Exact-match behavior, active claims, failure interfaces, Party field defaults, normalized values, timezone admission, request hashes, portable rows, invariant precedence, source-port phases, and harness migration have closed interfaces and explicit defaults.
- [x] Every proposed workflow has dependencies, validation, status, and a handoff exit criterion.
- [x] Every proposed slice identifies authority, dependencies, intended change, affected owners, contract risk, acceptance, validation, rollback, and a binary completion criterion.
- [x] The user-authorized behavior, public-contract, source-version, test, migration, and harness changes remain gated by applicable owner adoption and serial slice completion.
- [x] Validation commands are discovered from the public Make surface; no direct runner command was invented.
- [x] No owner contradiction was found. Any future contradiction must be recorded as `BLOCKED: owner contradiction` without choosing a side.
- [x] The framework/live-repository mismatch is recorded as a planning finding.
- [x] The analysis notes and NLSpec writing guide are identified as non-authoritative inputs and are not cited as proof of current behavior.
- [x] The live absence of Party `custom_attrs`, optional-field projection mismatch, global bundle-version grammar, and Network Flow-only tzdb ownership are recorded and resolved as planning findings.
- [x] All seven handoff areas preserve the prior session and contain a current session entry naming the next action.
- [x] The clean-cutover preference is explicit: no compatibility aliases, dual paths, dual writers, or deprecation layer.
- [x] The tracker records the user's implementation authorization and still forbids beginning a slice before its dependency and checkpoint pass.
- [x] RB-001 through RB-004 are planning-complete and implementation-resolved without compatibility paths: S-02 resolved RB-001, S-03 resolved RB-002, S-04 resolved RB-003, and S-05 resolved RB-004.
- [x] S-00 through S-09, WF-08, and PT-001 through PT-015 are complete; final broad validation passes 655/655 and the sole retained-run skip is justified.

### 12.1 Closure traceability

| Blocker | Tracker requirements | Required owner | Acceptance | Execution slice | Planning status | Implementation status |
| --- | --- | --- | --- | --- | --- | --- |
| RB-001 | `PTR-REQ-101` through `PTR-REQ-104` | Core 02 identity/claim semantics; Core 01 error/Imports/replay; Core 04 acceptance | `PTR-AC-101` through `PTR-AC-120` | S-00, S-01, S-02 | DONE | DONE — owner, projection, migration, runtime, recovery, safe-error, and scoped evidence closure pass. |
| RB-002 | `PTR-REQ-201` through `PTR-REQ-204` | Core 01 Party fields, timezone, hash; Core 02 identity reference; Core 04 acceptance | `PTR-AC-201` through `PTR-AC-208` | S-00, S-01, S-03 | DONE | DONE — owner, projection, immutable policy, packaged timezone, canonical hash, replay disposition, and scoped evidence closure pass. |
| RB-003 | `PTR-REQ-301` through `PTR-REQ-302` | Core 01 Incident Portability; Core 04 acceptance; coordinated source-owner projections | `PTR-AC-301` through `PTR-AC-308` | S-00, S-01, S-04 | DONE | DONE — owner, projection, v3-only runtime, exact Party portability, safe atomic failure, changed-owner closure, and no-v2 audit pass. |
| RB-004 | `PTR-REQ-401` through `PTR-REQ-402` | Existing Testing Harness NLSpec plus authored verification/family/topology inputs | `PTR-AC-401` through `PTR-AC-404` | S-05 | DONE | DONE — semantic rows, causal sentinels, fixture/topology parity, destination-owner closure, and public evidence-class gates pass with no active predecessor. |

**Tracker completion:** Planning, S-00 through S-09, WF-00 through WF-08, and PT-001 through PT-015 are complete. RB-001 through RB-004 are resolved with active claims, immutable field/timezone/hash policy, Bundle v3-only portability, causal harness ownership, root-only contributions, private providers, one-time Workbook transport translation, and clean generated/static/browser/owner/broad evidence. The complete cutover is ready for atomic review and handoff; no intermediate slice is deployable or releasable. Domain vocabulary unchanged.
