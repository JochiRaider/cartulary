# test-harness-backend> Module Refactoring Tracker and Handoff

## 1. Scope and Source Posture

- Target path: `tools/harness/backend`.
- Target label: `test-harness-backend>`.
- Normalized safe target ID: `test-harness-backend`.
- Output path: `docs/handoffs/test-harness-backend-module-refactor-tracker.md`.
- Planning-only status: this tracker records repository facts, architectural findings, workflow sequencing, and handoff state only.
- Allowed changes for this session: this tracker file only.
- Non-goals: no production code, tests, contracts, generated artifacts, package configuration, migrations, harness helper files, task-surface outputs, or baseline files are to be modified by this planning task.
- Source hierarchy: adopted subsystem NLSpecs for their named scope; Core 00 through Core 04 for implementation-conformance behavior; Core 05 only for claim-bearing timed or fixture-sensitive publication; domain vocabulary and implementation-support guides for terms and harness mechanics; current repository code and tests for implementation state; prior plans and handoffs as evidence only.
- Owner documents inspected: `docs/handoffs/cartulary_modular_refactor_planning_framework.md`; `AGENTS.md`; `docs/testing-harness-nlspec.md`; `docs/domain.md`; `docs/spec/00_document_set_status_and_precedence.md`; `docs/spec/01_architecture_storage_and_view_contracts.md`; `docs/spec/02_domain_model_schema_and_history.md`; `docs/spec/03_workbook_interaction_collaboration_and_workflows.md`; `docs/spec/04_security_deployment_and_conformance.md`; `tools/generated_artifact_policy.json`; `tools/task_surface_manifest.json`; `tools/execution_topology_manifest.json`.
- Repository files inspected: every file listed in Section 2, plus `Makefile`, `tools/harness/execution/make-node-tools.mjs`, `tools/harness/static-analysis/harness-import-boundary.mjs`, `tools/harness/static-analysis/backend-module-boundary-check-cli.mjs`, `tools/harness/static-analysis/go-govulncheck.sh`, `tools/harness/tests/test-harness-contracts.mjs`, `tools/harness/scheduler/adapters/backend.mjs`, `tools/harness/scheduler/adapters/schedule-context.mjs`, and `tools/harness/output/test-output/target-start-stats.mjs`.
- Implementation requires a later authorized task. This tracker does not authorize movement, deletion, renaming, schema changes, Make target changes, generated output refreshes, or behavior changes.

Architectural source posture: `tools/harness/backend` exists, but the directory name is not evidence of a permanent product module boundary. Testing Harness NLSpec Section 4.1 states that `tools/harness/**` implementation paths are helper realization details, while public Make targets, `command_id` values, schema IDs, output and failure contracts, and retained artifact paths remain the durable compatibility surface. Current inspection found backend harness target planning, shard scheduling, Go execution, migration/schema drift support, duration-baseline support, security-finding normalization, and characterization tests. It did not find legitimate workbook orchestration behavior owned here for timeline, workbook projections, revisions, collaboration, imports/tabular ingest, entities/indicators, evidence, links, saved views, view contracts, frontend shell/controller state, or grid-adapter/vendor integration.

## 2. Current-State Repository Inventory

| Path | Current responsibility | Exported/public symbols or package surface | Inbound callers | Outbound dependencies | Tests touching it | Generated artifacts or contracts touched | Suspected target owner module | Risk level | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `tools/harness/backend/backend-shard-plan.mjs` | Adapter from backend target rows to Go shard plans. | `targetPlanRowsForGoShards`, `collectGoShardPlan`, `collectGoShardsForTarget`. | Duration artifacts, shard CLI, duration coverage CLI, scheduler backend adapter, harness-contract tests. | `go-shard-plan.mjs`, `target-plan.mjs`. | `test-harness-contracts.mjs`, `test-run-go-target.sh`, `test-go-test-duration-baselines.sh`. | `cartulary.go_shard_plan.v3` data and scheduler inputs. | Backend shard-plan facade. | Medium | Legitimate thin facade; preserve row discovery behavior. |
| `tools/harness/backend/backend-target-plan.mjs` | Re-export facade for phase-accounting and target-plan helpers. | Re-exports manifest helpers plus `collectTargetPlanRows`, `findTargetDescriptor`. | Target runner, target-plan coverage CLI, Postgres fixture budget CLI. | `phase-accounting/phase-manifest.mjs`, `target-plan.mjs`. | `test-run-go-target.sh`, harness-contract import-boundary fixture. | Backend target-plan rows. | Backend target-plan facade. | Medium | Thin facade, but it exposes phase-accounting internals to backend callers. |
| `tools/harness/backend/build-go-artifact.sh` | Make-owned Go binary build wrapper used through readiness/build cache. | Shell entrypoint requiring `GO`, `BUILD_OUTPUT`, `BUILD_PACKAGE`, `BUILD_LABEL`, `CARTULARY_TEST_TARGET`, `RUN_PHASE_SCRIPT`, cache dirs. | `Makefile`, task/execution manifests, build targets `build-server`, `build-migrate`, `build-operator`, internal `testservices-build`. | `tools/harness/execution/run-phase-common.sh` indirectly through `RUN_PHASE_SCRIPT`; Go toolchain. | Build wrapper smoke covered indirectly by harness/build checks. | Build-artifact cache schema `cartulary.cache.build_artifact.v1`; retained summaries. | Build-readiness backend adapter. | Medium | Preserve build output digest and phase wrapper semantics. |
| `tools/harness/backend/check-migrations.sh` | Migration input validation and scratch Postgres apply verification. | Shell CLI `check-migrations.sh [--mode input|scratch|all]`. | Public `migration-drift`; internal `migration-input-drift`, `migration-scratch-apply`; task-surface manifests; tests. | Docker Compose, dev-services readiness, `cmd/migrate` or `CARTULARY_MIGRATE_BIN`, `migration-history-cli.mjs`. | `tests/test-check-migrations.sh`. | `tools/migration_history_manifest.json`; migration drift retained artifacts. | Migration drift helper. | High | Service-resource mutation risk; preserve scratch DB cleanup and diagnostics. |
| `tools/harness/backend/drift/manifests.mjs` | Compatibility re-export for backend drift manifest validators. | Re-exports migration-history and schema-object-ownership modules. | No live production imports found; archive references only. | `migration-history.mjs`, `schema-object-ownership.mjs`. | JSON-shape and archive-era references only. | Migration/schema object ownership manifest shape APIs. | Compatibility shim. | Low | Candidate for later removal only after proving no supported callers. |
| `tools/harness/backend/duration/baselines.mjs` | Compatibility re-export for Go duration baseline helpers. | Re-exports `go-duration-baselines.mjs`. | No live production imports found; archive references only. | `go-duration-baselines.mjs`. | Duration baseline tests indirectly through canonical file. | `cartulary.go_test_duration_baselines.v4`. | Compatibility shim. | Low | Defer removal until compatibility policy is explicit. |
| `tools/harness/backend/go-duration-artifacts.mjs` | Reads retained Go shard artifacts and derives observed duration components. | `normalizePositiveInteger`, `sortedObject`, `collectObservedGoShardArtifacts`. | Duration baseline update and drift CLIs. | `go-duration-baselines.mjs`, `backend-shard-plan.mjs`, diagnostics fixture reporting. | `tests/test-go-test-duration-baselines.sh`. | Duration baseline observations; fixture timing evidence. | Backend duration accounting. | Medium | Depends on retained artifact layout under `_shared`. |
| `tools/harness/backend/go-duration-baselines.mjs` | Go duration baseline constants, file resolution, schema helpers, key builders. | `goDurationBaselineSchemaID`, defaults, `readGoDurationBaseline`, `readGoDurationBaselineMaps`, `withGoDurationBaselineFile`, key helpers. | Duration artifacts, shard planner, duration CLIs, compatibility re-export. | Node fs/path only. | `tests/test-go-test-duration-baselines.sh`. | `tools/go_test_duration_baselines.json`; schema `cartulary.go_test_duration_baselines.v4`. | Backend duration accounting. | Medium | Public Make targets depend on schema and defaults. |
| `tools/harness/backend/go-shard-plan-cli.mjs` | Human/JSON CLI for Go shard plan inspection. | CLI `--json`, `--target`. | Backend tests and phase-accounting target-plan smoke by path. | `backend-shard-plan.mjs`. | `tests/test-run-go-target.sh`, `phase-accounting/tests/test-print-target-plan.sh`. | `cartulary.go_shard_plan.v3`. | Backend diagnostics CLI. | Medium | Raw helper path is private, but output is characterization-sensitive. |
| `tools/harness/backend/go-shard-plan.mjs` | Builds Go shard plan from target rows, baselines, fixture policy, and phase selection. | `collectGoShardPlanFromRows`, `collectGoShardPlan`, `collectGoShardsForTargetFromRows`, `collectGoShardsForTarget`; private CLI subcommands. | Shard facade, target runner, Postgres fixture budget CLI, scheduler phase-slice, scheduler tests, harness-contract tests. | `go-duration-baselines.mjs`, dynamic `target-plan.mjs`. | `tests/test-run-go-target.sh`, `scheduler/tests/test-service-backed-scheduler.sh`, harness-contract tests. | `cartulary.go_shard_plan.v3`; scheduler work units; fixture assignment metadata. | Backend shard scheduler helper. | High | Central scheduling/accounting logic; preserve shard names, ordering, targets, and fixture policies. |
| `tools/harness/backend/go-target-aggregate.mjs` | Aggregate regex/package and fixture assignment helpers for target rows. | `aggregateRegex`, `aggregatePackages`, `fixturePolicyAssignments`, `resetTableAssignments`, `collectAggregateEmissions`. | `target-plan.mjs`, `go-target-runner.mjs`. | None outside standard JS. | `tests/test-run-go-target.sh` indirectly. | Phase summary emission and aggregate command metadata. | Backend target aggregation helper. | Medium | Shared helper with semantic ownership in backend target execution. |
| `tools/harness/backend/go-target-plan-coverage-cli.mjs` | Verifies target-plan rows cover authoritative and support Go manifest rows and aggregate commands. | CLI `--commands`, `--root`, `--quiet`. | Backend runner tests; archive references. | `backend-target-plan.mjs`, `go-target-runner.mjs`. | `tests/test-run-go-target.sh`. | Target-plan coverage diagnostics. | Backend target-plan verifier. | Medium | Characterization gate for plan-to-command consistency. |
| `tools/harness/backend/go-target-runner.mjs` | Runs backend Go targets, captures shard reports, validates runtime binaries, emits phase summaries and target summaries. | `createGoTargetContext`, `renderCommand`, `prepareSharedArtifactDir`, lock/capture/finalize helpers, `runGoTargetCLI`, CLI commands for backend targets and shard finalization. | Task-surface public backend targets through runner, coverage CLI, backend tests, manifest path refs, harness-contract path refs. | `go-target-aggregate.mjs`, `go-shard-plan.mjs`, contract facade, test-output context, `backend-target-plan.mjs`. | `tests/test-run-go-target.sh`, harness-contract tests, scheduler tests indirectly. | Target summaries, phase summaries, timing spans, runtime-binaries artifact, build artifact cache validation. | Backend Go execution facade. | High | Mixed orchestration, artifact, locking, redaction, and CLI behavior; strongest split candidate. |
| `tools/harness/backend/go-test-duration-baseline-coverage-cli.mjs` | Verifies duration baseline file covers planned Go shards. | CLI `--baseline-file`. | Public `go-test-duration-baseline-coverage`; Make node tools; task-surface manifests. | `go-duration-baselines.mjs`, `backend-shard-plan.mjs`. | `tests/test-go-test-duration-baselines.sh`. | `cartulary.go_test_duration_baselines.v4`; retained tool summary. | Backend duration accounting CLI. | Medium | Default `check` includes this target. |
| `tools/harness/backend/go-test-duration-baseline-drift-cli.mjs` | Compares observed retained shard timing against duration baselines. | CLI `--baseline-file <path> <results-dir>`. | Public `go-test-duration-baseline-drift`; Make node tools; task-surface manifests. | Scheduler duration drift helpers, duration artifacts, duration baselines. | `tests/test-go-test-duration-baselines.sh`. | Duration drift retained artifact and diagnostics. | Backend duration drift CLI. | Medium | Requires retained results; should not be run without result root. |
| `tools/harness/backend/go-test-duration-baselines-cli.mjs` | Updates Go test duration baseline JSON from successful retained service-backed results. | CLI `--prune-observed-packages`, `--allow-command-overhead-decrease`, `--baseline-file`, `<successful-results-dir>`. | Public `go-test-duration-baselines`; Make node tools; task-surface manifests. | Scheduler duration drift helpers, duration artifacts, duration baselines. | `tests/test-go-test-duration-baselines.sh`. | Writes `tools/go_test_duration_baselines.json` or override baseline; schema v4. | Backend duration baseline updater. | High | Mutating target; implementation sessions must avoid accidental baseline refresh unless authorized. |
| `tools/harness/backend/govulncheck-findings.mjs` | Parses govulncheck JSON stream, redacts, validates, and writes normalized findings summary. | CLI `--input`, `--output`; schema local constant. | `tools/harness/static-analysis/go-govulncheck.sh`. | Contract facade `redactValue`, `validateSchemaSync`. | Static-analysis govulncheck tests indirectly. | `cartulary.govulncheck_findings.v1`; retained `govulncheck-findings.json`. | Static-analysis security helper candidate. | Medium | Current location is backend-adjacent but ownership may fit static-analysis/security. |
| `tools/harness/backend/migration-history-cli.mjs` | CLI wrapper for migration history validation. | CLI `--root`; prints pass/fail diagnostics. | `check-migrations.sh`, migration tests. | `migration-history.mjs`. | `tests/test-check-migrations.sh`. | `tools/migration_history_manifest.json`. | Migration drift CLI. | Medium | Diagnostics are asserted by tests. |
| `tools/harness/backend/migration-history.mjs` | Validates migration history manifest shape, contiguous versions, hashes, and phase-shaped naming policy. | `migrationHistorySchemaID`, `validateMigrationHistoryManifestShape`, `validateMigrationHistory`. | CLI, drift compatibility shim, JSON-shape checker. | Contract JSON-shape helpers, `db/migrations`. | `tests/test-check-migrations.sh`, generated-artifacts JSON shape tests. | `cartulary.migration_history_manifest.v1`. | Migration/schema drift helper. | High | Coupled to Core 01 migration history policy; preserve hash and naming diagnostics. |
| `tools/harness/backend/postgres-fixture-budget-cli.mjs` | Checks actual service fixture events against planned Postgres fixture budgets. | CLI `--targets <csv>`. | Scheduler service-backed tests by path; possible scheduled helper use. | Diagnostics fixture reporting, `go-shard-plan.mjs`, `backend-target-plan.mjs`. | `scheduler/tests/test-service-backed-scheduler.sh`. | Service fixture event evidence and budget diagnostics. | Backend service fixture accounting. | Medium | Uses hard-coded harness self-test budget exceptions. |
| `tools/harness/backend/run-go-manifest-phase.sh` | Runs selected Go tests from phase manifest rows and emits manifest phase summary. | Shell helper `run-go-manifest-phase.sh "<label>" <phase> <section> <coverage> [execution_dependency] -- <go test...>`. | Run-phase tests; legacy helper references. | Execution run-phase common, phase manifest CLI, test-output helper. | `tools/harness/tests/test-run-phase.sh`. | `cartulary.test_phase_summary.v3` via test-output helper. | Backend Go phase wrapper. | Medium | Preserves support for manifest-based Go phase evidence. |
| `tools/harness/backend/run-go-phase.sh` | Runs regex-selected Go tests and emits Go phase summary. | Shell helper `run-go-phase.sh "<label>" "<pattern>" -- <go test...>`. | Backend runner tests, run-phase tests, task-surface report tests. | Execution run-phase common, test-output helper. | `tests/test-run-go-target.sh`, `tools/harness/tests/test-run-phase.sh`. | `cartulary.test_phase_summary.v3` via test-output helper. | Backend Go phase wrapper. | Medium | Output mode and zero-match diagnostics are tested. |
| `tools/harness/backend/runner/go-shards.mjs` | Compatibility re-export for shard-plan row-based helpers. | Re-exports `collectGoShardPlanFromRows`, `collectGoShardsForTargetFromRows`. | No live production imports found; archive references only. | `go-shard-plan.mjs`. | Harness-contract only indirectly. | Go shard-plan contract. | Compatibility shim. | Low | Defer removal until compatibility policy is explicit. |
| `tools/harness/backend/schema-object-ownership-cli.mjs` | CLI wrapper for schema object ownership validation. | CLI `--root`; prints pass/fail diagnostics. | Archive references; direct helper usage possible. | `schema-object-ownership.mjs`. | JSON-shape tests indirectly. | `tools/schema_object_ownership_manifest.json`. | Migration/schema drift CLI. | Medium | Diagnostic text may be consumed by drift checks. |
| `tools/harness/backend/schema-object-ownership.mjs` | Validates schema object owner manifest and scans migration SQL for object coverage. | `schemaObjectOwnershipSchemaID`, `validateSchemaObjectOwnershipManifestShape`, `validateSchemaObjectOwnership`. | CLI, drift compatibility shim, JSON-shape checker. | Contract JSON-shape helpers, `db/migrations`. | Generated-artifacts JSON shape tests. | `cartulary.schema_object_ownership_manifest.v1`. | Migration/schema drift helper. | High | Uses SQL scanning heuristics; owner manifest is implementation-support evidence. |
| `tools/harness/backend/target-plan.mjs` | Builds normalized backend target rows from execution topology, phase manifests, support rows, raw aggregates, fixture policy, and runtime binaries. | `collectTargetPlanRows`, `collectTargetNames`, `findTargetDescriptor`, `knownManifestPhases`; private CLI subcommands. | Backend facades, shard planner, diagnostics target-plan CLI, task guidance, scheduler phase-slice, scheduler schedule context, output target-start stats, generated schedule rendering. | Execution topology loader, phase manifest helpers, aggregate helpers. | `tests/test-run-go-target.sh`, scheduler tests, generated-artifacts tests, harness-contract tests. | Backend target-plan row shape, target-plan JSON diagnostics, scheduler inputs. | Backend target-plan data provider. | High | Accidental coupling point across diagnostics, scheduler, output, and generated-artifact code. |
| `tools/harness/backend/tests/test-check-migrations.sh` | Characterizes migration input/scratch validation and migration history manifest diagnostics. | Shell test script. | Harness smoke check `harness-smoke-check-migrations`. | Backend migration helpers and synthetic fixture repos. | Selected by `check-harness-smoke`/harness smoke tiers. | Migration history and drift behavior. | Backend harness test. | Medium | Creates scratch fixtures under `tmp/`; not production code. |
| `tools/harness/backend/tests/test-go-test-duration-baselines.sh` | Characterizes duration baseline update, drift, coverage, contamination handling, and Make wrapper behavior. | Shell test script. | Harness smoke check `harness-smoke-go-test-duration-baselines`. | Duration baseline CLIs and Make public targets. | Selected by harness smoke tiers. | Go duration baseline schema and retained timing evidence. | Backend duration harness test. | Medium | Mutates only temp baseline fixtures during tests. |
| `tools/harness/backend/tests/test-run-go-target.sh` | Characterizes Go target runner, shard plan, phase summary accounting, identity reuse, locks, finalizer errors, and synthetic manifest validation. | Shell test script. | Harness smoke check `harness-smoke-run-go-target`. | Backend runner, shard/target plan helpers, phase-map checker, test-output helper. | Selected by harness smoke tiers. | Phase summaries, target timing, target summaries, shard-plan JSON. | Backend runner harness test. | High | Broad coverage for highest-risk runner behavior. |

## 3. Module Boundary Diagnosis

Current classification: mixed-responsibility harness backend package. It contains legitimate private facades for backend target/shard planning, but it also acts as an accidental cross-subsystem coupling point for diagnostics, scheduler planning, output accounting, generated-artifact schedule rendering, duration baselines, migration/schema drift, and security finding normalization.

| Responsibility found | Current location | Correct owner candidate | Keep / move / split / defer | Evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| Backend Go target row discovery. | `target-plan.mjs`, `backend-target-plan.mjs`. | Backend target-plan facade with phase-accounting inputs hidden behind DTOs. | Split. | Imported by diagnostics, scheduler, output, generated-artifacts, and backend runner. | Keep public row shape stable until callers are isolated. |
| Backend Go shard scheduling. | `go-shard-plan.mjs`, `backend-shard-plan.mjs`, `runner/go-shards.mjs`. | Backend shard-plan facade. | Keep, then narrow. | Scheduler and tests import shard helpers. | Compatibility re-export can be removed only after caller proof. |
| Backend Go command execution and artifact collation. | `go-target-runner.mjs`. | Backend execution facade plus output/reporting helper boundaries. | Split. | File mixes locks, redaction, runtime binary validation, report collation, CLI routing, phase summary emission. | Highest implementation risk. |
| Aggregate regex/package and fixture assignment. | `go-target-aggregate.mjs`. | Backend target execution helper. | Keep. | Used by target-plan and target-runner. | Cohesive enough to remain private. |
| Migration input and scratch apply verification. | `check-migrations.sh`, `migration-history*.mjs`. | Migration/schema drift support, likely generated-artifacts or database-contract helper. | Defer. | Public `migration-drift` uses the shell wrapper; JSON shape checker imports validator. | Owner decision needed before movement. |
| Schema object ownership validation. | `schema-object-ownership*.mjs`. | Migration/schema drift support, likely generated-artifacts or database-contract helper. | Defer. | Core 01 treats owner manifest as implementation-support evidence. | Avoid turning manifest into product behavior owner. |
| Go duration baseline management. | `go-duration-*`, `go-test-duration-*`, `duration/baselines.mjs`. | Backend duration accounting helper. | Keep, then rationalize re-exports. | Public duration baseline targets and tests. | Mutating update target requires explicit authorization. |
| Security findings normalization. | `govulncheck-findings.mjs`. | Static-analysis/security helper. | Defer. | Only live caller is `go-govulncheck.sh`. | Backend directory is probably not the best semantic owner. |
| Go phase shell wrappers. | `run-go-phase.sh`, `run-go-manifest-phase.sh`. | Backend Go phase wrapper or execution output helper. | Defer. | Tests assert output behavior and zero-match diagnostics. | Preserve shell CLI shape while deciding owner. |
| Go binary build wrapper. | `build-go-artifact.sh`. | Build/readiness helper. | Defer. | Make build targets and cache-artifact wrapper invoke it. | Backend because it builds Go, but not target-plan behavior. |
| Harness characterization tests. | `tools/harness/backend/tests/*`. | Backend harness tests. | Keep. | Harness smoke manifest rows select them. | Tests are implementation-support evidence. |
| Workbook orchestration behavior. | None found in target. | Product modules under `internal/modules`, `apps/web`, `packages/*` as applicable. | Defer/no action. | Inspected files contain harness mechanics only. | Do not move product workbook responsibilities because none are present here. |

## 4. Public Contract and Behavior Freeze Map

| Contract | Current owner | Evidence | Existing tests | Required characterization tests | Refactor risk | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Public Make targets `backend-unit`, `backend-store`, `backend-integration`, `backend-process`. | Testing Harness NLSpec and task-surface manifest. | `tools/task_surface_manifest.json`, `tools/execution_topology_manifest.json`, runner CLI. | `test-run-go-target.sh`, scheduler tests, harness-contract. | Preserve target status, summaries, artifacts, and failure mapping before movement. | High | `backend-integration-support` is check-internal, not public active. |
| Internal/check helper target `backend-integration-support`. | Task-surface manifest and scheduler semantics. | Check-internal row and runner CLI command. | `test-run-go-target.sh`, scheduler tests. | Characterize shared/reused shard behavior if runner split. | High | Support evidence must not mix with authoritative target ownership. |
| Public Make target `migration-drift` and internal migration helpers. | Testing Harness NLSpec and task-surface manifest. | Manifest rows for migration input and scratch apply. | `test-check-migrations.sh`. | Preserve mode parsing, scratch DB cleanup, migration anchors, and diagnostics. | High | Scratch mode mutates service resources. |
| Public Make targets `go-test-duration-baselines`, `go-test-duration-baseline-coverage`, `go-test-duration-baseline-drift`. | Testing Harness NLSpec and task-surface manifest. | Make-node tools and task-surface rows. | `test-go-test-duration-baselines.sh`. | Preserve schema v4, contamination rejection, drift messages, and Make wrapper result behavior. | High | Baseline update target is intentionally mutating. |
| Public build targets `build-server`, `build-migrate`, `build-operator` and internal `testservices-build`. | Testing Harness NLSpec and task-surface manifest. | `Makefile`, task-surface rows. | Build/readiness harness checks. | Preserve build cache keys, output digest, retained summaries. | Medium | Build outputs are not product behavior changes by themselves. |
| Security findings artifact `govulncheck-findings.json`. | Testing Harness NLSpec and static-analysis/security target. | `go-govulncheck.sh`, findings schema ID. | Static-analysis govulncheck tests, JSON shape checks. | Preserve redaction, blocking reachability, exit code mapping. | Medium | Owner candidate is static-analysis/security. |
| Backend target-plan diagnostics and row shape. | Testing Harness NLSpec diagnostics behavior and task-surface. | `target-plan.mjs`, diagnostics `target-plan-cli.mjs`, task guidance imports. | `test-run-go-target.sh`, `test-print-target-plan.sh`, harness-contract. | Preserve row fields, target filtering, aggregate CLI outputs. | High | Raw helper path is private but diagnostic output is observable through Make. |
| Go shard-plan JSON and shard CLI behavior. | Backend harness helpers under Testing Harness NLSpec mechanics. | `go-shard-plan.mjs`, `go-shard-plan-cli.mjs`. | `test-run-go-target.sh`, scheduler tests. | Preserve schema ID, shard names, phase prefixing, sort order, fixture assignment fields. | High | Scheduler depends on stable shard identities. |
| Phase summaries, target summaries, timing spans, and retained artifacts. | Testing Harness NLSpec Sections 7-9 and output helpers. | `go-target-runner.mjs`, `run-go-*.sh`, test-output helper. | `test-run-go-target.sh`, output tests, harness-contract. | Preserve accounting modes, failure classes, artifact paths, and run-root identity protection. | High | Contract-sensitive retained evidence. |
| Migration history manifest shape. | Core 01 migration policy and harness JSON shape checks. | `migration-history.mjs`, `tools/migration_history_manifest.json`. | `test-check-migrations.sh`, JSON shape tests. | Preserve contiguous version, hash, marker, and phase-shaped filename diagnostics. | High | Manifest is implementation-support evidence, not product owner. |
| Schema object ownership manifest shape. | Core 01 schema ownership policy and harness JSON shape checks. | `schema-object-ownership.mjs`, `tools/schema_object_ownership_manifest.json`. | JSON shape tests. | Preserve owner coverage and ambiguity diagnostics. | High | SQL scanner movement needs strong characterization. |
| HTTP routes. | Product Core 01/Core 04, not this target. | No route handlers found under target. | Not applicable for tracker target. | TODO: only required if future implementation touches route code. | Low | No direct route contract affected by tracker-only work. |
| WebSocket events. | Product Core 01/Core 03/Core 04, not this target. | No WebSocket handlers found under target. | Not applicable for tracker target. | TODO: only required if future implementation touches collaboration code. | Low | No direct WebSocket contract affected. |
| Workbook rows, saved views, view schema, grid adapter, frontend shell. | Product Core 01/Core 03, `apps/web`, and packages, not this target. | No workbook/UI code found under target. | Not applicable for tracker target. | TODO: only required if future implementation crosses product modules. | Low | Record as architectural non-ownership. |

## 5. Coupling and Boundary Findings

| Finding | Evidence | Risk | Classification | Proposed owner | Required planning action |
| --- | --- | --- | --- | --- | --- |
| `target-plan.mjs` is a central cross-subsystem data provider. | Imported by diagnostics, scheduler, output, generated-artifact rendering, and backend helpers. | Public diagnostic, scheduler, and artifact drift if row shape changes. | should_fix | Backend target-plan facade with narrow DTOs. | Plan caller-by-caller facade preservation before internal split. |
| `go-target-runner.mjs` mixes execution, security, locking, runtime binary verification, report collation, and output emission. | Direct source inspection shows all concerns in one file. | High chance of artifact or failure-mapping drift during refactor. | must_fix | Backend execution facade plus output/reporting helpers. | Require characterization before splitting and checkpoint each extraction. |
| Backend shard helpers are consumed by scheduler code. | `scheduler/adapters/backend.mjs` imports `backend-shard-plan.mjs`; `phase-slice-plan.mjs` imports `go-shard-plan.mjs`. | Scheduler selection drift. | should_fix | Scheduler adapter consuming backend shard facade only. | Keep adapter import path stable; avoid direct scheduler imports of private internals. |
| Duration baseline updater intentionally writes generated/support JSON. | `go-test-duration-baselines-cli.mjs` uses `writeFileSync` on baseline file. | Accidental mutation during verification. | must_fix | Backend duration accounting. | Mark update target as authorized-only; use coverage/drift for validation unless `RESULTS_DIR` is authorized. |
| Migration/schema validators live under backend though they govern database-contract support. | `migration-history.mjs` and `schema-object-ownership.mjs` imported by JSON shape checker. | Owner ambiguity; product schema policy could be mistaken for backend test behavior. | should_fix | Generated-artifacts or database drift support. | Decide owner before moving; preserve manifest schema IDs. |
| `govulncheck-findings.mjs` lives under backend but is called by static-analysis security wrapper. | Only live caller is `go-govulncheck.sh`. | Misplaced ownership; low movement risk if CLI path preserved. | should_fix | Static-analysis/security helper. | Plan wrapper-compatible move or facade. |
| Compatibility re-exports exist without live non-archive imports. | `drift/manifests.mjs`, `duration/baselines.mjs`, `runner/go-shards.mjs`. | Low, but removal could break undocumented users. | defer | Compatibility policy owner. | Inventory supported compatibility paths before deletion. |
| Harness import boundary does not make backend helper paths public. | Import-boundary check rejects planning and private core imports, not backend helper imports. | Future callers may depend on private paths. | should_fix | Harness static-analysis boundary. | Add or revise guardrails only after owner facades are stable. |
| Generated task-surface and execution topology reference backend paths. | Manifests and generated Make include backend scripts. | Hand-edit risk and generated drift. | intentional/no_action | Generated-artifacts owner. | Update owner inputs and run generators only in later authorized tasks. |
| No workbook orchestration behavior found in target. | All inspected files are harness mechanics. | Wrong refactor target could move product responsibilities that are absent. | intentional/no_action | Product modules remain outside target. | Record non-ownership; do not invent product module moves. |
| Test-only assumptions are intentionally under harness tests. | `tests/*` create synthetic repos, manifests, baselines, and temp outputs. | Low if kept test-local; high if copied into runtime. | intentional/no_action | Backend harness tests. | Preserve fixture isolation and avoid production reuse. |

## 6. Refactor Workstreams

| Workflow ID | Name | Class: root/chain/parallel | Required previous workflows | Required subsequent workflows | Goal | Files likely involved | Validation | Handoff checkpoint |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| WF-00 | Session/source bootstrap and tracker initialization | root | none | WF-01 | Record scope, source hierarchy, and tracker-only constraints. | This tracker. | `make lint-markdown` after tracker creation. | Tracker exists with scope posture and session log. |
| WF-01 | Target inventory | chain | WF-00 | WF-02, WF-04 | Inventory every file under `tools/harness/backend`. | All 29 target files. | No code validation; repository inspection only. | Section 2 complete. |
| WF-02 | Contract-owner mapping | chain | WF-01 | WF-03, WF-05 | Map Make targets, schemas, artifacts, and non-owned product contracts. | Task-surface manifests, Testing Harness NLSpec, target helpers. | `make json-shape-check` if manifest assumptions change later. | Section 4 complete. |
| WF-03 | Characterization test gap analysis | chain | WF-02 | WF-06 | Identify existing tests and missing characterization before movement. | `tests/*`, harness-contract, scheduler tests. | `make check-harness-smoke`, `make harness-contract` in later implementation. | Required tests mapped to contract risk. |
| WF-04 | Boundary/coupling scan | chain | WF-01 | WF-05 | Classify coupling and misplaced ownership. | Target files and caller imports. | `make lint-scripts`, `make backend-module-boundary-check` later. | Section 5 complete. |
| WF-05 | Facade or ownership redesign plan | chain | WF-02, WF-04 | WF-06 | Decide stable backend, duration, migration/schema, static-analysis ownership seams. | `target-plan.mjs`, `go-target-runner.mjs`, drift/security helpers. | Harness-contract before and after each move. | Owner decisions recorded before code edits. |
| WF-06 | Slice sequencing plan | chain | WF-03, WF-05 | WF-07 | Convert redesign into behavior-preserving slices. | Backend helper packages and callers. | Slice-specific Make targets. | Section 7 complete. |
| WF-07 | Harness/test/accounting update plan | chain | WF-06 | WF-08 | Plan tests, task-surface metadata, and generated-artifact handling. | Harness tests, task/execution owner inputs if needed. | `make generated-artifact-policy-check`, `make json-shape-check`, `make harness-contract`. | Validation matrix and no generated hand-edits. |
| WF-08 | Validation and final handoff | chain | WF-07 | none | Run selected validation and append handoff notes. | Tracker and later implementation diffs. | Narrow first, broad only by risk. | Session log current and next action clear. |

## 7. Proposed Refactor Slice Plan

| Slice ID | Depends on | Intended change | Files/packages likely involved | Contract risks | Tests to add or preserve | Validation command | Rollback note | Completion criterion |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S-00 | none | Create this tracker only. | `docs/handoffs/test-harness-backend-module-refactor-tracker.md`. | Documentation drift only. | Preserve tracker binary criteria. | `make lint-markdown`. | Revert tracker file only. | Tracker exists and no other files changed. |
| S-01 | S-00 | Preserve or narrow backend target/shard facades without changing public row or shard shapes. | `backend-target-plan.mjs`, `backend-shard-plan.mjs`, `target-plan.mjs`, `go-shard-plan.mjs`, scheduler/diagnostic callers. | Target-plan JSON, shard-plan JSON, scheduler selection, task guidance. | Preserve `test-run-go-target.sh`, `test-print-target-plan.sh`, harness-contract. | `make lint-scripts`; `make harness-contract`; `make check-harness-smoke`. | Revert facade/caller changes as one unit. | Callers use explicit backend facades and outputs are unchanged. |
| S-02 | S-01 | Split `go-target-runner.mjs` internals behind execution/reporting helpers while keeping CLI behavior. | `go-target-runner.mjs`, possible new backend-private helpers, output/test-output callers. | Phase summaries, target summaries, timing spans, locks, redaction, runtime binary validation. | Add focused runner helper tests if extraction is not directly covered. Preserve `test-run-go-target.sh`. | `make lint-scripts`; `make check-harness-smoke`; `make harness-contract`. | Revert runner extraction without changing CLI callers. | CLI commands and retained artifacts match existing tests. |
| S-03 | S-01 | Decide whether `govulncheck-findings.mjs` should move behind static-analysis/security ownership. | `govulncheck-findings.mjs`, `static-analysis/go-govulncheck.sh`, task manifests if path changes. | Findings schema, redaction, exit code mapping, generated task-surface path references. | Preserve static-analysis govulncheck tests and JSON shape checks. | `make lint-scripts`; `make go-vulncheck` only when toolchain ready; `make harness-contract`. | Keep old path as wrapper until path contract decision is complete. | Security wrapper has same behavior; owner path decision recorded. |
| S-04 | S-01 | Decide owner for migration/schema drift helpers and move only behind a compatible facade if approved. | `migration-history*.mjs`, `schema-object-ownership*.mjs`, `check-migrations.sh`, JSON-shape checker. | Manifest schema IDs, migration diagnostics, scratch apply behavior. | Preserve `test-check-migrations.sh`, generated-artifacts JSON shape tests. | `make json-shape-check`; `make migration-drift` when service-backed validation is appropriate. | Leave old CLI wrapper until Make and tests no longer reference it. | Manifest validators have clear owner and unchanged diagnostics. |
| S-05 | S-01 | Rationalize duration-baseline compatibility re-exports. | `duration/baselines.mjs`, `runner/go-shards.mjs`, duration CLIs. | Baseline schema, shard planning imports, private compatibility paths. | Preserve `test-go-test-duration-baselines.sh`. | `make go-test-duration-baseline-coverage`; `make check-harness-smoke`. | Restore re-export files if any supported caller exists. | Dead compatibility paths are either justified or removed after proof. |
| S-06 | S-02, S-03, S-04, S-05 | Remove obsolete compatibility paths and reduce guardrail allowlists only after callers move. | Backend helpers, harness import-boundary tests, generated owner inputs if path metadata changes. | Generated drift, task-surface path references, private-path consumers. | Add import-boundary fixtures for new owner facades. | `make lint-scripts`; `make generated-artifact-policy-check`; `make harness-contract`; `make check`. | Revert cleanup separately from behavior-preserving moves. | No orphaned old path remains unless explicitly deferred. |
| S-07 | S-00 | Any public Make target, schema, artifact path, or behavior change. | TODO: depends on approved behavior change. | Public harness contract changes. | Requires owner-doc update first. | TODO: command discovery required after owner decision. | Do not start without authorization. | Marked `requires later authorization`. |

## 8. Validation Plan

| Validation layer | Command | Scope | Required before implementation? | Notes |
| --- | --- | --- | --- | --- |
| unit | `make check-harness-smoke` | Harness smoke projection including backend smoke checks. | yes for backend helper movement | Check-internal target; not proof of full direct public target behavior. |
| integration | `make backend-unit`; `make backend-store`; `make backend-integration`; `make backend-process` | Public backend Go target behavior and service-backed target execution. | yes for runner/shard changes, no for tracker-only | Service-backed targets may require local services/tooling. |
| e2e/browser | TODO: command discovery required only if future slice touches browser or frontend code | No current target file owns browser/frontend behavior. | no | Current tracker found no browser or grid-adapter code under target. |
| generated drift | `make generated-artifact-policy-check`; `make json-shape-check`; `make migration-drift` | Generated policy, JSON shapes, migration drift and scratch apply. | yes if manifests, migration/schema helpers, or generated owner inputs change | `migration-drift` is service-backed and mutates scratch DB resources. |
| import-boundary/static | `make lint-scripts`; `make lint-shell`; `make backend-module-boundary-check`; `make harness-contract` | JS/shell syntax, backend module guardrails, harness import-boundary tests. | yes for implementation slices | `harness-contract` is the extended harness contract target. |
| full check | `make check` | Developer verification gate. | no for tracker-only; yes after high-risk implementation if narrow checks pass | Broaden only when risk or ownership requires it. |

Commands discovered from `make help`, `tools/task_surface_manifest.json`, `tools/execution_topology_manifest.json`, `tools/harness/execution/make-node-tools.mjs`, and generated Make surface inspection. Do not run `make go-test-duration-baselines` unless a retained successful `RESULTS_DIR` is intentionally authorized because it updates baseline JSON.

## 9. Top-Level Work Tracker

| ID | Work item | Workstream | Status | Depends on | Evidence or artifact | Exit condition |
| --- | --- | --- | --- | --- | --- | --- |
| T-001 | Create tracker and lock target scope. | WF-00 | DONE | none | This tracker. | Target path, label, output path, and planning-only limits recorded. |
| T-002 | Inventory every target file. | WF-01 | DONE | T-001 | Section 2 inventory. | All 29 files under `tools/harness/backend` inventoried. |
| T-003 | Map public and internal harness contracts. | WF-02 | DONE | T-002 | Section 4 freeze map. | Make targets, schemas, artifacts, and non-owned product contracts listed. |
| T-004 | Identify existing characterization coverage. | WF-03 | DONE | T-003 | Sections 2, 4, 8. | Existing backend harness tests and gaps recorded. |
| T-005 | Diagnose boundary and coupling risks. | WF-04 | DONE | T-002 | Sections 3 and 5. | Mixed-responsibility and ownership findings classified. |
| T-006 | Plan behavior-preserving refactor slices. | WF-06 | DONE | T-003, T-005 | Section 7 slice table. | Each slice has dependency, validation, rollback, and completion criterion. |
| T-007 | Decide owner for security and migration/schema helpers. | WF-05 | DONE | T-005 | `docs/testing-harness-nlspec.md` Section 4.1A. | Owner decision recorded before moving files. |
| T-008 | Plan and apply generated/task-surface updates for moved helper paths. | WF-07 | DONE | T-007 | `tools/execution_topology_manifest.json`; `make phase-schedules`; `make phase-schedule-drift`. | Owner inputs named, generated outputs refreshed by Make, and no generated file hand-edited. |
| T-009 | Execute approved implementation slices. | WF-08 | TODO | T-006, T-007 | Later authorized task. | Behavior-preserving implementation complete and validated. |
| T-010 | Validate tracker-only document. | WF-08 | DONE | T-001 | `make lint-markdown` passed at 2026-07-04T03:53:43Z. | Markdown validation run and result logged. |
| T-011 | Close helper ownership blockers RB-001 through RB-005. | WF-05/WF-07/WF-08 | DONE | T-007, T-008 | NLSpec 4.1A, moved helper paths, import-boundary tests, duration retained-run fixtures. | RB rows resolved and narrow validation passed. |

## 10. Session Handoff Log

### Scope and authority

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-04T03:48:54Z | Codex tracker creation | Tracker created from approved plan; target exists; no prior tracker file was found. | Touched this tracker only; inspected framework, AGENTS, harness NLSpec, domain, Core 00-04, generated policy, task/execution manifests. | `sed`, `find`, `git status`, `date`, prior `make help`. | Scope recorded as planning-only and later implementation required. | None for tracker creation. | Run markdown validation and record result. |
| 2026-07-04T04:23:02Z | Codex remediation | RB-001 through RB-005 closed by NLSpec 4.1A and implementation slices; public Make names, `command_id`s, schema IDs, artifact paths, output/failure contracts, and cleanup behavior preserved. | Touched this tracker plus NLSpec, harness helpers/tests, execution topology owner input, and generated task-surface outputs. | `make lint-markdown`; `make phase-schedules`; `make phase-schedule-drift`. | Tracker updated with resolved ownership and generated-output posture. | Broader `go-target-runner.mjs` split remains future refactor work. | Continue with runner/internal split only under a later focused task. |

### Backend module boundary

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-04T03:48:54Z | Codex tracker creation | `tools/harness/backend` classified as mixed-responsibility private harness backend helper area. | Inspected all 29 files under `tools/harness/backend`; touched tracker only. | `find`, `sed`, import/path map Node inspection from prior planning. | Found target/shard planning, runner, duration, migration/schema, security, build, phase wrappers, and tests. | Owner decisions still needed for security and migration/schema helper moves. | Start WF-05 before code movement. |
| 2026-07-04T04:23:02Z | Codex remediation | Backend target/shard facades retained; `backend-duration-accounting.mjs` added; security and database-contract helpers moved out of backend; unsupported re-export paths deleted. | `tools/harness/backend/backend-target-plan.mjs`, `backend-shard-plan.mjs`, `backend-duration-accounting.mjs`, moved/deleted backend helper paths. | `rg`; `node --check`; import-boundary scan. | Non-owner live imports now use owner facades; old compatibility re-exports are absent. | `go-target-runner.mjs` remains mixed-responsibility. | Treat runner split as separate behavior-preserving extraction. |

### Frontend module boundary, if applicable

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-04T03:48:54Z | Codex tracker creation | No frontend shell, view-contract, or grid-adapter behavior found under target. | Touched tracker only. | Target inventory and caller search from prior planning. | Frontend boundary is not in scope for this target. | None. | Revisit only if a later slice touches `apps/web` or `packages/*`. |

### Contract and codegen

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-04T03:48:54Z | Codex tracker creation | Public contract freeze mapped to Make targets, `command_id`s, schema IDs, artifacts, and generated task-surface references. | Inspected task/execution manifests and generated policy; touched tracker only. | Node JSON manifest queries from prior planning. | No generated files edited. | Path changes later may require owner inputs and generator targets. | Keep generated outputs read-only until later authorized implementation. |
| 2026-07-04T04:23:02Z | Codex remediation | Execution topology owner input points migration drift at `tools/harness/generated-artifacts/database-contract-drift/check-migrations.sh`; task-surface generated outputs refreshed through Make. | `tools/execution_topology_manifest.json`, `tools/task_surface_manifest.json`, `tools/task_surface.generated.mk`, `tools/execution_topology_render_index.json`. | `make phase-schedules`; `make phase-schedule-drift`; `make generated-artifact-policy-check`; `make json-shape-check`. | Generated mirrors and drift checks passed. | None for generated path drift. | Keep future path moves routed through owner inputs and Make render targets. |

### Tests and harness

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-04T03:48:54Z | Codex tracker creation | Existing characterization tests identified for migrations, duration baselines, and Go target runner; tracker-only markdown validation passed. | Inspected backend test scripts; touched tracker only. | `sed`; prior `make help`; `make lint-markdown`. | `make lint-markdown` passed at 2026-07-04T03:53:43Z. | Implementation validation still requires later authorized refactor slices. | Start WF-05 owner decisions before code movement. |
| 2026-07-04T04:23:02Z | Codex remediation | Harness tests now cover owner-facade imports, unsupported private helper denial, moved migration helper path, and retained-run duration safety. | `tools/harness/tests/test-harness-contracts.mjs`, `tools/harness/backend/tests/test-check-migrations.sh`, `tools/harness/backend/tests/test-go-test-duration-baselines.sh`. | `make lint-scripts`; `make lint-shell`; `make harness-contract`; `make check-harness-smoke`; `make go-test-duration-baseline-coverage`; `make migration-drift`. | All listed commands passed. | Standalone duration drift/update against a real retained run was not run because no explicit retained `RESULTS_DIR` was selected for mutation. | Use explicit retained run evidence for any future baseline update. |

### Security and authorization

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-04T03:48:54Z | Codex tracker creation | Security-sensitive behavior found in govulncheck findings redaction, runtime binary digest validation, secure artifact writes, and public-target run-root identity protection. | Inspected `govulncheck-findings.mjs`, `go-target-runner.mjs`, `go-govulncheck.sh`; touched tracker only. | `sed`, caller search. | No authorization route behavior found under target. | Security helper ownership decision pending. | Treat security/failure mappings as frozen before movement. |
| 2026-07-04T04:23:02Z | Codex remediation | Govulncheck findings normalization moved to static-analysis/security ownership; old backend path is unsupported private. | `tools/harness/static-analysis/govulncheck-findings.mjs`, `tools/harness/static-analysis/go-govulncheck.sh`, `tools/harness/static-analysis/harness-import-boundary.mjs`. | `make go-vulncheck`; `make harness-contract`; `make lint-shell`; `make lint-scripts`. | Security scan and import-boundary validation passed. | None for RB-001. | Keep `GOVULNCHECK_DB`, findings schema, redaction, and exit mapping frozen unless NLSpec changes. |

### Open risks and next session

| Time | Agent/session | Current state | Files inspected or touched | Commands run | Result | Blockers | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-04T03:48:54Z | Codex tracker creation | Main risks are accidental public harness drift, generated path drift, and hidden scheduler/output coupling. | Touched tracker only. | `git status` before edit showed clean worktree. | Tracker now seeds workstreams and slices. | Actual refactor requires later authorization. | After validation, hand off with tracker path and remaining blockers. |
| 2026-07-04T04:23:02Z | Codex remediation | Helper ownership blockers are resolved; remaining risk is broader backend runner cohesion, not RB ownership closure. | Touched tracker only for handoff update. | `git status`; `rg`; validation commands listed above. | Narrow validation passed; broad `make check` not run after targeted gates. | Full runner split and full-gate validation remain future work. | Use this tracker plus NLSpec 4.1A as the handoff basis for future backend runner refactor. |

## 11. Open Questions and Blockers

| ID | Question or blocker | Why it matters | Needed authority or evidence | Current status |
| --- | --- | --- | --- | --- |
| RB-001 | Should `govulncheck-findings.mjs` move to static-analysis/security ownership? | Current backend location does not match the only live caller. | Harness owner decision plus static-analysis tests. | RESOLVED: moved to `tools/harness/static-analysis/govulncheck-findings.mjs`; validated with `make go-vulncheck`. |
| RB-002 | Should migration/schema manifest validators remain under backend or move to generated-artifacts/database drift support? | They govern database-contract implementation-support evidence, not backend target execution. | Testing Harness NLSpec owner interpretation and JSON-shape caller plan. | RESOLVED: moved to `tools/harness/generated-artifacts/database-contract-drift/`; validated with `make json-shape-check` and `make migration-drift`. |
| RB-003 | Are `drift/manifests.mjs`, `duration/baselines.mjs`, and `runner/go-shards.mjs` supported compatibility paths? | Safe deletion depends on compatibility expectations beyond tracked callers. | Caller inventory plus owner decision. | RESOLVED: classified `unsupported_private` in NLSpec 4.1A and deleted; import-boundary fixtures reject reintroduction. |
| RB-004 | Should harness import-boundary rules constrain direct imports from backend internals after facades are stable? | Prevents recurrence of cross-subsystem private-path coupling. | Harness static-analysis owner decision and facade names. | RESOLVED: `harness-import-boundary.mjs` enforces backend facade imports and unsupported-private denial; `make harness-contract` passed. |
| RB-005 | Which retained run, if any, may be used for duration-baseline drift or update checks? | Duration update target mutates baseline files; drift needs a retained result root. | Explicit `RESULTS_DIR` from a successful service-backed run. | RESOLVED AS RULE: NLSpec 4.1A defines retained-run eligibility; `go-duration-artifacts.mjs` rejects invalid evidence before drift/update; no mutating baseline update was run. |

## 12. Binary Completion Criteria

- DONE: every file in `tools/harness/backend` is inventoried or explicitly out of scope.
- DONE: every discovered public contract risk has an owner and test posture.
- DONE: every proposed workflow has dependencies and exit criteria.
- DONE: every proposed implementation slice is behavior-preserving unless explicitly marked `requires later authorization`.
- DONE: validation commands are discovered or marked `TODO` with a reason.
- DONE: contradictions are marked `BLOCKED: owner contradiction`; none were found in this planning pass.
- DONE: repository/framework mismatches are recorded as planning findings, especially that the framework target label does not make `tools/harness/backend` a permanent module boundary.
- DONE: handoff sections are current enough for another agent to continue without rediscovery for tracker planning.
