{
  "schema_id": "cartulary.requirement_catalog.v1",
  "owner_id": "package.protocol_ts",
  "requirements": [
    {
      "requirement_id": "package.protocol_ts.requirement.behavior_contract",
      "statement": "package.protocol_ts frontend postconditions remain exact across retained selectors and declared execution profiles.",
      "status": "active"
    },
    {
      "requirement_id": "package.protocol_ts.requirement.openapi_generated_surface",
      "statement": "Selected public HTTP operations generate closed request and response types, deterministic path and query bindings, and secret-safe runtime validators from the canonical OpenAPI artifact without exporting the raw document to browser runtime entrypoints.",
      "status": "active"
    },
    {
      "requirement_id": "package.protocol_ts.requirement.shared_contract",
      "statement": "The TypeScript protocol facade decodes closed generated contracts without widening wire shapes or exposing unsafe payload detail.",
      "status": "active"
    }
  ]
}
